---
phase: 07-motor-de-gera-o-cont-bil-mensal-e-anual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/competencia.ts
  - src/lib/geracao-tarefas-contabil.ts
  - src/lib/geracao-tarefas-contabil-anual.ts
  - tests/geracao-tarefas-contabil.test.ts
  - tests/geracao-tarefas-contabil-anual.test.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05]
must_haves:
  truths:
    - "gerarTarefasDoMesContabil produz as 8 rotinas mensais para LUCRO_REAL e LUCRO_PRESUMIDO, e zero para SIMPLES_NACIONAL"
    - "obrigacoesAnuaisParaCompetencia dispara DEFIS só em fevereiro, ECD só em abril, ECF só em junho — cada uma exatamente 1 vez ao varrer as 12 competências de um ano"
    - "calcularPrazoAnual posiciona o vencimento no ano SEGUINTE ao ano-base e antecipa para dia útil anterior quando cai em fim de semana/feriado"
    - "O enum TipoObrigacao no banco aceita os 11 novos valores (8 mensais + ECD/ECF/DEFIS) após db push"
  artifacts:
    - path: "src/lib/geracao-tarefas-contabil.ts"
      provides: "Catálogo mensal Contábil (8 rotinas, Record<RegimeTributario,...>) + gerarTarefasDoMesContabil"
      exports: ["CATALOGO_OBRIGACOES_CONTABIL", "TITULO_OBRIGACAO_CONTABIL", "gerarTarefasDoMesContabil", "TipoObrigacaoContabil"]
      contains: "EXTRATO_BANCARIO"
    - path: "src/lib/geracao-tarefas-contabil-anual.ts"
      provides: "Catálogo anual (ECD/ECF/DEFIS) + obrigacoesAnuaisParaCompetencia + calcularPrazoAnual"
      exports: ["CATALOGO_OBRIGACOES_ANUAIS", "TITULO_OBRIGACAO_ANUAL", "obrigacoesAnuaisParaCompetencia", "calcularPrazoAnual", "TipoObrigacaoAnual"]
      contains: "regimesElegiveis"
    - path: "src/lib/competencia.ts"
      provides: "competenciaAnualSchema (formato YYYY)"
      contains: "competenciaAnualSchema"
    - path: "prisma/schema.prisma"
      provides: "11 novos valores no enum TipoObrigacao"
      contains: "DEFIS"
    - path: "tests/geracao-tarefas-contabil.test.ts"
      provides: "Testes unitários do catálogo mensal Contábil"
    - path: "tests/geracao-tarefas-contabil-anual.test.ts"
      provides: "Testes unitários do catálogo anual + varredura de 12 meses"
  key_links:
    - from: "src/lib/geracao-tarefas-contabil-anual.ts"
      to: "src/lib/dia-util.ts"
      via: "import anticiparParaDiaUtil"
      pattern: "anticiparParaDiaUtil"
    - from: "src/lib/geracao-tarefas-contabil.ts"
      to: "src/lib/competencia.ts"
      via: "import competenciaSchema for defensive validation"
      pattern: "competenciaSchema"
---

<objective>
Construir a camada de cálculo puro do motor Contábil: estender o enum `TipoObrigacao` (Prisma) com os 11 novos valores, criar os dois catálogos puros (mensal por regime e anual por periodicidade), adicionar `competenciaAnualSchema`, e escrever os testes unitários (Wave 0) que cobrem CONT-01 a CONT-05. Nenhuma orquestração transacional nesta plan — isso é a Plan 02.

Purpose: Isolar toda a lógica de domínio determinística (funções puras, sem Prisma/auth/cron) numa camada testável exaustivamente sem banco, espelhando exatamente a separação já usada para Fiscal (`geracao-tarefas.ts`) e DP (`geracao-tarefas-dp.ts`). É a peça da qual a Plan 02 depende.
Output: `prisma/schema.prisma` (enum estendido + db push aplicado), `src/lib/geracao-tarefas-contabil.ts`, `src/lib/geracao-tarefas-contabil-anual.ts`, `src/lib/competencia.ts` (estendido), 2 arquivos de teste verdes.
</objective>

<artifacts_produced>
## Artifacts this phase produces (símbolos NOVOS criados por esta plan)

Novos valores de enum `TipoObrigacao` (em `prisma/schema.prisma`): `EXTRATO_BANCARIO`, `LANCAMENTO_EXTRATOS`, `FOLHA_CONTABIL`, `FISCAL_CONTABIL`, `BAIXA_IMPOSTOS`, `PERDCOMP`, `FORNECEDORES_CLIENTES`, `BALANCO`, `ECD`, `ECF`, `DEFIS`.

Novos símbolos exportados:
- `src/lib/geracao-tarefas-contabil.ts`: type `TipoObrigacaoContabil`, const `ROTINAS_CONTABIL_MENSAL`, const `CATALOGO_OBRIGACOES_CONTABIL`, const `TITULO_OBRIGACAO_CONTABIL`, function `gerarTarefasDoMesContabil`, type `TarefaParaCriarContabil` (ou reuso de `TarefaParaCriar`).
- `src/lib/geracao-tarefas-contabil-anual.ts`: type `TipoObrigacaoAnual`, type `ObrigacaoAnualRegra`, const `CATALOGO_OBRIGACOES_ANUAIS`, const `TITULO_OBRIGACAO_ANUAL`, function `obrigacoesAnuaisParaCompetencia`, function `calcularPrazoAnual`.
- `src/lib/competencia.ts`: const `competenciaAnualSchema`.
- New files: `tests/geracao-tarefas-contabil.test.ts`, `tests/geracao-tarefas-contabil-anual.test.ts`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-CONTEXT.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-RESEARCH.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender enum TipoObrigacao + competenciaAnualSchema</name>
  <files>prisma/schema.prisma, src/lib/competencia.ts</files>
  <read_first>
    - prisma/schema.prisma (enum TipoObrigacao, linhas 36-46 — já estendido uma vez para DP; e model Tarefa linhas 118-143, confirmar que competencia é String? e a constraint @@unique([empresaId, tipoObrigacao, competencia]) já cobre os novos valores)
    - src/lib/competencia.ts (competenciaSchema existente, linhas 19-21 — replicar o padrão de regex estrito)
  </read_first>
  <action>
    Adicionar ao bloco `enum TipoObrigacao` (após o último valor atual `INSS`, sem remover nenhum existente) exatamente estes 11 valores, na ordem: `EXTRATO_BANCARIO`, `LANCAMENTO_EXTRATOS`, `FOLHA_CONTABIL`, `FISCAL_CONTABIL`, `BAIXA_IMPOSTOS`, `PERDCOMP`, `FORNECEDORES_CLIENTES`, `BALANCO`, `ECD`, `ECF`, `DEFIS` (nomes confirmados em D-05/A2 do RESEARCH.md — distintos de `FOLHA`/`FISCAL` já usados por DP/Fiscal para evitar colisão). Nenhuma outra mudança de schema: `Tarefa.competencia` permanece `String?` e a constraint `@@unique([empresaId, tipoObrigacao, competencia])` já cobre os novos valores automaticamente (mudança puramente aditiva).

    Em `src/lib/competencia.ts`, adicionar a const exportada `competenciaAnualSchema` usando `z.string().regex(/^\d{4}$/, "Competência anual deve estar no formato YYYY")` — irmã de `competenciaSchema` (que valida `"YYYY-MM"`). Não alterar `competenciaSchema` nem `competenciaAtual`.
  </action>
  <verify>
    <automated>npx prisma validate && npx vitest run tests/competencia.test.ts 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEFIS\|ECD\|ECF\|EXTRATO_BANCARIO\|BALANCO" prisma/schema.prisma` retorna ao menos 5 (os 11 valores presentes no enum)
    - `npx prisma validate` sai com código 0 (schema sintaticamente válido)
    - `src/lib/competencia.ts` exporta `competenciaAnualSchema` e `competenciaAnualSchema.safeParse("2026").success === true` e `competenciaAnualSchema.safeParse("2026-01").success === false`
    - `competenciaSchema` permanece inalterado (ainda valida "2026-01" como sucesso e "2026" como falha)
  </acceptance_criteria>
  <done>Enum TipoObrigacao contém os 11 novos valores, schema valida, e competenciaAnualSchema valida formato "YYYY".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Catálogos puros Contábil (mensal + anual) com testes Wave 0</name>
  <files>src/lib/geracao-tarefas-contabil.ts, src/lib/geracao-tarefas-contabil-anual.ts, tests/geracao-tarefas-contabil.test.ts, tests/geracao-tarefas-contabil-anual.test.ts</files>
  <read_first>
    - src/lib/geracao-tarefas.ts (catálogo Fiscal — análogo EXATO para o mensal Contábil: `Record<RegimeTributario, ObrigacaoRegra[]>`, `gerarTarefasDoMes`, uso de `calcularPrazoBaseDiaFixo` + `anticiparParaDiaUtil`, `TITULO_OBRIGACAO`, `TarefaParaCriar`)
    - src/lib/geracao-tarefas-dp.ts (padrão de validação defensiva via `competenciaSchema.safeParse` + `throw new Error` nas linhas 64-78 — adotar no mensal Contábil)
    - src/lib/dia-util.ts (anticiparParaDiaUtil + calcularPrazoBaseDiaFixo — reusar verbatim, sem modificar)
    - src/lib/competencia.ts (competenciaSchema e o competenciaAnualSchema criado na Task 1)
    - tests/geracao-tarefas-dp.test.ts (estrutura de teste de função pura a espelhar)
    - .planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-RESEARCH.md (Pattern 1, Pattern 2, Code Examples — a função anual já foi desenhada e validada por execução local; usar verbatim)
    - .planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-PATTERNS.md (Pattern Assignments para os dois arquivos)
  </read_first>
  <action>
    Criar `src/lib/geracao-tarefas-contabil.ts` (mensal, análogo ao Fiscal NÃO ao DP — varia por regime): exportar type `TipoObrigacaoContabil` (union dos 8 nomes da Task 1: EXTRATO_BANCARIO, LANCAMENTO_EXTRATOS, FOLHA_CONTABIL, FISCAL_CONTABIL, BAIXA_IMPOSTOS, PERDCOMP, FORNECEDORES_CLIENTES, BALANCO); definir a const `ROTINAS_CONTABIL_MENSAL` com as 8 entradas `{ tipo, diaBase }` nos dias-base de D-02 (EXTRATO_BANCARIO=1, LANCAMENTO_EXTRATOS=10, FOLHA_CONTABIL=14, FISCAL_CONTABIL=17, BAIXA_IMPOSTOS=22, PERDCOMP=22, FORNECEDORES_CLIENTES=25, BALANCO=28); exportar `CATALOGO_OBRIGACOES_CONTABIL: Record<RegimeTributario, ObrigacaoRegraContabil[]>` com `LUCRO_REAL: ROTINAS_CONTABIL_MENSAL`, `LUCRO_PRESUMIDO: ROTINAS_CONTABIL_MENSAL` (D-04: mesmas datas) e `SIMPLES_NACIONAL: []` (D-03: zero rotinas); exportar `TITULO_OBRIGACAO_CONTABIL: Record<TipoObrigacaoContabil, string>` com rótulos legíveis em pt-BR (ex.: "Extrato Bancário", "Lançamento de Extratos", "Folha (Contábil)", "Fiscal (Contábil)", "Baixa de Impostos", "PERDCOMP", "Fornecedores e Clientes", "Balanço"); exportar `gerarTarefasDoMesContabil(empresas: { id; regimeTributario; responsavelId }[], competencia: string): TarefaParaCriar[]` espelhando `gerarTarefasDoMes` mas com validação defensiva no topo `if (!competenciaSchema.safeParse(competencia).success) throw new Error(...)` (padrão DP). O prazo de cada rotina é `anticiparParaDiaUtil(calcularPrazoBaseDiaFixo(competencia, regra.diaBase))`. O título segue o padrão `${TITULO_OBRIGACAO_CONTABIL[regra.tipo]} — ${nomeMes}/${ano}`.

    Criar `src/lib/geracao-tarefas-contabil-anual.ts` (periodicidade nova) usando o Code Example do RESEARCH.md verbatim: exportar type `TipoObrigacaoAnual = "DEFIS" | "ECD" | "ECF"`; `TITULO_OBRIGACAO_ANUAL: Record<TipoObrigacaoAnual, string>` (DEFIS="DEFIS", ECD="ECD (Escrituração Contábil Digital)", ECF="ECF (Escrituração Contábil Fiscal)"); type `ObrigacaoAnualRegra = { tipo; mesCriacao; mesVencimento; diaVencimento; regimesElegiveis: RegimeTributario[] }`; const `CATALOGO_OBRIGACOES_ANUAIS` com DEFIS(mesCriacao=2, mesVencimento=3, diaVencimento=31, regimesElegiveis=["SIMPLES_NACIONAL"]), ECD(mesCriacao=4, mesVencimento=5, diaVencimento=31, regimesElegiveis=["LUCRO_REAL","LUCRO_PRESUMIDO"]), ECF(mesCriacao=6, mesVencimento=7, diaVencimento=31, regimesElegiveis=["LUCRO_REAL","LUCRO_PRESUMIDO"]) — D-06/D-07; `obrigacoesAnuaisParaCompetencia(competencia: string)` que parseia `[anoAtual, mesAtual]` de `competencia.split("-").map(Number)` (NUNCA `new Date()` — Pitfall determinismo), filtra `CATALOGO_OBRIGACOES_ANUAIS` por `regra.mesCriacao === mesAtual` e mapeia para `{ regra, competenciaAnual: String(anoAtual), anoVencimento: anoAtual + 1 }` (Pitfall 2: SEMPRE ano seguinte); `calcularPrazoAnual(anoVencimento, mesVencimento, diaVencimento)` que faz `anticiparParaDiaUtil(new Date(anoVencimento, mesVencimento - 1, diaVencimento))`. Validar a competência mensal recebida com `competenciaSchema.safeParse` no topo de `obrigacoesAnuaisParaCompetencia` (a entrada é "YYYY-MM" — o mês de execução), lançando Error se inválida.

    Criar `tests/geracao-tarefas-contabil.test.ts` (mirror de tests/geracao-tarefas-dp.test.ts): (a) LUCRO_REAL e LUCRO_PRESUMIDO geram exatamente 8 tarefas cada, com os tipos e dias-base de D-02; (b) SIMPLES_NACIONAL gera 0 tarefas; (c) o prazo de cada rotina é dia útil (nunca sábado/domingo/feriado); (d) competência inválida "2026-1" lança Error.

    Criar `tests/geracao-tarefas-contabil-anual.test.ts` usando o padrão de varredura de 12 meses do Code Examples do RESEARCH.md: (a) varrendo as 12 competências de 2026, cada obrigação anual dispara exatamente 1 vez (`{ DEFIS: 1, ECD: 1, ECF: 1 }`); (b) DEFIS dispara só em fevereiro, ECD só em abril, ECF só em junho; (c) para competência "2026-04" (ECD), `competenciaAnual === "2026"` e `anoVencimento === 2027` (Pitfall 2); (d) `calcularPrazoAnual(2027, 7, 31)` (ECF) cai em sexta 30/07/2027 por antecipação de fim de semana (caso verificado no RESEARCH.md), e `calcularPrazoAnual(2027, 3, 31)` (DEFIS) permanece 31/03/2027 (quarta, sem ajuste); (e) DEFIS tem `regimesElegiveis` = só SIMPLES_NACIONAL, ECD/ECF = LUCRO_REAL+LUCRO_PRESUMIDO (Pitfall 3).
  </action>
  <verify>
    <automated>npx vitest run tests/geracao-tarefas-contabil.test.ts tests/geracao-tarefas-contabil-anual.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/geracao-tarefas-contabil.test.ts tests/geracao-tarefas-contabil-anual.test.ts` passa com todos os testes verdes
    - `gerarTarefasDoMesContabil([{id,regimeTributario:"LUCRO_REAL",responsavelId}], "2026-03")` retorna array de length 8; com `regimeTributario:"SIMPLES_NACIONAL"` retorna length 0
    - `obrigacoesAnuaisParaCompetencia("2026-02").map(r=>r.regra.tipo)` === ["DEFIS"]; `obrigacoesAnuaisParaCompetencia("2026-01")` === []
    - `obrigacoesAnuaisParaCompetencia("2026-04")[0].anoVencimento === 2027` e `[0].competenciaAnual === "2026"`
    - Nenhum dos dois novos arquivos lê `new Date()` / `Date.now()` dentro das funções de cálculo (determinismo): `grep -nE "new Date\(\)|Date.now" src/lib/geracao-tarefas-contabil.ts src/lib/geracao-tarefas-contabil-anual.ts` retorna 0 ocorrências (o `new Date(ano,mes,1)` de 3 args para nomeMes é permitido; a proibição é só o construtor sem args / Date.now)
  </acceptance_criteria>
  <done>Os dois catálogos puros existem, exportam os símbolos listados em artifacts_produced, e ambos os arquivos de teste passam verdes cobrindo CONT-01 a CONT-05.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Aplicar schema push no banco</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (confirmar que o enum estendido na Task 1 está salvo antes de empurrar)
    - .planning/STATE.md (decisão [Phase 02-01]/[03-01]: ambiente Neon sem shadow database usa `prisma db push`, não `migrate dev`)
  </read_first>
  <action>
    Executar `npx prisma db push` para sincronizar os 11 novos valores do enum `TipoObrigacao` com o banco Neon. A mudança é puramente ADITIVA (adicionar valores a um enum não remove nem altera dados existentes), então `prisma db push` simples basta — NÃO usar `--accept-data-loss` salvo se o Prisma reportar mudança destrutiva inesperada (não deve ocorrer). Em seguida rodar `npx prisma generate` para regenerar o `@prisma/client` com os novos enum members (necessário para que `tsc` e os blocos da Plan 02 enxerguem ECD/ECF/DEFIS etc.).

    Esta task é OBRIGATÓRIA e BLOQUEANTE: build e tsc passariam mesmo sem o push (os tipos vêm do schema/config, não do banco vivo), criando um falso-positivo de verificação — a geração real de tarefas Contábil falharia em runtime ao tentar inserir um enum value que o banco não conhece.
  </action>
  <verify>
    <automated>npx prisma db push && npx prisma generate && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` completa sem erro e reporta o schema em sincronia com o banco
    - `npx prisma generate` regenera o client sem erro
    - Após generate, o tipo `TipoObrigacao` importável de `@prisma/client` inclui os 11 novos membros (verificável: `npx tsc --noEmit` passa quando código referencia `"DEFIS" satisfies TipoObrigacao` ou similar)
  </acceptance_criteria>
  <done>Banco Neon sincronizado com o enum estendido; client Prisma regenerado; tsc verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| competência (string) → função pura de geração | Entrada textual que vira chave de idempotência; formato não canônico quebra a constraint @@unique |
| schema.prisma → banco Neon | Migração de enum; valor não aplicado no banco causa falha de runtime na inserção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `gerarTarefasDoMesContabil` / `obrigacoesAnuaisParaCompetencia` (competência não canônica) | mitigate | Validar a competência recebida com `competenciaSchema.safeParse` (mensal) no topo das funções puras, lançando Error em formato inválido — mesmo padrão de `gerarTarefasDoMesDp` (V5 Input Validation) |
| T-07-02 | Tampering | enum TipoObrigacao no banco | mitigate | Task 3 [BLOCKING] aplica `prisma db push`; sem ela a inserção de ECD/ECF/DEFIS falharia em runtime (corrupção/erro de gravação) |
| T-07-03 | Tampering | colisão de competência anual "YYYY" com mensal "YYYY-MM" sob mesmo tipoObrigacao | accept | Os 11 enum values são todos distintos entre si e dos existentes; a chave composta @@unique(empresaId, tipoObrigacao, competencia) já desambigua — sem colisão possível (formato "YYYY" 4 chars vs "YYYY-MM" 7 chars com hífen) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH.md Package Legitimacy Audit: N/A — date-fns/date-holidays/@prisma/client já em uso produtivo) |
</threat_model>

<verification>
- `npx vitest run tests/geracao-tarefas-contabil.test.ts tests/geracao-tarefas-contabil-anual.test.ts` verde (CONT-01 a CONT-05)
- `npx prisma validate` + `npx prisma db push` + `npx prisma generate` sem erro
- `npx tsc --noEmit` verde após generate
- `npm test` (suite completa) verde — confirma que Fiscal/DP/IDOR existentes não regridem
</verification>

<success_criteria>
- Enum TipoObrigacao no banco contém os 11 novos valores (db push aplicado)
- `gerarTarefasDoMesContabil` gera 8 rotinas para LUCRO_REAL/PRESUMIDO, 0 para SIMPLES_NACIONAL
- `obrigacoesAnuaisParaCompetencia` dispara DEFIS/ECD/ECF exatamente 1x por ano, no mês de criação correto, com anoVencimento = ano-base + 1
- `competenciaAnualSchema` valida "YYYY" e rejeita "YYYY-MM"
- Ambos os arquivos de teste novos verdes
</success_criteria>

<output>
Create `.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-01-SUMMARY.md` when done
</output>

---
phase: 07-motor-de-gera-o-cont-bil-mensal-e-anual
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/modules/tarefas/geracao.ts
  - src/app/(app)/tarefas/actions.ts
  - src/app/(app)/tarefas/gerar-tarefas-button.tsx
  - tests/geracao.idempotencia.test.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05]
must_haves:
  truths:
    - "executarGeracaoMensal gera, na mesma transação, as 8 rotinas mensais Contábil para empresas LUCRO_REAL/PRESUMIDO com responsável CONTABIL"
    - "Em fevereiro/abril/junho, executarGeracaoMensal também cria as obrigações anuais DEFIS/ECD/ECF para empresas elegíveis pelo regime, sem duplicar em reexecução"
    - "Empresa sem responsável CONTABIL é pulada (mensal e anual) e listada uma única vez em semResponsavelContabil, nunca bloqueando outras empresas ou os eixos Fiscal/DP"
    - "semResponsavelContabil chega ponta a ponta do motor até o toast na UI"
  artifacts:
    - path: "src/modules/tarefas/geracao.ts"
      provides: "Blocos Contábil mensal + anual na mesma transação; retorno estendido com semResponsavelContabil"
      contains: "semResponsavelContabil"
    - path: "src/app/(app)/tarefas/actions.ts"
      provides: "AcaoGeracaoResult e gerarTarefasDoMesAction propagando semResponsavelContabil"
      contains: "semResponsavelContabil"
    - path: "src/app/(app)/tarefas/gerar-tarefas-button.tsx"
      provides: "Toast de aviso para empresas sem responsável Contábil"
      contains: "semResponsavelContabil"
    - path: "tests/geracao.idempotencia.test.ts"
      provides: "Casos de integração Contábil mensal+anual: geração, pular-e-listar, idempotência anual"
  key_links:
    - from: "src/modules/tarefas/geracao.ts"
      to: "src/lib/geracao-tarefas-contabil.ts"
      via: "import gerarTarefasDoMesContabil"
      pattern: "gerarTarefasDoMesContabil"
    - from: "src/modules/tarefas/geracao.ts"
      to: "src/lib/geracao-tarefas-contabil-anual.ts"
      via: "import obrigacoesAnuaisParaCompetencia / calcularPrazoAnual"
      pattern: "obrigacoesAnuaisParaCompetencia"
    - from: "src/modules/tarefas/geracao.ts"
      to: "EmpresaResponsavelSetor (setor CONTABIL)"
      via: "responsaveisPorSetor where setor CONTABIL"
      pattern: "setor:\\s*\"CONTABIL\""
---

<objective>
Estender `executarGeracaoMensal` com o terceiro bloco (Contábil mensal, regime-based) e o quarto bloco (Contábil anual, condicional ao mês), ambos dentro da MESMA transação já existente, seguindo literalmente o molde do bloco DP da Fase 6. Propagar o novo campo `semResponsavelContabil` ponta a ponta (motor → Server Action → toast na UI) e estender os testes de integração de idempotência.

Purpose: Conectar a camada de cálculo puro (Plan 01) ao motor transacional real, fechando CONT-01 a CONT-05 com geração efetiva no banco. Preserva o invariante "uma execução mensal, uma transação, snapshot+geração sobem ou caem juntos" e a idempotência via constraint @@unique (nunca um segundo mecanismo de controle).
Output: `geracao.ts` com 2 blocos novos + retorno estendido, `actions.ts` e `gerar-tarefas-button.tsx` propagando `semResponsavelContabil`, casos novos em `geracao.idempotencia.test.ts` verdes.
</objective>

<artifacts_produced>
## Artifacts this phase produces (símbolos NOVOS criados por esta plan)

- `src/modules/tarefas/geracao.ts`: novo campo de retorno `semResponsavelContabil: { empresaId: string; nome: string }[]` em `executarGeracaoMensal`; variáveis locais `empresasContabil`, `comResponsavelContabil`, `semResponsavelContabil`, `tarefasContabilMensal`, `tarefasContabilAnual`, `regrasAnuais`.
- `src/app/(app)/tarefas/actions.ts`: novo campo `semResponsavelContabil` no membro `ok: true` de `AcaoGeracaoResult`.
- `src/app/(app)/tarefas/gerar-tarefas-button.tsx`: novo bloco de `toast.warning` para `semResponsavelContabil`.

Símbolos consumidos (criados na Plan 07-01, NÃO drift): `gerarTarefasDoMesContabil`, `CATALOGO_OBRIGACOES_CONTABIL`, `TITULO_OBRIGACAO_CONTABIL`, `obrigacoesAnuaisParaCompetencia`, `calcularPrazoAnual`, `TITULO_OBRIGACAO_ANUAL`, `TipoObrigacaoAnual`, e os 11 enum values novos de `TipoObrigacao`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-CONTEXT.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-RESEARCH.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-PATTERNS.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-01-SUMMARY.md
@src/modules/tarefas/geracao.ts
@src/lib/geracao-tarefas-dp.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Blocos Contábil mensal + anual em executarGeracaoMensal</name>
  <files>src/modules/tarefas/geracao.ts</files>
  <read_first>
    - src/modules/tarefas/geracao.ts (estado atual — o bloco DP da Fase 6 nas linhas 91-120 é o molde EXATO a replicar; o tipo de retorno nas linhas 64-68; o merge + createMany nas linhas 122-141)
    - src/lib/geracao-tarefas-contabil.ts (gerarTarefasDoMesContabil — assinatura `(empresas: {id, regimeTributario, responsavelId}[], competencia) => TarefaParaCriar[]`, criado na Plan 01)
    - src/lib/geracao-tarefas-contabil-anual.ts (obrigacoesAnuaisParaCompetencia, calcularPrazoAnual, TITULO_OBRIGACAO_ANUAL, CATALOGO_OBRIGACOES_ANUAIS — criados na Plan 01)
    - .planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-RESEARCH.md (Pattern 3 — código verbatim dos dois blocos; Pitfall 3 — filtrar por regra.regimesElegiveis dinâmico, nunca hardcoded; Pitfall 4 — deduplicar semResponsavelContabil por empresaId)
    - .planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-PATTERNS.md (seção geracao.ts — imports a adicionar e merge/persist pattern)
  </read_first>
  <action>
    Adicionar os imports no topo: `import { gerarTarefasDoMesContabil } from "@/lib/geracao-tarefas-contabil";` e `import { obrigacoesAnuaisParaCompetencia, calcularPrazoAnual, TITULO_OBRIGACAO_ANUAL } from "@/lib/geracao-tarefas-contabil-anual";`.

    Estender a assinatura de retorno de `executarGeracaoMensal` para incluir `semResponsavelContabil: { empresaId: string; nome: string }[]` ao lado de `criadas`, `puladas`, `semResponsavelDp`.

    Bloco Contábil MENSAL (após o bloco DP, dentro da mesma `tx`): `tx.empresa.findMany({ where: { ativo: true, regimeTributario: { in: ["LUCRO_REAL", "LUCRO_PRESUMIDO"] } }, select: { id, nome, regimeTributario, responsaveisPorSetor: { where: { setor: "CONTABIL" }, select: { usuarioId: true } } } })` — D-03 (SIMPLES_NACIONAL excluído) + filtro setor CONTABIL OBRIGATÓRIO (Pitfall 2 da Fase 6, replicado). Separar em `comResponsavelContabil` (responsaveisPorSetor.length > 0) e a sublista `semResponsavelContabil` (length === 0, mapeada para `{empresaId, nome}`) — D-11 pular e listar, nunca throw. Chamar `gerarTarefasDoMesContabil(comResponsavelContabil.map(e => ({ id: e.id, regimeTributario: e.regimeTributario, responsavelId: e.responsaveisPorSetor[0].usuarioId })), competencia)` → `tarefasContabilMensal`.

    Bloco Contábil ANUAL (após o mensal, mesma `tx`): `const regrasAnuais = obrigacoesAnuaisParaCompetencia(competencia)`. Inicializar `let tarefasContabilAnual: TarefaParaCriar[] = []` e um acumulador de empresas-sem-responsável anuais. Para cada `{ regra, competenciaAnual, anoVencimento }` em `regrasAnuais`: consultar `tx.empresa.findMany({ where: { ativo: true, regimeTributario: { in: regra.regimesElegiveis } }, select: { id, nome, responsaveisPorSetor: { where: { setor: "CONTABIL" }, select: { usuarioId } } } })` — CRÍTICO: usar `regra.regimesElegiveis` DINAMICAMENTE (Pitfall 3 — DEFIS=SIMPLES_NACIONAL é o inverso de ECD/ECF; nunca reusar o filtro hardcoded do bloco mensal). Para cada empresa COM responsável, acrescentar a `tarefasContabilAnual` um objeto `{ empresaId, responsavelId: responsaveisPorSetor[0].usuarioId, titulo: \`${TITULO_OBRIGACAO_ANUAL[regra.tipo]} — ${competenciaAnual}\`, tipoObrigacao: regra.tipo, competencia: competenciaAnual, prazo: calcularPrazoAnual(anoVencimento, regra.mesVencimento, regra.diaVencimento) }`. Empresas SEM responsável vão para o mesmo acumulador de `semResponsavelContabil`.

    Deduplicar `semResponsavelContabil` por `empresaId` (Map/Set) antes do retorno (Pitfall 4) — unir as ocorrências do bloco mensal e dos blocos anuais disparados naquele mês, listando cada empresa uma única vez.

    Estender o merge: `const tarefas = [...tarefasFiscal, ...tarefasDp, ...tarefasContabilMensal, ...tarefasContabilAnual];`. O `createMany({ skipDuplicates: true })` permanece inalterado (idempotência via @@unique cobre os novos tipos e o formato "YYYY"). Adicionar `semResponsavelContabil` (deduplicado) aos DOIS pontos de retorno (o early-return quando tarefas.length === 0 e o retorno final).
  </action>
  <verify>
    <automated>npx vitest run tests/geracao.idempotencia.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'setor:\s*"CONTABIL"' src/modules/tarefas/geracao.ts` retorna ao menos 2 (bloco mensal + bloco anual)
    - `grep -c "regra.regimesElegiveis" src/modules/tarefas/geracao.ts` retorna ao menos 1 (filtro anual dinâmico, Pitfall 3)
    - `executarGeracaoMensal` retorna objeto com chave `semResponsavelContabil` em AMBOS os caminhos de return (early-return e final)
    - Existe deduplicação por empresaId aplicada a semResponsavelContabil antes do retorno (Map/Set sobre empresaId)
    - O loop Fiscal (`empresa.responsavelId`) e o bloco DP (`setor: "DP"`) permanecem inalterados em comportamento
    - `npx tsc --noEmit` verde
  </acceptance_criteria>
  <done>executarGeracaoMensal gera Contábil mensal + anual na mesma transação, pula e lista empresas sem responsável CONTABIL (deduplicado), e retorna semResponsavelContabil.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagar semResponsavelContabil até a UI + estender testes de integração</name>
  <files>src/app/(app)/tarefas/actions.ts, src/app/(app)/tarefas/gerar-tarefas-button.tsx, tests/geracao.idempotencia.test.ts</files>
  <read_first>
    - src/app/(app)/tarefas/actions.ts (AcaoGeracaoResult linhas 14-21; gerarTarefasDoMesAction linhas 289-320, destruturação e retorno do resultado de executarGeracaoMensal)
    - src/app/(app)/tarefas/gerar-tarefas-button.tsx (bloco de toast de semResponsavelDp nas linhas 31-41 — molde EXATO para o aviso Contábil)
    - tests/geracao.idempotencia.test.ts (mock de @/lib/db nas linhas 11-40, beforeEach 42-56, e os casos DP existentes — a estrutura empresaFindManyMock retorna por chamada na ordem dos findMany dentro da transação)
    - .planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-RESEARCH.md (Pitfall 5 — grep todos os call sites de executarGeracaoMensal; Validation Architecture — casos a cobrir)
    - .planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-PATTERNS.md (seção actions.ts — type e action body a estender)
  </read_first>
  <action>
    Em `actions.ts`: estender o membro `ok: true` de `AcaoGeracaoResult` com `semResponsavelContabil: { empresaId: string; nome: string }[]` (ao lado de `semResponsavelDp`). Em `gerarTarefasDoMesAction`, estender a destruturação `const { criadas, puladas, semResponsavelDp, semResponsavelContabil } = await executarGeracaoMensal(...)` e o retorno `return { ok: true, criadas, puladas, semResponsavelDp, semResponsavelContabil }`.

    Em `gerar-tarefas-button.tsx`: após o bloco de toast.warning de `semResponsavelDp`, adicionar um bloco análogo para `resultado.semResponsavelContabil` — se `length > 0`, `toast.warning` listando os nomes e instruindo o DONO a atribuir um responsável Contábil na tela de Empresas (texto adaptado: "empresa(s) Lucro Real/Presumido sem responsável Contábil atribuído"). Espelhar exatamente a forma do bloco DP existente (linhas 36-41).

    Em `tests/geracao.idempotencia.test.ts`: estender o mock de `@/lib/db` para suportar as queries adicionais de empresa do bloco Contábil (mensal e anual) — o `empresaFindManyMock` é chamado múltiplas vezes dentro da transação (Fiscal, DP, Contábil mensal, e 0+ vezes por obrigação anual); usar `mockResolvedValueOnce` em sequência ou um mock que discrimine por argumentos `where`. Adicionar os casos: (a) competência normal (ex.: "2026-03", mês sem obrigação anual) — empresa LUCRO_REAL com responsável CONTABIL gera as 8 rotinas mensais Contábil; (b) empresa LUCRO_REAL/PRESUMIDO sem responsável CONTABIL é pulada e aparece UMA vez em semResponsavelContabil, sem bloquear Fiscal/DP; (c) competência "2026-04" (mês de criação ECD) — empresas elegíveis recebem a tarefa anual ECD com competencia "2026" e prazo em 2027; empresa SIMPLES_NACIONAL NÃO recebe ECD (Pitfall 3); (d) segunda execução da mesma competência (mensal e anual) não cria nenhuma tarefa nova (skipDuplicates → criadas=0). Após editar, rodar grep nos call sites de `executarGeracaoMensal(` (gerarTarefasDoMesAction, geracao.idempotencia.test.ts, geracao.actions.test.ts) confirmando que nenhum quebra com o campo novo (Pitfall 5).
  </action>
  <verify>
    <automated>npx vitest run tests/geracao.idempotencia.test.ts tests/geracao.actions.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "semResponsavelContabil" src/app/(app)/tarefas/actions.ts` retorna ao menos 3 (tipo + destruturação + retorno)
    - `grep -c "semResponsavelContabil" src/app/(app)/tarefas/gerar-tarefas-button.tsx` retorna ao menos 1 (bloco de toast)
    - `npx vitest run tests/geracao.idempotencia.test.ts` passa, incluindo o caso de empresa sem responsável CONTABIL listada exatamente 1x e o caso ECD em abril com SIMPLES_NACIONAL excluída
    - `npx vitest run tests/geracao.actions.test.ts` continua verde (call site existente não regride — Pitfall 5)
    - `npx tsc --noEmit` verde (campo novo propagado ponta a ponta, sem erro de tipo)
  </acceptance_criteria>
  <done>semResponsavelContabil chega do motor até o toast na UI; testes de integração cobrem geração mensal, pular-e-listar deduplicado, disparo anual em abril e idempotência de reexecução.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron / botão DONO → executarGeracaoMensal | Geração sem sessão (cron) — deliberadamente sem escopo de autorização; o responsável correto vem da query por setor |
| empresa.responsaveisPorSetor → tarefa.responsavelId | Filtro de setor errado atribui tarefa Contábil ao responsável Fiscal/DP |
| competência → blocos de geração | String que vira chave de idempotência mensal e ano-base anual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CONT-01 | Tampering | bloco Contábil (mensal e anual) — atribuição de responsável | mitigate | Filtrar `responsaveisPorSetor` por `where: { setor: "CONTABIL" }` em TODAS as queries do bloco (V4 Access Control); replica a mitigação validada na Fase 6 para DP — nunca ler `empresa.responsavelId` legado |
| T-CONT-02 | Denial of Service | executarGeracaoMensal (empresa sem responsável CONTABIL) | mitigate | "Pular e listar" por empresa (D-11), nunca abortar a transação — uma empresa sem responsável não bloqueia Fiscal/DP nem demais empresas Contábil |
| T-CONT-03 | Tampering | bloco anual aplicado ao regime errado (DEFIS a Lucro Real, ECD/ECF a Simples) | mitigate | Query de empresas elegíveis lê `regra.regimesElegiveis` dinamicamente (Pitfall 3), nunca filtro hardcoded compartilhado entre as 3 obrigações; teste explícito de SIMPLES_NACIONAL excluída de ECD |
| T-CONT-04 | Spoofing/Elevation | cron sem autenticação dispara geração | accept | Invariante já documentado desde a Fase 3: executarGeracaoMensal é deliberadamente sem escopo de sessão por design (cron não tem usuário); inalterado nesta fase. O gatilho manual `gerarTarefasDoMesAction` mantém o guard `role === "DONO"` |
| T-CONT-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run tests/geracao.idempotencia.test.ts tests/geracao.actions.test.ts` verde
- `npx tsc --noEmit` verde
- `npm test` (suite completa) verde — confirma que Fiscal/DP/IDOR/dashboards existentes não regridem após estender o motor
- grep manual: todos os call sites de `executarGeracaoMensal(` aceitam o retorno estendido sem erro (Pitfall 5)
</verification>

<success_criteria>
- executarGeracaoMensal gera Contábil mensal (8 rotinas, LUCRO_REAL/PRESUMIDO, responsável CONTABIL) e anual (DEFIS/ECD/ECF no mês de criação correto) na mesma transação
- Empresa sem responsável CONTABIL é pulada e listada uma única vez (deduplicada) sem bloquear nada
- Reexecução da mesma competência não duplica tarefas (mensal nem anual)
- semResponsavelContabil chega do motor até o toast na UI
- Suite completa verde (sem regressão Fiscal/DP)
</success_criteria>

<output>
Create `.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-02-SUMMARY.md` when done
</output>

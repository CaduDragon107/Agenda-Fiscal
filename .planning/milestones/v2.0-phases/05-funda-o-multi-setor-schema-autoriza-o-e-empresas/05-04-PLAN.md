---
phase: 05-funda-o-multi-setor-schema-autoriza-o-e-empresas
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/app/(app)/empresas/empresa-form.tsx
  - src/app/(app)/empresas/novo/page.tsx
  - src/app/(app)/empresas/[id]/editar/page.tsx
  - src/app/(app)/empresas/empresas-table.tsx
  - src/app/(app)/empresas/page.tsx
  - src/app/(app)/empresas/derive-rows.ts
  - tests/empresas.derive-rows.test.ts
autonomous: false
requirements: [SETOR-01, SETOR-03, EMPR-03]
user_setup: []
must_haves:
  truths:
    - "O form de empresa expõe 3 seletores de responsável (Fiscal/DP/Contábil) + checkbox 'Tem funcionários CLT?'"
    - "Cada seletor lista apenas colaboradores do seu setor (SETOR-03); DP/Contábil têm opção 'Sem responsável'"
    - "Quando não-DONO, os 3 seletores ficam disabled (UX); enforcement real é server-side (Plan 03)"
    - "DONO vê 3 colunas de responsável na tabela; colaborador vê só a coluna do seu setor (D-10)"
    - "Para viewer não-DONO, deriveEmpresaRows omite (null) os responsáveis dos outros setores NO DATA LAYER — nomes/ids cross-setor nunca entram no EmpresaRow/payload RSC/HTML (D-10, fronteira de segurança)"
    - "Filtro 'Sem responsável DP/Contábil' (DONO-only) e badge âmbar 'Sem responsável' nas células (D-03)"
    - "Colaborador DP/Contábil sem empresas atribuídas vê mensagem de estado vazio explicativa (D-09)"
  artifacts:
    - path: "src/app/(app)/empresas/empresa-form.tsx"
      provides: "3 Selects de responsável por setor + Checkbox CLT"
      contains: "responsavelFiscalId"
    - path: "src/app/(app)/empresas/empresas-table.tsx"
      provides: "colunas setor-aware + filtro/badge sem responsável + estado vazio por setor"
      contains: "Sem responsavel"
    - path: "src/app/(app)/empresas/page.tsx"
      provides: "wiring de 3 listas de responsáveis por setor + setor do usuário para a tabela; chama deriveEmpresaRows"
    - path: "src/app/(app)/empresas/derive-rows.ts"
      provides: "deriveEmpresaRows — derivação setor-aware com omissão cross-setor para não-DONO (fronteira de segurança D-10)"
      contains: "deriveEmpresaRows"
    - path: "tests/empresas.derive-rows.test.ts"
      provides: "regressão automatizada da omissão cross-setor (anti-leak scan)"
      contains: "deriveEmpresaRows"
  key_links:
    - from: "src/app/(app)/empresas/page.tsx"
      to: "src/modules/empresas/queries.ts"
      via: "listarResponsaveis por setor"
      pattern: "listarResponsaveis"
    - from: "src/app/(app)/empresas/page.tsx"
      to: "src/app/(app)/empresas/derive-rows.ts"
      via: "deriveEmpresaRows(empresas, role, setor) omite responsáveis cross-setor antes de passar rows à tabela (não-DONO)"
      pattern: "deriveEmpresaRows"
    - from: "src/app/(app)/empresas/empresa-form.tsx"
      to: "src/app/(app)/actions.ts"
      via: "FormData com responsavelFiscalId/DpId/ContabilId + temFuncionariosClt"
      pattern: "responsavelDpId|temFuncionariosClt"
---

<objective>
Estender a UI de empresas para o modelo multi-setor: o form ganha 3 seletores de responsável (Fiscal/DP/Contábil, filtrados por setor, disabled para não-DONO) e o checkbox "Tem funcionários CLT?"; a tabela ganha colunas de responsável setor-aware (DONO vê 3, colaborador vê só a sua), o filtro/badge "Sem responsável" (DONO-only) e o estado vazio explicativo por setor; as páginas (lista/novo/editar) passam as 3 listas de responsáveis e o setor do usuário.

Purpose: Fecha a fase tornando o modelo de dados visível e editável (Success Criteria 2, 3, 5). Sem esta camada a UI fica incoerente com o backend (RESEARCH.md Anti-Pattern: a UI de 3 seletores e o write do junction devem chegar juntos). Inclui o checkpoint humano de verificação visual dos comportamentos D-09/D-10, que não têm infra de teste de componente.
Output: form + tabela + páginas estendidos; verificação humana dos estados por setor.
</objective>

<execution_context>
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-CONTEXT.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 seletores de responsável + checkbox CLT no empresa-form e wiring das páginas novo/editar</name>
  <files>src/app/(app)/empresas/empresa-form.tsx, src/app/(app)/empresas/novo/page.tsx, src/app/(app)/empresas/[id]/editar/page.tsx</files>
  <read_first>
    - src/app/(app)/empresas/empresa-form.tsx (form atual — campo único responsavelId, grid 2-col, padrão Select, defaultValues, onSubmit montando FormData)
    - src/app/(app)/empresas/novo/page.tsx (wiring atual — listarResponsaveis sem filtro)
    - src/app/(app)/empresas/[id]/editar/page.tsx (wiring de edição — buscarEmpresaPorId, mapeamento do prop empresa)
    - src/app/(app)/empresas/importar/_components/StepReview.tsx (único uso de Checkbox no codebase — padrão checked/onCheckedChange a copiar)
    - src/modules/empresas/schema.ts (já estendido no Plan 03 — EmpresaInput com 3 campos + temFuncionariosClt)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-UI-SPEC.md (seção "Form layout placement" — ordem Fiscal/DP/Contábil, placeholders, disabled não-DONO, checkbox CLT + helper text)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md (seção empresa-form.tsx)
  </read_first>
  <action>
    empresa-form.tsx: estender `EmpresaFormProps` para receber `responsaveisFiscal`, `responsaveisDp`, `responsaveisContabil` (cada `ResponsavelOption[]`), `isDono: boolean`, e estender o prop `empresa` com `responsavelFiscalId`, `responsavelDpId: string | null`, `responsavelContabilId: string | null`, `temFuncionariosClt: boolean`. Ajustar `defaultValues` para os novos campos (Fiscal default `empresa?.responsavelFiscalId ?? ""`; DP/Contábil `?? ""`; temFuncionariosClt `?? false`).
    Substituir o `FormField` único de "Responsável" por um grid `grid grid-cols-1 gap-4 md:grid-cols-3` com 3 `FormField`/`Select` na ordem fixa: Responsável Fiscal, Responsável DP, Responsável Contábil. Fiscal usa placeholder "Selecione o responsável" e lista `responsaveisFiscal`. DP e Contábil usam placeholder "Sem responsável", primeira `SelectItem value=""` rotulada "Sem responsável", depois `responsaveisDp`/`responsaveisContabil`. Quando `!isDono`, passar `disabled` aos 3 `Select` (UX apenas — enforcement real é server-side no Plan 03).
    Adicionar, após `particularidades` e antes da barra de botões, um `FormItem` full-width com `Checkbox` (checked/onCheckedChange resolvendo para `field.onChange(checked === true)`, aria-label "Tem funcionários CLT?") + `FormLabel` "Tem funcionários CLT?" + helper `<p className="text-xs text-muted-foreground">` com o texto exato da UI-SPEC sobre obrigações de Folha/FGTS/INSS/eSocial (Fase 6). Este checkbox é o único mecanismo de preenchimento do campo CLT (D-05): a revisão das 197 empresas é manual, dentro do sistema — NÃO existe import em lote de planilha externa para este campo, e nenhuma task deste plano deve criar um.
    onSubmit: `formData.set` para `responsavelFiscalId`, `responsavelDpId` (string vazia se null), `responsavelContabilId`, `temFuncionariosClt` (`String(values.temFuncionariosClt)`), além dos campos atuais. Remover o `set("responsavelId", ...)` antigo.
    novo/page.tsx: substituir a única chamada `listarResponsaveis()` por `Promise.all` de `listarResponsaveis("FISCAL")`, `listarResponsaveis("DP")`, `listarResponsaveis("CONTABIL")`; passar como props + `isDono={session.user.role === "DONO"}`.
    editar/page.tsx: idem para as 3 listas; estender o map do prop `empresa` para incluir os 3 responsáveis derivados da relação `responsaveisPorSetor` (procurar a entrada por setor para extrair `responsavelFiscalId`/`DpId`/`ContabilId`) + `temFuncionariosClt`. Passar `isDono`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "responsavelFiscalId" "src/app/(app)/empresas/empresa-form.tsx" && grep -q "temFuncionariosClt" "src/app/(app)/empresas/empresa-form.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpo (props/tipos coerentes com o schema do Plan 03).
    - empresa-form.tsx contém os 3 campos `responsavelFiscalId`/`responsavelDpId`/`responsavelContabilId` e `temFuncionariosClt`, com grid `md:grid-cols-3`.
    - DP e Contábil têm uma `SelectItem value=""` rotulada "Sem responsável".
    - Quando `!isDono`, os 3 Selects recebem `disabled`.
    - novo/page.tsx e editar/page.tsx chamam `listarResponsaveis` por setor (FISCAL/DP/CONTABIL) e passam `isDono`.
  </acceptance_criteria>
  <done>Form com 3 seletores por setor + checkbox CLT; páginas novo/editar passam as 3 listas e isDono; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Tabela setor-aware (colunas por setor, filtro/badge "Sem responsável", estado vazio por setor) + derivação setor-aware no data layer da page</name>
  <files>src/app/(app)/empresas/empresas-table.tsx, src/app/(app)/empresas/page.tsx, src/app/(app)/empresas/derive-rows.ts</files>
  <read_first>
    - src/app/(app)/empresas/empresas-table.tsx (EmpresaRow, EmpresasTableProps, coluna única "responsavel" linhas ~135-139, useMemo dadosFiltrados, filtro isDono-gated, estado vazio genérico ~200-218)
    - src/app/(app)/empresas/page.tsx (wiring atual — listarEmpresas, listarResponsaveis, isDono, props para EmpresasTable)
    - src/app/(app)/empresas/importar/_components/StepReview.tsx (padrão badge âmbar "Sem regime" + filtro a espelhar)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-UI-SPEC.md (seções "Table column placement", copy do filtro/badge/estado vazio, cor âmbar bg-amber-500)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md (seção empresas-table.tsx)
  </read_first>
  <action>
    empresas-table.tsx: estender `EmpresaRow` com `responsavelDpId: string | null`, `responsavelContabilId: string | null` e os nomes por setor (ex.: `responsavelFiscal`/`responsavelDp`/`responsavelContabil` como `{ id; nome } | null`). IMPORTANTE: estes campos são preenchidos por `deriveEmpresaRows` (data layer, descrito abaixo) — para um viewer não-DONO os campos dos outros setores já chegam `null` à tabela. Estender `EmpresasTableProps` com `setor: "FISCAL" | "DP" | "CONTABIL" | null` (setor do usuário logado).
    Colunas (D-10, segunda barreira defensiva sobre a omissão do data layer): se `isDono`, renderizar 3 colunas "Responsável Fiscal"/"Responsável DP"/"Responsável Contábil", cada célula mostrando o nome OU, se null, o badge âmbar `<Badge className="bg-amber-500 text-white">Sem responsável</Badge>`. Se NÃO-DONO, renderizar exatamente 1 coluna "Responsável {label do setor do usuário}", mostrando só o responsável do PRÓPRIO setor de cada linha. (Os campos dos outros setores já vêm `null` de `deriveEmpresaRows`, então não há dado cross-setor para renderizar; esconder a coluna é redundância defensiva, NÃO o controle primário.) Posição: onde a coluna única "Responsável" está hoje (entre Regime e Ações).
    Filtro "Sem responsável" (D-03, DONO-only): adicionar estado `semResponsavelFiltro: "TODAS" | "DP" | "CONTABIL"` e, dentro do `isDono ? (...) : null` da barra de filtros, dois toggles `<Button size="sm" variant={ativo ? "default" : "outline"}>` rotulados "Sem responsável DP" e "Sem responsável Contábil". Estender o `useMemo` de `dadosFiltrados`: se `semResponsavelFiltro === "DP"` e `responsavelDpId !== null` -> excluir; idem CONTABIL. Incluir `semResponsavelFiltro` nas deps do useMemo.
    Estado vazio por setor (D-09): quando `empresas.length === 0` E `!isDono` E `setor !== "FISCAL"` (ou seja DP/Contábil), renderizar um NOVO branch usando o mesmo wrapper `flex flex-col items-center gap-4 py-16 text-center` com heading "Nenhuma empresa atribuída a você ainda" (`text-xl font-semibold`) e corpo `max-w-md text-sm text-muted-foreground` "Você ainda não é responsável por nenhuma empresa no setor {SETOR_LABEL}. Fale com o dono do escritório para receber suas atribuições." ({SETOR_LABEL} = "DP" ou "Contábil"). DONO e colaborador Fiscal mantêm o estado vazio genérico atual (com CTA importar/nova empresa) — não substituir esse.
    page.tsx — derivação setor-aware no DATA LAYER (D-10, fronteira de exposição de dados — CRÍTICO, Blocker fix): extrair a montagem do `EmpresaRow` para uma função pura exportável e testável `deriveEmpresaRows(empresas, viewerRole, viewerSetor)` em `src/app/(app)/empresas/derive-rows.ts` (novo arquivo). Regra de omissão: quando `viewerRole !== "DONO"`, o `EmpresaRow` de cada empresa DEVE conter APENAS o campo de responsável do PRÓPRIO setor do viewer (ex.: viewer DP → só `responsavelDp` + `responsavelDpId` populados a partir de `responsaveisPorSetor`); os campos dos outros 2 setores (`responsavelFiscal`/`responsavelContabil` e respectivos ids) DEVEM ser `null`/omitidos — NUNCA construídos nem lidos de `responsaveisPorSetor` para um viewer não-DONO. Os nomes/ids dos outros setores não podem entrar na árvore React / payload RSC / HTML inicial de uma sessão não-DONO (a omissão é no servidor, antes de passar `rows` para `<EmpresasTable>` — não é só esconder coluna na renderização). Apenas quando `viewerRole === "DONO"` os 3 setores são populados. page.tsx chama `deriveEmpresaRows(empresas, session.user.role, session.user.setor)` e passa o resultado + `setor={session.user.setor}` para `EmpresasTable`. Manter `listarEmpresas(session.user)` (escopo setor-aware já aplicado no backend — nenhum `.filter()` client-side por responsável, T-01-IDOR-READ).
    NOTA (warning scope_sanity): a renderização de coluna em empresas-table.tsx (acima) continua sendo setor-aware como segunda barreira defensiva, mas a fronteira de segurança REAL é esta omissão no data layer — para um viewer não-DONO os dados dos outros setores simplesmente não existem no `EmpresaRow`, então não há o que vazar mesmo via "view source"/RSC flight/DevTools.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Sem responsavel\|Sem responsável" "src/app/(app)/empresas/empresas-table.tsx" && grep -q "bg-amber-500" "src/app/(app)/empresas/empresas-table.tsx" && grep -q "deriveEmpresaRows" "src/app/(app)/empresas/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpo.
    - **DATA-LAYER OMISSION (D-10, security-critical — Blocker fix):** `deriveEmpresaRows` existe em `src/app/(app)/empresas/derive-rows.ts` e, para `viewerRole !== "DONO"`, retorna `EmpresaRow`s onde APENAS o campo do próprio setor do viewer está populado e os campos dos outros 2 setores (nomes E ids) são `null` — os dados cross-setor nunca são construídos no objeto passado a `<EmpresasTable>` (não é só omissão de coluna na renderização).
    - empresas-table.tsx renderiza 3 colunas de responsável quando `isDono`, e exatamente 1 (do setor do usuário) quando não-DONO; nenhuma coluna de outro setor é renderizada para colaborador (segunda barreira defensiva sobre a omissão no data layer).
    - Badge `bg-amber-500 text-white` "Sem responsável" aparece em célula de responsável null.
    - Filtros "Sem responsável DP"/"Sem responsável Contábil" só aparecem para DONO e filtram corretamente via useMemo.
    - Estado vazio por setor (D-09) aparece para colaborador DP/Contábil sem empresas; DONO/Fiscal mantêm o estado vazio genérico.
    - page.tsx chama `deriveEmpresaRows(empresas, session.user.role, session.user.setor)` e passa `setor` do usuário para a tabela.
  </acceptance_criteria>
  <done>Tabela setor-aware com colunas por papel; derivação setor-aware de EmpresaRow no data layer (omissão cross-setor para não-DONO); filtro/badge sem responsável e estado vazio por setor; page faz o wiring; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste automatizado da omissão cross-setor de deriveEmpresaRows (D-10, regressão de segurança)</name>
  <files>tests/empresas.derive-rows.test.ts</files>
  <read_first>
    - src/app/(app)/empresas/derive-rows.ts (função `deriveEmpresaRows` criada na Task 2 — assinatura `(empresas, viewerRole, viewerSetor)` e shape de `EmpresaRow`)
    - tests/empresas.queries.test.ts (padrão Vitest deste módulo — estrutura de fixtures de empresa com `responsaveisPorSetor`, sem precisar de db real para uma função pura)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md (Validation Architecture — Vitest 4.1.8, `npm test`)
  </read_first>
  <behavior>
    - Test 1 (DP colaborador): `deriveEmpresaRows(fixtures, "COLABORADOR", "DP")` para uma empresa que tem responsável nos 3 setores → cada `EmpresaRow` tem `responsavelDp`/`responsavelDpId` populados, e `responsavelFiscal`, `responsavelFiscalId`, `responsavelContabil`, `responsavelContabilId` TODOS `null` (nem nome nem id de Fiscal/Contábil presentes em nenhum campo da row).
    - Test 2 (Contábil colaborador): idem para `"CONTABIL"` — só campos Contábil populados; Fiscal/DP `null`.
    - Test 3 (DONO): `deriveEmpresaRows(fixtures, "DONO", null)` → os 3 setores populados (nenhum strip).
    - Test 4 (varredura anti-vazamento): para a saída do viewer não-DONO, nenhum valor (nome ou id) pertencente aos outros setores aparece em QUALQUER chave da row serializada (`JSON.stringify(row)` não contém os nomes/ids Fiscal/Contábil do fixture) — guarda contra vazamento por campo extra/derivado não previsto.
  </behavior>
  <action>
    Criar `tests/empresas.derive-rows.test.ts` testando a função pura `deriveEmpresaRows` (sem db, sem render — isola a fronteira de segurança do RSC). Montar fixtures de 1-2 empresas onde os 3 setores têm responsável distinto (nomes/ids reconhecíveis, ex.: "FISCAL_NOME"/"DP_NOME"/"CONTABIL_NOME"). Implementar os 4 testes do bloco behavior. Test 4 (varredura) deve afirmar via `JSON.stringify(rows)` que, para viewer não-DONO, os nomes/ids dos setores que NÃO são o do viewer não aparecem em lugar nenhum — convertendo a parte crítica de D-10 de manual-only para automatizada (a verificação humana da Task 4 continua para a camada visual/UX D-09/D-10, mas a não-exposição de dados cross-setor agora tem cobertura de regressão).
  </action>
  <verify>
    <automated>npx vitest run tests/empresas.derive-rows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/empresas.derive-rows.test.ts` GREEN.
    - Test 4 (varredura `JSON.stringify`) afirma ausência total de nomes/ids cross-setor na saída de viewer não-DONO — se a omissão do data layer regredir, este teste FALHA.
    - Os testes não dependem de db nem de render (função pura).
  </acceptance_criteria>
  <done>Cobertura automatizada de regressão da omissão cross-setor de `deriveEmpresaRows`; a não-exposição de dados D-10 deixa de ser manual-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Verificação humana dos estados setor-aware (D-09/D-10)</name>
  <what-built>
    Form de empresa com 3 seletores de responsável (Fiscal/DP/Contábil, filtrados por setor, disabled para não-DONO) + checkbox "Tem funcionários CLT?"; tabela de empresas com colunas de responsável setor-aware, filtro/badge âmbar "Sem responsável" (DONO-only) e estado vazio explicativo para colaboradores DP/Contábil sem atribuições (D-09/D-10 — sem infra de teste de componente, verificação manual obrigatória).
  </what-built>
  <how-to-verify>
    1. `npm run dev` e abrir /empresas.
    2. Logar como DONO (dono@escritorio.com.br): a tabela deve mostrar 3 colunas "Responsável Fiscal/DP/Contábil"; empresas sem responsável DP/Contábil mostram badge âmbar "Sem responsável"; os toggles "Sem responsável DP" e "Sem responsável Contábil" aparecem e filtram a lista. Editar uma empresa: os 3 seletores estão habilitados; o checkbox "Tem funcionários CLT?" e o helper text aparecem após Particularidades; cada seletor lista só colaboradores do seu setor (DP lista DP1-4, não Contábil/Fiscal). Salvar e confirmar persistência (reabrir a empresa).
    3. Logar como colaborador DP (dp1@escritorio.com.br, senha "trocar-no-primeiro-login"), ANTES de qualquer atribuição: a lista deve mostrar a mensagem "Nenhuma empresa atribuída a você ainda no setor DP — fale com o dono", NÃO o estado vazio genérico. Atribuir (como DONO) uma empresa de DP a dp1 (que TAMBÉM tenha responsável Fiscal/Contábil definidos), depois revisitar como dp1: ver apenas 1 coluna "Responsável DP", nunca Fiscal/Contábil; abrir o form de edição e confirmar que os 3 seletores estão disabled. VERIFICAÇÃO DE PAYLOAD (D-10 data layer): com a sessão de dp1, abrir "Exibir código-fonte da página" (view-source) ou a aba Network → resposta RSC da rota /empresas e confirmar (Ctrl+F) que os nomes/ids do responsável Fiscal e Contábil daquela empresa NÃO aparecem em lugar nenhum no payload — só o responsável DP. (A regressão automatizada na Task 3 já cobre isso; este passo é a confirmação manual de ponta a ponta.)
    4. Logar como colaborador Fiscal existente (Caio / colaborador1@escritorio.com.br): confirmar que continua vendo a SUA carteira Fiscal normalmente (sem regressão), com 1 coluna "Responsável Fiscal".
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas observados</resume-signal>
  <action>Pausar para verificação humana. Rodar `npm run dev`, seguir os passos de how-to-verify acima logando como DONO, colaborador DP (dp1) e colaborador Fiscal existente, e confirmar os comportamentos D-09 (estado vazio por setor) e D-10 (colunas por setor, sem vazamento cross-setor). Não prosseguir até receber "approved".</action>
  <verify><human-check>Comportamentos D-09/D-10 confirmados visualmente: 3 colunas para DONO; 1 coluna do próprio setor para colaborador; badge âmbar "Sem responsável"; filtros DONO-only; estado vazio explicativo para DP/Contábil; sem regressão Fiscal.</human-check></verify>
  <done>Usuário responde "approved" após validar os estados setor-aware no navegador.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase (plan) produces

- **Component prop changes:** `EmpresaFormProps` gains `responsaveisFiscal`, `responsaveisDp`, `responsaveisContabil`, `isDono`, and `empresa.{responsavelFiscalId, responsavelDpId, responsavelContabilId, temFuncionariosClt}`
- **Component prop changes:** `EmpresasTableProps` gains `setor`; `EmpresaRow` gains `responsavelDpId`, `responsavelContabilId`, and per-sector responsável objects
- **New UI elements:** 3 responsável Selects + CLT Checkbox in `empresa-form.tsx`; sector-aware responsável columns, "Sem responsável" amber badge + DONO-only filter toggles, sector-filtered empty state in `empresas-table.tsx`
- **Page wiring:** `novo/page.tsx`, `[id]/editar/page.tsx`, `page.tsx` call `listarResponsaveis` per sector and pass `isDono`/`setor`
- **New data-layer module:** `derive-rows.ts` with `deriveEmpresaRows(empresas, viewerRole, viewerSetor)` — sector-aware `EmpresaRow` derivation that omits (nulls) cross-sector responsável names/ids for non-DONO viewers BEFORE they reach the React tree / RSC payload (primary D-10 security control)
- **Test added:** `tests/empresas.derive-rows.test.ts` — automated regression on the cross-sector omission (incl. `JSON.stringify` anti-leak scan), converting the security-critical part of D-10 from manual-only to automated
- **Human verification:** D-09/D-10 visual/RBAC checkpoint incl. RSC/view-source payload confirmation
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server data derivation → RSC payload/browser | for a non-DONO viewer, other sectors' responsável names/ids must be omitted at the data layer (`deriveEmpresaRows`) so they never enter the `EmpresaRow`, the RSC flight payload, the initial HTML, or React DevTools — column hiding is NOT sufficient |
| client form → Server Action | disabled selects are UX only; real authority is the server-side guard (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Information Disclosure | colaborador's RSC payload/initial HTML includes other sectors' responsável names/ids (data-layer leak; column hiding alone still ships the values in the flight payload) | mitigate | DATA-LAYER OMISSION: `deriveEmpresaRows(empresas, role, setor)` populates ONLY the viewer's own-sector responsável field for non-DONO and sets the other 2 sectors' name/id fields to `null` — the cross-sector values are never constructed in the `EmpresaRow`, so they cannot appear in the RSC flight payload, the initial HTML, "view source", or React DevTools. Column-level hiding in `empresas-table.tsx` is a second defensive barrier, not the primary control. Verified by automated regression test (Task 3, `JSON.stringify` anti-leak scan) AND human checkpoint step 3 |
| T-05-16 | Elevation of Privilege | non-DONO bypasses disabled selects via crafted FormData | mitigate | Disabled selects are UX only; the authoritative DONO-only guard is server-side in `actions.ts` (Plan 03, T-05-10) — this plan adds no new authority, only the disabled UX signal |
| T-05-17 | Information Disclosure | client-side `.filter()` reintroduced for responsável visibility (IDOR-READ) | mitigate | page.tsx renders exactly what `listarEmpresas(session.user)` (sector-aware scope) returns; the "Sem responsável" filter operates only over already-scoped, already-fetched rows (197-row client filter, DONO-only) — no client-side responsável-scope filtering |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; UI-SPEC confirms all shadcn primitives (Select, Checkbox, Badge) already installed |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run tests/empresas.derive-rows.test.ts` green (cross-sector data-layer omission has automated regression coverage — anti-leak `JSON.stringify` scan).
- Full suite `npm test` green (no regression in server-side tests from page wiring changes).
- Human checkpoint: D-09/D-10 behaviors confirmed for DONO, DP colaborador (incl. RSC/view-source payload check), and existing Fiscal colaborador (no regression).
</verification>

<success_criteria>
- Form exposes 3 sector-filtered responsável selectors + CLT checkbox; DP/Contábil have "Sem responsável" option.
- DONO sees 3 responsável columns; colaborador sees only their sector's column — and cross-sector responsável names/ids are omitted at the DATA LAYER (`deriveEmpresaRows`), never present in the non-DONO RSC payload/HTML (automated test asserts this).
- "Sem responsável" filter + amber badge (DONO-only); sector-filtered empty state for DP/Contábil colaboradores.
- No regression for existing Fiscal colaboradores.
</success_criteria>

<output>
Create `.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-04-SUMMARY.md` when done
</output>

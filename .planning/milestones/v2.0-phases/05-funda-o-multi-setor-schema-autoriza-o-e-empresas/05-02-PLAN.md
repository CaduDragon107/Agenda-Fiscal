---
phase: 05-funda-o-multi-setor-schema-autoriza-o-e-empresas
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/types/next-auth.d.ts
  - src/auth.ts
  - src/auth.config.ts
  - src/lib/visibility-scope.ts
  - tests/setup.ts
  - tests/visibility-scope.setor.test.ts
  - tests/auth.setor.test.ts
autonomous: true
requirements: [SETOR-01, SETOR-02]
must_haves:
  truths:
    - "Ao logar, session.user.setor carrega o Setor do Usuario (null para DONO)"
    - "withVisibilityScope para COLABORADOR de DP retorna responsaveisPorSetor.some({setor:'DP', usuarioId}) — setor E usuarioId no MESMO filtro"
    - "withVisibilityScope para DONO retorna {} independente de setor"
    - "COLABORADOR sem setor falha SEGURO (nenhuma empresa), nunca {}"
    - "A suite IDOR/visibilidade existente passa INALTERADA (regression gate)"
  artifacts:
    - path: "src/lib/visibility-scope.ts"
      provides: "withVisibilityScope(user, setor?) e withTarefaScope setor-aware; SessionUser ganha setor"
      contains: "responsaveisPorSetor"
    - path: "src/types/next-auth.d.ts"
      provides: "setor em Session/User/JWT nos 4 módulos augmentados"
      contains: "AppSetor"
    - path: "tests/visibility-scope.setor.test.ts"
      provides: "Fixtures multi-setor GREEN"
  key_links:
    - from: "src/auth.config.ts"
      to: "src/types/next-auth.d.ts"
      via: "callbacks jwt/session atribuem token.setor / session.user.setor"
      pattern: "token\\.setor|session\\.user\\.setor"
    - from: "src/lib/visibility-scope.ts"
      to: "prisma/schema.prisma"
      via: "filtro relacional responsaveisPorSetor.some"
      pattern: "responsaveisPorSetor"
---

<objective>
Tornar a autorização setor-aware: propagar `Usuario.setor` pelo JWT/sessão do Auth.js v5 (4-module augmentation) e estender `withVisibilityScope`/`withTarefaScope` com parâmetro `setor` explícito, sem quebrar nenhum call-site existente nem nenhum teste de IDOR/visibilidade já existente.

Purpose: É o coração de segurança da fase. Um colaborador de DP só pode ver/editar empresas onde ele é o responsável de DP, mesmo que outra pessoa seja o responsável Fiscal da mesma empresa (Success Criteria 4). O bug clássico (filtro setor sem usuarioId = ver todas do setor) e o trust de input de cliente (Pitfall B3) são fechados aqui. Os scaffolds RED do Plan 01 ficam GREEN.
Output: setor na sessão; scope functions setor-aware; fixtures multi-setor verdes; regression gate intacto.
</objective>

<execution_context>
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-CONTEXT.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagar Usuario.setor pelo JWT/sessão (4-module augmentation + authorize + callbacks)</name>
  <files>src/types/next-auth.d.ts, src/auth.ts, src/auth.config.ts, tests/auth.setor.test.ts</files>
  <read_first>
    - src/types/next-auth.d.ts (padrão exato de augmentation de 4 módulos: next-auth, @auth/core/types, next-auth/jwt, @auth/core/jwt — augmentar só os 2 primeiros falha silenciosamente)
    - src/auth.ts (authorize: select + return shape; só lugar onde senhaHash é selecionado)
    - src/auth.config.ts (callbacks jwt/session; edge-safe — NÃO importar @/lib/db nem bcryptjs aqui)
    - tests/auth.setor.test.ts (scaffold RED do Plan 01 a tornar GREEN)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md (seções src/types/next-auth.d.ts, src/auth.ts, src/auth.config.ts)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md (Pattern 3)
  </read_first>
  <behavior>
    - Test 1 (auth.setor.test.ts): callback jwt copia `user.setor` para `token.setor` quando `user` presente.
    - Test 2: callback session copia `token.setor` para `session.user.setor`.
    - Test 3 (edge): DONO com setor null propaga `null` corretamente (não vira undefined nem quebra).
  </behavior>
  <action>
    next-auth.d.ts: definir `export type AppSetor = "FISCAL" | "DP" | "CONTABIL"`. Adicionar `setor: AppSetor | null` em `Session.user`, `User` e `JWT` nos QUATRO `declare module` (`next-auth`, `@auth/core/types`, `next-auth/jwt`, `@auth/core/jwt`), espelhando exatamente a augmentation já existente de `id`/`role`.
    auth.ts: no `findUnique` do `authorize`, adicionar `setor: true` ao `select` (junto de id/nome/email/role/senhaHash — NUNCA expor senhaHash fora desta função). No objeto de retorno, adicionar `setor: usuario.setor` junto de role.
    auth.config.ts: no callback `jwt`, dentro do `if (user)`, adicionar `token.setor = user.setor;`. No callback `session`, dentro do `if (session.user)`, adicionar `session.user.setor = token.setor;`. Manter o arquivo edge-safe (sem db/bcrypt).
    Tornar tests/auth.setor.test.ts GREEN com asserts reais sobre os callbacks (importando-os de auth.config ou testando a transformação token<->session conforme estrutura do arquivo).
  </action>
  <verify>
    <automated>npx vitest run tests/auth.setor.test.ts tests/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/auth.setor.test.ts` passa (GREEN) verificando jwt e session propagam setor.
    - `tests/auth.test.ts` existente continua GREEN (regression).
    - next-auth.d.ts contém `setor` nos 4 `declare module`.
    - auth.config.ts NÃO importa `@/lib/db` nem `bcryptjs` (continua edge-safe).
    - `npx tsc --noEmit` não reporta erro de tipo em `session.user.setor` / `token.setor`.
  </acceptance_criteria>
  <done>setor flui Usuario -> authorize -> JWT -> session; auth.setor.test.ts verde; auth.test.ts intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tornar withVisibilityScope/withTarefaScope setor-aware sem regressão</name>
  <files>src/lib/visibility-scope.ts, tests/setup.ts, tests/visibility-scope.setor.test.ts</files>
  <read_first>
    - src/lib/visibility-scope.ts (estado atual — SessionUser, withVisibilityScope, withTarefaScope; preservar compatibilidade de call-site)
    - tests/setup.ts (mockDonoUser/mockColaboradorUser/mockOtherColaboradorUser — precisam ganhar campo setor)
    - tests/visibility-scope.test.ts (regression gate — NÃO editar; ler para garantir que asserts atuais continuam válidos)
    - tests/visibility-scope.setor.test.ts (scaffold RED do Plan 01 a tornar GREEN)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md (seção src/lib/visibility-scope.ts — shape exato + regra de segurança)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md (Pattern 4 + nota de compatibilidade de call-site)
    - .planning/research/PITFALLS.md (Pitfall B3 — filtro combinado setor AND usuarioId)
  </read_first>
  <behavior>
    - Test 1: COLABORADOR setor=DP -> `{ responsaveisPorSetor: { some: { setor: "DP", usuarioId } } }`.
    - Test 2: DONO (qualquer setor / null) -> `{}`.
    - Test 3 (edge crítico): COLABORADOR setor=null -> resultado NÃO igual a `{}` (fail-safe, nunca alarga visão).
    - Regression: os 3 testes existentes de visibility-scope.test.ts continuam passando inalterados.
  </behavior>
  <action>
    visibility-scope.ts: estender `SessionUser` com `setor: "FISCAL" | "DP" | "CONTABIL" | null`. Mudar a assinatura para `withVisibilityScope(user: SessionUser, setor: SessionUser["setor"] = user.setor): Prisma.EmpresaWhereInput` — o default torna o 2º parâmetro opcional, preservando todos os call-sites `...withVisibilityScope(user)` existentes sem editá-los. Corpo: DONO -> `{}`; se `!setor` -> retornar um filtro fail-safe que não casa nenhuma empresa (ex.: `{ id: "__no_setor_defined__" }`) — NUNCA `{}` (Pitfall B3, evitar widening); senão -> `{ responsaveisPorSetor: { some: { setor, usuarioId: user.id } } }`. REGRA DE SEGURANÇA: `setor` e `usuarioId` DEVEM ficar no MESMO objeto `some`, nunca dois `where` separados — split filter deixaria colaborador ver todas empresas do setor.
    withTarefaScope: nesta fase a forma permanece (`Tarefa.setor` não existe ainda — Phase 6/7). Apenas atualizar o tipo `SessionUser` (já compartilhado). DONO -> `{}`, COLABORADOR -> `{ responsavelId: user.id }` (inalterado).
    tests/setup.ts: adicionar `setor` ao shape `SessionUser` e a cada factory: mockDonoUser -> `setor: null`; mockColaboradorUser -> `setor: "FISCAL"`; mockOtherColaboradorUser -> `setor: "FISCAL"`. Adicionar 2 novas factories `mockDpColaboradorUser` (setor "DP") e `mockContabilColaboradorUser` (setor "CONTABIL") seguindo o mesmo shape. Estes defaults FISCAL preservam o comportamento dos testes de regressão existentes.
    Tornar tests/visibility-scope.setor.test.ts GREEN (asserts reais conforme behavior acima).
    NÃO editar tests/visibility-scope.test.ts, tests/empresas.idor.test.ts, tests/tarefas.idor.test.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/visibility-scope.test.ts tests/visibility-scope.setor.test.ts tests/empresas.idor.test.ts tests/tarefas.idor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/visibility-scope.setor.test.ts` GREEN (DP colaborador combina setor+usuarioId no mesmo some; DONO {}; setor null fail-safe).
    - `tests/visibility-scope.test.ts`, `tests/empresas.idor.test.ts`, `tests/tarefas.idor.test.ts` GREEN sem edição (`git diff --name-only` não os inclui).
    - `grep -c "responsaveisPorSetor" src/lib/visibility-scope.ts` >= 1, e o filtro tem `setor` e `usuarioId` no mesmo objeto `some` (verificável por leitura — não dois where separados).
    - Resultado de COLABORADOR sem setor NUNCA é `{}`.
  </acceptance_criteria>
  <done>Scope functions setor-aware com filtro combinado; fixtures multi-setor verdes; regression gate inalterado.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase (plan) produces

- **Type:** `AppSetor` ("FISCAL" | "DP" | "CONTABIL") in `src/types/next-auth.d.ts`; `Session.user.setor`, `User.setor`, `JWT.setor` (all 4 augmented modules)
- **Function signature change:** `withVisibilityScope(user: SessionUser, setor?: SessionUser["setor"])` — 2nd param optional, defaults to `user.setor`
- **Type change:** `SessionUser` (visibility-scope.ts) gains `setor: "FISCAL" | "DP" | "CONTABIL" | null`
- **Test factories:** `mockDpColaboradorUser`, `mockContabilColaboradorUser` in `tests/setup.ts`; existing factories gain `setor`
- **Tests made GREEN:** `tests/visibility-scope.setor.test.ts`, `tests/auth.setor.test.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action/Component | `setor` for scoping must come from authenticated session, never request input |
| session JWT → scope function | `withVisibilityScope` builds the Prisma where-clause that gates all empresa reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | RBAC widening — `setor` filter applied without `usuarioId` lets colaborador see ALL empresas of their sector | mitigate | `setor` and `usuarioId` combined in the SAME `some: {...}` relation filter; explicit test in `visibility-scope.setor.test.ts` asserts the combined shape (Pitfall B3) |
| T-05-06 | Tampering | client supplies arbitrary `setor` (query string/form) to widen visibility | mitigate | `setor` defaults to `user.setor` from authenticated session; scope functions never read `setor` from request input — sourced from signed JWT only (Pattern 3/4) |
| T-05-07 | Information Disclosure | COLABORADOR with `setor=null` falling through to `{}` (sees everything) | mitigate | Fail-safe branch returns non-matching filter (`{ id: "__no_setor_defined__" }`), never `{}`; tested explicitly |
| T-05-08 | Information Disclosure | regression in existing Fiscal IDOR behavior masked by new code | mitigate | Existing IDOR/visibility test suite run UNMODIFIED as a hard regression gate; setup.ts defaults preserve FISCAL behavior |
| T-05-09 | Information Disclosure | `senhaHash` leaked via widened auth select | accept | `authorize` select adds only `setor: true`; senhaHash select unchanged and confined to auth.ts |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase; no install task present |
</threat_model>

<verification>
- `npx vitest run tests/visibility-scope.test.ts tests/visibility-scope.setor.test.ts tests/empresas.idor.test.ts tests/tarefas.idor.test.ts tests/auth.test.ts tests/auth.setor.test.ts` all green.
- `npx tsc --noEmit` clean for setor types.
- No existing IDOR/visibility/auth test file edited.
</verification>

<success_criteria>
- `session.user.setor` populated on login (null for DONO).
- `withVisibilityScope` setor-aware with combined `setor`+`usuarioId` filter; DONO `{}`; null setor fail-safe.
- Existing IDOR/visibility suite green, unmodified.
</success_criteria>

<output>
Create `.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-02-SUMMARY.md` when done
</output>

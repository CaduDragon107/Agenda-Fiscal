---
phase: 05-funda-o-multi-setor-schema-autoriza-o-e-empresas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - scripts/backfill-responsavel-setor.mjs
  - scripts/backfill-setor-colaboradores-fiscal.mjs
  - tests/visibility-scope.setor.test.ts
  - tests/auth.setor.test.ts
autonomous: true
requirements: [SETOR-01, SETOR-02]
must_haves:
  truths:
    - "O banco tem a tabela empresa_responsavel_setor, o enum Setor, Usuario.setor e Empresa.temFuncionariosClt"
    - "As 197 empresas têm exatamente 1 registro EmpresaResponsavelSetor setor=FISCAL cada (197 linhas FISCAL, nem mais nem menos)"
    - "Os 4 colaboradores Fiscais existentes (colaborador1-4) têm setor=FISCAL definido (nenhum COLABORADOR fica com setor null)"
    - "Os 7 placeholders DP1-4/Contabil1-3 existem com setor correto e login funcional"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Setor enum, EmpresaResponsavelSetor model, Usuario.setor, Empresa.temFuncionariosClt; Empresa.responsavelId mantido"
      contains: "model EmpresaResponsavelSetor"
    - path: "scripts/backfill-responsavel-setor.mjs"
      provides: "Backfill 197 Empresa.responsavelId -> 197 linhas FISCAL com count assertion"
    - path: "scripts/backfill-setor-colaboradores-fiscal.mjs"
      provides: "Backfill setor=FISCAL nos 4 colaboradores existentes + verificação de 0 COLABORADOR sem setor"
    - path: "prisma/seed.ts"
      provides: "7 placeholders DP1-4/Contabil1-3 com setor, mesma senha bcrypt literal"
    - path: "tests/visibility-scope.setor.test.ts"
      provides: "Scaffold de fixtures multi-setor (RED até Plan 02 implementar)"
    - path: "tests/auth.setor.test.ts"
      provides: "Scaffold de propagação setor no JWT/session (RED até Plan 02 implementar)"
  key_links:
    - from: "scripts/backfill-responsavel-setor.mjs"
      to: "prisma/schema.prisma"
      via: "db.empresaResponsavelSetor.upsert keyed on empresaId_setor"
      pattern: "empresaResponsavelSetor\\.upsert"
---

<objective>
Estabelecer toda a fundação de dados da multi-setorialidade: o schema Prisma (enum Setor, junction table EmpresaResponsavelSetor, Usuario.setor, Empresa.temFuncionariosClt), o push para o banco Neon, e os dois backfills verificados por contagem (197 empresas -> 197 linhas FISCAL; 4 colaboradores Fiscais existentes -> setor=FISCAL). Também cria os 7 placeholders DP/Contábil no seed e os scaffolds de teste das Waves seguintes.

Purpose: Tudo nas Waves 2-4 (autorização setor-aware, backend de empresas, UI) depende de o schema existir no banco e dos backfills estarem verificados. Sem o push e os backfills, qualquer leitura setor-aware quebra e os 4 colaboradores Fiscais reais (em produção) veriam zero empresas (regressão crítica — Pitfall RESEARCH.md).
Output: Schema migrado e pushado, 2 scripts de backfill executados e verificados, seed estendido, 2 scaffolds de teste.
</objective>

<execution_context>
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-CONTEXT.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md
@.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender prisma/schema.prisma com Setor, junction table, Usuario.setor e Empresa.temFuncionariosClt</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (estado atual — ver model Usuario, model Empresa, model EmpresaRegimeHistorico como analog de junction/history table; enum Role como analog de enum)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md (seção `prisma/schema.prisma` — shape exato dos novos modelos)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md (Pattern 1 — convenções cuid()/@@map/@updatedAt)
  </read_first>
  <action>
    Adições ESTRITAMENTE ADITIVAS (nenhum drop, nenhum rename). Manter `Empresa.responsavelId` (String, obrigatório) e a relação `responsavel` / `empresas Empresa[] @relation("ResponsavelEmpresa")` EXATAMENTE como estão — o motor de geração Fiscal (src/modules/tarefas/geracao.ts) ainda lê essa coluna; remover quebraria o Fiscal em produção (Pitfall B1 / D-01).
    Adicionar: (a) enum `Setor` com valores `FISCAL`, `DP`, `CONTABIL`. (b) Em `model Usuario`: campo `setor Setor?` (nullable — null para DONO cross-setor; obrigatório de facto para COLABORADOR via backfill) e relação `responsaveisPorSetor EmpresaResponsavelSetor[]`. (c) Em `model Empresa`: campo `temFuncionariosClt Boolean @default(false)` (EMPR-03, D-04) e relação `responsaveisPorSetor EmpresaResponsavelSetor[]`. (d) Novo `model EmpresaResponsavelSetor` com `id String @id @default(cuid())`, `empresaId String`, `empresa Empresa @relation(fields: [empresaId], references: [id])`, `setor Setor`, `usuarioId String`, `usuario Usuario @relation(fields: [usuarioId], references: [id])`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`, `@@unique([empresaId, setor])`, `@@index([usuarioId, setor])`, `@@map("empresa_responsavel_setor")`. O `@@unique([empresaId, setor])` garante "exatamente 1 responsável por setor por empresa" no nível do banco (sem checagem manual TOCTOU).
  </action>
  <verify>
    <automated>grep -q "model EmpresaResponsavelSetor" prisma/schema.prisma && grep -q "enum Setor" prisma/schema.prisma && grep -q "temFuncionariosClt" prisma/schema.prisma && grep -q "responsavelId" prisma/schema.prisma && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` retorna sucesso (schema válido).
    - `grep -c "responsavelId" prisma/schema.prisma` > 0 (coluna legada PRESERVADA — não removida).
    - Schema contém `enum Setor`, `model EmpresaResponsavelSetor`, `setor Setor?` em Usuario, `temFuncionariosClt Boolean @default(false)` em Empresa, e `@@unique([empresaId, setor])`.
  </acceptance_criteria>
  <done>Schema editado de forma aditiva, `npx prisma validate` passa, coluna `responsavelId` intacta.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push do schema para o banco e regeneração do Prisma Client</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (já editado na Task 1)
    - .planning/STATE.md (decisões Phase 02-01/03-01: `npx prisma db push` é o padrão estabelecido para este ambiente Neon sem shadow database)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md (Pattern 1 — sequência de migração concreta passos 2-3)
  </read_first>
  <action>
    [BLOCKING] Rodar `npx prisma db push` para aplicar o schema aditivo ao banco Neon (novas colunas nullable + nova tabela + novo enum NÃO exigem `--accept-data-loss`; se o comando reclamar de perda de dados, PARAR e reportar — uma mudança aditiva não deveria ser destrutiva). Em seguida rodar `npx prisma generate` para regenerar o client e expor os tipos `Setor`/`EmpresaResponsavelSetor` ao TypeScript. Esta task é OBRIGATÓRIA e deve completar ANTES de Task 3/4 (backfills dependem da tabela existir no banco) e antes de qualquer plano posterior. Build e type-check passariam sem o push (tipos vêm do schema.prisma, não do banco vivo) — criando falso-positivo; por isso o push é gate explícito.
  </action>
  <verify>
    <automated>npx prisma generate && node --env-file=.env -e "const {PrismaClient}=require('@prisma/client');const db=new PrismaClient();db.empresaResponsavelSetor.count().then(n=>{console.log('junction rows:',n);return db.$disconnect();}).catch(e=>{console.error(e.message);process.exit(1);})"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` completa sem erro e sem exigir `--accept-data-loss`.
    - `npx prisma generate` regenera o client sem erro.
    - A query `db.empresaResponsavelSetor.count()` executa contra o banco vivo sem lançar (tabela existe), retornando um número (0 antes do backfill).
  </acceptance_criteria>
  <done>Schema aplicado ao Neon via `db push`, client regenerado, tabela `empresa_responsavel_setor` consultável no banco vivo.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill 197 empresas -> 197 linhas FISCAL (com count assertion) e setor=FISCAL nos 4 colaboradores existentes</name>
  <files>scripts/backfill-responsavel-setor.mjs, scripts/backfill-setor-colaboradores-fiscal.mjs</files>
  <read_first>
    - scripts/atualizar-responsaveis.mjs (analog exato — convenção dry-run por padrão, --apply obrigatório, finally db.$disconnect())
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md (seção `scripts/backfill-responsavel-setor.mjs` e seção `prisma/seed.ts` — o segundo backfill de updateMany para os 4 colaboradores)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-RESEARCH.md (Pattern 2 + Pitfall "update: {} silently fails to backfill setor")
  </read_first>
  <action>
    Criar DOIS scripts one-off distintos (não conflundir num só — são backfills de tabelas diferentes, RESEARCH.md é explícito).
    Script A `scripts/backfill-responsavel-setor.mjs`: ler todas as `Empresa` (`select: { id, responsavelId }`), imprimir modo (DRY-RUN por padrão / APPLY com flag `--apply`) e contagem de empresas (esperado 197). Com `--apply`, para cada empresa fazer `db.empresaResponsavelSetor.upsert({ where: { empresaId_setor: { empresaId, setor: "FISCAL" } }, update: { usuarioId: responsavelId }, create: { empresaId, setor: "FISCAL", usuarioId: responsavelId } })` (idempotente). Ao final, VERIFICAÇÃO obrigatória: `const totalFiscal = await db.empresaResponsavelSetor.count({ where: { setor: "FISCAL" } })`; se `totalFiscal !== empresas.length`, imprimir `FALHA DE VERIFICAÇÃO` e setar `process.exitCode = 1`. `finally { await db.$disconnect() }`. D-01: NÃO criar nenhuma linha DP/CONTABIL (responsáveis DP/Contábil começam null nas 197 empresas).
    Script B `scripts/backfill-setor-colaboradores-fiscal.mjs`: mesma convenção dry-run/--apply. Com `--apply`, `db.usuario.updateMany({ where: { email: { in: ["colaborador1@escritorio.com.br","colaborador2@escritorio.com.br","colaborador3@escritorio.com.br","colaborador4@escritorio.com.br"] } }, data: { setor: "FISCAL" } })`. Verificação obrigatória: `const semSetor = await db.usuario.count({ where: { role: "COLABORADOR", setor: null } })`; se `semSetor !== 0`, imprimir falha e `process.exitCode = 1`. Este passo é o mais fácil de esquecer e o mais provável de quebrar o Fiscal em produção (Pitfall RESEARCH.md) — sem ele, os 4 Fiscais reais cairiam no branch fail-safe de withVisibilityScope (Plan 02) e veriam zero empresas.
    Executar ambos com `--apply` contra o banco e confirmar exit code 0.
  </action>
  <verify>
    <automated>node --env-file=.env scripts/backfill-responsavel-setor.mjs --apply && node --env-file=.env scripts/backfill-setor-colaboradores-fiscal.mjs --apply</automated>
  </verify>
  <acceptance_criteria>
    - Script A executado com `--apply` imprime `Empresas no banco: 197` (ou o total real) e `Linhas FISCAL no junction table: 197` iguais, exit code 0.
    - Script B executado com `--apply` resulta em `db.usuario.count({ where: { role: "COLABORADOR", setor: null } })` === 0, exit code 0.
    - Ambos os scripts seguem o padrão dry-run-por-padrão (rodar sem `--apply` não escreve nada).
    - Cross-check manual (Pitfall B1): nenhuma empresa com `responsavelId` divergente da linha FISCAL do junction.
  </acceptance_criteria>
  <done>Os dois backfills aplicados e verificados: 197 linhas FISCAL no junction, 0 COLABORADOR sem setor.</done>
</task>

<task type="auto">
  <name>Task 4: Estender seed com 7 placeholders DP/Contábil e criar scaffolds de teste das Waves seguintes</name>
  <files>prisma/seed.ts, tests/visibility-scope.setor.test.ts, tests/auth.setor.test.ts</files>
  <read_first>
    - prisma/seed.ts (estado atual — array `usuarios` + loop upsert com bcrypt.hash literal)
    - tests/visibility-scope.test.ts (analog exato de estrutura describe/it para o novo arquivo setor)
    - tests/setup.ts (helpers mock — precisarão de campo setor na Wave 1; aqui apenas referência)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-PATTERNS.md (seções `prisma/seed.ts` e `tests/visibility-scope.setor.test.ts`)
    - .planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    seed.ts: importar `Setor` de `@prisma/client` junto com `Role`. Adicionar `setor` em cada entrada existente do array `usuarios` (Dono -> `null`; Colaborador 1-4 -> `Setor.FISCAL`). Acrescentar 7 novas entradas (D-06/D-07/D-08): DP1-DP4 (`role: Role.COLABORADOR, setor: Setor.DP`, emails `dp1@escritorio.com.br`..`dp4@...`) e Contabil1-Contabil3 (`setor: Setor.CONTABIL`, emails `contabil1@...`..`contabil3@...`). Manter o loop upsert EXATO (`bcrypt.hash("trocar-no-primeiro-login", 10)`, `update: {}`) — a senha literal é D-08, não inventar nova convenção. NOTA: `update: {}` não retroage setor em usuários existentes — isso é coberto pelo Script B da Task 3, não aqui.
    tests/visibility-scope.setor.test.ts (NOVO, scaffold RED): criar arquivo com `describe("withVisibilityScope — setor-aware (v2.0)")` e 3 blocos `it` (DP colaborador via `responsaveisPorSetor.some({setor:'DP', usuarioId})`; DONO -> `{}`; COLABORADOR sem setor falha SEGURO != `{}`). Como `withVisibilityScope` ainda tem a assinatura antiga nesta wave, marcar os `it` como `it.todo(...)` OU escrever os asserts esperados que ficarão RED até Plan 02 — preferir asserts reais (RED) para que Plan 02 os torne GREEN. NÃO editar tests/visibility-scope.test.ts existente.
    tests/auth.setor.test.ts (NOVO, scaffold RED): criar arquivo com `describe("auth callbacks — setor (v2.0)")` e um `it.todo` ou assert RED descrevendo que o callback jwt copia `user.setor` para `token.setor` e o callback session copia `token.setor` para `session.user.setor`. Plan 02 implementa.
    CRÍTICO: NÃO editar nenhum arquivo de teste IDOR/visibilidade EXISTENTE (visibility-scope.test.ts, empresas.idor.test.ts, tarefas.idor.test.ts, empresas.queries.test.ts) — eles são regression gate inalterado.
  </action>
  <verify>
    <automated>grep -q "Setor.DP" prisma/seed.ts && grep -q "contabil3@escritorio.com.br" prisma/seed.ts && grep -q "trocar-no-primeiro-login" prisma/seed.ts && test -f tests/visibility-scope.setor.test.ts && test -f tests/auth.setor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - seed.ts contém 12 entradas de usuário (1 Dono + 4 Fiscal + 4 DP + 3 Contábil), cada COLABORADOR com `setor` explícito, Dono com `setor: null`.
    - A literal `"trocar-no-primeiro-login"` permanece intacta no loop de hash (D-08).
    - `tests/visibility-scope.setor.test.ts` e `tests/auth.setor.test.ts` existem.
    - `git diff --name-only` NÃO inclui tests/visibility-scope.test.ts, tests/empresas.idor.test.ts, tests/tarefas.idor.test.ts, tests/empresas.queries.test.ts (regression gate intacto).
  </acceptance_criteria>
  <done>Seed estendido com 7 placeholders + setor; 2 scaffolds de teste criados; nenhum teste de regressão editado.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase (plan) produces

- **Prisma enum:** `Setor` (FISCAL, DP, CONTABIL)
- **Prisma model:** `EmpresaResponsavelSetor` (table `empresa_responsavel_setor`, `@@unique([empresaId, setor])`, `@@index([usuarioId, setor])`)
- **Prisma fields:** `Usuario.setor Setor?`, `Usuario.responsaveisPorSetor`, `Empresa.temFuncionariosClt Boolean @default(false)`, `Empresa.responsaveisPorSetor`
- **Preserved (do NOT drop):** `Empresa.responsavelId`, `Empresa.responsavel`, `Usuario.empresas @relation("ResponsavelEmpresa")`
- **Scripts:** `scripts/backfill-responsavel-setor.mjs` (entry point: `main()`, flag `--apply`), `scripts/backfill-setor-colaboradores-fiscal.mjs` (entry point: `main()`, flag `--apply`)
- **Seed:** 7 new `Usuario` rows (dp1-4@escritorio.com.br, contabil1-3@escritorio.com.br)
- **Test files:** `tests/visibility-scope.setor.test.ts`, `tests/auth.setor.test.ts` (scaffolds, made GREEN by Plan 02)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script CLI → Postgres (Neon) | Backfill scripts write directly to production data; no auth scope applied (one-off admin tooling) |
| schema → live DB | `prisma db push` mutates production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | backfill scripts writing wrong usuarioId into junction | mitigate | Idempotent `upsert` keyed on `empresaId_setor` reading directly from `Empresa.responsavelId`; mandatory count-assertion (197==197) with `process.exitCode=1` on mismatch; cross-check query for divergent rows (Pitfall B1) |
| T-05-02 | Denial of Service | destructive `db push` dropping/altering live columns | mitigate | Additive-only schema change; `db push` run WITHOUT `--accept-data-loss`; task stops and reports if push reports data loss; `responsavelId` explicitly preserved |
| T-05-03 | Elevation of Privilege | 4 existing Fiscal colaboradores left with `setor=null` → fail-safe branch hides all their empresas (production regression) | mitigate | Dedicated Script B updateMany sets `setor=FISCAL`; verification asserts `count({role:COLABORADOR, setor:null}) === 0` before phase done |
| T-05-04 | Information Disclosure | seed/backfill query accidentally selecting `senhaHash` | accept | Scripts select only `{id, responsavelId}` / use `updateMany` with no select; seed uses upsert without reading hashes back — no hash exposure surface |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable); no install task present |
</threat_model>

<verification>
- `npx prisma validate` passes; `npx prisma db push` applied additively to Neon; `npx prisma generate` succeeds.
- Backfill Script A: 197 FISCAL junction rows == empresa count, exit 0.
- Backfill Script B: 0 COLABORADOR with null setor, exit 0.
- Seed contains 12 users with explicit setor.
- No existing IDOR/visibility test file modified.
</verification>

<success_criteria>
- Schema migrated and pushed to live DB; `EmpresaResponsavelSetor` queryable.
- Both backfills verified by count assertions (197 FISCAL rows; 0 colaborador without setor).
- 7 placeholders seeded with functional login; 4 existing Fiscal colaboradores have setor=FISCAL.
- `Empresa.responsavelId` preserved untouched.
</success_criteria>

<output>
Create `.planning/phases/05-funda-o-multi-setor-schema-autoriza-o-e-empresas/05-01-SUMMARY.md` when done
</output>

---
phase: 06-motor-de-gera-o-departamento-pessoal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/tarefas.dp.test.ts
autonomous: true
requirements: [DP-05]
must_haves:
  truths:
    - "Um COLABORADOR de DP consegue criar uma tarefa avulsa e atribuí-la a si mesmo, reusando criarTarefa sem mudança de código"
    - "Um COLABORADOR de DP NÃO consegue atribuir uma tarefa avulsa a um colega que não é dele (mesma regra setor-aware já validada na Phase 5)"
    - "A reutilização de criarTarefa/withVisibilityScope/withTarefaScope para DP-05 é coberta por teste de regressão automatizado, sem alterar a action existente"
  artifacts:
    - path: "tests/tarefas.dp.test.ts"
      provides: "Teste de regressão DP-05: tarefa avulsa de DP respeita escopo setor-aware"
      contains: "mockDpColaboradorUser"
  key_links:
    - from: "tests/tarefas.dp.test.ts"
      to: "src/app/(app)/tarefas/actions.ts (criarTarefa)"
      via: "import { criarTarefa }"
      pattern: "criarTarefa"
    - from: "tests/tarefas.dp.test.ts"
      to: "tests/setup.ts (mockDpColaboradorUser)"
      via: "import { mockDpColaboradorUser }"
      pattern: "mockDpColaboradorUser"
---

<objective>
Cobrir DP-05 (tarefa avulsa para a equipe de DP) por reuso direto do mecanismo já existente — `criarTarefa` + `withVisibilityScope`/`withTarefaScope`, que já são setor-aware desde a Phase 5 (D-07 do CONTEXT.md). Esta plan NÃO altera nenhum código de produção: ela adiciona um teste de regressão dedicado (`tests/tarefas.dp.test.ts`) que prova que um colaborador de DP consegue criar/atribuir tarefa avulsa dentro do seu escopo de setor, e que o escopo continua barrando atribuições fora dele.

Purpose: DP-05 é satisfeito por composição da fundação multi-setor já entregue na Phase 5 (`withVisibilityScope` retorna `{ responsaveisPorSetor: { some: { setor: "DP", usuarioId } } }` para colaborador DP). O risco real não é implementar — é assumir cobertura sem prová-la. Este teste fecha essa lacuna sem reabrir código já validado.

Output: `tests/tarefas.dp.test.ts` (novo) — teste de regressão verde provando o reuso setor-aware para DP-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-CONTEXT.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md
@src/app/(app)/tarefas/actions.ts
@src/lib/visibility-scope.ts
@tests/tarefas.idor.test.ts
@tests/setup.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-03)

Newly-created symbols introduced by this plan (exclude from source-drift verification):

- `tests/tarefas.dp.test.ts` (new file): regression test suite for DP-05 (DP collaborator avulsa task scoping)

Reused without modification: `criarTarefa` (src/app/(app)/tarefas/actions.ts), `withVisibilityScope`/`withTarefaScope` (src/lib/visibility-scope.ts), `mockDpColaboradorUser`/`mockDonoUser` (tests/setup.ts). NO production code is created or changed by this plan.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Criar teste de regressão DP-05 (tarefa avulsa de DP respeita escopo setor-aware)</name>
  <files>tests/tarefas.dp.test.ts</files>
  <read_first>
    - tests/tarefas.idor.test.ts (arquivo inteiro — padrão exato de vi.mock("@/lib/db"), vi.mock("@/auth"), vi.mock("next/cache"); estrutura dos casos de criarTarefa/IDOR; mockados empresa.findFirst, tarefa.create)
    - src/app/(app)/tarefas/actions.ts (criarTarefa linhas 40-110 — fluxo: auth → safeParse → empresa.findFirst escopado via withVisibilityScope → guard "COLABORADOR só atribui a si mesmo" → tarefa.create)
    - src/lib/visibility-scope.ts (withVisibilityScope linhas 62-83: branch DP retorna { responsaveisPorSetor: { some: { setor: "DP", usuarioId: user.id } } }; withTarefaScope linhas 93-98)
    - tests/setup.ts (mockDpColaboradorUser id "user_dp_1" setor "DP"; mockDonoUser; mockOtherColaboradorUser)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-CONTEXT.md (D-07: reuso direto, sem mudança de fluxo)
  </read_first>
  <action>
    Criar tests/tarefas.dp.test.ts espelhando o padrão de mock de tests/tarefas.idor.test.ts (mesmos `vi.mock` de `@/lib/db`, `@/auth`, `next/cache`; mocks de `empresa.findFirst` e `tarefa.create`). Importar `criarTarefa` de `@/app/(app)/tarefas/actions` e `mockDpColaboradorUser` de `./setup`. Casos a cobrir:
    1. COLABORADOR de DP cria tarefa avulsa atribuída a SI MESMO: `authMock` resolve `{ user: mockDpColaboradorUser() }` (id "user_dp_1"); `empresaFindFirstMock` resolve `{ id: "empresa_dp" }` (empresa dentro do escopo DP do colaborador); o FormData tem `responsavelId === "user_dp_1"` (== session.user.id); `tarefaCreateMock` resolve uma tarefa criada → `criarTarefa(formData)` retorna `{ ok: true }`. Assertar que `empresa.findFirst` foi chamado com um `where` que espalha `withVisibilityScope` do usuário DP (ou seja, o filtro `responsaveisPorSetor`/escopo DP foi aplicado — defesa anti-IDOR de create).
    2. COLABORADOR de DP NÃO pode atribuir a outro usuário: mesmo setup, mas `responsavelId` no FormData é diferente de "user_dp_1" → `criarTarefa` retorna `{ ok: false, error: "não autorizado" }` e `tarefa.create` NÃO é chamado (guard "COLABORADOR só se atribui").
    3. COLABORADOR de DP barrado em empresa fora do seu escopo: `empresaFindFirstMock` resolve `null` (empresa existe mas não é do escopo DP do colaborador) → `criarTarefa` retorna `{ ok: false, error: "não encontrado" }`, `tarefa.create` NÃO chamado.
    Construir o `FormData` com `new FormData()` + `.set(...)` para os campos titulo/descricao/empresaId/responsavelId/prazo, espelhando o que `criarTarefa` lê (actions.ts linhas 48-52). NÃO alterar nenhum arquivo de produção — DP-05 é reuso puro (D-07).
  </action>
  <verify>
    <automated>npx vitest run tests/tarefas.dp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/tarefas.dp.test.ts existe, importa `criarTarefa` e `mockDpColaboradorUser`.
    - O teste do caso 1 (auto-atribuição DP) passa e assere que `empresa.findFirst` foi chamado com escopo de visibilidade aplicado.
    - O teste do caso 2 (atribuição a terceiro) assere retorno `{ ok: false, error: "não autorizado" }` e `tarefa.create` não chamado.
    - O teste do caso 3 (empresa fora do escopo) assere retorno `{ ok: false, error: "não encontrado" }`.
    - Nenhum arquivo em `src/` foi modificado por esta plan (DP-05 é reuso puro).
    - `npx vitest run tests/tarefas.dp.test.ts` passa (verde).
  </acceptance_criteria>
  <done>O teste de regressão DP-05 cobre auto-atribuição válida, bloqueio de atribuição a terceiros e bloqueio de empresa fora do escopo DP, provando o reuso setor-aware de criarTarefa sem alterar produção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → criarTarefa (Server Action) | Input de tarefa avulsa atravessa a action; já validado por tarefaSchema + escopo anti-IDOR (Phase 2/5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-DP-05 | Elevation of Privilege | criarTarefa (colaborador DP atribuindo fora do escopo) | mitigate | Já mitigado na Phase 5 via withVisibilityScope/withTarefaScope setor-aware; esta plan adiciona teste de regressão dedicado (casos 2 e 3 da Task 1) confirmando que o controle não regrediu — sem novo código, apenas prova |
| T-06-DP-05-IDOR | Tampering | criarTarefa (empresa fora do escopo de visibilidade DP) | mitigate | A action já faz `empresa.findFirst({ where: { id, ...withVisibilityScope(user) } })` antes do create (anti-IDOR); o caso 3 do teste assere que empresa fora do escopo retorna "não encontrado" sem criar tarefa |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado (RESEARCH.md Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `npx vitest run tests/tarefas.dp.test.ts` verde.
- `git diff --stat src/` vazio para esta plan (nenhuma mudança de produção).
</verification>

<success_criteria>
- DP-05: comprovado por teste de regressão que um colaborador de DP cria/atribui tarefa avulsa dentro do seu escopo de setor e é barrado fora dele, reusando o mecanismo existente sem alteração de código.
</success_criteria>

<output>
Create `.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-03-SUMMARY.md` when done
</output>

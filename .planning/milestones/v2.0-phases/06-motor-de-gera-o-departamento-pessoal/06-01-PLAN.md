---
phase: 06-motor-de-gera-o-departamento-pessoal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/dia-util.ts
  - src/lib/geracao-tarefas-dp.ts
  - tests/dia-util.test.ts
  - tests/geracao-tarefas-dp.test.ts
autonomous: true
requirements: [DP-01, DP-02, DP-03, DP-04]
must_haves:
  truths:
    - "O enum TipoObrigacao do banco aceita os valores FOLHA, ESOCIAL, FGTS e INSS"
    - "calcularQuintoDiaUtil('2026-06') retorna terça-feira 07/07/2026 e calcularQuintoDiaUtil('2026-12') retorna sexta-feira 08/01/2027"
    - "gerarTarefasDoMesDp produz exatamente 4 tarefas (FOLHA, ESOCIAL, FGTS, INSS) por empresa CLT, independente de regime tributário"
    - "FOLHA vence no 5º dia útil do mês seguinte; ESOCIAL no dia 07; FGTS e INSS no dia 15 — os 3 últimos antecipam para o dia útil anterior se caírem em fim de semana/feriado"
  artifacts:
    - path: "src/lib/geracao-tarefas-dp.ts"
      provides: "Catálogo flat de obrigações DP + gerador puro gerarTarefasDoMesDp"
      contains: "CATALOGO_OBRIGACOES_DP"
    - path: "src/lib/dia-util.ts"
      provides: "calcularQuintoDiaUtil (contagem de dia útil para frente)"
      contains: "calcularQuintoDiaUtil"
    - path: "prisma/schema.prisma"
      provides: "enum TipoObrigacao estendido com 4 valores de DP"
      contains: "FOLHA"
  key_links:
    - from: "src/lib/geracao-tarefas-dp.ts"
      to: "src/lib/dia-util.ts"
      via: "import { anticiparParaDiaUtil, calcularQuintoDiaUtil }"
      pattern: "calcularQuintoDiaUtil"
---

<objective>
Construir a camada de cálculo puro do motor de DP: estender o enum Prisma `TipoObrigacao` com os 4 novos valores de DP (FOLHA, ESOCIAL, FGTS, INSS), adicionar a função pura nova `calcularQuintoDiaUtil` em `dia-util.ts` (contagem de dia útil "para frente", para a Folha de Pagamento), e criar o catálogo irmão `geracao-tarefas-dp.ts` com o gerador puro `gerarTarefasDoMesDp`. Esta plan NÃO toca no orquestrador transacional (`geracao.ts`) nem na Server Action — isso é a Plan 06-02, que consome os símbolos criados aqui.

Purpose: Isolar a lógica de domínio testável-sem-mocks (catálogo + cálculo de prazo) da orquestração transacional, replicando exatamente a separação já existente entre `src/lib/geracao-tarefas.ts` (puro) e `src/modules/tarefas/geracao.ts` (I/O). Implementa DP-01 (Folha), DP-02 (FGTS), DP-03 (INSS), DP-04 (eSocial).

Output: `prisma/schema.prisma` (enum estendido + db push aplicado), `src/lib/dia-util.ts` (com `calcularQuintoDiaUtil`), `src/lib/geracao-tarefas-dp.ts` (novo), e os dois arquivos de teste cobrindo cada comportamento.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-CONTEXT.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-PATTERNS.md
@src/lib/geracao-tarefas.ts
@src/lib/dia-util.ts
@tests/geracao-tarefas.test.ts
@tests/dia-util.test.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-01)

Newly-created symbols introduced by this plan (exclude from source-drift verification):

- `prisma/schema.prisma`: enum `TipoObrigacao` members `FOLHA`, `ESOCIAL`, `FGTS`, `INSS` (additive)
- `src/lib/dia-util.ts`: exported function `calcularQuintoDiaUtil(competencia: string): Date`
- `src/lib/geracao-tarefas-dp.ts` (new file): exported type `TipoObrigacaoDp`; exported const `CATALOGO_OBRIGACOES_DP`; exported const `TITULO_OBRIGACAO_DP`; exported function `gerarTarefasDoMesDp(empresas, competencia)`; (internal) type `ObrigacaoRegraDp`, internal `calcularPrazoDp` / `calcularPrazoBaseDiaFixo` helpers
- `tests/geracao-tarefas-dp.test.ts` (new file): test suite for `gerarTarefasDoMesDp`
- `tests/dia-util.test.ts`: new `describe("calcularQuintoDiaUtil")` block

Reused without modification: `anticiparParaDiaUtil`, the `hd` singleton, and `isDiaUtil` in `src/lib/dia-util.ts`; the `TarefaParaCriar` shape pattern from `src/lib/geracao-tarefas.ts`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 RED): Escrever testes falhando para calcularQuintoDiaUtil e gerarTarefasDoMesDp</name>
  <files>tests/dia-util.test.ts, tests/geracao-tarefas-dp.test.ts</files>
  <read_first>
    - tests/dia-util.test.ts (helper `mesmaData` nas linhas 16-22 — REUSAR, não redefinir; estrutura dos casos `anticiparParaDiaUtil` linhas 24-71)
    - tests/geracao-tarefas.test.ts (estrutura completa do arquivo analog — mirror para o teste DP)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-PATTERNS.md (seções "tests/geracao-tarefas-dp.test.ts (new)" e "tests/dia-util.test.ts (extend)" — código-alvo exato dos testes)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md (Code Examples: valores verificados 07/07/2026 e 08/01/2027; Pitfall 1 — casos de borda exigidos)
  </read_first>
  <behavior>
    - tests/dia-util.test.ts ganha `describe("calcularQuintoDiaUtil")` com: (a) competência "2026-06" → 07/07/2026 (terça); (b) competência "2026-12" → 08/01/2027 (sexta, empurrado pelo feriado de Ano Novo); (c) asserção de que o resultado nunca cai em sábado/domingo/feriado nacional, cobrindo ao menos 2 anos diferentes.
    - tests/geracao-tarefas-dp.test.ts (novo): (a) `gerarTarefasDoMesDp([{id,responsavelId}], "2026-06")` produz exatamente os 4 tipos `["ESOCIAL","FGTS","FOLHA","INSS"]`; (b) FOLHA vence em 07/07/2026 para competência 2026-06; (c) ESOCIAL dia-base 7, FGTS/INSS dia-base 15, antecipando se cair em fds/feriado; (d) cada tarefa carrega empresaId, responsavelId, competencia e titulo não-vazio.
  </behavior>
  <action>
    Importar `calcularQuintoDiaUtil` de `@/lib/dia-util` na linha de imports existente de tests/dia-util.test.ts e adicionar o bloco `describe("calcularQuintoDiaUtil")` reusando o helper `mesmaData` já definido no arquivo. Os valores esperados são exatos: competência "2026-06" → `new Date(2026, 6, 7)`; competência "2026-12" → `new Date(2027, 0, 8)`. Para a asserção (c), reusar o padrão `new Holidays("BR")` já importado no arquivo (linha 2) mais `isSaturday`/`isSunday` de date-fns para confirmar que o resultado é dia útil, varrendo competências de pelo menos 2 anos.
    Criar tests/geracao-tarefas-dp.test.ts espelhando tests/geracao-tarefas.test.ts: importar `gerarTarefasDoMesDp` de `@/lib/geracao-tarefas-dp`. O shape de entrada de empresa é `{ id: string; responsavelId: string }` (SEM `regimeTributario` — DP não varia por regime). Asserções de tipo: ordenar `resultado.map(t => t.tipoObrigacao).sort()` e comparar com `["ESOCIAL","FGTS","FOLHA","INSS"].sort()`. Para FOLHA usar `prazo.getFullYear()===2026`, `prazo.getMonth()===6`, `prazo.getDate()===7`. Estes testes DEVEM falhar agora (RED) porque os símbolos `calcularQuintoDiaUtil` e `gerarTarefasDoMesDp` ainda não existem.
    NÃO criar nenhuma instalação — Vitest já está configurado.
  </action>
  <verify>
    <automated>npx vitest run tests/dia-util.test.ts tests/geracao-tarefas-dp.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|is not a function|geracao-tarefas-dp)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/geracao-tarefas-dp.test.ts` existe e importa `gerarTarefasDoMesDp` de `@/lib/geracao-tarefas-dp`.
    - `tests/dia-util.test.ts` contém o literal `describe("calcularQuintoDiaUtil"` e importa `calcularQuintoDiaUtil`.
    - Rodar os dois arquivos AGORA falha (RED) — a falha é por símbolo/módulo inexistente, não por erro de sintaxe do teste.
    - O helper `mesmaData` NÃO foi redefinido (continua só uma definição no arquivo).
  </acceptance_criteria>
  <done>Os dois arquivos de teste existem, cobrem todos os comportamentos listados em `<behavior>`, e falham por símbolos ainda inexistentes (estado RED esperado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Estender enum TipoObrigacao, adicionar calcularQuintoDiaUtil e criar geracao-tarefas-dp.ts</name>
  <files>prisma/schema.prisma, src/lib/dia-util.ts, src/lib/geracao-tarefas-dp.ts</files>
  <read_first>
    - prisma/schema.prisma (bloco `enum TipoObrigacao` linhas 36-42 — estender aditivamente)
    - src/lib/dia-util.ts (arquivo inteiro — REUSAR `hd` singleton e `isDiaUtil`, NÃO instanciar segundo `Holidays("BR")` nem duplicar a checagem de feriado)
    - src/lib/geracao-tarefas.ts (arquivo inteiro — analog exato; copiar a estrutura de `calcularPrazoBase`, `TarefaParaCriar`, `TITULO_OBRIGACAO`, padrão `flatMap`)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-PATTERNS.md (código-alvo exato de `CATALOGO_OBRIGACOES_DP`, `calcularQuintoDiaUtil` e `gerarTarefasDoMesDp`)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md (Pattern 1 nota crítica: FOLHA NÃO passa por anticiparParaDiaUtil; Don't Hand-Roll)
  </read_first>
  <action>
    1. Em prisma/schema.prisma, adicionar aditivamente ao `enum TipoObrigacao` os 4 valores `FOLHA`, `ESOCIAL`, `FGTS`, `INSS` após `DAS` — NÃO renomear nem remover nenhum valor existente (D-06: granular, um membro por obrigação, consistente com ICMS/PIS_COFINS/SPED_FISCAL/SPED_CONTRIBUICOES já separados). Nenhuma outra mudança de schema: `Tarefa.tipoObrigacao` e a constraint `@@unique([empresaId, tipoObrigacao, competencia])` já cobrem qualquer valor de enum.
    2. Em src/lib/dia-util.ts, adicionar `addDays, addMonths` ao import de `date-fns` (linha 26) e exportar a função pura nova `calcularQuintoDiaUtil(competencia: string): Date` que: parseia "YYYY-MM", computa `mesVencimento = addMonths(new Date(ano, mes-1, 1), 1)`, e conta 5 dias úteis PARA FRENTE usando o `isDiaUtil` já existente (avança com `addDays(atual, 1)` quando não é dia útil; incrementa contador quando é). O resultado já É dia útil por construção — NÃO compor com `anticiparParaDiaUtil`.
    3. Criar src/lib/geracao-tarefas-dp.ts como catálogo FLAT (não `Record<RegimeTributario, ...>`): exportar `type TipoObrigacaoDp = "FOLHA" | "ESOCIAL" | "FGTS" | "INSS"`; `type ObrigacaoRegraDp` discriminado por `regra` (`"QUINTO_DIA_UTIL"` para FOLHA, `"DIA_BASE"` com `diaBase` para os demais); `const CATALOGO_OBRIGACOES_DP` com FOLHA=QUINTO_DIA_UTIL, ESOCIAL=DIA_BASE/7, FGTS=DIA_BASE/15, INSS=DIA_BASE/15; `const TITULO_OBRIGACAO_DP: Record<TipoObrigacaoDp,string>` = { FOLHA:"Folha de Pagamento", ESOCIAL:"Fechamento eSocial", FGTS:"FGTS", INSS:"INSS" }. Reusar/replicar `calcularPrazoBase` do catálogo Fiscal (renomear para `calcularPrazoBaseDiaFixo` neste arquivo) para o branch DIA_BASE, aplicando `anticiparParaDiaUtil` no resultado; para o branch QUINTO_DIA_UTIL usar `calcularQuintoDiaUtil` SEM antecipar. Exportar `gerarTarefasDoMesDp(empresas: { id: string; responsavelId: string }[], competencia: string)` com o mesmo shape de retorno `TarefaParaCriar` (titulo no formato `"${TITULO_OBRIGACAO_DP[tipo]} — ${nomeMes}/${ano}"`, `tipoObrigacao: tipo`, `competencia`, `prazo`). Importar `anticiparParaDiaUtil` e `calcularQuintoDiaUtil` de `./dia-util`.
    Depois das edições, rodar os testes da Task 1 — devem ficar verdes (GREEN). NÃO migrar o loop Fiscal nem tocar em geracao.ts/actions.ts nesta plan.
  </action>
  <verify>
    <automated>npx vitest run tests/dia-util.test.ts tests/geracao-tarefas-dp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' prisma/schema.prisma | grep -cE '^\s*(FOLHA|ESOCIAL|FGTS|INSS)\s*$'` retorna 4.
    - Os valores ICMS, PIS_COFINS, SPED_FISCAL, SPED_CONTRIBUICOES, DAS continuam presentes no enum (nenhuma remoção/rename).
    - `src/lib/geracao-tarefas-dp.ts` exporta `CATALOGO_OBRIGACOES_DP` com FGTS e INSS ambos com `diaBase: 15` e ESOCIAL com `diaBase: 7`.
    - `calcularQuintoDiaUtil` em dia-util.ts reusa `isDiaUtil` existente e NÃO contém um segundo `new Holidays(`.
    - `src/lib/geracao-tarefas-dp.ts` NÃO aplica `anticiparParaDiaUtil` sobre o resultado de `calcularQuintoDiaUtil`.
    - `npx vitest run tests/dia-util.test.ts tests/geracao-tarefas-dp.test.ts` passa (GREEN).
  </acceptance_criteria>
  <done>Enum estendido aditivamente; `calcularQuintoDiaUtil` e `geracao-tarefas-dp.ts` implementados reusando os helpers existentes; os testes da Task 1 passam.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Aplicar o schema push no banco (npx prisma db push)</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (confirmar que os 4 valores de enum já foram adicionados na Task 2 antes de rodar o push)
    - .planning/STATE.md (decisões [Phase 02-01]/[Phase 03-01]: `npx prisma db push` é usado neste projeto em vez de `prisma migrate dev` — ambiente Neon sem shadow database)
  </read_first>
  <action>
    [BLOCKING] Este task é obrigatório e DEVE rodar APÓS a Task 2 (edições de schema completas) e ANTES de qualquer verificação que dependa dos novos valores de enum no banco vivo. Rodar `npx prisma db push` para sincronizar o enum `TipoObrigacao` estendido com o banco Neon. Adição de valores de enum é não-destrutiva — o comando simples deve bastar. Caso (e somente caso) o push reporte mudanças destrutivas, rodar `npx prisma db push --accept-data-loss` (mesmo padrão da decisão [Phase 03-01] no STATE.md, aplicada após confirmar 0 linhas afetadas). Em seguida rodar `npx prisma generate` para regenerar o client TypeScript com os novos valores de enum (decisão [Phase 05-04]: generate necessário antes de qualquer `tsc --noEmit` quando o schema mudou). NÃO usar `prisma migrate dev` (ambiente Neon sem shadow database).
    Nota de verificação falso-positiva: build e type-check passariam SEM este push (os tipos vêm do client gerado de schema.prisma, não do banco vivo) — por isso este task é mandatório antes de a fase passar pela verificação de runtime.
  </action>
  <verify>
    <automated>npx prisma generate && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` completa sem erro (enum sincronizado com o banco).
    - `npx prisma generate` completa sem erro.
    - `npx tsc --noEmit` passa — o client gerado expõe os novos valores `FOLHA`/`ESOCIAL`/`FGTS`/`INSS` em `TipoObrigacao`.
  </acceptance_criteria>
  <done>O enum estendido está aplicado no banco Neon e o Prisma Client regenerado expõe os 4 novos valores; type-check verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.prisma → banco Neon | Migração de schema (db push) altera a definição do enum no banco gerenciado |
| código de aplicação → date-holidays | Cálculo de prazo depende da fonte de feriados nacionais (mesma instância singleton já validada) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `calcularQuintoDiaUtil` (fonte de feriados divergente) | mitigate | Reusar o `hd` singleton e `isDiaUtil` já existentes em dia-util.ts; nunca instanciar segundo `Holidays("BR")` — fonte única de feriados (acceptance criteria da Task 2 + asserção de teste de borda Ano Novo) |
| T-06-02 | Tampering | `prisma db push` (mudança destrutiva acidental) | mitigate | Adição de enum é não-destrutiva; `--accept-data-loss` só se o push reportar destruição, após confirmação — mesmo padrão já usado em [Phase 03-01] |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado nesta fase (RESEARCH.md Package Legitimacy Audit: não aplicável — date-fns/date-holidays/@prisma já instalados e em uso produtivo) |
</threat_model>

<verification>
- `npx vitest run tests/dia-util.test.ts tests/geracao-tarefas-dp.test.ts` verde.
- `npx tsc --noEmit` verde após `npx prisma generate`.
- Enum `TipoObrigacao` no banco contém FOLHA/ESOCIAL/FGTS/INSS além dos 5 valores Fiscais.
</verification>

<success_criteria>
- DP-01/DP-02/DP-03/DP-04: o catálogo puro `geracao-tarefas-dp.ts` gera as 4 obrigações de DP com os prazos corretos (Folha 5º dia útil; eSocial 07; FGTS/INSS 15 com antecipação), comprovado pelos testes unitários verdes.
- Schema do banco sincronizado e Prisma Client regenerado (Task 3 [BLOCKING]).
</success_criteria>

<output>
Create `.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-01-SUMMARY.md` when done
</output>

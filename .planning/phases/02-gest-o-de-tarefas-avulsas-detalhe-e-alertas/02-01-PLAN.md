---
phase: 02-gest-o-de-tarefas-avulsas-detalhe-e-alertas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/visibility-scope.ts
  - src/lib/alert-prazo.ts
  - tests/alert-prazo.test.ts
  - tests/tarefas.crud.test.ts
  - tests/tarefas.idor.test.ts
  - tests/tarefas.queries.test.ts
  - tests/visibility-scope.test.ts
autonomous: true
requirements:
  - TASK-03
  - TASK-04
  - TASK-05
  - ALRT-01

must_haves:
  truths:
    - "npx prisma validate retorna sem erros após adicionar Tarefa + TarefaHistorico"
    - "calcularAlertaPrazo retorna emoji 🔴 e label 'Atrasada' quando prazo < now e status PENDENTE (per D-06, D-07)"
    - "calcularAlertaPrazo retorna emoji 🟡 e label 'Prazo próximo' quando prazo <= now+3d e status PENDENTE (per D-06, D-07)"
    - "calcularAlertaPrazo retorna sem emoji para tarefa com prazo > now+3d ou status CONCLUIDA"
    - "withTarefaScope retorna {} para DONO e { responsavelId: user.id } para COLABORADOR (per D-14)"
    - "Todos os stubs de teste Wave 0 existem e rodam sem syntax errors"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Modelos Tarefa + TarefaHistorico + enum TarefaStatus; relações inversas em Usuario e Empresa"
      contains: "model Tarefa"
    - path: "src/lib/visibility-scope.ts"
      provides: "withTarefaScope function exportada"
      exports: ["withTarefaScope"]
    - path: "src/lib/alert-prazo.ts"
      provides: "calcularAlertaPrazo helper puro"
      exports: ["calcularAlertaPrazo", "AlertaPrazo"]
    - path: "tests/alert-prazo.test.ts"
      provides: "Testes unitários para calcularAlertaPrazo — ALRT-01"
    - path: "tests/tarefas.crud.test.ts"
      provides: "Stubs de teste para TASK-03 e TASK-04"
    - path: "tests/tarefas.idor.test.ts"
      provides: "Stubs de teste IDOR para T-02-IDOR"
    - path: "tests/tarefas.queries.test.ts"
      provides: "Stubs de teste para TASK-05"
  key_links:
    - from: "src/lib/visibility-scope.ts"
      to: "src/modules/tarefas/queries.ts"
      via: "withTarefaScope import"
      pattern: "withTarefaScope"
    - from: "src/lib/alert-prazo.ts"
      to: "src/app/(app)/tarefas/tarefas-table.tsx"
      via: "calcularAlertaPrazo import"
      pattern: "calcularAlertaPrazo"
---

<objective>
Schema migration + alert helper + scope function + Wave 0 test stubs.

Esta plan executa o desbloqueio total da Fase 2: aplica a migration do banco adicionando os modelos Tarefa e TarefaHistorico (per D-01 a D-05), estende `visibility-scope.ts` com `withTarefaScope` (per D-14), cria o helper puro `calcularAlertaPrazo` (per D-06, D-07), e cria todos os stubs de teste Wave 0 exigidos pelo VALIDATION.md. Após esta plan, o banco tem o schema correto, os testes de alertas rodam verdes, e os stubs restantes existem prontos para serem preenchidos nas plans subsequentes.

Purpose: Esta é a plan bloqueante — nenhuma outra plan pode ser executada sem o schema aplicado ao banco e os tipos Prisma gerados. O executor deve concluir esta plan antes de qualquer outra da Fase 2.

Output: schema.prisma atualizado + banco migrado + `withTarefaScope` + `calcularAlertaPrazo` + 5 arquivos de teste (4 novos + 1 estendido).
</objective>

<execution_context>
@c:/Users/Novo/AppData/Roaming/Claude/claude/gsd-core/workflows/execute-plan.md
@c:/Users/Novo/AppData/Roaming/Claude/claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gest-o-de-tarefas-avulsas-detalhe-e-alertas/02-CONTEXT.md
@.planning/phases/02-gest-o-de-tarefas-avulsas-detalhe-e-alertas/02-RESEARCH.md
@.planning/phases/02-gest-o-de-tarefas-avulsas-detalhe-e-alertas/02-VALIDATION.md
@prisma/schema.prisma
@src/lib/visibility-scope.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [BLOCKING] Schema Prisma — adicionar Tarefa + TarefaHistorico + enum TarefaStatus e aplicar ao banco</name>
  <files>prisma/schema.prisma</files>
  <behavior>
    - `npx prisma validate` passa sem erros após as edições
    - `npx prisma db push` aplica ao banco Neon sem erros
    - `npx prisma generate` gera tipos TypeScript incluindo `Prisma.TarefaWhereInput` e `Prisma.TarefaHistoricoWhereInput`
    - O model Tarefa tem as 4 indexes obrigatórias: responsavelId, empresaId, prazo, status
    - TarefaHistorico tem onDelete: Cascade no campo tarefaId
    - Nenhum erro de "Ambiguous relation" — nomes de relação explícitos presentes
  </behavior>
  <action>
Editar `prisma/schema.prisma` adicionando os seguintes blocos, na ordem exata:

1. Adicionar o enum `TarefaStatus` imediatamente após o enum `RegimeTributario` existente:

```
enum TarefaStatus {
  PENDENTE
  CONCLUIDA
}
```

2. Adicionar o model `Tarefa` após o model `EmpresaRegimeHistorico`:

```
model Tarefa {
  id            String       @id @default(cuid())
  titulo        String
  descricao     String?
  empresaId     String
  empresa       Empresa      @relation(fields: [empresaId], references: [id])
  responsavelId String
  responsavel   Usuario      @relation("ResponsavelTarefa", fields: [responsavelId], references: [id])
  prazo         DateTime
  status        TarefaStatus @default(PENDENTE)
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  historico     TarefaHistorico[]

  @@index([responsavelId])
  @@index([empresaId])
  @@index([prazo])
  @@index([status])
  @@map("tarefas")
}
```

3. Adicionar o model `TarefaHistorico` imediatamente após o model `Tarefa`:

```
model TarefaHistorico {
  id             String   @id @default(cuid())
  tarefaId       String
  tarefa         Tarefa   @relation(fields: [tarefaId], references: [id], onDelete: Cascade)
  concluidoPorId String
  concluidoPor   Usuario  @relation("ConcluiuTarefa", fields: [concluidoPorId], references: [id])
  concluidoEm    DateTime @default(now())

  @@index([tarefaId])
  @@map("tarefa_historico")
}
```

4. No model `Usuario`, adicionar as relações inversas após `empresas Empresa[] @relation("ResponsavelEmpresa")`:

```
  tarefasResponsavel  Tarefa[]          @relation("ResponsavelTarefa")
  tarefasConcluidas   TarefaHistorico[] @relation("ConcluiuTarefa")
```

5. No model `Empresa`, adicionar a relação inversa após `regimeHistorico EmpresaRegimeHistorico[]`:

```
  tarefas  Tarefa[]
```

6. Após editar o arquivo, executar em sequência:
   - `npx prisma validate` — deve retornar sem erros
   - `npx prisma db push` — aplica ao banco Neon (usa DATABASE_URL do .env.local)
   - `npx prisma generate` — regenera o Prisma Client com os novos tipos

PITFALL (per RESEARCH.md Pitfall 1): As duas relações de Tarefa e TarefaHistorico para Usuario DEVEM ter nomes explícitos (@relation("ResponsavelTarefa") e @relation("ConcluiuTarefa")). Sem eles, Prisma retorna erro "Ambiguous relation". O schema acima já inclui os nomes corretos — não remover.

PITFALL (per RESEARCH.md Pitfall 2): O model Empresa DEVE ter `tarefas Tarefa[]` (relação inversa). Sem ele, `prisma generate` falha com erro de relação ausente em Empresa.
  </action>
  <verify>
    <automated>npx prisma validate && echo "SCHEMA OK"</automated>
  </verify>
  <done>
`npx prisma validate` retorna sem erros. `npx prisma db push` aplica as tabelas `tarefas` e `tarefa_historico` ao banco. `npx prisma generate` completa sem erros. O arquivo `prisma/schema.prisma` contém `model Tarefa`, `model TarefaHistorico`, `enum TarefaStatus`, e as relações inversas em Usuario e Empresa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calcularAlertaPrazo helper + withTarefaScope + testes verdes</name>
  <files>
    src/lib/alert-prazo.ts,
    src/lib/visibility-scope.ts,
    tests/alert-prazo.test.ts,
    tests/visibility-scope.test.ts
  </files>
  <behavior>
    - calcularAlertaPrazo(prazoOntem, "PENDENTE") retorna { emoji: "🔴", label: "Atrasada" }
    - calcularAlertaPrazo(prazoAmanha, "PENDENTE") retorna { emoji: "🟡", label: "Prazo próximo" }
    - calcularAlertaPrazo(prazo7dias, "PENDENTE") retorna { emoji: "", label: "" }
    - calcularAlertaPrazo(qualquerPrazo, "CONCLUIDA") retorna { textClass: incluindo "line-through" }
    - withTarefaScope({ id: "u1", role: "DONO" }) retorna {}
    - withTarefaScope({ id: "u1", role: "COLABORADOR" }) retorna { responsavelId: "u1" }
  </behavior>
  <action>
1. Criar `src/lib/alert-prazo.ts` com o helper `calcularAlertaPrazo` (per D-06, D-07, RESEARCH.md Pattern 6):

O arquivo exporta o tipo `AlertaPrazo` e a função `calcularAlertaPrazo(prazo: Date, status: "PENDENTE" | "CONCLUIDA"): AlertaPrazo`.

Lógica:
- Se status === "CONCLUIDA": retornar { emoji: "", label: "", badgeClass: "variant-secondary", textClass: "text-muted-foreground line-through" }
- Calcular `agora = new Date()` e `em3Dias = new Date(agora.getTime() + 3 * 24 * 60 * 60 * 1000)`
- Se prazo < agora: retornar { emoji: "🔴", label: "Atrasada", badgeClass: "bg-red-100 text-red-700 border-red-200 dark:bg-red-900/30 dark:text-red-400 dark:border-red-800", textClass: "text-red-600 dark:text-red-400" }
- Se prazo <= em3Dias: retornar { emoji: "🟡", label: "Prazo próximo", badgeClass: "bg-amber-100 text-amber-700 border-amber-200 dark:bg-amber-900/30 dark:text-amber-400 dark:border-amber-800", textClass: "text-amber-600 dark:text-amber-400" }
- Caso normal: retornar { emoji: "", label: "", badgeClass: "variant-outline", textClass: "text-muted-foreground" }

O tipo `AlertaPrazo` tem campos: `emoji: string`, `label: string`, `badgeClass: string`, `textClass: string`.

2. Editar `src/lib/visibility-scope.ts` adicionando a função `withTarefaScope` no final do arquivo (per D-14, RESEARCH.md Pattern 1):

```typescript
/**
 * Aplica escopo de visibilidade de tarefas conforme papel do usuário.
 * - DONO: vê todas as tarefas → retorna {} (per D-14)
 * - COLABORADOR: vê apenas tarefas onde responsavelId === user.id → retorna { responsavelId: user.id }
 *
 * Toda query de Tarefa DEVE espalhar este retorno no where.
 * NUNCA chamar db.tarefa.findMany sem withTarefaScope(user).
 */
export function withTarefaScope(user: SessionUser): Prisma.TarefaWhereInput {
  if (user.role === "DONO") {
    return {};
  }
  return { responsavelId: user.id };
}
```

NOTA: `Prisma.TarefaWhereInput` só estará disponível após a Task 1 ter executado `npx prisma generate`. Confirmar que a Task 1 foi concluída antes desta task.

3. Criar `tests/alert-prazo.test.ts` com testes unitários reais (não stubs) para `calcularAlertaPrazo` (per ALRT-01, VALIDATION.md):

Os testes cobrem:
- Caso "Atrasada": prazo = ontem (now - 1 dia), status "PENDENTE" → emoji "🔴", label "Atrasada"
- Caso "Prazo próximo": prazo = amanhã (now + 1 dia), status "PENDENTE" → emoji "🟡", label "Prazo próximo"
- Caso limite "Prazo próximo": prazo = now + 3 dias exatos, status "PENDENTE" → emoji "🟡"
- Caso "Normal": prazo = now + 7 dias, status "PENDENTE" → emoji "", label ""
- Caso "Concluída": prazo = ontem, status "CONCLUIDA" → sem emoji, textClass contém "line-through"

O helper é puro (sem I/O), então os testes não precisam de mocks. Usar datas relativas calculadas com `new Date(Date.now() + N * 24 * 60 * 60 * 1000)`.

4. Editar `tests/visibility-scope.test.ts` adicionando casos de `withTarefaScope` no final dos testes existentes (per AUTH-02, VALIDATION.md):

Casos a adicionar:
- `withTarefaScope({ id: "u123", role: "DONO" })` retorna `{}`
- `withTarefaScope({ id: "u123", role: "COLABORADOR" })` retorna `{ responsavelId: "u123" }`

Ler o arquivo existente para ver o padrão de import/describe antes de editar.
  </action>
  <verify>
    <automated>npx vitest run tests/alert-prazo.test.ts tests/visibility-scope.test.ts</automated>
  </verify>
  <done>
`npx vitest run tests/alert-prazo.test.ts tests/visibility-scope.test.ts` retorna verde com todos os casos passando. `src/lib/alert-prazo.ts` existe e exporta `calcularAlertaPrazo` e `AlertaPrazo`. `src/lib/visibility-scope.ts` exporta `withTarefaScope`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test stubs — tarefas.crud, tarefas.idor, tarefas.queries</name>
  <files>
    tests/tarefas.crud.test.ts,
    tests/tarefas.idor.test.ts,
    tests/tarefas.queries.test.ts
  </files>
  <action>
Criar três arquivos de teste stub. Cada arquivo deve ter a estrutura correta de Vitest para rodar sem syntax errors. Os stubs marcam os testes como `.todo` ou usam `vi.fn()` placeholders onde a implementação real (Server Actions, queries) ainda não existe. Os stubs NÃO devem importar módulos inexistentes — usar apenas imports condicionais ou `vi.mock`.

Verificar o padrão dos testes existentes em `tests/visibility-scope.test.ts` e `tests/empresas.idor.test.ts` antes de criar os stubs.

1. `tests/tarefas.crud.test.ts` — stubs para TASK-03 e TASK-04:
   - describe "criarTarefa" com: it.todo "cria tarefa com todos campos obrigatórios", it.todo "retorna { ok: false } sem título", it.todo "retorna { ok: false } sem empresaId", it.todo "retorna { ok: false } sem prazo"
   - describe "concluirTarefa" com: it.todo "muda status para CONCLUIDA e cria TarefaHistorico", it.todo "é idempotente se já CONCLUIDA"
   - describe "excluirTarefa" com: it.todo "remove a tarefa do banco"

2. `tests/tarefas.idor.test.ts` — stubs para T-02-IDOR:
   - describe "IDOR — concluirTarefa" com: it.todo "COLABORADOR não pode concluir tarefa de outro colaborador — retorna { ok: false } com 'não encontrado'"
   - describe "IDOR — excluirTarefa" com: it.todo "COLABORADOR não pode excluir tarefa de outro colaborador — retorna { ok: false } com 'não encontrado'"

3. `tests/tarefas.queries.test.ts` — stubs para TASK-05:
   - describe "buscarTarefaPorId" com: it.todo "retorna null para tarefa fora do escopo do COLABORADOR", it.todo "DONO consegue buscar qualquer tarefa por id", it.todo "retorna a tarefa correta para o COLABORADOR responsável"

IMPORTANTE: Os stubs devem usar `it.todo("descrição")` (sem callback) para não falhar por import de módulos inexistentes. O arquivo deve importar apenas `{ describe, it }` do Vitest no mínimo.
  </action>
  <verify>
    <automated>npx vitest run tests/tarefas.crud.test.ts tests/tarefas.idor.test.ts tests/tarefas.queries.test.ts</automated>
  </verify>
  <done>
`npx vitest run tests/tarefas.crud.test.ts tests/tarefas.idor.test.ts tests/tarefas.queries.test.ts` executa sem syntax errors. Todos os testes aparecem como "todo" no output (skipped). Nenhum teste falha por erro de import ou sintaxe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma schema → banco Neon | `db push` aplica DDL; schema inválido pode derrubar o processo de migration |
| `withTarefaScope` → query | Todo where de Tarefa DEVE incluir o retorno desta função; desvio = IDOR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-IDOR | Elevation of Privilege | `withTarefaScope` / queries de Tarefa | mitigate | `withTarefaScope` retorna `{ responsavelId: user.id }` para COLABORADOR — aplicado em TODA query de Tarefa nos plans seguintes; testado em `tests/visibility-scope.test.ts` e `tests/tarefas.idor.test.ts` |
| T-02-SCHEMA | Tampering | `prisma/schema.prisma` — relações sem nome explícito | mitigate | Nomes de relação explícitos (@relation("ResponsavelTarefa"), @relation("ConcluiuTarefa")) per RESEARCH.md Pitfall 1; `npx prisma validate` bloqueia nomes ausentes |
| T-02-SC | Tampering | npm install (date-fns — única dep nova) | accept | `date-fns` auditado no RESEARCH.md Package Legitimacy Audit como aprovado; ~9 anos no registry, ~80M downloads/semana; esta plan não instala dependências |
</threat_model>

<verification>
Após concluir todas as tasks:

```bash
# Validar schema
npx prisma validate

# Rodar suite completa de alertas + scope
npx vitest run tests/alert-prazo.test.ts tests/visibility-scope.test.ts

# Rodar stubs (devem aparecer como todo/skipped, não falhar)
npx vitest run tests/tarefas.crud.test.ts tests/tarefas.idor.test.ts tests/tarefas.queries.test.ts

# Suite completa
npx vitest run
```
</verification>

<success_criteria>
1. `npx prisma validate` retorna sem erros — schema sintaticamente válido com Tarefa + TarefaHistorico + TarefaStatus
2. `npx prisma db push` aplicou as tabelas `tarefas` e `tarefa_historico` ao banco Neon
3. `tests/alert-prazo.test.ts` — todos os 5 casos passam (atrasada, próximo, limite 3 dias, normal, concluída)
4. `tests/visibility-scope.test.ts` — casos de `withTarefaScope` (DONO e COLABORADOR) passam
5. `tests/tarefas.crud.test.ts`, `tests/tarefas.idor.test.ts`, `tests/tarefas.queries.test.ts` — existem e rodam sem erros (stubs como `.todo`)
6. `src/lib/alert-prazo.ts` exporta `calcularAlertaPrazo` e `AlertaPrazo`
7. `src/lib/visibility-scope.ts` exporta `withTarefaScope`
</success_criteria>

<output>
Create `.planning/phases/02-gest-o-de-tarefas-avulsas-detalhe-e-alertas/02-01-SUMMARY.md` when done
</output>

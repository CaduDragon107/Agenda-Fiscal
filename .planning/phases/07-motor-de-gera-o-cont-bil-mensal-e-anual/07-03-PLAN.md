---
phase: 07-motor-de-gera-o-cont-bil-mensal-e-anual
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/tarefas.contabil.test.ts
autonomous: true
requirements: [CONT-06]
must_haves:
  truths:
    - "Um COLABORADOR do setor CONTABIL cria tarefa avulsa atribuída a si mesmo, dentro do seu escopo setor-aware, sem nenhuma mudança de código de produção"
    - "criarTarefa generaliza para o setor CONTABIL por composição com withVisibilityScope/withTarefaScope já validados na Fase 5"
  artifacts:
    - path: "tests/tarefas.contabil.test.ts"
      provides: "Teste de regressão CONT-06 (tarefa avulsa Contábil), mirror de tests/tarefas.dp.test.ts"
      contains: "mockContabilColaboradorUser"
  key_links:
    - from: "tests/tarefas.contabil.test.ts"
      to: "src/app/(app)/tarefas/actions.ts"
      via: "import criarTarefa"
      pattern: "criarTarefa"
    - from: "tests/tarefas.contabil.test.ts"
      to: "tests/setup.ts"
      via: "import mockContabilColaboradorUser"
      pattern: "mockContabilColaboradorUser"
---

<objective>
Provar, por teste de regressão, que CONT-06 (tarefa avulsa atribuível aos colaboradores Contábil) já está satisfeito por composição com o escopo setor-aware da Fase 5 — sem nenhuma mudança em código de produção. É o mesmo padrão já aplicado a DP-05 na Fase 6 (`tests/tarefas.dp.test.ts`).

Purpose: Travar a garantia de que `criarTarefa` funciona para o setor CONTABIL exatamente como para DP/Fiscal, evitando regressão futura. D-12: reuso direto do mecanismo existente, zero código novo de produção.
Output: `tests/tarefas.contabil.test.ts` verde.
</objective>

<artifacts_produced>
## Artifacts this phase produces (símbolos NOVOS criados por esta plan)

- New file: `tests/tarefas.contabil.test.ts` (apenas teste; nenhum símbolo de produção novo).

Símbolos consumidos (já existentes, NÃO drift): `criarTarefa` (src/app/(app)/tarefas/actions.ts), `mockContabilColaboradorUser` (tests/setup.ts — já existe), `withVisibilityScope`/`withTarefaScope` (src/lib/visibility-scope.ts).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-CONTEXT.md
@.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-PATTERNS.md
@tests/tarefas.dp.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teste de regressão CONT-06 — tarefa avulsa Contábil</name>
  <files>tests/tarefas.contabil.test.ts</files>
  <read_first>
    - tests/tarefas.dp.test.ts (mirror EXATO — copiar estrutura: mocks de @/lib/db, @/auth, next/cache; describe/it; asserts sobre empresaFindFirstMock e tarefaCreateMock)
    - tests/setup.ts (mockContabilColaboradorUser já existe — nome "Contabil1", setor "CONTABIL"; usar em vez de mockDpColaboradorUser)
    - src/app/(app)/tarefas/actions.ts (criarTarefa — assinatura e fluxo, para garantir que os mocks refletem o caminho real)
    - src/lib/visibility-scope.ts (withVisibilityScope/withTarefaScope retornam escopo por setor CONTABIL quando user.setor === "CONTABIL")
  </read_first>
  <action>
    Criar `tests/tarefas.contabil.test.ts` espelhando `tests/tarefas.dp.test.ts` linha a linha, trocando: `mockDpColaboradorUser` → `mockContabilColaboradorUser` (já exportado por tests/setup.ts), todas as referências de setor "DP" → "CONTABIL" nos comentários/asserts, e o nome do describe para "CONT-06 — criarTarefa (tarefa avulsa, colaborador Contábil)". Cobrir os mesmos cenários do DP: (a) COLABORADOR de Contábil cria tarefa avulsa atribuída a si mesmo dentro do seu escopo de setor (empresaFindFirstMock resolve uma empresa dentro do escopo CONTABIL, tarefaCreateMock é chamado com responsavelId = o próprio colaborador); (b) o caminho escopado é respeitado (a query de empresa passa pelo escopo setor-aware, não acessa empresa fora do setor). NÃO alterar nenhum arquivo de produção — esta plan é exclusivamente um teste de regressão (D-12, mesmo padrão de DP-05/06-03).
  </action>
  <verify>
    <automated>npx vitest run tests/tarefas.contabil.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tarefas.contabil.test.ts` existe e importa `mockContabilColaboradorUser` de "./setup"
    - `grep -c "CONTABIL" tests/tarefas.contabil.test.ts` retorna ao menos 1
    - `npx vitest run tests/tarefas.contabil.test.ts` passa com todos os testes verdes
    - Nenhum arquivo em `src/` foi modificado por esta plan (apenas o arquivo de teste novo)
  </acceptance_criteria>
  <done>Teste de regressão CONT-06 verde, provando que tarefa avulsa de Contábil reusa o escopo setor-aware sem mudança de produção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| COLABORADOR Contábil → criarTarefa | Atribuição de tarefa avulsa restrita pelo escopo setor-aware (não pode atribuir fora do seu setor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CONT-06 | Elevation of Privilege | criarTarefa (colaborador Contábil) | mitigate | `withVisibilityScope`/`withTarefaScope` já setor-aware desde a Fase 5 (V4 Access Control) — esta plan adiciona o teste de regressão que trava o comportamento, sem mudar produção (D-12) |
| T-CONT-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run tests/tarefas.contabil.test.ts` verde
- `git status` confirma que apenas `tests/tarefas.contabil.test.ts` foi criado (nenhuma mudança em src/)
- `npm test` (suite completa) verde
</verification>

<success_criteria>
- Teste de regressão CONT-06 verde
- Zero mudança em código de produção (D-12)
- COLABORADOR Contábil cria tarefa avulsa dentro do escopo setor-aware
</success_criteria>

<output>
Create `.planning/phases/07-motor-de-gera-o-cont-bil-mensal-e-anual/07-03-SUMMARY.md` when done
</output>

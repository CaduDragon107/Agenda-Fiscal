---
phase: 10-notifica-es-in-app
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/components/ui/popover.tsx
  - src/components/ui/scroll-area.tsx
  - src/app/(app)/notification-bell.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/app-sidebar.tsx
  - src/modules/tarefas/queries.ts
autonomous: false
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-04]

must_haves:
  truths:
    - "Um sino com badge de contagem aparece no header de qualquer página autenticada (D-07, D-09)"
    - "Clicar no sino abre um dropdown inline (Popover) listando as notificações não-lidas (D-08)"
    - "Cada item do dropdown linka para /tarefas/[id] e marca como lida ao clicar (D-08, D-04)"
    - "Existe 'Marcar todas como lidas' quando há ≥1 item (UI-SPEC)"
    - "A badge antiga do item 'Tarefas' na sidebar foi removida (D-10) — sem duplicação de contagem"
    - "A sincronização sob demanda roda no AppLayout antes de renderizar (D-02)"
  artifacts:
    - path: "src/app/(app)/notification-bell.tsx"
      provides: "Client component: Popover + badge + lista + mark-as-read"
      exports: ["NotificationBell"]
    - path: "src/components/ui/popover.tsx"
      provides: "shadcn Popover primitive (CLI-gerado)"
    - path: "src/components/ui/scroll-area.tsx"
      provides: "shadcn ScrollArea primitive (CLI-gerado)"
    - path: "src/app/(app)/layout.tsx"
      provides: "Wiring: sync + fetch não-lidas + render do sino; badge removida"
      contains: "sincronizarNotificacoes"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "sincronizarNotificacoes + listarNotificacoesNaoLidas"
      via: "import de @/modules/notificacoes/{sync,queries}"
      pattern: "sincronizarNotificacoes|listarNotificacoesNaoLidas"
    - from: "src/app/(app)/notification-bell.tsx"
      to: "marcarNotificacaoComoLidaAction"
      via: "onClick na row (Server Action)"
      pattern: "marcarNotificacaoComoLidaAction"
    - from: "src/app/(app)/notification-bell.tsx item"
      to: "/tarefas/[id]"
      via: "Link href"
      pattern: "/tarefas/"
---

<objective>
Construir a UI da Phase 10: instalar os primitivos shadcn `popover` e `scroll-area`, criar o client component `NotificationBell` (sino no header com badge, dropdown inline de não-lidas, mark-as-read ao clicar), ligar tudo no `AppLayout` (sincronização sob demanda + busca de não-lidas + render do sino), e remover a badge antiga da sidebar (D-10) eliminando a fonte de contagem duplicada.

Purpose: Esta é a entrega visível ao usuário — o sino que satisfaz os critérios de sucesso 1-4 do ROADMAP. Consome inteiramente o backend da Plan 10-02. A remoção da badge antiga (D-10) garante fonte única de contagem.
Output: 2 primitivos shadcn + `NotificationBell` + `layout.tsx` religado + sidebar sem badge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-notifica-es-in-app/10-CONTEXT.md
@.planning/phases/10-notifica-es-in-app/10-UI-SPEC.md
@.planning/phases/10-notifica-es-in-app/10-PATTERNS.md
@src/app/(app)/layout.tsx
@src/app/(app)/app-sidebar.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 10-03)

New symbols:
- `NotificationBell` — client component (`"use client"`), props `{ notificacoes, unreadCount }` — `src/app/(app)/notification-bell.tsx`
- `src/components/ui/popover.tsx` — shadcn Popover (Popover, PopoverTrigger, PopoverContent)
- `src/components/ui/scroll-area.tsx` — shadcn ScrollArea

Modified:
- `src/app/(app)/layout.tsx` — chama `sincronizarNotificacoes` + `listarNotificacoesNaoLidas` + `contarNaoLidas`, renderiza `<NotificationBell>` no header; remove `contarAlertasTarefas` import/call e prop `contadorAlertas`
- `src/app/(app)/app-sidebar.tsx` — remove prop `contadorAlertas` e o bloco `<span>` da badge (D-10)
- `src/modules/tarefas/queries.ts` — remove `contarAlertasTarefas` (agora órfã após D-10; confirmado único call-site era layout.tsx)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Instalar primitivos shadcn (popover, scroll-area) e criar NotificationBell</name>
  <files>src/components/ui/popover.tsx, src/components/ui/scroll-area.tsx, src/app/(app)/notification-bell.tsx</files>
  <read_first>
    - src/app/(app)/app-sidebar.tsx (linhas 1-7 imports lucide/Link/usePathname; linhas 100-107 padrão da badge `aria-label` + cap `> 99 ? "99+"`; linhas 143-172 shape DropdownMenu trigger/content a espelhar com Popover)
    - src/components/ui/dropdown-menu.tsx (convenção de wrapper shadcn já instalado — confirmar que popover.tsx ainda não existe)
    - .planning/phases/10-notifica-es-in-app/10-UI-SPEC.md (Component & Interaction Notes itens 1-6; Copywriting Contract — textos exatos; Color — tags coloridas por tipo)
    - .planning/phases/10-notifica-es-in-app/10-PATTERNS.md (seção notification-bell.tsx — shape Popover+ScrollArea+Link)
  </read_first>
  <action>
    Parte A — instalar primitivos via CLI (NÃO escrever à mão): `npx shadcn@latest add popover` e `npx shadcn@latest add scroll-area`. Confirmar que criam `src/components/ui/popover.tsx` e `src/components/ui/scroll-area.tsx`. (RESEARCH Package Legitimacy Audit já aprovou @radix-ui/react-popover/scroll-area — pipeline shadcn confiável, sem checkpoint de pacote.)

    Parte B — criar `src/app/(app)/notification-bell.tsx` (`"use client"`). Props: `{ notificacoes: Array<{ id: string; tipo: "VENCENDO" | "ATRASADA" | "AVULSA_ATRIBUIDA"; tarefa: { id: string; titulo: string; prazo: Date | string; empresa: { nome: string } } }>; unreadCount: number }`. Estrutura: `<Popover>` com `<PopoverTrigger asChild>` envolvendo um `<Button variant="ghost" size="icon" className="relative">` com ícone `Bell` (lucide-react) e badge de contagem (`<span>` absoluto, classes do UI-SPEC, cap `unreadCount > 99 ? "99+" : unreadCount`, só renderiza quando `unreadCount > 0`). `aria-label`: quando `unreadCount > 0` → `"${unreadCount} notificações não lidas"`, senão `"Notificações"` (UI-SPEC Copywriting). `<PopoverContent align="end" className="w-80 p-0">` contém: heading "Notificações" (text-base/600) + botão ghost "Marcar todas como lidas" (só visível quando `notificacoes.length > 0`, chama `marcarTodasComoLidasAction`); um `<ScrollArea className="max-h-96">` com a lista. Cada item é um `<Link href={\`/tarefas/${n.tarefa.id}\`} onClick={() => marcarNotificacaoComoLidaAction(n.id)}>` com 2 linhas (anatomia UI-SPEC item 4): linha 1 = tag de tipo (Badge colorido) + título da tarefa (text-sm/600); linha 2 = empresa + prazo formatado (text-xs text-muted-foreground). Tags por tipo (UI-SPEC Color + Copywriting): VENCENDO → "Vencendo em breve" amber (`bg-amber-100 text-amber-700 border-amber-200 dark:...`); ATRASADA → "Atrasada" vermelho (`bg-red-100 text-red-700 ...`, reusar classes de `alert-prazo.ts`); AVULSA_ATRIBUIDA → "Nova tarefa atribuída" azul/neutro. Empty state quando `notificacoes.length === 0`: heading "Nenhuma notificação" + corpo "Você será avisado aqui quando uma tarefa sua estiver vencendo, atrasada, ou quando receber uma tarefa avulsa." (`text-muted-foreground`, centralizado). Importar as Server Actions de `@/app/(app)/notification-actions`. Formatar `prazo` com `date-fns` (já instalado — Phase 02-02), ex. `format(new Date(n.tarefa.prazo), "dd/MM/yyyy")`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/popover.tsx` e `src/components/ui/scroll-area.tsx` existem.
    - `src/app/(app)/notification-bell.tsx` existe, inicia com `"use client"`, exporta `NotificationBell`.
    - `grep -n "marcarNotificacaoComoLidaAction" src/app/(app)/notification-bell.tsx` mostra uso no onClick.
    - `grep -n "marcarTodasComoLidasAction" src/app/(app)/notification-bell.tsx` mostra uso.
    - `grep -n "/tarefas/" src/app/(app)/notification-bell.tsx` mostra o Link por item (D-08).
    - As 3 strings de tag aparecem: "Vencendo em breve", "Atrasada", "Nova tarefa atribuída".
    - A string de empty state "Nenhuma notificação" aparece.
    - O cap `> 99 ? "99+"` está presente (convenção UI-SPEC item 5).
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>Primitivos shadcn instalados; `NotificationBell` renderiza sino+badge+dropdown de não-lidas com tags coloridas, links e mark-as-read; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Ligar sync+fetch no AppLayout e remover a badge da sidebar (D-02, D-07, D-10)</name>
  <files>src/app/(app)/layout.tsx, src/app/(app)/app-sidebar.tsx, src/modules/tarefas/queries.ts</files>
  <read_first>
    - src/app/(app)/layout.tsx (estado atual — linha 10 import `contarAlertasTarefas`, linha 27 call, linha 31 prop pass, linhas 33-36 header onde o sino entra)
    - src/app/(app)/app-sidebar.tsx (linha 50 prop `contadorAlertas` no type, linha 53 no destructuring, linhas 100-107 bloco `<span>` da badge a remover)
    - src/modules/tarefas/queries.ts (linhas 98-114 `contarAlertasTarefas` — função a remover após D-10)
    - .planning/phases/10-notifica-es-in-app/10-RESEARCH.md (Pitfall 5 — remoção atômica em 3 arquivos para não deixar prop/import órfão)
  </read_first>
  <action>
    Parte A — `src/app/(app)/layout.tsx`: remover `import { contarAlertasTarefas } from "@/modules/tarefas/queries"` e a linha `const contadorAlertas = await contarAlertasTarefas(session.user)`. Adicionar imports de `@/modules/notificacoes/sync` (`sincronizarNotificacoes`), `@/modules/notificacoes/queries` (`listarNotificacoesNaoLidas`, `contarNaoLidas`) e `./notification-bell` (`NotificationBell`). Após o guard de sessão e antes do render: `await sincronizarNotificacoes(session.user)` (D-02 — roda a cada carregamento), depois `const notificacoes = await listarNotificacoesNaoLidas(session.user)` e `const unreadCount = await contarNaoLidas(session.user)`. No JSX: remover `contadorAlertas={contadorAlertas}` do `<AppSidebar>`; dentro do `<header>`, após o `<Separator>`, inserir `<NotificationBell notificacoes={notificacoes} unreadCount={unreadCount} />` com um wrapper `className="ml-auto"` para empurrar à direita (UI-SPEC item 1).

    Parte B — `src/app/(app)/app-sidebar.tsx`: remover `contadorAlertas: number` do type `AppSidebarProps`, remover `contadorAlertas` do destructuring de parâmetros, e remover INTEIRAMENTE o bloco `{contadorAlertas > 0 && ( <span ...>...</span> )}` (linhas ~100-107). Não deixar import/prop órfão (Pitfall 5).

    Parte C — `src/modules/tarefas/queries.ts`: remover a função `contarAlertasTarefas` (e seu comentário JSDoc) — confirmado que o único call-site era `layout.tsx`, agora removido. Antes de remover, rodar `grep -rn "contarAlertasTarefas" src/` para confirmar zero call-sites remanescentes além da própria definição.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "contarAlertasTarefas" src/` retorna ZERO resultados (função e todos os call-sites removidos — D-10, Pitfall 5).
    - `grep -n "contadorAlertas" src/app/(app)/app-sidebar.tsx` retorna vazio (prop e badge removidas).
    - `grep -n "contadorAlertas" src/app/(app)/layout.tsx` retorna vazio.
    - `grep -n "sincronizarNotificacoes" src/app/(app)/layout.tsx` mostra a chamada antes do render (D-02).
    - `grep -n "NotificationBell" src/app/(app)/layout.tsx` mostra o render no header.
    - `grep -n "ml-auto" src/app/(app)/layout.tsx` confirma o sino empurrado à direita (UI-SPEC item 1).
    - `npx tsc --noEmit` limpo (sem prop órfã — Pitfall 5).
    - `npm run build` completa com sucesso.
  </acceptance_criteria>
  <done>AppLayout sincroniza e busca não-lidas, renderiza o sino no header; badge antiga da sidebar e `contarAlertasTarefas` removidas sem órfãos; build e tsc limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual do sino, dropdown e remoção da badge</name>
  <what-built>
    Sino de notificações no header (visível em qualquer página autenticada) com badge de contagem de não-lidas; dropdown inline (Popover) listando notificações não-lidas com tags coloridas (Vencendo em breve / Atrasada / Nova tarefa atribuída), cada item linkando para a tarefa e marcando como lida ao clicar; botão "Marcar todas como lidas"; empty state. A badge antiga do item "Tarefas" na sidebar foi removida (fonte única de contagem agora é o sino).
  </what-built>
  <action>
    Esta é uma task de verificação humana (checkpoint:human-verify). O executor NÃO escreve código aqui — pausa e apresenta ao usuário (dono do escritório) os passos de verificação visual/funcional do sino, dropdown, links, mark-as-read e remoção da badge da sidebar descritos em <how-to-verify>, e aguarda o sinal de retorno. Toda a automação relevante já foi entregue nas Tasks 1 e 2 desta plan.
  </action>
  <verify>
    <human-check>Usuário confirma os 8 passos de <how-to-verify> contra a 10-UI-SPEC.md (sino, dropdown inline, tags coloridas, links, mark-as-read, badge da sidebar ausente, escopo DONO vs COLABORADOR, empty state).</human-check>
  </verify>
  <done>Usuário respondeu "approved"; nenhum problema visual/funcional pendente.</done>
  <how-to-verify>
    1. Rodar `npm run dev` e logar como um usuário com tarefas pendentes (ex.: um colaborador FISCAL, ou o DONO para visão geral).
    2. Em qualquer página (/empresas, /tarefas, /dashboards), confirmar que o sino aparece no canto direito do header com um número de badge (se houver não-lidas).
    3. Clicar no sino: confirmar que abre um dropdown inline (NÃO navega de página) listando as notificações, cada uma com tag colorida correta (amarela=vencendo, vermelha=atrasada, azul=avulsa), título da tarefa, empresa e prazo.
    4. Clicar em um item: confirmar que navega para /tarefas/[id] e que, ao voltar/recarregar, aquele item sumiu do dropdown e o badge decrementou (marcado como lida — D-04).
    5. Clicar em "Marcar todas como lidas" (com ≥1 item): confirmar que a lista esvazia e o badge zera.
    6. Abrir a sidebar e confirmar que o item "Tarefas" NÃO tem mais nenhuma badge/contagem (D-10 — sem duplo número).
    7. Logar como DONO e confirmar que o sino mostra notificações de tarefas de vários responsáveis (NOTF-04); logar como COLABORADOR e confirmar que só vê as próprias.
    8. Com zero não-lidas, confirmar o empty state "Nenhuma notificação".
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas visuais/funcionais encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (NotificationBell) → Server Action | clique no item dispara `marcarNotificacaoComoLidaAction(id)` — id vem do prop server-fornecido, mas a action revalida ownership server-side |
| Server Component (AppLayout) → browser | a lista de não-lidas é serializada como prop; deve estar pré-escopada server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-HYDRATION | Information Disclosure | prop `notificacoes` serializada do AppLayout | mitigate | Lista vem de `listarNotificacoesNaoLidas` (escopada por `withTarefaScope` na Plan 10-02) ANTES da serialização — nenhum fetch-all-then-filter client-side; o cliente nunca recebe notificações fora do escopo |
| T-10-CLIENT-MUT | Tampering | `marcarNotificacaoComoLidaAction` chamada do cliente | mitigate | A action (Plan 10-02) revalida ownership via `findFirst` escopado por tarefa server-side — o cliente não pode marcar uma notificação fora do escopo mesmo forjando o id |
| T-10-SC | Tampering | `npx shadcn add popover/scroll-area` | mitigate | RESEARCH Package Legitimacy Audit: @radix-ui/react-popover VERIFIED no npm registry (1.1.17), scroll-area mesmo trust-tier (monorepo Radix); pipeline shadcn já confiável (10+ componentes instalados) — verdict OK, sem [ASSUMED]/[SUS]/[SLOP], sem checkpoint de pacote requerido |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo (sem prop/import órfão — D-10, Pitfall 5).
- `npm run build` completa.
- `npm test` (suíte completa) verde — sem regressão.
- `grep -rn "contarAlertasTarefas" src/` retorna zero (D-10 limpo).
- Checkpoint humano: sino visível, dropdown funcional, links, mark-as-read, badge da sidebar ausente, escopo DONO vs COLABORADOR (D-07/D-08/D-09/D-10/NOTF-04).
</verification>

<success_criteria>
- Critério de sucesso ROADMAP 1: sino com badge no header visível em qualquer página.
- Critérios 2-4: notificações de vencendo/atrasada/avulsa aparecem no sino.
- Critério 5 (NOTF-04): COLABORADOR só vê as suas, DONO vê de todos.
- D-08: dropdown inline com links para /tarefas/[id].
- D-09: badge conta não-lidas.
- D-10: badge antiga da sidebar removida, fonte única de contagem.
</success_criteria>

<output>
Create `.planning/phases/10-notifica-es-in-app/10-03-SUMMARY.md` when done
</output>

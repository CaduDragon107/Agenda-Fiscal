---
phase: 10
slug: notificacoes-in-app
status: draft
shadcn_initialized: true
preset: radix-nova
created: 2026-06-29
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `components.json` present) |
| Preset | `radix-nova` (style), baseColor `neutral`, cssVariables on, menuColor `default`, menuAccent `subtle` |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react (already used in `app-sidebar.tsx`: `Building2`, `ListChecks`, `LogOut`, `Users`, `LayoutDashboard`) |
| Font | `var(--font-sans)` (Geist Sans, project default — `src/app/globals.css`) |

**New shadcn components required for this phase:**

| Component | Status | Action |
|-----------|--------|--------|
| `popover` | Not installed (`src/components/ui/` has no `popover.tsx`) | Install via `npx shadcn add popover` — used for the bell dropdown panel (D-08: inline dropdown, not navigation) |
| `dropdown-menu` | Already installed, already used (`app-sidebar.tsx`) | Reuse only if a `Popover` proves insufficient for focus-trap behavior; default choice is `Popover` since the bell needs a custom-shaped panel (list of notification rows), not a menu of actions |
| `scroll-area` | Not installed | Install via `npx shadcn add scroll-area` — caps dropdown height when notification list grows (Claude's Discretion item in CONTEXT.md: list could grow large) |
| `badge` | Already installed | Reuse `Badge` (`src/components/ui/badge.tsx`) for the unread-count pill on the bell, sized down |

No third-party registries. `Registry: shadcn official` only — see Registry Safety section.

---

## Spacing Scale

Declared values (must be multiples of 4), matching project-wide convention already in use (`app-sidebar.tsx`, `layout.tsx` use `gap-2`, `px-4`, `p-4`/`p-6`):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-badge gap on the bell, internal row padding adjustments |
| sm | 8px | Gap between header elements (bell sits next to `SidebarTrigger`/`Separator`, matching existing `gap-2`) |
| md | 16px | Default padding inside dropdown panel and notification rows (`p-4`) |
| lg | 24px | Not used in this phase (no full-page layout introduced — dropdown only) |
| xl | 32px | Not used |

Exceptions:
- Bell **icon-only touch target**: 32px (`size-8`, matches existing `Button` `size="icon"` = `size-8`) — consistent with `SidebarTrigger` sizing already in the header.
- Notification row minimum height: 48px (touch-target floor, nearest standard-scale value) even though visual content may be shorter — applies to dropdown list items per accessibility convention.

---

## Typography

Reusing the project's existing 3-tier scale (no new sizes introduced — verified against `tarefas/page.tsx`, `app-sidebar.tsx`):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Label | 12px (`text-xs`) | 400 (regular) | 1.4 — used for badge count, empresa/timestamp metadata under each notification |
| Heading | 16px (`text-base`) | 600 (semibold) | 1.2 — dropdown panel title "Notificações" |
| Display | not used | — | — |

Notes:
- Notification row title (tarefa name) uses Body 14px/600 (semibold) to read as the primary line within each row — the only other emphasis weight besides Heading.
- Exactly 2 weights used project-wide and in this phase: 400 (regular — body copy, labels/metadata) and 600 (semibold — panel heading, notification row titles). No 500/medium weight introduced.

---

## Color

Reusing existing token set from `src/app/globals.css` (oklch neutral base + semantic red/amber already validated in `src/lib/alert-prazo.ts`). No new colors introduced.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` / `var(--popover)` | Dropdown panel background, page background |
| Secondary (30%) | `var(--secondary)` / `var(--muted)` | Notification row hover state, empty-state panel background |
| Accent (10%) | `var(--destructive)` (red, existing token) | Reserved EXCLUSIVELY for: (1) unread-count badge on the bell icon, (2) "Atrasada" notification type tag — matches `alert-prazo.ts` `badgeClass` for atrasada (`bg-red-100 text-red-700 ...`/dark variants) |
| Secondary semantic | Amber (existing `alert-prazo.ts` palette: `bg-amber-100 text-amber-700 border-amber-200` / dark variants) | Reserved EXCLUSIVELY for "Vencendo em breve" notification type tag — same threshold/palette already used in task list alerts, per D-02/NOTF-01 requirement to reuse the same visual threshold |
| Tertiary semantic | `var(--chart-1)` (blue, existing chart token) or `var(--accent)` neutral | Reserved EXCLUSIVELY for "Tarefa avulsa atribuída" notification type tag — this is a new notification type with no precedent color in `alert-prazo.ts`; using a neutral-blue distinguishes it from the red/amber prazo-alert pair without inventing a new palette outside existing chart tokens |
| Destructive | `var(--destructive)` | "Marcar todas como lidas" is non-destructive (reversible is not needed — already a one-way state per D-04, but it is not data-loss); no destructive-confirm UI needed for this phase (no delete action exists for notifications, per CONTEXT.md — only "ler" transitions, no delete) |

Accent reserved for: **bell badge count number, "Atrasada" tag (red), "Vencendo" tag (amber), "Avulsa atribuída" tag (blue/neutral)** — never any other element in the dropdown (links, buttons, panel chrome stay neutral/secondary).

**Focal point:** the unread notification rows (with their colored type tag) are the primary visual anchor of the dropdown panel; the "Marcar todas como lidas" CTA in the header is secondary, styled as a plain/ghost text-link-weight button so it never competes with the colored row tags for attention.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Marcar todas como lidas" (button inside dropdown header, only visible when ≥1 unread item exists) |
| Bell aria-label (badge present) | "{N} notificações não lidas" (mirrors existing pattern in `app-sidebar.tsx`: `aria-label={`${contadorAlertas} tarefas com alertas de prazo`}`) |
| Bell aria-label (no unread) | "Notificações" |
| Dropdown panel heading | "Notificações" |
| Empty state heading | "Nenhuma notificação" |
| Empty state body | "Você será avisado aqui quando uma tarefa sua estiver vencendo, atrasada, ou quando receber uma tarefa avulsa." |
| Notification type tag — vencendo | "Vencendo em breve" |
| Notification type tag — atrasada | "Atrasada" |
| Notification type tag — avulsa | "Nova tarefa atribuída" |
| Item click / mark-as-read affordance | No separate label — clicking the row both marks as read (D-04) and navigates to `/tarefas/[id]` (D-08); no extra confirmation copy needed since this is non-destructive and reversible-by-resync (D-05) |
| "Ver tudo" link (if list exceeds display cap) | "Ver todas as notificações" — only required if Claude's Discretion item (item cap) is implemented with a cap; if no cap is implemented in this phase, omit |
| Error state (sync fails silently) | No user-facing error copy planned — sync failure should fail closed (show last-known badge count / empty list) rather than show an error toast, since this is a passive background sync (D-02), not a user-initiated action with a failure mode the user needs to react to. If executor surfaces a toast on hard failure: "Não foi possível atualizar notificações agora." |
| Destructive actions | None in this phase — no delete/dismiss-individually action exists per CONTEXT.md (notifications transition PENDENTE→lida only, or disappear when source tarefa is concluded/cancelled, never via explicit user delete) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `popover`, `scroll-area` (new), `badge`, `button` (existing) | not required — official registry, no vetting gate applicable |

No third-party registries declared for this phase.

---

## Component & Interaction Notes (for planner/executor)

1. **Placement**: Bell renders in `src/app/(app)/layout.tsx` header, between `Separator` and the (currently empty) right side of the flex row — `<header className="flex h-14 shrink-0 items-center gap-2 border-b px-4">`. Use `ml-auto` on the bell wrapper to push it to the right edge of the header, consistent with common "actions on the right" header convention (not contradicted by any existing pattern in this codebase, since the header is currently sparse).
2. **Server/Client split**: Per established pattern in `AppLayout` (Server Component fetching `contarAlertasTarefas` and passing as prop to client `AppSidebar`), the bell's initial unread count and sync should be fetched server-side in `AppLayout` and passed as a prop to a new client component (e.g. `NotificationBell`), which owns the `Popover` open state and "mark as read" Server Action calls. This mirrors the exact prop-drilling pattern already used for `contadorAlertas`.
3. **Sidebar badge removal (D-10)**: Remove `contadorAlertas` prop from `AppSidebar`, the badge `<span>` block (lines ~100-107 of `app-sidebar.tsx`), and the now-unused `contarAlertasTarefas` import/call in `layout.tsx`. This is a deletion, not a new visual — no new contract needed beyond "remove cleanly, no orphaned prop types."
4. **Dropdown row anatomy** (top to bottom, per row): type tag (Badge, colored per Color section) + tarefa título (Body 14px/600 semibold) on one line; empresa nome + prazo formatted date (Label 12px/400 regular, `text-muted-foreground`) on the line below — mirrors the existing two-line density pattern visible in `app-sidebar.tsx`'s user footer (`text-sm font-medium` + `text-xs text-muted-foreground`).
5. **Badge count cap**: Reuse exact existing convention from `app-sidebar.tsx` line 105: `{contadorAlertas > 99 ? "99+" : contadorAlertas}` — apply identically to the new bell badge count.
6. **Empty state**: Render inside the `Popover` content area itself (not a separate page) — centered text, no illustration needed (project has no illustration system established elsewhere), using `text-muted-foreground` body copy per the Color/Typography contract above.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---
phase: 10-notifica-es-in-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/notificacoes.sync.test.ts
  - tests/notificacoes.idor.test.ts
  - tests/notificacoes.read-action.test.ts
  - src/lib/alert-prazo.ts
  - prisma/schema.prisma
autonomous: true
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-04]

must_haves:
  truths:
    - "A tabela notificacoes existe no banco com a constraint única (tarefaId, usuarioId, tipo)"
    - "O enum NotificacaoTipo (VENCENDO, ATRASADA, AVULSA_ATRIBUIDA) existe no schema e no client gerado"
    - "Excluir uma tarefa que possui notificações não lança erro de FK (cascade)"
    - "Existe uma função pura compartilhada que classifica o patamar de prazo (ATRASADA/VENCENDO/NORMAL) reutilizada por calcularAlertaPrazo"
    - "Os 3 arquivos de teste de notificações existem com stubs que falham/it.todo (Wave 0)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Notificacao model + NotificacaoTipo enum + inverse relations em Usuario e Tarefa"
      contains: "model Notificacao"
    - path: "src/lib/alert-prazo.ts"
      provides: "classificarPatamarPrazo(prazo, status) pura, fonte única do limiar de 3 dias"
      exports: ["classificarPatamarPrazo", "calcularAlertaPrazo"]
    - path: "tests/notificacoes.sync.test.ts"
      provides: "Stubs Wave 0 para NOTF-01/02/03, idempotência, patamar, conclusão"
    - path: "tests/notificacoes.idor.test.ts"
      provides: "Stubs Wave 0 para NOTF-04 (escopo de visibilidade sync + read)"
    - path: "tests/notificacoes.read-action.test.ts"
      provides: "Stubs Wave 0 para anti-IDOR do mark-as-read"
  key_links:
    - from: "prisma/schema.prisma Notificacao.tarefaId"
      to: "Tarefa.id"
      via: "@relation onDelete: Cascade"
      pattern: "onDelete: Cascade"
    - from: "src/lib/alert-prazo.ts calcularAlertaPrazo"
      to: "classificarPatamarPrazo"
      via: "chamada interna (sem duplicar o limiar de 3 dias)"
      pattern: "classificarPatamarPrazo"
---

<objective>
Estabelecer a fundação pura e persistente da Phase 10: o modelo `Notificacao` + enum `NotificacaoTipo` no Prisma (com cascade e constraint única de idempotência), a extração de uma função pura compartilhada de classificação de patamar de prazo (fonte única do limiar de 3 dias, garantindo que NOTF-01/NOTF-02 nunca divirjam do alerta visual existente), e os stubs de teste Wave 0 que as plans seguintes vão preencher.

Purpose: Sem o schema aplicado no banco e a função de limiar compartilhada, as plans de backend (10-02) e UI (10-03) não têm contrato para implementar. A função compartilhada elimina o risco de drift entre o alerta visual (`calcularAlertaPrazo`) e a sincronização de notificações.
Output: `prisma/schema.prisma` estendido + banco com a tabela `notificacoes`, `classificarPatamarPrazo` exportada de `alert-prazo.ts`, 3 arquivos de teste stub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-notifica-es-in-app/10-CONTEXT.md
@.planning/phases/10-notifica-es-in-app/10-RESEARCH.md
@.planning/phases/10-notifica-es-in-app/10-PATTERNS.md
@.planning/phases/10-notifica-es-in-app/10-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 10-01)

New symbols:
- `enum NotificacaoTipo` — values `VENCENDO`, `ATRASADA`, `AVULSA_ATRIBUIDA` (prisma/schema.prisma)
- `model Notificacao` — fields: `id`, `tarefaId`, `tarefa`, `usuarioId`, `usuario`, `tipo`, `lida`, `createdAt`; `@@unique([tarefaId, usuarioId, tipo])`; `@@index([usuarioId, lida])`; `@@map("notificacoes")` (prisma/schema.prisma)
- Inverse relation `notificacoes Notificacao[]` on `model Usuario` and `model Tarefa`
- `classificarPatamarPrazo(prazo: Date, status: "PENDENTE" | "CONCLUIDA"): "ATRASADA" | "VENCENDO" | "NORMAL"` — pure function exported from `src/lib/alert-prazo.ts`
- New test files: `tests/notificacoes.sync.test.ts`, `tests/notificacoes.idor.test.ts`, `tests/notificacoes.read-action.test.ts` (Wave 0 stubs)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Criar stubs de teste Wave 0 para o domínio de notificações</name>
  <files>tests/notificacoes.sync.test.ts, tests/notificacoes.idor.test.ts, tests/notificacoes.read-action.test.ts</files>
  <read_first>
    - tests/tarefas.idor.test.ts (template exato de mock: `vi.mock("@/lib/db")`, `vi.mock("@/auth")`, `vi.mock("next/cache")`, formato das asserções `findFirstMock.mock.calls[0][0]`)
    - tests/setup.ts (fixtures `mockColaboradorUser`, `mockOtherColaboradorUser`, `mockDonoUser`, `mockChefeFiscalUser`, `mockDpColaboradorUser` — usar estes, não inventar novos)
    - .planning/phases/10-notifica-es-in-app/10-VALIDATION.md (mapa Requirement → comando `-t "<nome>"`, define os nomes exatos dos testes por substring)
  </read_first>
  <action>
    Criar 3 arquivos de teste stub usando `it.todo(...)` (sem callbacks, para não importar módulos ainda inexistentes — padrão Wave 0 já usado na Phase 02-01). Os títulos dos `it.todo` DEVEM conter as substrings que o VALIDATION.md mapeia para `-t`:
    - `tests/notificacoes.sync.test.ts`: `it.todo` para "vencendo" (NOTF-01), "atrasada" (NOTF-02), "avulsa" (NOTF-03), "idempot" (D-03), "patamar" (D-04/D-05), "conclusao" (D-06).
    - `tests/notificacoes.idor.test.ts`: `it.todo` cobrindo "COLABORADOR sync escopado a responsavelId" e "DONO sync/read vê todas" (NOTF-04, T-V4).
    - `tests/notificacoes.read-action.test.ts`: `it.todo` para "marcar como lida anti-IDOR escopado por tarefa" e "avulsa some na conclusao da tarefa" (D-06, V4).
    Cada arquivo abre com um comentário de cabeçalho descrevendo o contrato (mesma convenção de `tarefas.idor.test.ts`). Não escrever a lógica de mock real ainda — apenas o `describe` + `it.todo`. As plans 10-02 vão converter os `it.todo` em testes reais.
  </action>
  <verify>
    <automated>npx vitest run tests/notificacoes.sync.test.ts tests/notificacoes.idor.test.ts tests/notificacoes.read-action.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 arquivos existem em `tests/`.
    - `npx vitest run tests/notificacoes.*.test.ts` reporta os testes como `todo` (não falha a suite — `it.todo` é skip-pendente, não red).
    - `tests/notificacoes.sync.test.ts` contém `it.todo` cujos títulos casam (substring) com: "vencendo", "atrasada", "avulsa", "idempot", "patamar", "conclusao".
    - Nenhum arquivo importa módulos ainda inexistentes (`@/modules/notificacoes/*`, `@/app/(app)/notification-actions`) no escopo de módulo.
  </acceptance_criteria>
  <done>Os 3 arquivos de teste stub existem e rodam como `todo` sem erro de import.</done>
</task>

<task type="auto">
  <name>Task 2: Extrair classificarPatamarPrazo e adicionar o modelo Notificacao ao schema</name>
  <files>src/lib/alert-prazo.ts, prisma/schema.prisma</files>
  <read_first>
    - src/lib/alert-prazo.ts (estado atual de `calcularAlertaPrazo`, linhas 47-73 — o cálculo de `agora`/`em3Dias` e os ramos atrasada/vencendo/normal a serem extraídos)
    - prisma/schema.prisma (linhas 32-35 `enum TarefaStatus` como template de enum; linhas 133-158 `model Tarefa` para o `@@unique`; linhas 160-170 `model TarefaHistorico` para o `onDelete: Cascade`; linhas 61-80 `model Usuario` e linhas 149 de `Tarefa` para onde adicionar as relações inversas)
    - .planning/phases/10-notifica-es-in-app/10-PATTERNS.md (seção "prisma/schema.prisma" — shapes exatos a copiar)
  </read_first>
  <action>
    Parte A — `src/lib/alert-prazo.ts`: extrair uma função pura `classificarPatamarPrazo(prazo: Date, status: "PENDENTE" | "CONCLUIDA"): "ATRASADA" | "VENCENDO" | "NORMAL"`. Regra (idêntica ao limiar atual): status CONCLUIDA → "NORMAL"; senão `agora = new Date()`, `em3Dias = new Date(agora.getTime() + 3 * 24 * 60 * 60 * 1000)`; `prazo < agora` → "ATRASADA"; `prazo <= em3Dias` → "VENCENDO"; senão "NORMAL". Refatorar `calcularAlertaPrazo` para CHAMAR `classificarPatamarPrazo` internamente (mapear "ATRASADA"→objeto vermelho, "VENCENDO"→objeto amber, "NORMAL"/CONCLUIDA→ALERTA_NORMAL) — o limiar de 3 dias deve existir em UM só lugar (a função extraída). Não alterar a assinatura nem o retorno de `calcularAlertaPrazo` (testes existentes em `tests/alert-prazo.test.ts` devem continuar verdes). Per NOTF-01/NOTF-02 "mesmo limiar dos alertas visuais existentes".

    Parte B — `prisma/schema.prisma`: adicionar `enum NotificacaoTipo { VENCENDO ATRASADA AVULSA_ATRIBUIDA }` (per D-01). Adicionar `model Notificacao` com: `id String @id @default(cuid())`; `tarefaId String`; `tarefa Tarefa @relation(fields: [tarefaId], references: [id], onDelete: Cascade)` (Cascade obrigatório — Pitfall 4: sem isso `excluirTarefa` quebra com P2003); `usuarioId String`; `usuario Usuario @relation(fields: [usuarioId], references: [id], onDelete: Cascade)`; `tipo NotificacaoTipo`; `lida Boolean @default(false)` (per D-12, booleano global por linha); `createdAt DateTime @default(now())`; `@@unique([tarefaId, usuarioId, tipo])` (per D-03 — incluir `tipo` é obrigatório, Pitfall 2: sem `tipo` o patamar VENCENDO→ATRASADA quebra D-05); `@@index([usuarioId, lida])`; `@@map("notificacoes")`. Adicionar relação inversa `notificacoes Notificacao[]` em `model Usuario` (junto de `tarefasResponsavel`) e em `model Tarefa` (junto de `historico`).
  </action>
  <verify>
    <automated>npx prisma validate && npx vitest run tests/alert-prazo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` retorna sucesso (schema válido).
    - `grep -n "classificarPatamarPrazo" src/lib/alert-prazo.ts` mostra a função exportada E uma chamada dentro de `calcularAlertaPrazo`.
    - O literal `3 * 24 * 60 * 60 * 1000` aparece exatamente UMA vez em `src/lib/alert-prazo.ts` (dentro de `classificarPatamarPrazo`).
    - `prisma/schema.prisma` contém `model Notificacao`, `enum NotificacaoTipo`, `@@unique([tarefaId, usuarioId, tipo])`, `onDelete: Cascade` em ambas as FKs, `@@map("notificacoes")`.
    - `model Usuario` e `model Tarefa` cada um contém `notificacoes Notificacao[]`.
    - `npx vitest run tests/alert-prazo.test.ts` permanece verde (assinatura de `calcularAlertaPrazo` inalterada).
  </acceptance_criteria>
  <done>`classificarPatamarPrazo` é a fonte única do limiar de 3 dias; o schema tem o modelo Notificacao válido com cascade e constraint única correta; testes de alert-prazo verdes.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar o schema no banco (prisma db push)</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (estado pós-Task 2 — modelo Notificacao deve estar presente antes do push)
    - .planning/STATE.md:102 (decisão registrada: "[Phase 02-01]: npx prisma db push usado em vez de prisma migrate dev (ambiente Neon sem shadow database)")
  </read_first>
  <action>
    Executar `npx prisma db push` para criar a tabela `notificacoes` e o enum `NotificacaoTipo` no Postgres Neon. Esta é uma mudança ADITIVA (tabela nova, nenhuma coluna removida/renomeada de tabela existente) — NÃO deve reportar perda de dados. Se o push reportar perda de dados inesperada, PARAR e revisar (não usar `--accept-data-loss` cegamente para mudança aditiva). Após o push, rodar `npx prisma generate` para regenerar o client TypeScript com `Notificacao`/`NotificacaoTipo` disponíveis (necessário antes de qualquer `tsc --noEmit` nas plans seguintes — decisão Phase 05-04). Esta task é OBRIGATÓRIA: build e type-check passam sem o push (tipos vêm do client gerado, não do banco vivo), criando um falso-positivo de verificação — a fase NÃO pode passar verificação sem a tabela existir.
  </action>
  <verify>
    <automated>npx prisma generate && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` completa sem erro e sem perda de dados.
    - `npx prisma generate` completa; o client gerado expõe `db.notificacao` e `NotificacaoTipo`.
    - `npx tsc --noEmit` permanece limpo (zero erros) — confirma que o client gerado é coerente com o schema.
  </acceptance_criteria>
  <done>A tabela `notificacoes` existe no Postgres Neon e o Prisma Client tipado expõe `db.notificacao`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Server Action/Server Component | input não confiável; nenhuma entrada de cliente nesta plan (apenas schema + helper puro + stubs) |
| aplicação → Postgres (Prisma) | a constraint `@@unique` e `onDelete: Cascade` são a defesa de integridade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `Notificacao.@@unique([tarefaId, usuarioId, tipo])` | mitigate | Constraint inclui `tipo` (D-03) — impede duplicata e habilita idempotência via `createMany skipDuplicates`; sem `tipo` quebra D-05 (Pitfall 2) |
| T-10-02 | Denial of Service | `excluirTarefa` existente vs FK órfã | mitigate | `onDelete: Cascade` em `Notificacao.tarefaId` (Pitfall 4) — exclusão de tarefa não lança P2003 |
| T-10-SC | Tampering | npm/shadcn installs | mitigate | Nenhum install nesta plan; pacotes shadcn (popover/scroll-area) entram na Plan 10-03; RESEARCH Package Legitimacy Audit aprovou @radix-ui/react-popover (verificado no registry) e marcou todos como OK — sem [ASSUMED]/[SUS]/[SLOP] |
</threat_model>

<verification>
- `npx prisma validate` e `npx prisma db push` aplicam o schema sem perda de dados.
- `npx prisma generate` regenera o client; `db.notificacao` disponível.
- `npx tsc --noEmit` limpo.
- `npx vitest run tests/alert-prazo.test.ts` verde (refator de `calcularAlertaPrazo` não regrediu).
- `npx vitest run tests/notificacoes.*.test.ts` roda os stubs como `todo`.
</verification>

<success_criteria>
- Tabela `notificacoes` existe no banco com `@@unique([tarefaId, usuarioId, tipo])` e cascade em ambas as FKs.
- `classificarPatamarPrazo` exportada e usada por `calcularAlertaPrazo` (limiar de 3 dias em um só lugar).
- 3 arquivos de teste stub existem e rodam como `todo`.
</success_criteria>

<output>
Create `.planning/phases/10-notifica-es-in-app/10-01-SUMMARY.md` when done
</output>

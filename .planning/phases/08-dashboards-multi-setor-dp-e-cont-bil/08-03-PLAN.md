---
phase: 08-dashboards-multi-setor-dp-e-cont-bil
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/app/(app)/dashboards/guard.ts
  - src/app/(app)/dashboards/page.tsx
  - src/app/(app)/dashboards/empty-state.tsx
  - src/modules/dashboard/queries.ts
autonomous: false
requirements: [DP-06, DP-07, DP-08, CONT-07, CONT-08, CONT-09]
must_haves:
  truths:
    - "O dono vê 3 abas (Fiscal/DP/Contábil) em /dashboards, cada uma com os 3 cards (desempenho/evolução/ranking)"
    - "A aba DP só mostra dados de empresas com temFuncionariosClt=true; a aba Contábil mostra todas as 197 empresas"
    - "Cada card vazio mostra um estado vazio com texto que menciona o setor explicitamente (D-04)"
    - "Usuário não-DONO recebe 404 (notFound) ao acessar /dashboards, inalterado"
    - "src/modules/dashboard/ (singular, órfão) foi deletado e não há imports remanescentes dele"
  artifacts:
    - path: "src/app/(app)/dashboards/empty-state.tsx"
      provides: "EmptyState parametrizado por setor (D-04)"
      exports: ["EmptyState"]
    - path: "src/app/(app)/dashboards/guard.ts"
      provides: "carregarDadosDashboards fan-out dos 3 setores"
      exports: ["carregarDadosDashboards"]
    - path: "src/app/(app)/dashboards/page.tsx"
      provides: "Tabs por setor + SectorDashboard helper"
  key_links:
    - from: "src/app/(app)/dashboards/guard.ts"
      to: "src/modules/dashboards/queries.ts"
      via: "chamadas parametrizadas por setor"
      pattern: "listarDesempenhoColaboradoresMesAtual"
    - from: "src/app/(app)/dashboards/page.tsx"
      to: "src/app/(app)/dashboards/empty-state.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
---

<objective>
Entregar a UI multi-setor: adicionar abas (Fiscal/DP/Contábil) na página `/dashboards` existente (D-01), fazer o guard buscar os 3 setores em paralelo, extrair o `EmptyState` para um componente parametrizado por setor (D-04), reusar os 3 componentes de chart/tabela inalterados, e deletar o módulo órfão `src/modules/dashboard/` (singular) fechando o Success Criterion #5.

Purpose: É a entrega visível ao dono. Tudo abaixo já existe (queries parametrizadas no Plan 02, helper no Plan 01) — esta plan é wiring + limpeza + verificação humana.
Output: `/dashboards` com 3 abas funcionais, `empty-state.tsx` novo, módulo órfão deletado, checkpoint humano de verificação visual.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-UI-SPEC.md
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-PATTERNS.md
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Símbolos NOVOS criados por esta fase (Plan 03):
- `EmptyState` parametrizado por `setor` (componente, `src/app/(app)/dashboards/empty-state.tsx`) — substitui a função inline homônima em page.tsx
- `COPY` (const de cópia por setor, em empty-state.tsx)
- `SectorDashboard` (helper local em page.tsx que renderiza os 3 cards de um setor)
- `carregarDadosDashboards` assinatura estendida (retorna `Record<Setor, {desempenho, evolucao, ranking}>` em vez de um único objeto)
- DELEÇÃO: `src/modules/dashboard/queries.ts` (módulo singular órfão, removido)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Guard fan-out por setor + EmptyState parametrizado + Tabs na página</name>
  <files>src/app/(app)/dashboards/guard.ts, src/app/(app)/dashboards/empty-state.tsx, src/app/(app)/dashboards/page.tsx</files>
  <read_first>
    - src/app/(app)/dashboards/guard.ts (arquivo completo — guard lines auth/role devem ficar verbatim no topo)
    - src/app/(app)/dashboards/page.tsx (arquivo completo — layout de 3 cards a extrair em SectorDashboard; EmptyState inline a remover)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md (Code Examples — guard fan-out verbatim; EmptyState COPY verbatim; Pattern 3 — Tabs verbatim)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-UI-SPEC.md (Copywriting Contract linhas 97-119 — textos exatos por setor; Interaction Contract — ordem das abas, default Fiscal, empty state por card)
    - src/components/ui/tabs.tsx (componente shadcn já instalado — assinatura de Tabs/TabsList/TabsTrigger/TabsContent)
  </read_first>
  <action>
    1. `guard.ts`: manter as 3 linhas de guard (`auth()` → redirect("/login"); `role !== "DONO"` → `notFound()`) VERBATIM no topo, antes de qualquer query. Substituir o corpo de busca única pelo fan-out por setor (08-RESEARCH.md Code Examples, copiar verbatim): `empresaScopePorSetor = { FISCAL: {}, DP: { temFuncionariosClt: true }, CONTABIL: {} }` (D-02/D-03), `setores.map` com `Promise.all` chamando as 3 queries parametrizadas por setor, retornando `Record<"FISCAL"|"DP"|"CONTABIL", { desempenhoColaboradores, evolucaoMensal, rankingEmpresas }>`.
    2. Criar `src/app/(app)/dashboards/empty-state.tsx` (08-RESEARCH.md Code Examples / 08-UI-SPEC Copywriting Contract, copiar verbatim): `COPY` record keyed por "FISCAL"|"DP"|"CONTABIL" com heading/body exatos da UI-SPEC (DP: "Ainda não há dados suficientes de DP" + body com "de DP"; Contábil análogo); componente `EmptyState({ setor })`. ClassNames idênticos ao original (`flex flex-col items-center gap-2 py-16 text-center`, `max-w-md text-sm text-muted-foreground`).
    3. `page.tsx`: importar `Tabs, TabsContent, TabsList, TabsTrigger` de `@/components/ui/tabs` e `EmptyState` de `./empty-state`; remover a função inline `EmptyState` antiga (linhas 81-91). Extrair o bloco de 3 Cards num helper local `SectorDashboard({ setor, dados })` que renderiza Desempenho/Evolução/Ranking, cada Card com seu próprio check de array vazio chamando `<EmptyState setor={setor} />` (cada card independente — UI-SPEC Interaction Contract). Renderizar `<Tabs defaultValue="FISCAL">` com TabsList (labels "Fiscal"/"DP"/"Contábil" nesta ordem) e 3 TabsContent mapeando os setores para `<SectorDashboard>`. searchParams.meses continua aplicado per-dataset, inalterado.
    Não modificar os 3 componentes de chart/tabela — são reusados via props.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/dashboards.rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `guard.ts` retorna um Record com chaves FISCAL/DP/CONTABIL, cada uma com desempenhoColaboradores/evolucaoMensal/rankingEmpresas; as 3 linhas de guard auth/role estão inalteradas e executam antes de qualquer query.
    - `empresaScopePorSetor.DP` é `{ temFuncionariosClt: true }` e `.CONTABIL` é `{}`.
    - `empty-state.tsx` exporta `EmptyState` e os textos DP/Contábil mencionam o setor explicitamente (contêm "de DP" / "de Contábil"), divergindo do texto Fiscal (D-04).
    - `page.tsx` renderiza `<Tabs defaultValue="FISCAL">` com 3 TabsTrigger ("Fiscal", "DP", "Contábil") e não contém mais a função inline EmptyState.
    - `tests/dashboards.rbac.test.ts` (guard DONO-only / notFound) permanece verde — sem regressão de autorização (T-4-01).
    - `npx tsc --noEmit` passa.
  </acceptance_criteria>
  <done>A página /dashboards tem 3 abas por setor, cada card com estado vazio setor-aware, guard DONO-only intacto, componentes de chart reusados sem cópia.</done>
</task>

<task type="auto">
  <name>Task 2: Deletar módulo órfão singular e verificar zero imports</name>
  <files>src/modules/dashboard/queries.ts</files>
  <read_first>
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md (Pitfall 4 — re-rodar o scan de imports DEPOIS das edições da fase, antes de deletar; o grep deve ter aspas/barra logo após "dashboard" sem o "s")
    - .planning/STATE.md (Blockers/Concerns "[Phase 8] NOVO" — deletar o módulo órfão singular durante esta fase)
  </read_first>
  <action>
    Re-rodar o scan de imports como ÚLTIMO passo antes de deletar (Pitfall 4 — após as edições do Plan 02/03, para pegar um typo `@/modules/dashboard` sem "s"): `grep -rn "modules/dashboard[\"'/]" src/` — confirmar que o único match é o comentário dentro do próprio arquivo órfão (`src/modules/dashboard/queries.ts` linha ~7), nenhum import real. Se aparecer qualquer import real de `@/modules/dashboard` (singular), corrigir para `@/modules/dashboards` (plural) ANTES de deletar.
    Deletar o arquivo `src/modules/dashboard/queries.ts` e o diretório `src/modules/dashboard/` (singular). Confirmar com `npx tsc --noEmit` que nada quebra após a deleção (se quebrar com "Cannot find module '@/modules/dashboard/queries'", o import era um typo de plural — corrigir).
  </action>
  <verify>
    <automated>test ! -e "src/modules/dashboard/queries.ts" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/dashboard/queries.ts` e o diretório `src/modules/dashboard/` não existem mais.
    - `grep -rn "modules/dashboard[\"'/]" src/` não retorna nenhum import real (zero matches após a deleção, já que o único match era o comentário no próprio arquivo deletado).
    - `npx tsc --noEmit` passa sem erro "Cannot find module" relacionado a dashboard.
  </acceptance_criteria>
  <done>O código órfão do módulo de dashboard singular antigo foi removido, satisfazendo Success Criterion #5 ("sem código órfão").</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual dos dashboards DP e Contábil</name>
  <files>src/app/(app)/dashboards/page.tsx</files>
  <action>
    Checkpoint de verificação humana — nenhuma edição de código nesta task. O executor primeiro automatiza tudo verificável (suíte de testes completa) e só então pausa para o dono confirmar a fidelidade visual/UX dos dashboards DP e Contábil contra a 08-UI-SPEC.md, seguindo os passos de how-to-verify abaixo.
  </action>
  <what-built>
    Página /dashboards com 3 abas (Fiscal/DP/Contábil), cada uma com os 3 dashboards (Desempenho por colaborador, Evolução mensal, Empresas com mais atrasos), dados scoped por setor (DP só CLT, Contábil universo cheio), estados vazios com texto que menciona o setor, e o módulo órfão singular removido. Suíte de testes completa verde.
  </what-built>
  <how-to-verify>
    1. Rodar `npx vitest run` — toda a suíte deve estar verde (regressão Fiscal + novos testes DP/CONTABIL).
    2. Subir o app (`npm run dev`) e logar como DONO.
    3. Acessar `/dashboards` — confirmar 3 abas "Fiscal", "DP", "Contábil", com "Fiscal" selecionada por default.
    4. Clicar na aba "DP" — confirmar os 3 cards (Desempenho/Evolução/Ranking). Se ainda não houver dados de DP suficientes, confirmar que o estado vazio diz "Ainda não há dados suficientes de DP" (não o texto genérico do Fiscal).
    5. Clicar na aba "Contábil" — mesma verificação, texto vazio menciona "de Contábil".
    6. Confirmar paleta de cores semáforo (azul/verde/amarelo/laranja/vermelho) e barras agrupadas na Evolução mensal, idênticas ao Fiscal.
    7. (Opcional) Deslogar e tentar acessar /dashboards como COLABORADOR — deve dar 404.
  </how-to-verify>
  <verify>
    <automated>npx vitest run</automated>
    <human-check>Dono confirma as 3 abas, os escopos por setor (DP só CLT, Contábil universo cheio) e os textos de estado vazio setor-aware conforme os passos de how-to-verify.</human-check>
  </verify>
  <done>Suíte completa verde E o dono aprovou a verificação visual das abas DP e Contábil.</done>
  <resume-signal>Digite "approved" ou descreva divergências visuais/de dados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /dashboards (RSC) | acesso a dados agregados de todos os colaboradores; deve ser DONO-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Information Disclosure | guard.ts DONO-only gate | mitigate (reuso) | Guard `notFound()` pré-query inalterado; cobre os 3 setores num único entry point; tests/dashboards.rbac.test.ts regride o gate |
| T-08-05 | Tampering | deleção do módulo órfão quebrando import novo por typo | mitigate | Re-scan de imports (Pitfall 4) como último passo antes de deletar + tsc --noEmit pós-deleção |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo (Tabs já instalado; 08-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run` suíte completa verde (regressão Fiscal + DP/CONTABIL)
- `npx tsc --noEmit` passa
- `src/modules/dashboard/` não existe mais
- Checkpoint humano de verificação visual aprovado
</verification>

<success_criteria>
- O dono vê os 3 tipos de dashboard por setor em abas na mesma página (D-01), com estados vazios setor-aware (D-04) e escopos de empresa corretos (D-02/D-03).
- Success Criterion #5 satisfeito: queries reaproveitam um único módulo parametrizado e o código órfão singular foi deletado.
</success_criteria>

<output>
Create `.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-03-SUMMARY.md` when done
</output>

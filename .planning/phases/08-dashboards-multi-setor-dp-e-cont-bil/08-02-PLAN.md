---
phase: 08-dashboards-multi-setor-dp-e-cont-bil
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/modules/dashboards/queries.ts
  - src/modules/dashboards/snapshot.ts
  - tests/dashboards.queries.test.ts
  - tests/dashboards.snapshot.test.ts
autonomous: true
requirements: [DP-06, DP-07, DP-08, CONT-07, CONT-08, CONT-09]
must_haves:
  truths:
    - "listarDesempenhoColaboradoresMesAtual(mes, setor, empresaWhereExtra) retorna só dados daquele setor, com universo de empresas filtrado por empresaWhereExtra"
    - "listarEvolucaoMensal(quantidadeMeses, setor) lê linhas DesempenhoMensal filtradas por setor para meses fechados (D-05 preservado) E gera o ponto live (mês corrente) com AMBOS os helpers internos sector-scoped — desempenho E categorias 'criadas'"
    - "listarRankingEmpresas(inicio, fim, setor, empresaWhereExtra) ranqueia só empresas daquele setor"
    - "calcularSnapshotMensal grava uma linha por (colaborador, setor) derivando setor de Usuario.setor"
    - "Os dashboards Fiscal continuam idênticos em comportamento após a parametrização (sem regressão)"
  artifacts:
    - path: "src/modules/dashboards/queries.ts"
      provides: "3 funções de query parametrizadas por setor"
      exports: ["listarDesempenhoColaboradoresMesAtual", "listarEvolucaoMensal", "listarRankingEmpresas"]
    - path: "src/modules/dashboards/snapshot.ts"
      provides: "calcularSnapshotMensal setor-aware (1 linha por colaborador+setor)"
      exports: ["calcularSnapshotMensal", "LinhaSnapshotMensal"]
  key_links:
    - from: "src/modules/dashboards/queries.ts"
      to: "src/lib/tipo-obrigacao-setor.ts"
      via: "import tarefaSetorWhere"
      pattern: "tarefaSetorWhere"
    - from: "src/modules/dashboards/snapshot.ts"
      to: "db.usuario (setor)"
      via: "join de Usuario.setor por responsavelId"
      pattern: "setor"
---

<objective>
Parametrizar por setor a camada de leitura e de snapshot dos dashboards, SEM duplicar módulos (Pitfall B4 / Success Criterion #5). As 3 funções exportadas de `queries.ts` ganham um parâmetro `setor` (e um `empresaWhereExtra` opcional para o filtro CLT de DP, D-02), e `calcularSnapshotMensal` passa a gravar uma linha por (colaborador, setor) lendo `Usuario.setor`, para que os meses fechados de DP/Contábil sejam congelados de forma isolada por setor (D-05).

Purpose: É o coração da fase — toda a lógica de agregação por setor vive aqui. A UI (Plan 03) apenas consome estas assinaturas. O Fiscal NÃO pode regredir.
Output: `queries.ts` e `snapshot.ts` parametrizados por setor, com testes estendidos cobrindo DP/CONTABIL e regressão Fiscal verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-PATTERNS.md
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Símbolos NOVOS / assinaturas ESTENDIDAS por esta fase (Plan 02) — novos parâmetros são criações desta fase:
- `setor: Setor` e `empresaWhereExtra: Prisma.EmpresaWhereInput` como novos parâmetros de `listarDesempenhoColaboradoresMesAtual`, `listarEvolucaoMensal` (sem empresaWhereExtra), `listarRankingEmpresas`
- `setor: Setor` como novo parâmetro do helper interno `calcularCategoriasCriadas` (não exportado, mas precisa ser sector-scoped para que o ponto live de listarEvolucaoMensal não misture setores)
- `LinhaSnapshotMensal.setor` (novo campo no tipo de retorno de snapshot)
- `setor: Setor` como novo parâmetro derivado internamente em `calcularSnapshotMensal` (via Usuario.setor)
- Consome `tarefaSetorWhere` e `TIPOS_OBRIGACAO_POR_SETOR` de `src/lib/tipo-obrigacao-setor.ts` (criados no Plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrizar as 3 funções de queries.ts por setor</name>
  <files>src/modules/dashboards/queries.ts, tests/dashboards.queries.test.ts</files>
  <behavior>
    - listarDesempenhoColaboradoresMesAtual(mes, "DP", {temFuncionariosClt:true}): a query de tarefas só conta tarefas do setor DP (via tarefaSetorWhere); a query de carteira (db.empresa.groupBy) só conta empresas com temFuncionariosClt=true.
    - listarDesempenhoColaboradoresMesAtual(mes, "CONTABIL"): conta tarefas Contábil, universo de empresas completo (empresaWhereExtra={}).
    - listarEvolucaoMensal(n, "DP"): o groupBy de db.desempenhoMensal filtra por setor="DP"; a chamada recursiva ao desempenho live propaga setor + empresaWhereExtra; E a chamada a calcularCategoriasCriadas no ponto live também propaga setor (os 5 campos "criadas" do ponto corrente só contam tarefas daquele setor).
    - calcularCategoriasCriadas(mes, competencia, "DP"): a query db.tarefa.findMany das "criadas" é filtrada por tarefaSetorWhere("DP") — totalCriadas/totalConcluidasNoPeriodo/totalPendentesSemMotivo/totalPendentesComMotivo/totalVencidas só refletem tarefas DP; idem para "CONTABIL" e "FISCAL".
    - listarRankingEmpresas(inicio, fim, "DP", {temFuncionariosClt:true}): a query de tarefas é filtrada por tarefaSetorWhere; o universo de empresas do ranking respeita empresaWhereExtra (filtro de relação empresa.temFuncionariosClt).
    - Chamada SEM setor explícito não é mais válida (assinatura obrigatória) — os testes Fiscal existentes passam a chamar com "FISCAL" e devem produzir os MESMOS resultados de antes (regressão).
  </behavior>
  <read_first>
    - src/modules/dashboards/queries.ts (arquivo completo atual — assinaturas e queries exatas a estender; ATENÇÃO ESPECIAL ao helper `calcularCategoriasCriadas` ~linha 155 e ao seu call site ~linha 274 dentro de listarEvolucaoMensal — ambos precisam de setor)
    - src/lib/tipo-obrigacao-setor.ts (helper criado no Plan 01 — assinatura de tarefaSetorWhere)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md (Pattern 2 + Code Examples; Pitfall 3 — onde empresaWhereExtra é load-bearing vs redundante)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-PATTERNS.md (seção queries.ts, linhas 24-99 — pontos de merge exatos por número de linha)
    - tests/dashboards.queries.test.ts (estrutura atual dos testes a estender — ler antes de editar)
  </read_first>
  <action>
    Adicionar imports `import type { Setor, Prisma } from "@prisma/client";` e `import { tarefaSetorWhere } from "@/lib/tipo-obrigacao-setor";` em queries.ts.
    1. `listarDesempenhoColaboradoresMesAtual(mes, setor, empresaWhereExtra = {})`: fundir `...tarefaSetorWhere(setor)` no `where` da query `db.tarefa.findMany` de concluídas (merge via spread no top-level = AND implícito); aplicar `...empresaWhereExtra` no `where` do `db.empresa.groupBy` de carteiras (Pitfall 3 — este é o único lugar onde empresaWhereExtra é load-bearing no desempenho; NÃO aplicar à query de Tarefa).
    2. `calcularCategoriasCriadas(mes, competencia, setor)`: adicionar `setor: Setor` como 3º parâmetro; fundir `...tarefaSetorWhere(setor)` no top-level do `where` do `db.tarefa.findMany` das "criadas" (spread no top-level, ao lado do `OR` existente = AND implícito entre o filtro de setor e o filtro competencia/createdAt — MESMO padrão usado nas outras funções acima). Sem essa correção, os 5 campos "criadas" do ponto live de listarEvolucaoMensal misturariam tarefas de todos os setores, contradizendo D-05 (consistência interna do gráfico) e DP-07/CONT-08. Preservar o comentário CRÍTICO de continuidade live→frozen do helper verbatim.
    3. `listarEvolucaoMensal(quantidadeMeses = 3, setor)`: adicionar `setor` como 2º parâmetro; adicionar `setor` ao `where` do `db.desempenhoMensal.groupBy` (`where: { competencia: { in: competenciasFechadas }, setor }`); propagar `setor` (e empresaWhereExtra se o consumidor passar) na chamada interna a `listarDesempenhoColaboradoresMesAtual` (~linha 264); E propagar `setor` na chamada a `calcularCategoriasCriadas(new Date(), mesAtual, setor)` (~linha 274) — AMBOS os helpers do ponto live precisam ser sector-scoped, caso contrário só metade do ponto corrente fica isolada por setor.
    4. `listarRankingEmpresas(periodoInicio, periodoFim, setor, empresaWhereExtra = {})`: fundir `...tarefaSetorWhere(setor)` no `where` de `db.tarefa.findMany`; como o universo do ranking É o universo de empresas, aplicar `empresaWhereExtra` via filtro de relação `empresa: { ...empresaWhereExtra }` no mesmo where da tarefa (esta é a única exceção documentada à regra "nunca filtrar Tarefa por empresaWhereExtra", per 08-PATTERNS.md linha 97 — porque ranking não tem db.empresa query separada).
    Preservar TODOS os comentários críticos existentes (D-05, concluidoEm-no-range, responsavelId-não-Empresa.responsavelId, continuidade live→frozen) e o formato de retorno (array plano serializável, nunca Map).
    Estender `tests/dashboards.queries.test.ts` (seguir convenção do arquivo existente — vi.mock de db) com casos parametrizados para setor="DP" (CLT-filtered) e setor="CONTABIL" (universo cheio) nas 3 funções, mantendo os casos Fiscal existentes passando agora com "FISCAL" explícito. Adicionar especificamente um caso que assere que o PONTO LIVE (mês corrente) de listarEvolucaoMensal — os 5 campos "criadas" (totalCriadas et al.) — conta SOMENTE tarefas do setor informado (DP/CONTABIL/FISCAL) e NÃO mistura tarefas de outros setores, espelhando explicitamente por-setor o teste Fiscal-only existente.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - As 3 funções exportadas têm `setor: Setor` como parâmetro obrigatório; desempenho e ranking têm `empresaWhereExtra: Prisma.EmpresaWhereInput = {}`.
    - O helper `calcularCategoriasCriadas` recebe `setor: Setor` e funde `...tarefaSetorWhere(setor)` no top-level do where de `db.tarefa.findMany`.
    - O call site de `calcularCategoriasCriadas` dentro de `listarEvolucaoMensal` (~linha 274) passa `setor`, de modo que o ponto live tem AMBOS os helpers (desempenho E criadas) sector-scoped.
    - `tests/dashboards.queries.test.ts` cobre DP e CONTABIL para as 3 funções e está verde.
    - Existe um teste que prova que os 5 campos "criadas" do ponto live (mês corrente) de listarEvolucaoMensal contam apenas tarefas do setor informado (por-setor, não Fiscal-only implícito).
    - Os casos Fiscal pré-existentes continuam verdes (chamados com "FISCAL"), provando ausência de regressão.
    - `queries.ts` importa `tarefaSetorWhere` de `@/lib/tipo-obrigacao-setor`.
    - `db.desempenhoMensal.groupBy` em listarEvolucaoMensal inclui `setor` no where.
    - `npx tsc --noEmit` sem novos erros.
  </acceptance_criteria>
  <done>As 3 queries de dashboard são parametrizadas por setor num único módulo, com o ponto live de evolução totalmente sector-scoped (desempenho E criadas), testes DP/CONTABIL verdes e zero regressão Fiscal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tornar calcularSnapshotMensal setor-aware (1 linha por colaborador+setor)</name>
  <files>src/modules/dashboards/snapshot.ts, tests/dashboards.snapshot.test.ts</files>
  <behavior>
    - calcularSnapshotMensal lê Usuario.setor para cada colaborador presente e inclui `setor` em cada LinhaSnapshotMensal retornada.
    - Um colaborador FISCAL gera uma linha com setor="FISCAL"; um colaborador DP gera setor="DP"; etc. (na prática 1 colaborador pertence a exatamente 1 setor via Usuario.setor, então 1 linha por colaborador, agora rotulada pelo setor).
    - O critério de população (concluidoEm-no-range, nunca Tarefa.competencia) e o uso de Tarefa.responsavelId (nunca Empresa.responsavelId) permanecem inalterados.
    - Colaborador com Usuario.setor null não quebra (fallback documentado — pular ou rotular; decidir conforme dados reais, mas nunca lançar).
  </behavior>
  <read_first>
    - src/modules/dashboards/snapshot.ts (arquivo completo atual — função calcularSnapshotMensal e tipo LinhaSnapshotMensal)
    - src/modules/tarefas/geracao.ts (linhas ~95-105 — onde calcularSnapshotMensal é chamado e o resultado vai para tx.desempenhoMensal.createMany; o createMany agora precisa gravar a coluna setor)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md (Pitfall 1 — derivar setor via Usuario.setor join, não via tipoObrigacao, pois snapshot já agrupa por colaborador)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-PATTERNS.md (seção snapshot.ts, linhas 103-122)
    - tests/dashboards.snapshot.test.ts (estrutura atual dos testes a estender — ler antes de editar)
  </read_first>
  <action>
    Em `snapshot.ts`: adicionar `setor: Setor` ao tipo `LinhaSnapshotMensal`. Em `calcularSnapshotMensal`, após montar os mapas porColaborador/categoriasPorColaborador, fazer um lookup único `tx.usuario.findMany({ where: { id: { in: [...colaboradorIds] } }, select: { id: true, setor: true } })` (sem novo join na query de Tarefa — enriquecimento pós-agregação, conforme 08-PATTERNS.md linha 120) e popular o campo `setor` de cada linha retornada a partir desse mapa. Tratar `Usuario.setor` null de forma defensiva (não lançar). Importar `Setor` de @prisma/client.
    Em `src/modules/tarefas/geracao.ts`: garantir que o `tx.desempenhoMensal.createMany(...)` que persiste o resultado de calcularSnapshotMensal inclua o campo `setor` de cada linha (o spread do objeto LinhaSnapshotMensal já carrega `setor` se as linhas o contêm — verificar que o createMany não usa um select/pick que exclua setor).
    Estender `tests/dashboards.snapshot.test.ts` para cobrir a nova assinatura: mockar usuários com setores distintos e assertar que cada LinhaSnapshotMensal carrega o setor correto do colaborador.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `LinhaSnapshotMensal` inclui `setor: Setor`.
    - `calcularSnapshotMensal` retorna linhas cujo `setor` corresponde ao `Usuario.setor` do colaborador (verificado no teste com colaboradores de setores diferentes).
    - O `tx.desempenhoMensal.createMany` em geracao.ts grava a coluna setor (não falha por coluna NOT NULL ausente).
    - `tests/dashboards.snapshot.test.ts` está verde e cobre a derivação de setor.
    - Comentários críticos (concluidoEm-no-range, responsavelId) preservados verbatim.
    - `npx tsc --noEmit` sem novos erros.
  </acceptance_criteria>
  <done>O snapshot mensal grava linhas isoladas por setor, permitindo que listarEvolucaoMensal congele meses fechados por setor (D-05) sem misturar DP/Contábil/Fiscal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query layer → DB | agregação cross-setor; risco de vazamento de dados de um setor no dashboard de outro (integridade, não fronteira de segurança) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | contaminação cross-setor (DP vazando em Contábil) | mitigate | `tarefaSetorWhere` em TODAS as queries de Tarefa (incl. o helper interno `calcularCategoriasCriadas` do ponto live) + groupBy de DesempenhoMensal filtrado por setor; testes unitários DP/CONTABIL verificam isolamento, incluindo os 5 campos "criadas" do ponto live |
| T-08-04 | Information Disclosure | snapshot vazando senhaHash via relação crua de Usuario | mitigate | lookup de Usuario usa `select: { id, setor }` explícito, nunca `responsavel: true` (padrão já estabelecido em snapshot.ts) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase (08-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run tests/dashboards.queries.test.ts tests/dashboards.snapshot.test.ts` verde
- Casos Fiscal regressivos verdes (chamados com "FISCAL")
- Teste explícito de isolamento por setor do ponto live (5 campos "criadas") verde
- `npx tsc --noEmit` sem novos erros
</verification>

<success_criteria>
- As 6 capacidades de dashboard (DP-06/07/08, CONT-07/08/09) são servidas por um único módulo de queries parametrizado, sem duplicação.
- Meses fechados são congelados por setor; o ponto live (mês corrente) do gráfico de evolução é totalmente sector-scoped (desempenho E criadas); Fiscal não regride.
</success_criteria>

<output>
Create `.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-02-SUMMARY.md` when done
</output>
</content>
</invoke>

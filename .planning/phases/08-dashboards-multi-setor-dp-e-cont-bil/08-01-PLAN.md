---
phase: 08-dashboards-multi-setor-dp-e-cont-bil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/tipo-obrigacao-setor.ts
  - tests/tipo-obrigacao-setor.test.ts
  - scripts/backfill-desempenho-setor.mjs
autonomous: false
requirements: [DP-06, DP-07, DP-08, CONT-07, CONT-08, CONT-09]
must_haves:
  truths:
    - "DesempenhoMensal tem coluna setor NOT NULL com unique [competencia, colaboradorId, setor]"
    - "Todas as linhas pré-existentes de DesempenhoMensal têm setor = FISCAL após backfill"
    - "tarefaSetorWhere(setor) retorna um filtro Prisma OR que classifica recorrentes por tipoObrigacao e avulsas por responsavel.setor"
    - "Cada valor do enum TipoObrigacao aparece em exatamente um setor de TIPOS_OBRIGACAO_POR_SETOR"
  artifacts:
    - path: "src/lib/tipo-obrigacao-setor.ts"
      provides: "TIPOS_OBRIGACAO_POR_SETOR map + tarefaSetorWhere(setor) helper"
      exports: ["TIPOS_OBRIGACAO_POR_SETOR", "tarefaSetorWhere"]
    - path: "tests/tipo-obrigacao-setor.test.ts"
      provides: "Cobertura de completude do enum e shape do where"
    - path: "prisma/schema.prisma"
      provides: "DesempenhoMensal.setor + unique constraint atualizada"
      contains: "setor"
  key_links:
    - from: "src/lib/tipo-obrigacao-setor.ts"
      to: "@prisma/client (Setor, TipoObrigacao, Prisma)"
      via: "import type"
      pattern: "from \"@prisma/client\""
---

<objective>
Estabelecer a fundação de dados e de classificação por setor para os dashboards multi-setor: (1) o helper compartilhado `tarefaSetorWhere(setor)` que classifica qualquer linha `Tarefa` por setor (única fonte de verdade, evitando o anti-padrão de duplicação do Pitfall B4), e (2) a migração [BLOCKING] do modelo `DesempenhoMensal` para ganhar a dimensão `setor`, sem a qual DP-07/CONT-08 não conseguem satisfazer D-05 ("meses fechados nunca recalculados").

Purpose: Toda a camada de queries (Plan 02) e a UI (Plan 03) dependem do schema migrado e do helper de classificação. Esta é a Wave de fundação — nada downstream pode funcionar sem ela.
Output: `src/lib/tipo-obrigacao-setor.ts`, `tests/tipo-obrigacao-setor.test.ts`, schema `DesempenhoMensal` migrado + backfill verificado, `scripts/backfill-desempenho-setor.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Símbolos NOVOS criados por esta fase (Plan 01) — devem ser excluídos da verificação de drift por serem criações desta fase:
- `TIPOS_OBRIGACAO_POR_SETOR` (const, `src/lib/tipo-obrigacao-setor.ts`)
- `tarefaSetorWhere` (função, `src/lib/tipo-obrigacao-setor.ts`)
- `DesempenhoMensal.setor` (coluna nova no schema Prisma)
- `scripts/backfill-desempenho-setor.mjs` (script novo)
- `tests/tipo-obrigacao-setor.test.ts` (arquivo de teste novo)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Criar helper de classificação por setor + teste de completude do enum</name>
  <files>src/lib/tipo-obrigacao-setor.ts, tests/tipo-obrigacao-setor.test.ts</files>
  <read_first>
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md (Pattern 1, linhas 165-202 — código copy-paste-ready de TIPOS_OBRIGACAO_POR_SETOR e tarefaSetorWhere)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-PATTERNS.md (seção "src/lib/tipo-obrigacao-setor.ts", analogs alert-prazo.ts e visibility-scope.ts; e seção de teste, padrão de tests/dashboards.rbac.test.ts)
    - prisma/schema.prisma (enum TipoObrigacao, linhas 36-57; enum Setor, linhas 19-23 — fonte de verdade dos 20 valores a mapear)
    - src/lib/visibility-scope.ts (analog estrutural: função que retorna um Prisma.*WhereInput fragment para o caller fazer spread/AND)
  </read_first>
  <action>
    Criar `src/lib/tipo-obrigacao-setor.ts` exportando:
    1. `TIPOS_OBRIGACAO_POR_SETOR: Record<Setor, TipoObrigacao[]>` com o mapeamento DISJUNTO exato dos 20 valores do enum:
       - FISCAL: ICMS, PIS_COFINS, SPED_FISCAL, SPED_CONTRIBUICOES, DAS
       - DP: FOLHA, ESOCIAL, FGTS, INSS
       - CONTABIL: EXTRATO_BANCARIO, LANCAMENTO_EXTRATOS, FOLHA_CONTABIL, FISCAL_CONTABIL, BAIXA_IMPOSTOS, PERDCOMP, FORNECEDORES_CLIENTES, BALANCO, ECD, ECF, DEFIS
    2. `tarefaSetorWhere(setor: Setor): Prisma.TarefaWhereInput` retornando `{ OR: [ { tipoObrigacao: { in: TIPOS_OBRIGACAO_POR_SETOR[setor] } }, { tipoObrigacao: null, responsavel: { setor } } ] }`. Recorrentes (tipoObrigacao não-nulo) classificadas por tipoObrigacao; avulsas (tipoObrigacao IS NULL) classificadas pelo setor do responsável — o único sinal disponível para avulsas. Importar `Setor`, `TipoObrigacao` e `Prisma` como `import type` de `@prisma/client`.
    Criar `tests/tipo-obrigacao-setor.test.ts` (Vitest, sem mocks — função pura, sem I/O) cobrindo: (a) `tarefaSetorWhere("DP")` produz OR cuja primeira cláusula contém FOLHA/ESOCIAL/FGTS/INSS e cuja segunda cláusula é `{ tipoObrigacao: null, responsavel: { setor: "DP" } }`; (b) teste de COMPLETUDE — todo valor do enum `TipoObrigacao` (enumerar via objeto importado de @prisma/client) aparece em exatamente um setor de TIPOS_OBRIGACAO_POR_SETOR, sem sobreposição e sem omissão (este teste pega imediatamente um valor faltante/duplicado se o enum mudar).
    Não inserir blocos de código no schema nesta task — schema é a Task 2.
  </action>
  <verify>
    <automated>npx vitest run tests/tipo-obrigacao-setor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/tipo-obrigacao-setor.test.ts` passa (verde) com pelo menos um teste de classificação DP e um teste de completude do enum.
    - `src/lib/tipo-obrigacao-setor.ts` exporta `TIPOS_OBRIGACAO_POR_SETOR` e `tarefaSetorWhere`.
    - A soma dos comprimentos dos 3 arrays em TIPOS_OBRIGACAO_POR_SETOR é exatamente 20 (5 FISCAL + 4 DP + 11 CONTABIL).
    - `npx tsc --noEmit` não reporta erro no novo arquivo.
  </acceptance_criteria>
  <done>Helper de classificação por setor existe, é a única fonte de verdade do mapa TipoObrigacao→Setor, e seu teste de completude está verde.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Migrar DesempenhoMensal com coluna setor + db push + backfill verificado</name>
  <files>prisma/schema.prisma, scripts/backfill-desempenho-setor.mjs</files>
  <read_first>
    - prisma/schema.prisma (modelo DesempenhoMensal — ver 08-PATTERNS.md linhas 240-264 para o bloco atual literal e a mudança exata exigida; enum Setor já existe nas linhas 19-23)
    - .planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-RESEARCH.md (Pitfall 1 — por que a coluna é mandatória; Assumptions Log A2 — por que backfill "FISCAL" é seguro)
    - .planning/STATE.md (entradas [02-01] "npx prisma db push usado em vez de prisma migrate dev (ambiente Neon sem shadow database)" e [05-04] "npx prisma generate necessario antes de tsc" — convenção de migração deste projeto)
  </read_first>
  <action>
    Convenção de migração deste projeto: `npx prisma db push` (sem pasta prisma/migrations — Neon sem shadow database, confirmado nas decisões [02-01]/[03-01] do STATE.md). NÃO usar `prisma migrate dev`.
    Passos, nesta ordem exata:
    1. Antes de tocar no schema, capturar a contagem de linhas pré-migração: `SELECT COUNT(*) FROM desempenho_mensal` (via script Node usando @prisma/client ou prisma db execute) e registrar o número.
    2. Editar `prisma/schema.prisma`, modelo `DesempenhoMensal`: adicionar campo `setor Setor` (referenciando o enum Setor existente) e trocar `@@unique([competencia, colaboradorId])` por `@@unique([competencia, colaboradorId, setor])`. Como a tabela já tem linhas e `Setor` não tem default natural, adicionar o campo PRIMEIRO como opcional ou via default temporário, OU empregar o padrão deste projeto: adicionar a coluna, rodar `npx prisma db push --accept-data-loss` apenas se a verificação de linhas confirmar que o push não destrói dados (mirrors decisão [03-01]). Preferir: adicionar `setor Setor @default(FISCAL)` para o db push popular linhas existentes automaticamente, depois remover o `@default` num segundo push se desejado — mas o backfill explícito do passo 4 é a verificação canônica, não o default.
    3. Rodar `npx prisma db push` e em seguida `npx prisma generate` (client desatualizado vs schema migrado, conforme decisão [05-04]).
    4. Criar e rodar `scripts/backfill-desempenho-setor.mjs` que executa `UPDATE desempenho_mensal SET setor = 'FISCAL' WHERE setor IS NULL` (ou no-op se o @default já populou) e então verifica que a contagem de linhas com `setor = 'FISCAL'` é EXATAMENTE igual à contagem pré-migração do passo 1 (espelha o padrão "197 FISCAL rows" da Phase 5). Imprimir ambos os números e falhar com exit code não-zero se divergirem.
    Marcado autonomous:false / human-action porque `prisma db push` contra o banco Neon de produção pode exigir confirmação interativa e toca dados reais — o operador humano confirma a contagem pré/pós antes de prosseguir.
  </action>
  <verify>
    <automated>node scripts/backfill-desempenho-setor.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` modelo DesempenhoMensal contém `setor Setor` e `@@unique([competencia, colaboradorId, setor])`.
    - `node scripts/backfill-desempenho-setor.mjs` imprime contagem pré-migração e contagem de linhas FISCAL pós-backfill, e ambas são idênticas (exit code 0).
    - Nenhuma linha de DesempenhoMensal tem `setor IS NULL` após o script (a coluna é NOT NULL no schema final).
    - `npx prisma generate` executou sem erro e o tipo gerado de DesempenhoMensal inclui `setor: Setor`.
  </acceptance_criteria>
  <done>Coluna setor existe e é NOT NULL, unique constraint atualizada, e 100% das linhas históricas foram backfilladas para FISCAL com contagem verificada idêntica à pré-migração.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script Node → banco Neon | UPDATE em massa de dados de produção (backfill); risco de mis-tag de linhas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | backfill de DesempenhoMensal.setor | mitigate | Verificação de contagem pré/pós exata (espelha padrão Phase 5 "197 FISCAL rows"); script falha com exit não-zero se divergir |
| T-08-02 | Tampering | drift do mapa TipoObrigacao→Setor | mitigate | Teste de completude do enum garante que todo valor aparece em exatamente um setor — quebra na hora se um valor for adicionado/duplicado |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase (08-RESEARCH.md Package Legitimacy Audit confirma zero installs) |
</threat_model>

<verification>
- `npx vitest run tests/tipo-obrigacao-setor.test.ts` verde
- `node scripts/backfill-desempenho-setor.mjs` exit 0 com contagens idênticas
- `npx tsc --noEmit` sem novos erros
</verification>

<success_criteria>
- DesempenhoMensal tem dimensão setor com unique constraint composta e backfill FISCAL verificado.
- Helper de classificação por setor existe como única fonte de verdade, com teste de completude verde.
</success_criteria>

<output>
Create `.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-01-SUMMARY.md` when done
</output>

---
phase: 06-motor-de-gera-o-departamento-pessoal
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/modules/tarefas/geracao.ts
  - src/app/(app)/tarefas/actions.ts
  - src/app/(app)/tarefas/gerar-tarefas-button.tsx
  - tests/geracao.idempotencia.test.ts
  - tests/geracao.actions.test.ts
autonomous: true
requirements: [DP-01, DP-02, DP-03, DP-04]
must_haves:
  truths:
    - "executarGeracaoMensal lê empresas com temFuncionariosClt=true e o responsável de DP via responsaveisPorSetor filtrado por setor='DP', gerando as 4 tarefas de DP por empresa CLT com responsável atribuído"
    - "Empresas CLT sem responsável de DP são puladas (nenhuma tarefa criada) e listadas no campo semResponsavelDp do retorno, sem bloquear a geração Fiscal nem a geração de outras empresas CLT"
    - "Rodar a geração 2x na mesma competência não duplica nenhuma tarefa de DP (skipDuplicates via @@unique)"
    - "O loop Fiscal existente permanece inalterado, lendo Empresa.responsavelId (coluna legada)"
    - "semResponsavelDp é propagado ponta a ponta de executarGeracaoMensal → AcaoGeracaoResult → GerarTarefasButton (exibido ao DONO)"
  artifacts:
    - path: "src/modules/tarefas/geracao.ts"
      provides: "Segundo loop DP dentro da mesma transação + retorno estendido com semResponsavelDp"
      contains: "temFuncionariosClt"
    - path: "src/app/(app)/tarefas/actions.ts"
      provides: "AcaoGeracaoResult estendido com semResponsavelDp"
      contains: "semResponsavelDp"
    - path: "src/app/(app)/tarefas/gerar-tarefas-button.tsx"
      provides: "Exibição da lista de empresas sem responsável DP no resumo da geração (D-02)"
      contains: "semResponsavelDp"
  key_links:
    - from: "src/modules/tarefas/geracao.ts"
      to: "src/lib/geracao-tarefas-dp.ts"
      via: "import { gerarTarefasDoMesDp }"
      pattern: "gerarTarefasDoMesDp"
    - from: "src/modules/tarefas/geracao.ts"
      to: "responsaveisPorSetor (setor=DP)"
      via: "tx.empresa.findMany com where setor DP no select"
      pattern: "setor:\\s*\"DP\""
    - from: "src/app/(app)/tarefas/gerar-tarefas-button.tsx"
      to: "gerarTarefasDoMesAction"
      via: "leitura de resultado.semResponsavelDp"
      pattern: "semResponsavelDp"
---

<objective>
Estender o orquestrador transacional `executarGeracaoMensal` com um segundo loop que gera as tarefas de DP para empresas com `temFuncionariosClt=true`, lendo o responsável de DP via `responsaveisPorSetor` filtrado por `setor: "DP"`, pulando-e-listando (D-01/D-02/D-03) as empresas CLT sem responsável, e propagando a nova lista `semResponsavelDp` ponta a ponta até a UI do DONO. O loop Fiscal existente permanece intocado (decisão arquitetural explícita do RESEARCH.md: NÃO migrar Fiscal para a junction table nesta fase).

Purpose: Conectar o catálogo puro da Plan 06-01 ao motor de geração real, dentro da mesma `db.$transaction` que já garante atomicidade snapshot+geração, sem regressão no Fiscal. Fecha o ciclo das obrigações automáticas de DP (DP-01 a DP-04) e entrega o relatório de empresas sem responsável que o dono usa para atribuir manualmente.

Output: `geracao.ts` (segundo loop + retorno estendido), `actions.ts` (`AcaoGeracaoResult` estendido), `gerar-tarefas-button.tsx` (exibe `semResponsavelDp`), e os testes de integração (idempotência + actions) cobrindo os novos comportamentos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-CONTEXT.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md
@.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-PATTERNS.md
@src/modules/tarefas/geracao.ts
@src/app/(app)/tarefas/actions.ts
@src/app/(app)/tarefas/gerar-tarefas-button.tsx
@tests/geracao.idempotencia.test.ts
@tests/geracao.actions.test.ts
@src/lib/visibility-scope.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-02)

Newly-created/extended symbols introduced by this plan (exclude from source-drift verification):

- `src/modules/tarefas/geracao.ts`: extended return type of `executarGeracaoMensal` → `Promise<{ criadas: number; puladas: number; semResponsavelDp: { empresaId: string; nome: string }[] }>`; new internal DP loop (second `tx.empresa.findMany` with `temFuncionariosClt` + `responsaveisPorSetor` where `setor:"DP"`); `comResponsavelDp`/`semResponsavelDp` partition; call to `gerarTarefasDoMesDp`
- `src/app/(app)/tarefas/actions.ts`: extended `AcaoGeracaoResult` `ok:true` branch with `semResponsavelDp: { empresaId: string; nome: string }[]`
- `src/app/(app)/tarefas/gerar-tarefas-button.tsx`: UI rendering of `resultado.semResponsavelDp`
- `tests/geracao.idempotencia.test.ts`: new DP test cases (skip-and-list, positive-path with DP responsible, DP idempotency, DP-vs-FISCAL responsible filter regression)
- `tests/geracao.actions.test.ts`: assertion that `gerarTarefasDoMesAction` propagates `semResponsavelDp`

Consumed from Plan 06-01: `gerarTarefasDoMesDp`, `TipoObrigacaoDp`, enum members `FOLHA`/`ESOCIAL`/`FGTS`/`INSS`.
Reused unchanged: the existing Fiscal loop (`Empresa.responsavelId` legacy read), the snapshot block, `createMany({ skipDuplicates: true })`, the DONO-only auth guard and `competenciaSchema` validation in `gerarTarefasDoMesAction`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 RED): Estender testes de integração com os casos de DP (skip-and-list, idempotência, filtro de setor)</name>
  <files>tests/geracao.idempotencia.test.ts, tests/geracao.actions.test.ts</files>
  <read_first>
    - tests/geracao.idempotencia.test.ts (arquivo inteiro — o mock `empresaFindManyMock` hoje usa `mockResolvedValue` (não `Once`); o segundo loop DP fará `empresa.findMany` ser chamado 2x, então os testes existentes precisam sequenciar com `mockResolvedValueOnce`)
    - tests/geracao.actions.test.ts (arquivo inteiro — `findManyMock` também passará a ser chamado 2x; mesma necessidade de sequenciar)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-PATTERNS.md (seção "tests/geracao.idempotencia.test.ts (extend)" — template exato dos casos DP, incluindo o teste de regressão FISCAL-vs-DP responsável)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md (Code Examples: teste "empresa CLT sem responsável DP é pulada e listada"; Pitfall 2/3/4)
    - tests/setup.ts (mockDpColaboradorUser já existe, setor "DP")
  </read_first>
  <behavior>
    - Ajustar os testes EXISTENTES de idempotência/actions para sequenciar as DUAS chamadas de `empresa.findMany` (1ª = loop Fiscal, 2ª = loop DP) via `mockResolvedValueOnce`, e fazer a 2ª chamada (DP) resolver `[]` por padrão quando o teste só exercita o Fiscal — preservando os assertos Fiscais já verdes.
    - Novo caso: empresa CLT sem responsável DP (`responsaveisPorSetor: []`) é pulada (não gera tarefa) e aparece em `resultado.semResponsavelDp`, enquanto a tarefa Fiscal de outra empresa é criada normalmente (`criadas` reflete só o Fiscal).
    - Novo caso (positivo): empresa CLT COM responsável DP (`responsaveisPorSetor: [{ usuarioId: "dp_user" }]`) gera as 4 tarefas de DP atribuídas a `dp_user`.
    - Novo caso (regressão Pitfall 2): empresa com responsável FISCAL e DP simultâneos no `responsaveisPorSetor` — a query DP filtra por `setor:"DP"`, então o responsável da tarefa DP é o usuário DP, nunca o FISCAL.
    - Novo caso (idempotência DP): segunda execução na mesma competência com `createMany` retornando count 0 → `criadas` 0 / `puladas` reflete as DP já existentes.
    - Em tests/geracao.actions.test.ts: o resultado de `gerarTarefasDoMesAction` (caminho DONO sucesso) inclui o campo `semResponsavelDp` (array).
  </behavior>
  <action>
    Atualizar os mocks dos testes existentes para sequenciar `empresaFindManyMock.mockResolvedValueOnce(empresasFiscal).mockResolvedValueOnce(empresasClt)` (idempotencia) e o equivalente `findManyMock` (actions). Onde um teste Fiscal existente usa `mockResolvedValue(empresas)` sem segunda chamada, trocar para `.mockResolvedValueOnce(empresas).mockResolvedValueOnce([])` para que o loop DP receba lista vazia e não altere o `criadas` esperado. Adicionar os novos `it(...)` listados em `<behavior>` usando o template exato do 06-PATTERNS.md: o caso skip-and-list espera `resultado.semResponsavelDp` igual a `[{ empresaId: "e2", nome: "Empresa CLT sem DP" }]`. Para o caso de regressão de setor, a empresa CLT deve ter `responsaveisPorSetor: [{ usuarioId: "dp_user" }]` (a query já filtra por DP no select, então o mock só devolve a linha DP — assertar que a tarefa DP gerada tem `responsavelId === "dp_user"`). Estes testes DEVEM falhar agora (RED) porque o segundo loop DP, o campo `semResponsavelDp` e o consumo de `gerarTarefasDoMesDp` ainda não existem em geracao.ts/actions.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/geracao.idempotencia.test.ts tests/geracao.actions.test.ts 2>&1 | grep -E "(FAIL|semResponsavelDp|AssertionError)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/geracao.idempotencia.test.ts contém um `it` cujo corpo assere `resultado.semResponsavelDp` para o caso de empresa CLT sem responsável DP.
    - tests/geracao.idempotencia.test.ts contém um `it` de regressão que assere o `responsavelId` da tarefa DP é o usuário DP (não FISCAL).
    - tests/geracao.actions.test.ts assere a presença de `semResponsavelDp` no retorno do caminho DONO-sucesso.
    - Os testes Fiscais pré-existentes foram ajustados para sequenciar duas chamadas de `findMany` e continuam asserindo os mesmos `criadas`/`puladas`.
    - Rodar os dois arquivos AGORA falha (RED).
  </acceptance_criteria>
  <done>Os testes de integração cobrem skip-and-list, idempotência DP, filtro de setor e propagação de semResponsavelDp; falham por símbolos/comportamento ainda não implementados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Adicionar o segundo loop DP a executarGeracaoMensal e propagar semResponsavelDp pela action e UI</name>
  <files>src/modules/tarefas/geracao.ts, src/app/(app)/tarefas/actions.ts, src/app/(app)/tarefas/gerar-tarefas-button.tsx</files>
  <read_first>
    - src/modules/tarefas/geracao.ts (arquivo inteiro — o bloco Fiscal linhas 69-74 e a persistência linhas 80-86 ficam INALTERADOS; o segundo loop entra na MESMA db.$transaction)
    - src/app/(app)/tarefas/actions.ts (AcaoGeracaoResult linhas 14-16; gerarTarefasDoMesAction linhas 284-316 — só o destructure/return linhas 307-313 cresce; auth guard e competenciaSchema inalterados)
    - src/app/(app)/tarefas/gerar-tarefas-button.tsx (arquivo inteiro — handleClick lê resultado.criadas/puladas; estender para exibir semResponsavelDp)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-PATTERNS.md (código-alvo exato do segundo loop, do where setor:"DP" no select, e do retorno estendido)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md (Pattern 2; Anti-Patterns: não migrar Fiscal, não abortar transação, não esquecer where setor DP)
    - src/lib/geracao-tarefas-dp.ts (gerarTarefasDoMesDp criado na Plan 06-01 — assinatura de entrada { id, responsavelId }[])
  </read_first>
  <action>
    1. Em src/modules/tarefas/geracao.ts: importar `gerarTarefasDoMesDp` de `@/lib/geracao-tarefas-dp`. Manter o bloco de snapshot e o loop Fiscal (com `select: { id, regimeTributario, responsavelId }`) EXATAMENTE como estão. Adicionar, dentro da mesma `db.$transaction`, um segundo `tx.empresa.findMany({ where: { ativo: true, temFuncionariosClt: true }, select: { id: true, nome: true, responsaveisPorSetor: { where: { setor: "DP" }, select: { usuarioId: true } } } })`. CRÍTICO: o `where: { setor: "DP" }` dentro do `select` de `responsaveisPorSetor` é obrigatório (Pitfall 2 — sem ele, `[0]` pegaria o responsável errado). Particionar em `comResponsavelDp` (length>0) e `semResponsavelDp` (length===0, mapeado para `{ empresaId: e.id, nome: e.nome }`) via `Array.filter` em memória — NUNCA `throw` (D-03/Pitfall 3). Chamar `gerarTarefasDoMesDp(comResponsavelDp.map(e => ({ id: e.id, responsavelId: e.responsaveisPorSetor[0].usuarioId })), competencia)`. Mesclar `const tarefas = [...tarefasFiscal, ...tarefasDp]` ANTES do único `createMany({ skipDuplicates: true })` existente (não adicionar segundo createMany). Estender a assinatura de retorno para `Promise<{ criadas: number; puladas: number; semResponsavelDp: { empresaId: string; nome: string }[] }>` e retornar `semResponsavelDp` em TODOS os caminhos de retorno (inclusive o early-return de `tarefas.length === 0`, que deve devolver `semResponsavelDp` já calculado).
    2. Em src/app/(app)/tarefas/actions.ts: estender o branch `ok: true` de `AcaoGeracaoResult` para incluir `semResponsavelDp: { empresaId: string; nome: string }[]`. No corpo de `gerarTarefasDoMesAction`, desestruturar `const { criadas, puladas, semResponsavelDp } = await executarGeracaoMensal(...)` e incluir `semResponsavelDp` no objeto de retorno `ok: true`. Auth guard DONO e validação de competência permanecem inalterados.
    3. Em src/app/(app)/tarefas/gerar-tarefas-button.tsx: após o toast de sucesso existente, se `resultado.semResponsavelDp.length > 0`, exibir um segundo toast (ex.: `toast.warning`) informando quantas empresas CLT foram puladas por falta de responsável de DP (D-02) — texto direcionando o DONO a atribuir o responsável na tela de Empresas. Manter `router.refresh()`.
    NÃO migrar o loop Fiscal para `responsaveisPorSetor` — manter a leitura legada `responsavelId` (decisão arquitetural do RESEARCH.md). Após as edições, os testes da Task 1 desta plan e da Plan 06-01 devem ficar verdes.
  </action>
  <verify>
    <automated>npx vitest run tests/geracao.idempotencia.test.ts tests/geracao.actions.test.ts tests/geracao-tarefas-dp.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'setor: "DP"' src/modules/tarefas/geracao.ts` retorna ≥ 1 (filtro de setor presente no select).
    - O loop Fiscal em geracao.ts ainda lê `select: { id: true, regimeTributario: true, responsavelId: true }` (não migrado para junction).
    - geracao.ts NÃO contém um segundo `createMany` para DP — há um único `tarefa.createMany` que recebe `[...tarefasFiscal, ...tarefasDp]`.
    - geracao.ts NÃO contém `throw` no caminho de empresa sem responsável DP.
    - `AcaoGeracaoResult` em actions.ts inclui `semResponsavelDp` no branch `ok: true`.
    - gerar-tarefas-button.tsx referencia `resultado.semResponsavelDp`.
    - `npx vitest run` dos 3 arquivos passa e `npx tsc --noEmit` passa.
  </acceptance_criteria>
  <done>O segundo loop DP gera tarefas atribuídas ao responsável DP correto, pula-e-lista empresas sem responsável sem abortar, e semResponsavelDp chega à UI; Fiscal inalterado; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Rodar a suite completa para confirmar ausência de regressão Fiscal/IDOR</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-VALIDATION.md (Sampling Rate: suite completa por wave merge — crítico para Pitfall B3)
    - .planning/phases/06-motor-de-gera-o-departamento-pessoal/06-RESEARCH.md (Pitfall 4: grep todos os call-sites de executarGeracaoMensal e AcaoGeracaoResult)
  </read_first>
  <action>
    Rodar `npm test` (suite completa) para garantir que a extensão do retorno de `executarGeracaoMensal` e do `AcaoGeracaoResult` não quebrou nenhum consumidor existente (Pitfall 4) e que nenhuma regressão Fiscal/IDOR foi introduzida (Pitfall B3). Caso algum teste de outro arquivo (ex.: dashboards.snapshot, scheduler) consuma o retorno de `executarGeracaoMensal` e quebre por causa do novo campo, ajustar apenas o ponto de consumo para aceitar o campo extra — sem alterar a lógica de DP. NÃO editar a suite de regressão IDOR/visibilidade para "fazer passar"; se ela falhar, é regressão real a corrigir na lógica.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passa inteira (suite Fiscal, IDOR/visibilidade, DP, dashboards) — zero falhas.
    - Nenhum arquivo de teste de regressão IDOR/visibilidade pré-existente foi editado para mascarar uma falha.
  </acceptance_criteria>
  <done>Suite completa verde, confirmando DP gerado corretamente e Fiscal/IDOR sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron / botão DONO → executarGeracaoMensal | A geração roda sem sessão quando disparada por cron (invariante já documentado e aceito) |
| executarGeracaoMensal → responsaveisPorSetor (banco) | A query lê qual usuário será o responsável da tarefa DP — atribuição incorreta tem impacto de integridade |
| gerarTarefasDoMesAction → cliente | Action exposta; já protegida por guard DONO-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-DP-01 | Tampering | Query DP em geracao.ts (filtro de setor esquecido) | mitigate | `where: { setor: "DP" }` obrigatório dentro do select de `responsaveisPorSetor`; teste de regressão dedicado com empresa que tem FISCAL e DP simultâneos, asserindo que a tarefa DP usa o usuário DP (Task 1 caso de regressão; Task 2 acceptance `grep setor: "DP"`) |
| T-06-DP-02 | Denial of Service | executarGeracaoMensal (abort global por empresa sem responsável) | mitigate | "Pular e listar" por empresa via `Array.filter`, nunca `throw`; teste explícito com 100% das empresas CLT sem responsável confirmando que Fiscal continua gerando (Task 1) e acceptance "geracao.ts NÃO contém throw no caminho sem responsável" (Task 2) |
| T-06-DP-03 | Spoofing/Elevation | Cron sem autenticação dispara geração | accept | Invariante já documentado e aceito (cron não tem sessão; executarGeracaoMensal é deliberadamente sem escopo de visibilidade). Esta plan NÃO altera esse invariante — preserva o comentário/contrato existente no topo de geracao.ts |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH.md Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `npx vitest run tests/geracao.idempotencia.test.ts tests/geracao.actions.test.ts tests/geracao-tarefas-dp.test.ts` verde.
- `npx tsc --noEmit` verde.
- `npm test` (suite completa) verde — sem regressão Fiscal/IDOR.
- `grep 'setor: "DP"' src/modules/tarefas/geracao.ts` retorna o filtro presente.
</verification>

<success_criteria>
- DP-01..DP-04: empresas CLT com responsável DP recebem as 4 tarefas de DP atribuídas ao responsável DP correto, com prazos ajustados (via Plan 06-01).
- D-01/D-02/D-03: empresas CLT sem responsável DP são puladas e listadas em `semResponsavelDp`, sem bloquear a geração global; a lista chega ao DONO na UI.
- Idempotência: segunda execução na mesma competência não duplica tarefas de DP.
- Fiscal e IDOR sem regressão (suite completa verde).
</success_criteria>

<output>
Create `.planning/phases/06-motor-de-gera-o-departamento-pessoal/06-02-SUMMARY.md` when done
</output>

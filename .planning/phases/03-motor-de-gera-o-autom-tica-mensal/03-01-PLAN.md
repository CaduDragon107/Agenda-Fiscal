---
phase: 03-motor-de-gera-o-autom-tica-mensal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - prisma/schema.prisma
  - src/lib/competencia.ts
  - src/lib/dia-util.ts
  - src/lib/geracao-tarefas.ts
  - tests/dia-util.test.ts
  - tests/geracao-tarefas.test.ts
autonomous: true
requirements: [TASK-01, TASK-02]
must_haves:
  truths:
    - "A due date falling on a weekend is anticipated to the preceding Friday"
    - "A due date falling on a known 2026 national holiday is anticipated to the preceding business day"
    - "diaBase 31 in a 28/29-day February resolves to the last day of that month, not March"
    - "gerarTarefasDoMes produces 4 obligations for LUCRO_REAL, 2 for LUCRO_PRESUMIDO, 1 for SIMPLES_NACIONAL"
    - "The Tarefa table has tipoObrigacao, competencia columns and a unique constraint over (empresaId, tipoObrigacao, competencia) live in the database"
  artifacts:
    - path: "src/lib/dia-util.ts"
      provides: "anticiparParaDiaUtil pure function (holiday/weekend backward walk)"
      exports: ["anticiparParaDiaUtil"]
    - path: "src/lib/geracao-tarefas.ts"
      provides: "CATALOGO_OBRIGACOES catalog + gerarTarefasDoMes pure generator"
      exports: ["gerarTarefasDoMes", "TarefaParaCriar", "TipoObrigacao"]
    - path: "src/lib/competencia.ts"
      provides: "competenciaAtual() + competenciaSchema (YYYY-MM canonical format)"
      exports: ["competenciaAtual", "competenciaSchema"]
    - path: "prisma/schema.prisma"
      provides: "enum TipoObrigacao + Tarefa.tipoObrigacao/competencia + @@unique"
      contains: "enum TipoObrigacao"
  key_links:
    - from: "src/lib/geracao-tarefas.ts"
      to: "src/lib/dia-util.ts"
      via: "import anticiparParaDiaUtil"
      pattern: "anticiparParaDiaUtil"
    - from: "src/lib/dia-util.ts"
      to: "date-holidays"
      via: "new Holidays('BR') singleton"
      pattern: "new Holidays"
---

<objective>
Estabelecer a fundação pura e testável do motor de geração: estender o schema Prisma com `enum TipoObrigacao`, os campos `tipoObrigacao`/`competencia` em `Tarefa` e o índice único de idempotência (D-13); aplicar o push ao banco Neon; e implementar as duas funções puras mais difíceis de acertar — o catálogo de obrigações por regime + cálculo de prazo (D-02/D-03/D-04) e a antecipação de dia útil via `date-holidays` (D-05/D-06) — com seus testes unitários Wave 0.

Purpose: Isolar toda a lógica de data/catálogo (a parte mais propensa a bugs silenciosos — `isHoliday()` retorna `false | array`, e `setDate(date, 31)` rola para o mês seguinte) em funções puras sem I/O, exaustivamente testáveis sem banco nem mocks, antes de qualquer orquestração ou UI.
Output: Schema empurrado ao banco; `src/lib/competencia.ts`, `src/lib/dia-util.ts`, `src/lib/geracao-tarefas.ts`; testes verdes em `tests/dia-util.test.ts` e `tests/geracao-tarefas.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-CONTEXT.md
@.planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-RESEARCH.md
@.planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-PATTERNS.md
@.planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-VALIDATION.md
@prisma/schema.prisma
@src/lib/alert-prazo.ts
@tests/alert-prazo.test.ts
</context>

<artifacts_this_phase_produces>
Symbols created across the whole of Phase 3 (this plan creates the subset marked ✚ here; the
plan-review source-grounding pass should treat ALL of these as newly-created, not drift):

- ✚ `enum TipoObrigacao` (Prisma) with members `ICMS`, `PIS_COFINS`, `SPED_FISCAL`, `SPED_CONTRIBUICOES`, `DAS`
- ✚ `Tarefa.tipoObrigacao` (Prisma field, `TipoObrigacao?`)
- ✚ `Tarefa.competencia` (Prisma field, `String?`)
- ✚ `@@unique([empresaId, tipoObrigacao, competencia])` on `Tarefa`
- ✚ `@@index([competencia])` on `Tarefa`
- ✚ `competenciaAtual` (function, `src/lib/competencia.ts`)
- ✚ `competenciaSchema` (Zod schema, `src/lib/competencia.ts`)
- ✚ `anticiparParaDiaUtil` (function, `src/lib/dia-util.ts`)
- ✚ `CATALOGO_OBRIGACOES` (const map, `src/lib/geracao-tarefas.ts`)
- ✚ `TITULO_OBRIGACAO` (const map, `src/lib/geracao-tarefas.ts`)
- ✚ `gerarTarefasDoMes` (function, `src/lib/geracao-tarefas.ts`)
- ✚ `TarefaParaCriar` (type, `src/lib/geracao-tarefas.ts`)
- ✚ `TipoObrigacao` (TS union type, `src/lib/geracao-tarefas.ts`)
- `executarGeracaoMensal` (function, `src/modules/tarefas/geracao.ts`) — Plan 02
- `iniciarScheduler` (function, `src/lib/scheduler.ts`) — Plan 02
- `register` (function, `instrumentation.ts`) — Plan 02
- `gerarTarefasDoMesAction` (Server Action, `src/app/(app)/tarefas/actions.ts`) — Plan 03
- `GerarTarefasButton` (client component) — Plan 03
- Imported library symbols (new deps): `date-holidays` (`Holidays`), `node-cron` (`cron`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar deps + Wave 0 tests RED (dia-util + geracao-tarefas)</name>
  <files>package.json, tests/dia-util.test.ts, tests/geracao-tarefas.test.ts</files>
  <read_first>
    - .planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-RESEARCH.md (Patterns 1-2, Pitfalls 1-2, Assumptions A1/A3)
    - .planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-VALIDATION.md (Per-Task Verification Map, Wave 0 Requirements)
    - .planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-PATTERNS.md (sections for tests/geracao-tarefas.test.ts and tests/dia-util.test.ts)
    - tests/alert-prazo.test.ts (no-mock pure-function test convention to copy)
    - package.json (confirm date-fns@^4.4.0 present; date-holidays/node-cron absent)
  </read_first>
  <action>
    Install runtime deps `date-holidays@3.30.2` and `node-cron@4.4.1`, plus dev dep
    `@types/node-cron` (per RESEARCH.md Standard Stack; both packages Approved in the Package
    Legitimacy Audit, no postinstall scripts). Use the install command
    `npm install date-holidays@3.30.2 node-cron@4.4.1` then
    `npm install --save-dev @types/node-cron`.

    Then write Wave 0 RED tests (no `vi.mock` — these are pure functions, copy the describe/it
    structure of tests/alert-prazo.test.ts):

    `tests/dia-util.test.ts` — import `anticiparParaDiaUtil` from `@/lib/dia-util`. Required
    cases, with these exact `it(...)` title substrings so the VALIDATION.md `-t` filters match:
    (a) title containing "fim de semana": a Saturday input (e.g. `new Date(2026, 0, 17)` =
    sábado 17/01/2026) returns the preceding Friday `new Date(2026, 0, 16)`; a Sunday input
    returns the preceding Friday too. (b) title containing "feriado": a known 2026 NATIONAL
    holiday on a weekday — Independência `new Date(2026, 8, 7)` (07/09/2026, a Monday) — is
    anticipated to the preceding business day (Friday 04/09/2026); also assert
    `Sexta-feira Santa` 03/04/2026 (Pitfall 1 canary) anticipates correctly. (c) Add an
    assertion that the raw library call `new Holidays('BR').isHoliday(new Date(2026, 8, 7))`
    is `!== false` (Assumption A1/A3 resolution) and that a plain business day returns
    unchanged. Compare dates by `getFullYear()/getMonth()/getDate()` (ignore time).

    `tests/geracao-tarefas.test.ts` — import `gerarTarefasDoMes` from `@/lib/geracao-tarefas`.
    Required cases with these exact title substrings: (a) "catalogo": one empresa per regime
    yields obligation counts LUCRO_REAL=4 (ICMS, PIS_COFINS, SPED_FISCAL, SPED_CONTRIBUICOES),
    LUCRO_PRESUMIDO=2 (SPED_FISCAL, SPED_CONTRIBUICOES only — no ICMS, no PIS_COFINS),
    SIMPLES_NACIONAL=1 (DAS); assert each `tipoObrigacao` set per D-02. (b) "ultimo dia": for
    competência "2026-01" the SPED_CONTRIBUICOES (diaBase 31) vencimento month is February
    2026 — assert the resulting `prazo` falls in February (month index 1), NOT March, proving
    D-04 (Pitfall 2). Use a fixed competência that lands the base day on a business day so the
    test asserts the last-of-month clamp independent of anticipation, OR assert the prazo month
    is February regardless. (c) assert each returned object carries `empresaId`, `responsavelId`
    (= empresa.responsavelId, D-09), `competencia` (= input), and a non-empty `titulo`.

    These tests MUST FAIL initially (RED) because `src/lib/dia-util.ts` and
    `src/lib/geracao-tarefas.ts` do not exist yet — that is the expected Wave 0 state.
  </action>
  <verify>
    <automated>npx vitest run tests/dia-util.test.ts tests/geracao-tarefas.test.ts 2>&1 | grep -qE "Cannot find module|Failed to resolve import|failed" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `date-holidays` and `node-cron`; devDependencies include `@types/node-cron`
    - `tests/dia-util.test.ts` exists with `it` titles containing the substrings "fim de semana" and "feriado"
    - `tests/geracao-tarefas.test.ts` exists with `it` titles containing the substrings "catalogo" and "ultimo dia"
    - Running the two test files fails with a module-resolution / unimplemented error (RED state confirmed) — neither `@/lib/dia-util` nor `@/lib/geracao-tarefas` resolves yet
    - No `vi.mock` calls appear in either new test file (pure-function convention)
  </acceptance_criteria>
  <done>As duas suites Wave 0 existem, falham por módulo ausente (RED), e as deps date-holidays/node-cron/@types/node-cron estão no package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar competencia + dia-util + geracao-tarefas (GREEN)</name>
  <files>src/lib/competencia.ts, src/lib/dia-util.ts, src/lib/geracao-tarefas.ts</files>
  <read_first>
    - tests/dia-util.test.ts, tests/geracao-tarefas.test.ts (the RED tests from Task 1 — implement to satisfy them)
    - .planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-RESEARCH.md (Pattern 1 full draft of geracao-tarefas.ts, Pattern 2 full draft of dia-util.ts, Pitfall 4 competencia regex)
    - .planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-PATTERNS.md (geracao-tarefas.ts and dia-util.ts pattern assignments)
    - src/lib/alert-prazo.ts (JSDoc/pure-helper convention: header comment naming D-NN decisions)
  </read_first>
  <behavior>
    - anticiparParaDiaUtil(weekend date) → preceding Friday
    - anticiparParaDiaUtil(2026 national holiday on weekday) → preceding business day
    - anticiparParaDiaUtil(plain business day) → same date unchanged
    - gerarTarefasDoMes: LUCRO_REAL→4 obrigações, LUCRO_PRESUMIDO→2 (SPEDs only), SIMPLES_NACIONAL→1 (DAS)
    - gerarTarefasDoMes diaBase=31 for Jan competência → prazo in February (last day clamp, D-04)
    - competenciaSchema accepts "2026-01", rejects "2026-1" and "2026-13"
    - competenciaAtual() returns current month as canonical "YYYY-MM"
  </behavior>
  <action>
    Create `src/lib/competencia.ts`: export `competenciaSchema` = Zod string with regex
    `/^\d{4}-(0[1-9]|1[0-2])$/` (D-13 canonical key format, Pitfall 4); export
    `competenciaAtual(): string` = `format(new Date(), "yyyy-MM")` using date-fns `format`
    (never manual string concat — Pitfall 4). Header docblock cites D-13 and Pitfall 4.

    Create `src/lib/dia-util.ts` per RESEARCH.md Pattern 2: module-level singleton
    `const hd = new Holidays("BR")` (no state arg — national holidays only, REQUIREMENTS Out of
    Scope; instantiate ONCE at module scope, never per call). Internal `isDiaUtil(date)`:
    returns false if `isSaturday(date) || isSunday(date)` (date-fns), else returns
    `hd.isHoliday(date) === false`. CRITICAL: write `=== false`, never `=== true` (isHoliday
    returns `false | HolidayResult[]`, Pitfall 1). Export `anticiparParaDiaUtil(date: Date):
    Date` that walks backward one day at a time via date-fns `subDays(atual, 1)` until
    `isDiaUtil` is true, returning a new Date (no mutation, D-05/D-06). Header docblock cites
    D-05/D-06 and the isHoliday truthiness note.

    Create `src/lib/geracao-tarefas.ts` per RESEARCH.md Pattern 1: export TS union type
    `TipoObrigacao = "ICMS" | "PIS_COFINS" | "SPED_FISCAL" | "SPED_CONTRIBUICOES" | "DAS"`;
    const `CATALOGO_OBRIGACOES: Record<RegimeTributario, {tipo: TipoObrigacao; diaBase: number}[]>`
    exactly per D-02: LUCRO_REAL=[ICMS:20, PIS_COFINS:25, SPED_FISCAL:19, SPED_CONTRIBUICOES:31],
    LUCRO_PRESUMIDO=[SPED_FISCAL:19, SPED_CONTRIBUICOES:31], SIMPLES_NACIONAL=[DAS:20]; const
    `TITULO_OBRIGACAO` mapping each tipo to a display label (ICMS, PIS/COFINS, SPED Fiscal, SPED
    Contribuições, DAS). Internal `calcularPrazoBase(competencia, diaBase)`: split competência on
    "-", `mesVencimento = addMonths(new Date(ano, mes-1, 1), 1)` (D-03 — always next month),
    `dia = Math.min(diaBase, lastDayOfMonth(mesVencimento).getDate())` (D-04 — clamp 31 in short
    months, Pitfall 2), return `setDate(mesVencimento, dia)`. Export `TarefaParaCriar` type
    `{ empresaId, responsavelId, titulo, tipoObrigacao: TipoObrigacao, competencia: string, prazo:
    Date }`. Export `gerarTarefasDoMes(empresas: {id; regimeTributario: RegimeTributario;
    responsavelId}[], competencia: string): TarefaParaCriar[]` that flatMaps each empresa over
    CATALOGO_OBRIGACOES[regime], computing prazoBase then `prazo = anticiparParaDiaUtil(prazoBase)`
    (D-05), responsavelId = empresa.responsavelId (D-09), titulo combining TITULO_OBRIGACAO[tipo]
    with the competência month name (pt-BR) and year. Import `RegimeTributario` from
    `@prisma/client`. Header docblock cites D-01 through D-05 and D-09.
  </action>
  <verify>
    <automated>npx vitest run tests/dia-util.test.ts tests/geracao-tarefas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/dia-util.test.ts tests/geracao-tarefas.test.ts` passes (GREEN)
    - `src/lib/dia-util.ts` contains the substring `=== false` and `new Holidays("BR")` at module scope (singleton), and contains NO occurrence of `isHoliday` followed by `=== true`
    - `src/lib/geracao-tarefas.ts` CATALOGO_OBRIGACOES has exactly 4 entries for LUCRO_REAL, 2 for LUCRO_PRESUMIDO, 1 for SIMPLES_NACIONAL, and uses `Math.min` with `lastDayOfMonth`
    - `src/lib/geracao-tarefas.ts` imports `anticiparParaDiaUtil` from `./dia-util` and `RegimeTributario` from `@prisma/client`
    - `src/lib/competencia.ts` regex literal is `/^\d{4}-(0[1-9]|1[0-2])$/` and `competenciaAtual` uses date-fns `format`
    - None of the three files import `@/lib/db`, `@/auth`, or `node-cron` (purity preserved)
  </acceptance_criteria>
  <done>As três libs puras existem, são livres de I/O, e ambas as suites Wave 0 estão verdes.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Estender schema Prisma + db push</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (current Tarefa model lines 80-100, RegimeTributario enum lines 19-23, @@map/@@index conventions)
    - .planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-RESEARCH.md (Pattern 4 schema extension block, Postgres NULL-distinct caveat)
    - .planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-PATTERNS.md (prisma/schema.prisma pattern assignment)
    - .planning/STATE.md line 91 (npx prisma db push used instead of migrate dev — Neon has no shadow DB)
  </read_first>
  <action>
    Add `enum TipoObrigacao { ICMS PIS_COFINS SPED_FISCAL SPED_CONTRIBUICOES DAS }` placed
    alongside the existing enums (after `TarefaStatus`), matching the exact one-member-per-line
    indentation of `enum RegimeTributario`. Extend `model Tarefa` (D-13): add two nullable fields
    after `status` — `tipoObrigacao TipoObrigacao?` and `competencia String?` (nullable so Phase 2
    avulsa tasks keep them null and, per Postgres NULL-distinct semantics, never collide under the
    unique index). Add `@@unique([empresaId, tipoObrigacao, competencia])` and
    `@@index([competencia])` to the model's attribute block, following the existing `@@index`/
    `@@map("tarefas")` convention exactly (do not rename or reorder existing indexes). Do NOT add
    a new table — D-14 reuses the existing Tarefa/TarefaStatus.

    Then apply to the live Neon database with `npx prisma db push` (NOT `migrate dev` — Neon has
    no shadow database, per STATE.md line 91). The change is additive (new enum, two nullable
    columns, one new unique index, one new index) and should NOT be reported destructive; if and
    ONLY if the tool reports the change as destructive, fall back to
    `npx prisma db push --accept-data-loss` and note why in the SUMMARY. The push regenerates the
    Prisma client so `@prisma/client` exposes `TipoObrigacao` and the new fields.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `enum TipoObrigacao` with the 5 members ICMS, PIS_COFINS, SPED_FISCAL, SPED_CONTRIBUICOES, DAS
    - `model Tarefa` contains `tipoObrigacao TipoObrigacao?`, `competencia String?`, `@@unique([empresaId, tipoObrigacao, competencia])`, and `@@index([competencia])`
    - `npx prisma validate` exits 0
    - `npx prisma db push` completed against the Neon DB (push log present in SUMMARY; reports the new enum + columns + unique index applied, no data loss)
    - `npx tsc --noEmit` exits 0 (generated client types resolve `TipoObrigacao`)
    - No new `model` block was added (extends existing Tarefa, per D-14)
  </acceptance_criteria>
  <done>Schema estendido e empurrado ao banco Neon; cliente Prisma regenerado expõe TipoObrigacao e os novos campos; tipos compilam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| date input → date-holidays/date-fns | Internal computed dates only in this plan; no untrusted input crosses into the pure functions (competência string is validated by competenciaSchema before reaching callers in later plans) |
| schema migration → live DB | `npx prisma db push` mutates the Neon database structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | competência string used as idempotency key component | mitigate | `competenciaSchema` Zod regex `/^\d{4}-(0[1-9]|1[0-2])$/` enforces a single canonical form (Pitfall 4); enforced at the action boundary in Plan 03 — this plan creates the schema |
| T-3-02 | Tampering | `npx prisma db push` to Neon | accept | Additive-only change (new enum, nullable columns, new indexes); no `--accept-data-loss` expected; existing rows keep NULL tipoObrigacao/competencia which is the desired Phase 2 avulsa behavior |
| T-3-SC | Tampering | npm installs (date-holidays, node-cron, @types/node-cron) | mitigate | All three Approved in RESEARCH.md Package Legitimacy Audit (verified registry version + GitHub source + no postinstall scripts); pinned to exact verified versions 3.30.2 / 4.4.1 |
</threat_model>

<verification>
- `npx vitest run tests/dia-util.test.ts tests/geracao-tarefas.test.ts` green (Wave 0 TASK-02 + catalog TASK-01 coverage)
- `npx prisma validate` exits 0; `npx tsc --noEmit` exits 0 with new TipoObrigacao type resolvable
- `prisma db push` applied to Neon (additive)
</verification>

<success_criteria>
- Schema tem `enum TipoObrigacao`, `Tarefa.tipoObrigacao/competencia`, `@@unique([empresaId, tipoObrigacao, competencia])`, e está empurrado ao banco
- `anticiparParaDiaUtil` antecipa corretamente fins de semana E feriados nacionais de 2026 (testado)
- `gerarTarefasDoMes` produz o conjunto correto de obrigações por regime e respeita o último-dia-do-mês para diaBase 31 (testado)
- Funções puras sem qualquer import de db/auth/cron
</success_criteria>

<output>
Create `.planning/phases/03-motor-de-gera-o-autom-tica-mensal/03-01-SUMMARY.md` when done
</output>

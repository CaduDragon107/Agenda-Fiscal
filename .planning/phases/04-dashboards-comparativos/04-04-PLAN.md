---
phase: 04-dashboards-comparativos
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/app/(app)/dashboards/page.tsx
  - src/app/(app)/dashboards/desempenho-colaboradores-chart.tsx
  - src/app/(app)/dashboards/evolucao-mensal-chart.tsx
  - src/app/(app)/dashboards/ranking-empresas-table.tsx
  - src/app/(app)/app-sidebar.tsx
  - tests/dashboards.rbac.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "O DONO acessa /dashboards e vê 3 seções: desempenho por colaborador, evolução mensal e empresas com mais atrasos"
    - "Um COLABORADOR navegando direto para /dashboards recebe 404 (notFound), antes de qualquer query ao banco"
    - "O item de nav 'Dashboards' aparece na sidebar apenas para DONO e leva a /dashboards"
  artifacts:
    - path: "src/app/(app)/dashboards/page.tsx"
      provides: "Server Component: auth + guard DONO (notFound) + Promise.all dos 3 datasets + render 3 seções"
      contains: "notFound"
    - path: "src/app/(app)/dashboards/desempenho-colaboradores-chart.tsx"
      provides: "Bar chart % no prazo por colaborador (use client)"
    - path: "src/app/(app)/dashboards/evolucao-mensal-chart.tsx"
      provides: "Line/Area chart de evolução mensal (use client)"
    - path: "src/app/(app)/dashboards/ranking-empresas-table.tsx"
      provides: "Tabela TanStack ranking empresas + top-N bar (use client)"
    - path: "src/app/(app)/app-sidebar.tsx"
      provides: "Item nav Dashboards habilitado, DONO-only"
  key_links:
    - from: "src/app/(app)/dashboards/page.tsx"
      to: "src/modules/dashboards/queries.ts"
      via: "Promise.all([listarDesempenhoColaboradoresMesAtual, listarEvolucaoMensal, listarRankingEmpresas])"
      pattern: "listar(Desempenho|Evolucao|Ranking)"
    - from: "src/app/(app)/app-sidebar.tsx"
      to: "/dashboards"
      via: "Link gated por isDono"
      pattern: "/dashboards"
---

<objective>
Construir a camada de UI dos 3 dashboards: a página Server Component `/dashboards` com guard DONO-only (notFound), os 3 componentes de visualização client (bar chart de colaboradores, line/area de evolução, tabela+bar de ranking de empresas), e habilitar o item de nav na sidebar.

Purpose: É onde os 3 requisitos (DASH-01/02/03) viram tela visível para o dono. O guard server-side é a barreira real de acesso (T-4-01); a sidebar gated é defesa em profundidade.
Output: page.tsx + 3 componentes de chart/table + sidebar modificada + teste RBAC preenchido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboards-comparativos/04-CONTEXT.md
@.planning/phases/04-dashboards-comparativos/04-RESEARCH.md
@.planning/phases/04-dashboards-comparativos/04-PATTERNS.md
@.planning/phases/04-dashboards-comparativos/04-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
Símbolos NOVOS criados/modificados por esta plan:
- `src/app/(app)/dashboards/page.tsx` — `DashboardsPage` (async Server Component, default export)
- `src/app/(app)/dashboards/desempenho-colaboradores-chart.tsx` — `DesempenhoColaboradoresChart` (use client)
- `src/app/(app)/dashboards/evolucao-mensal-chart.tsx` — `EvolucaoMensalChart` (use client)
- `src/app/(app)/dashboards/ranking-empresas-table.tsx` — `RankingEmpresasTable` (use client)
- `src/app/(app)/app-sidebar.tsx` MODIFICADO: bloco SidebarMenuItem "Dashboards" habilitado, gated por `isDono`
- Casos de teste em tests/dashboards.rbac.test.ts: guard DONO-only
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Página /dashboards (Server Component) com guard DONO-only + teste RBAC</name>
  <files>src/app/(app)/dashboards/page.tsx, tests/dashboards.rbac.test.ts</files>
  <read_first>
    - src/app/(app)/tarefas/page.tsx (analog exato: async Server Component, auth() primeiro, Promise.all de datasets, shell `<div className="flex flex-col gap-6 p-6">` + `<h1 className="text-xl font-semibold">`)
    - src/app/(app)/tarefas/actions.ts (linhas 287-294: padrão de guard role !== "DONO" logo após auth())
    - .planning/phases/04-dashboards-comparativos/04-PATTERNS.md (Shared Patterns: DONO-only guard usa `notFound()` na page, não redirect/erro)
    - .planning/phases/04-dashboards-comparativos/04-UI-SPEC.md (Copywriting Contract: headings exatos; Layout: 3 Cards empilhados, gap-8 entre seções)
    - .planning/phases/04-dashboards-comparativos/04-RESEARCH.md (Pattern 5: default 6 meses; Open Question 2: ranking default últimos 3 meses)
    - tests/dashboards.rbac.test.ts (scaffold da Plan 01)
  </read_first>
  <action>
    Criar `src/app/(app)/dashboards/page.tsx` como `async function DashboardsPage()` (default export). Sequência de guard: `const session = await auth();` → `if (!session?.user) redirect("/login");` → `if (session.user.role !== "DONO") notFound();` (import `notFound` de next/navigation — padrão anti-IDOR "não encontrado, never 403"). O guard DEVE vir ANTES de qualquer chamada às queries.
    Após o guard, `Promise.all([listarDesempenhoColaboradoresMesAtual(new Date()), listarEvolucaoMensal(6), listarRankingEmpresas(inicio3Meses, hoje)])` (default 6 meses para evolução, últimos 3 meses para ranking — computar ranges via date-fns subMonths). Validar o query param `?meses=` (se usado) com `mesesSchema` (Plan 01) antes de passar a listarEvolucaoMensal.
    Renderizar o shell: `<div className="flex flex-col gap-8 p-6">`, `<div>` com `<h1 className="text-xl font-semibold">Dashboards</h1>` e subheading `<p className="text-sm text-muted-foreground">Visão geral de desempenho da equipe e das empresas</p>`. Três `<Card>` empilhados (gap-8 / xl token), cada um com `<CardHeader><CardTitle>` usando os headings exatos de UI-SPEC: "Desempenho por colaborador", "Evolução mensal", "Empresas com mais atrasos". Cada Card renderiza o componente client correspondente passando os dados como props serializáveis. Empty state quando não há dados: heading "Ainda não há dados suficientes" + body do UI-SPEC.
    Preencher tests/dashboards.rbac.test.ts: testar o guard — COLABORADOR e não-autenticado rejeitados antes de qualquer query (mockar auth() e as funções de queries; asserir que as queries NÃO são chamadas quando role !== DONO / sem sessão). Se testar a page diretamente for inviável (Server Component), extrair a lógica de guard para uma função testável OU testar via mock de next/navigation `notFound`/`redirect` sendo invocados; asserir que `listarDesempenhoColaboradoresMesAtual` etc. não são chamadas no caminho não-DONO.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contém `notFound()` no caminho `role !== "DONO"` e `redirect("/login")` no caminho sem sessão
    - O guard precede qualquer chamada de query (sem query antes do check de role)
    - As 3 funções de query são chamadas via Promise.all
    - Os headings exatos de UI-SPEC aparecem ("Dashboards", "Desempenho por colaborador", "Evolução mensal", "Empresas com mais atrasos")
    - Teste RBAC: queries NÃO são invocadas para COLABORADOR/não-autenticado
    - `npx vitest run tests/dashboards.rbac.test.ts` verde
  </acceptance_criteria>
  <done>/dashboards é gated por DONO (notFound para COLABORADOR), busca os 3 datasets em paralelo e renderiza as 3 seções; RBAC testado.</done>
</task>

<task type="auto">
  <name>Task 2: Componentes client de visualização (bar colaboradores, line evolução, ranking empresas)</name>
  <files>src/app/(app)/dashboards/desempenho-colaboradores-chart.tsx, src/app/(app)/dashboards/evolucao-mensal-chart.tsx, src/app/(app)/dashboards/ranking-empresas-table.tsx</files>
  <read_first>
    - .planning/phases/04-dashboards-comparativos/04-RESEARCH.md (Pattern 4: bar chart completo; Pattern 5: trend line/area; Pattern 7: ranking bar top-N + tabela; Pitfall 4: ChartContainer precisa min-h explícito)
    - .planning/phases/04-dashboards-comparativos/04-UI-SPEC.md (Color: usar var(--chart-N) sem hsl(); min-h-[260px]; Badge variant="destructive" para alto % atraso; tooltip format exato; column headers "Empresa"/"% de atraso"/"Total de tarefas")
    - src/app/(app)/tarefas/tarefas-table.tsx (analog da metade-tabela: imports TanStack, ColumnDef, useReactTable, JSX <Table>; Badge convention)
    - src/components/ui/chart.tsx (gerado na Plan 01 — API ChartContainer/ChartTooltip/ChartTooltipContent/ChartConfig)
  </read_first>
  <action>
    Criar os 3 componentes, cada um com `"use client";` como primeira linha literal. NUNCA buscar dados dentro deles — recebem props serializáveis vindas da page (Plan 04 Task 1).
    `desempenho-colaboradores-chart.tsx` (`DesempenhoColaboradoresChart`): bar chart per RESEARCH.md Pattern 4 — `ChartContainer` com `className="min-h-[260px] w-full"`, `BarChart` com XAxis `dataKey="nome"`, YAxis `domain={[0,100]}` formatado `${v}%`, Bar `dataKey="percentualNoPrazo"` `fill="var(--color-percentualNoPrazo)"`, ChartConfig com `color: "var(--chart-1)"` (sem wrapper hsl). Tooltip no formato exato do UI-SPEC: "{percentual}% no prazo ({totalConcluidas} tarefas, {totalEmpresas} empresas)".
    `evolucao-mensal-chart.tsx` (`EvolucaoMensalChart`): AreaChart ou LineChart per RESEARCH.md Pattern 5 sobre `Array<{ competencia, percentual }>`, `min-h-[260px]`, série usando `var(--chart-1)`.
    `ranking-empresas-table.tsx` (`RankingEmpresasTable`): per RESEARCH.md Pattern 7 — um BarChart horizontal (layout="vertical") dos top ~10 maiores percentualAtraso, MAIS uma TanStack Table (reusar wiring de tarefas-table.tsx: useReactTable, getCoreRowModel, getSortedRowModel, getFilteredRowModel, getPaginationRowModel, sem pagination manual em ~100-110 linhas) listando todas as empresas ordenadas desc. Colunas: "Empresa" / "% de atraso" / "Total de tarefas". Read-only (sem checkbox/excluir/editar). Para linhas de alto % atraso usar `Badge variant="destructive"` (token padrão shadcn, não as classes bespoke de calcularAlertaPrazo).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 arquivos começam com `"use client";`
    - Cada ChartContainer tem `min-h-[260px]` (Pitfall 4)
    - Cores referenciam `var(--chart-N)` / `var(--color-...)` — nenhum `hsl(var(...))` nem hex hardcoded
    - ranking-empresas-table.tsx usa `useReactTable` e colunas "Empresa"/"% de atraso"/"Total de tarefas"
    - Badge de alto % atraso usa `variant="destructive"`
    - `npx tsc --noEmit` sem erros; `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Os 3 componentes de visualização renderizam a partir de props serializáveis, com sizing e tokens de cor corretos; o build passa.</done>
</task>

<task type="auto">
  <name>Task 3: Habilitar item de nav Dashboards na sidebar (DONO-only)</name>
  <files>src/app/(app)/app-sidebar.tsx</files>
  <read_first>
    - src/app/(app)/app-sidebar.tsx (estado atual: bloco "Tarefas" SidebarMenuItem; placeholder "Dashboards" disabled; `const isDono = user.role === "DONO"` já existe; import LayoutDashboard já presente)
    - .planning/phases/04-dashboards-comparativos/04-PATTERNS.md (seção app-sidebar.tsx: substituir o placeholder disabled pelo shape asChild/Link/isActive de "Tarefas", envolto em {isDono && (...)})
    - .planning/phases/04-dashboards-comparativos/04-UI-SPEC.md (Layout: item gated por isDono, hide total para COLABORADOR)
  </read_first>
  <action>
    Em src/app/(app)/app-sidebar.tsx, substituir o bloco `<SidebarMenuItem><SidebarMenuButton disabled>...Dashboards...</SidebarMenuButton></SidebarMenuItem>` pelo mesmo shape do item "Tarefas": `<SidebarMenuButton asChild isActive={pathname?.startsWith("/dashboards")}><Link href="/dashboards"><LayoutDashboard /><span>Dashboards</span></Link></SidebarMenuButton>`. Envolver o `SidebarMenuItem` inteiro em `{isDono && (...)}` para que COLABORADOR nunca veja o item (defesa em profundidade — o gate real é server-side na page). Não adicionar import novo (`LayoutDashboard` já está importado).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - O bloco "Dashboards" não tem mais `disabled`; usa `<Link href="/dashboards">` com `isActive={pathname?.startsWith("/dashboards")}`
    - O item está envolto em `{isDono && ...}`
    - Nenhum novo import adicionado (LayoutDashboard reutilizado)
    - `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>O item Dashboards aparece na sidebar só para DONO e navega para /dashboards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (usuário) → /dashboards (Server Component) | COLABORADOR pode tentar navegar direto à rota, ignorando a sidebar que esconde o link |
| ?meses= (query param) → date-fns/Prisma | Input ajustável pelo usuário no seletor de período do DASH-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Elevation of Privilege | acesso direto a /dashboards por COLABORADOR | mitigate | Guard server-side `role !== "DONO" → notFound()` como PRIMEIRO statement após auth(), antes de qualquer query (ASVS V4) — idêntico ao padrão de gerarTarefasDoMesAction (Fase 3). A sidebar gated por isDono é só defesa em profundidade, nunca a barreira real. Testado em tests/dashboards.rbac.test.ts. |
| T-04-PARAM | Tampering | `?meses=` injetado/malformado chegando a subMonths/Prisma | mitigate | `mesesSchema` (Plan 01, zod coerce number min1 max24) valida antes de qualquer uso (ASVS V5); valor fora do range é rejeitado/cai no default, nunca passa cru. |
| T-04-LEAK3 | Information Disclosure | dados agregados de colaborador renderizados a quem não é DONO | mitigate | Coberto por T-4-01 (página inteira só renderiza para DONO); componentes client recebem props apenas após o guard. |
</threat_model>

<verification>
- `npx vitest run tests/dashboards.rbac.test.ts` verde.
- `npx tsc --noEmit` sem erros; `npm run build` conclui.
- `npm run test` suite completa verde.
</verification>

<success_criteria>
- /dashboards gated por DONO (notFound), 3 seções renderizadas.
- 3 componentes de visualização com tokens/sizing corretos.
- Item de nav Dashboards visível só para DONO.
</success_criteria>

<output>
Create `.planning/phases/04-dashboards-comparativos/04-04-SUMMARY.md` when done
</output>

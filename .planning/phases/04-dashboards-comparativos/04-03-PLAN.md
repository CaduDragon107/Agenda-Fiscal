---
phase: 04-dashboards-comparativos
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/modules/dashboards/queries.ts
  - tests/dashboards.queries.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "O dashboard de desempenho por colaborador é calculado on-the-fly para o mês corrente (% no prazo + volume absoluto)"
    - "O dashboard de evolução mensal mistura snapshots congelados (meses fechados) com 1 ponto live (mês corrente), sem recalcular o passado"
    - "O ranking de empresas considera atrasada uma tarefa CONCLUIDA fora do prazo OU PENDENTE com prazo < now(), ordenado desc por % atraso"
  artifacts:
    - path: "src/modules/dashboards/queries.ts"
      provides: "listarDesempenhoColaboradoresMesAtual, listarEvolucaoMensal, listarRankingEmpresas"
      exports: ["listarDesempenhoColaboradoresMesAtual", "listarEvolucaoMensal", "listarRankingEmpresas"]
  key_links:
    - from: "src/modules/dashboards/queries.ts"
      to: "db.desempenhoMensal"
      via: "leitura de meses fechados (nunca recalcula via Tarefa)"
      pattern: "desempenhoMensal\\.(groupBy|findMany)"
    - from: "src/modules/dashboards/queries.ts"
      to: "competenciaAtual"
      via: "distinguir mês corrente (live) de meses fechados (snapshot)"
      pattern: "competenciaAtual"
---

<objective>
Implementar o módulo de leitura/agregação dos 3 dashboards: desempenho por colaborador no mês corrente (live), evolução mensal (snapshot congelado + ponto live), e ranking de empresas por % de atraso (live, regra D-06).

Purpose: É a camada de dados pura de leitura que alimenta as páginas/charts (Plan 04). Concentra a regra crítica D-05 (meses fechados leem só `desempenhoMensal`, nunca recalculam) e as duas definições distintas de "atrasada" (D-02 para colaborador exclui PENDENTE; D-06 para empresa inclui PENDENTE vencida).
Output: `src/modules/dashboards/queries.ts` (NEW) com 3 funções exportadas + casos de teste "colaboradores"/"volume"/"ranking" preenchidos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboards-comparativos/04-CONTEXT.md
@.planning/phases/04-dashboards-comparativos/04-RESEARCH.md
@.planning/phases/04-dashboards-comparativos/04-PATTERNS.md
@src/lib/competencia.ts
@src/lib/visibility-scope.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
Símbolos NOVOS criados por esta plan:
- `src/modules/dashboards/queries.ts` exportando:
  - `listarDesempenhoColaboradoresMesAtual(mes: Date)` → `{ porColaborador: Map<string,{total,noPrazo}>, carteiras }` (ou estrutura serializável equivalente — confirmar shape consumido pela Plan 04)
  - `listarEvolucaoMensal(quantidadeMeses?: number)` → `Array<{ competencia, percentual }>`
  - `listarRankingEmpresas(periodoInicio: Date, periodoFim: Date)` → `Array<{ empresaId, nome, percentualAtraso, totalTarefas }>` ordenado desc
- Casos de teste em tests/dashboards.queries.test.ts: "colaboradores", "volume", "ranking"
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listarDesempenhoColaboradoresMesAtual + listarEvolucaoMensal (DASH-01 live + DASH-02 read)</name>
  <files>src/modules/dashboards/queries.ts, tests/dashboards.queries.test.ts</files>
  <behavior>
    - Test "colaboradores": tarefas CONCLUIDA com concluidoEm <= prazo entram em noPrazo (D-01); PENDENTE são excluídas do total (D-02).
    - Test "volume": o retorno traz volume absoluto por colaborador (nº de empresas na carteira) ao lado do percentual (D-03) — não oculta o tamanho da carteira.
    - Test (evolução): meses fechados são lidos de db.desempenhoMensal (mock), NUNCA recalculados via db.tarefa para essas competências (D-05); o mês corrente é o único ponto calculado live.
  </behavior>
  <read_first>
    - src/modules/tarefas/queries.ts (analog exato: imports `db`/`SessionUser`, convenção função-por-query com JSDoc CRITICAL, explicit select TAREFA_SELECT — nunca `responsavel: true`)
    - src/lib/competencia.ts (competenciaAtual; format/subMonths para janela de meses)
    - .planning/phases/04-dashboards-comparativos/04-RESEARCH.md (Pattern 2: agregação live em Map; Pattern 5: mistura snapshot fechado + ponto live; Anti-pattern: não recalcular meses fechados, não N+1)
    - tests/dashboards.queries.test.ts (scaffold it.todo da Plan 01)
  </read_first>
  <action>
    Criar `src/modules/dashboards/queries.ts`. Implementar `listarDesempenhoColaboradoresMesAtual(mes: Date)` per RESEARCH.md Pattern 2: UM `db.tarefa.findMany` das CONCLUIDA no período (filtro por historico.concluidoEm entre startOfMonth/endOfMonth via date-fns) selecionando `responsavelId`, `prazo`, e `historico` (`select { concluidoEm }`, `orderBy desc`, `take 1`); agregação em memória num Map por responsavelId (D-01: concluidoEm <= prazo). Buscar carteira via `db.empresa.groupBy({ by: ["responsavelId"], where: { ativo: true }, _count: { id: true } })` (contexto D-03). Retornar estrutura serializável (converter Map em array de objetos para passar como props ao client component da Plan 04 — Maps não serializam em props de Server→Client; confirmar shape `{ colaboradorId, nome?, percentualNoPrazo, totalConcluidas, totalEmpresas }`).
    Implementar `listarEvolucaoMensal(quantidadeMeses = 6)` per RESEARCH.md Pattern 5: computar as competências fechadas (subMonths a partir de hoje, reverse); UM `db.desempenhoMensal.groupBy({ by: ["competencia"], where: { competencia: { in: fechadas } }, _sum: { totalConcluidas, concluidasNoPrazo } })` (escritório-wide, sem recalcular Tarefa para meses fechados — D-05); somar o ponto live do mês corrente reusando a agregação de listarDesempenhoColaboradoresMesAtual(new Date()). Retornar `Array<{ competencia, percentual }>` em ordem cronológica com o mês corrente por último.
    Comentário JSDoc CRITICAL em cada função: explicar a regra D-05 (competências fechadas leem só desempenhoMensal, nunca recalculam via Tarefa/TarefaHistorico) — no lugar do comentário IDOR do analog.
    Para Usuario/colaborador nome, usar `select: { id: true, nome: true }` explícito quando necessário (nunca relação crua).
    Preencher em tests/dashboards.queries.test.ts os casos "colaboradores", "volume" e o caso de evolução (mock db.desempenhoMensal.groupBy; asserir que db.tarefa.findMany NÃO é chamado para competências fechadas).
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - queries.ts exporta `listarDesempenhoColaboradoresMesAtual` e `listarEvolucaoMensal`
    - listarEvolucaoMensal lê meses fechados via `db.desempenhoMensal.groupBy` e NÃO chama `db.tarefa.findMany` para competências fechadas (asserido por mock)
    - O retorno de desempenho por colaborador inclui o volume de empresas (D-03)
    - Nenhuma seleção de relação Usuario sem `select` explícito
    - Teste "colaboradores" e "volume" verdes
  </acceptance_criteria>
  <done>As duas queries (colaborador live, evolução snapshot+live) respeitam D-01/D-02/D-03/D-05 e estão testadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: listarRankingEmpresas (DASH-03, regra D-06 distinta)</name>
  <files>src/modules/dashboards/queries.ts, tests/dashboards.queries.test.ts</files>
  <behavior>
    - Test "ranking": uma tarefa CONCLUIDA com concluidoEm > prazo conta como atrasada; uma PENDENTE com prazo < now() TAMBÉM conta como atrasada (D-06 — regra distinta de D-02, que exclui PENDENTE).
    - Test "ranking": uma PENDENTE com prazo > now() NÃO conta como atrasada.
    - Test "ranking": o resultado é ordenado decrescente por percentualAtraso.
  </behavior>
  <read_first>
    - src/modules/dashboards/queries.ts (estado após Task 1 — adicionar a 3a função no mesmo arquivo, mesma convenção)
    - .planning/phases/04-dashboards-comparativos/04-RESEARCH.md (Pattern 6: query de ranking com regra D-06; Anti-pattern: não unificar a regra de atrasada de D-02 e D-06)
    - .planning/phases/04-dashboards-comparativos/04-CONTEXT.md (D-06 + Claude's Discretion: PENDENTE atrasada a partir de prazo < now())
    - tests/dashboards.queries.test.ts (scaffold "ranking")
  </read_first>
  <action>
    Adicionar `listarRankingEmpresas(periodoInicio: Date, periodoFim: Date)` em queries.ts per RESEARCH.md Pattern 6: UM `db.tarefa.findMany` filtrando `prazo` no range (índice `@@index([prazo])` já existe), selecionando `empresaId`, `empresa: { select: { nome: true } }`, `status`, `prazo`, `historico` (`select { concluidoEm }`, desc, take 1). Agregar em Map por empresaId. Definir atrasada = `(status === "CONCLUIDA" && concluidoEm > prazo) || (status === "PENDENTE" && prazo < now())` — D-06; NÃO reutilizar nenhum helper compartilhado com a regra D-02 de colaborador (são deliberadamente diferentes — Anti-pattern de RESEARCH.md). Retornar `Array<{ empresaId, nome, percentualAtraso, totalTarefas }>` ordenado desc por percentualAtraso.
    Período padrão (Claude's Discretion / RESEARCH.md Open Question 2): a função recebe o range explícito; o consumidor (Plan 04) usará default "últimos 3 meses" — documentar isso no JSDoc, mas manter a função parametrizada (cheap to change).
    Preencher os casos "ranking" em tests/dashboards.queries.test.ts (CONCLUIDA fora do prazo, PENDENTE vencida, PENDENTE não vencida, ordenação desc).
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - queries.ts exporta `listarRankingEmpresas`
    - A regra de atrasada inclui `status === "PENDENTE" && prazo < now()` (D-06) E `CONCLUIDA && concluidoEm > prazo`
    - Não há helper `isAtrasada` compartilhado entre a regra de colaborador (D-02) e a de empresa (D-06)
    - O resultado é ordenado decrescente por percentualAtraso
    - Empresa.nome selecionado via `select: { nome: true }` (não relação crua)
    - Testes "ranking" verdes
    - `npm run test` suite completa verde
  </acceptance_criteria>
  <done>listarRankingEmpresas aplica a regra D-06 (distinta de D-02), ordena desc por % atraso, e está testada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component (Plan 04) → queries module | As queries só devem ser chamadas a partir de contexto já gated por DONO (Plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-LEAK2 | Information Disclosure | seleção de relação Usuario/Empresa vazando campos sensíveis (senhaHash) | mitigate | Todas as queries usam `select` escalar explícito para relações Usuario/Empresa; nunca `responsavel: true`/`colaborador: true`/`empresa: true` cru (convenção TAREFA_SELECT de Fase 2). |
| T-04-FREEZE-R | Tampering | recálculo de mês fechado na leitura de evolução | mitigate | listarEvolucaoMensal lê competências fechadas exclusivamente de `db.desempenhoMensal`; teste asseta que `db.tarefa.findMany` NÃO é chamado para meses fechados (D-05). |
| T-04-AC | Elevation of Privilege | query chamada fora do gate DONO | accept (defesa real em Plan 04) | Gate DONO é server-side na page (Plan 04, T-4-01); este módulo é defesa-em-profundidade documentada, não a barreira primária — consistente com a arquitetura de Server Component gated do projeto. |
</threat_model>

<verification>
- `npx vitest run tests/dashboards.queries.test.ts` verde (colaboradores, volume, ranking).
- `npm run test` suite completa verde.
</verification>

<success_criteria>
- 3 funções de leitura exportadas, respeitando D-01/D-02/D-03/D-05 (colaborador/evolução) e D-06 (ranking).
- Meses fechados nunca recalculados na leitura.
</success_criteria>

<output>
Create `.planning/phases/04-dashboards-comparativos/04-03-SUMMARY.md` when done
</output>

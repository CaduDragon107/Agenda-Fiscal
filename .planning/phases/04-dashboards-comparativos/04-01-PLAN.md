---
phase: 04-dashboards-comparativos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/components/ui/chart.tsx
  - src/modules/dashboards/schema.ts
  - tests/dashboards.queries.test.ts
  - tests/dashboards.snapshot.test.ts
  - tests/dashboards.rbac.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "O banco tem a tabela desempenho_mensal com unique (competencia, colaboradorId)"
    - "src/components/ui/chart.tsx existe (gerado pelo shadcn CLI) e recharts está em package.json"
    - "Os 3 arquivos de teste existem com os casos esperados (it.todo ou red), prontos para as Waves 2/3 preencherem"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "model DesempenhoMensal"
      contains: "model DesempenhoMensal"
    - path: "src/components/ui/chart.tsx"
      provides: "ChartContainer/ChartConfig/ChartTooltip wrappers (shadcn chart)"
    - path: "src/modules/dashboards/schema.ts"
      provides: "mesesSchema (zod) para validar ?meses="
      exports: ["mesesSchema"]
    - path: "tests/dashboards.queries.test.ts"
      provides: "Scaffold DASH-01/DASH-03 calc rules"
    - path: "tests/dashboards.snapshot.test.ts"
      provides: "Scaffold DASH-02 freeze/idempotency/boundary"
    - path: "tests/dashboards.rbac.test.ts"
      provides: "Scaffold DONO-only guard"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Usuario"
      via: "DesempenhoMensal.colaborador relation"
      pattern: "colaborador\\s+Usuario"
---

<objective>
Estabelecer a fundação compartilhada da Fase 4: a tabela de snapshot `DesempenhoMensal`, o componente de gráfico shadcn (`chart`), o schema de validação de query param e os arquivos de teste (Wave 0) que as plans seguintes preencherão.

Purpose: Toda a fase depende destes artefatos — nenhuma query, gráfico ou snapshot pode ser escrito antes da tabela existir no banco, do `chart.tsx` ser copiado pelo CLI e dos arquivos de teste existirem para receber os casos vermelhos.
Output: `model DesempenhoMensal` + db push aplicado, `src/components/ui/chart.tsx`, `src/modules/dashboards/schema.ts`, e 3 arquivos de teste scaffold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboards-comparativos/04-CONTEXT.md
@.planning/phases/04-dashboards-comparativos/04-RESEARCH.md
@.planning/phases/04-dashboards-comparativos/04-PATTERNS.md
@.planning/phases/04-dashboards-comparativos/04-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
Símbolos NOVOS criados por esta plan (excluir de verificação de drift de símbolos):
- `model DesempenhoMensal` (Prisma) com campos: `id`, `competencia`, `colaboradorId`, `colaborador`, `totalConcluidas`, `concluidasNoPrazo`, `totalEmpresas`, `totalTarefasPeriodo`, `createdAt`; constraints `@@unique([competencia, colaboradorId])`, `@@index([competencia])`, `@@index([colaboradorId])`, `@@map("desempenho_mensal")`
- Nova relação reversa em `model Usuario`: `desempenhoMensal DesempenhoMensal[]`
- Tabela de banco `desempenho_mensal`
- `src/components/ui/chart.tsx` (gerado pelo shadcn CLI): `ChartContainer`, `ChartConfig`, `ChartTooltip`, `ChartTooltipContent`, `ChartLegend`, `ChartLegendContent`
- Dependência `recharts` (transitiva via shadcn CLI) em `package.json`
- `src/modules/dashboards/schema.ts` exportando `mesesSchema`
- Arquivos de teste: `tests/dashboards.queries.test.ts`, `tests/dashboards.snapshot.test.ts`, `tests/dashboards.rbac.test.ts`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar model DesempenhoMensal e aplicar db push</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (estado atual: convenções de `model Tarefa` linhas 88-113 e `model TarefaHistorico` linhas 115-125 — copiar cuid(), @@map snake_case, @@index, @@unique; e `model Usuario` linhas 38-53 onde adicionar a relação reversa)
    - .planning/phases/04-dashboards-comparativos/04-RESEARCH.md (Pattern 1: schema exato de DesempenhoMensal)
    - .planning/phases/04-dashboards-comparativos/04-PATTERNS.md (seção "prisma/schema.prisma — model DesempenhoMensal")
  </read_first>
  <action>
    Adicionar ao final de prisma/schema.prisma o `model DesempenhoMensal` com: `id String @id @default(cuid())`; `competencia String` (formato canônico "YYYY-MM" de src/lib/competencia.ts); `colaboradorId String`; `colaborador Usuario @relation(fields: [colaboradorId], references: [id])`; quatro contadores inteiros `totalConcluidas Int`, `concluidasNoPrazo Int`, `totalEmpresas Int`, `totalTarefasPeriodo Int`; `createdAt DateTime @default(now())`. Constraints: `@@unique([competencia, colaboradorId])`, `@@index([competencia])`, `@@index([colaboradorId])`, `@@map("desempenho_mensal")`.
    Adicionar a relação reversa correspondente em `model Usuario`: `desempenhoMensal DesempenhoMensal[]` (necessária porque a relação aponta para Usuario; Prisma exige o campo reverso).
    Aplicar a migração com `npx prisma db push` (ambiente Neon sem shadow database — mesmo método de Fase 02/03 registrado em STATE.md, NÃO usar `prisma migrate dev`). Em seguida `npx prisma generate` para atualizar o client tipado.
    Não definir valores percentuais pré-computados na tabela — os contadores brutos (numerador/denominador) ficam separados para a leitura decidir arredondamento (D-01/D-02/D-03). Não criar linhas por empresa nesta tabela (DASH-03 é live-only no v1 — A1 de RESEARCH.md).
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' prisma/schema.prisma | grep -c "model DesempenhoMensal"` retorna 1
    - schema contém `@@unique([competencia, colaboradorId])` e `@@map("desempenho_mensal")`
    - `model Usuario` contém `desempenhoMensal DesempenhoMensal[]`
    - `npx prisma validate` sai com código 0
    - `npx prisma db push` aplica sem erro; o client gerado expõe `db.desempenhoMensal`
  </acceptance_criteria>
  <done>Tabela desempenho_mensal existe no banco Neon com unique (competencia, colaboradorId); o Prisma Client tipado expõe `db.desempenhoMensal`.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar componente shadcn chart e criar schema de validação de meses</name>
  <files>src/components/ui/chart.tsx, src/modules/dashboards/schema.ts</files>
  <read_first>
    - src/lib/competencia.ts (analog exato para schema.ts — `competenciaSchema` é um `z.string().regex(...)` exportado de um módulo dedicado, com comentário de cabeçalho explicando o porquê)
    - CLAUDE.md (regra: instalar Recharts SOMENTE via `npx shadcn add chart`, nunca `npm install recharts`)
    - .planning/phases/04-dashboards-comparativos/04-RESEARCH.md (seção Installation; State of the Art — tokens são oklch, usar `var(--chart-N)` sem wrapper hsl)
  </read_first>
  <action>
    Rodar `npx shadcn@latest add chart` — isto copia `src/components/ui/chart.tsx` e adiciona `recharts` (3.8.1) como dependência transitiva em package.json. NÃO rodar `npm install recharts` manualmente (mandato CLAUDE.md e auditoria de legitimidade já feita em RESEARCH.md — recharts verdict OK).
    Criar `src/modules/dashboards/schema.ts` exportando `mesesSchema`: validar o parâmetro de janela do DASH-02 (`?meses=`). Usar `z.coerce.number().int().min(1).max(24)` com mensagem de erro clara, default tratado no consumidor (não no schema). Abrir o arquivo com um comentário de cabeçalho no mesmo estilo de src/lib/competencia.ts explicando que valida o query param antes de chegar em `subMonths`/Prisma (ASVS V5, Pitfall de tampering em RESEARCH.md Security Domain). Não inlinear regex de meses em nenhum outro lugar — sempre importar `mesesSchema`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/chart.tsx` existe e exporta `ChartContainer` e `ChartTooltipContent`
    - `recharts` aparece em package.json dependencies
    - `src/modules/dashboards/schema.ts` exporta `mesesSchema` com `min(1).max(24)`
    - `npx tsc --noEmit` sai com código 0 (sem erros de tipo)
  </acceptance_criteria>
  <done>O componente chart shadcn está disponível em src/components/ui/chart.tsx e `mesesSchema` valida o query param de janela do DASH-02.</done>
</task>

<task type="auto">
  <name>Task 3: Criar scaffolds de teste (Wave 0) para as 3 suites da fase</name>
  <files>tests/dashboards.queries.test.ts, tests/dashboards.snapshot.test.ts, tests/dashboards.rbac.test.ts</files>
  <read_first>
    - tests/geracao.idempotencia.test.ts (analog exato: convenção de mock `vi.mock("@/lib/db")` e estrutura de teste de idempotência via createMany skipDuplicates)
    - .planning/phases/04-dashboards-comparativos/04-VALIDATION.md (Per-Task Verification Map — nomes exatos dos casos: "colaboradores", "volume", "frozen", "idempot", "boundary", "ranking")
    - .planning/phases/04-dashboards-comparativos/04-RESEARCH.md (Phase Requirements → Test Map; Patterns 2/3/6 para o comportamento esperado)
  </read_first>
  <action>
    Criar os 3 arquivos de teste como scaffolds Wave 0 usando `it.todo(...)` sem callback (evita importar módulos ainda inexistentes — convenção de Fase 02-01 registrada em STATE.md). Cada `it.todo` deve carregar exatamente o substring que os comandos `-t` de VALIDATION.md filtram:
    - tests/dashboards.queries.test.ts: casos cujos títulos contenham "colaboradores" (D-01 concluidoEm<=prazo; D-02 exclui PENDENTE do denominador), "volume" (D-03 contexto absoluto retornado junto), "ranking" (D-06 atrasada inclui PENDENTE com prazo<now() E ordenação desc por % atraso).
    - tests/dashboards.snapshot.test.ts: casos contendo "frozen" (D-05 — meses fechados leem só desempenhoMensal, db.tarefa.findMany NÃO chamado), "idempot" (createMany skipDuplicates contra @@unique), "boundary" (snapshot fecha o mês ANTERIOR à competência passada — Pitfall 1).
    - tests/dashboards.rbac.test.ts: caso contendo "DONO" / não-autorizado (COLABORADOR e não autenticado rejeitados antes de qualquer query).
    Cada `it.todo` deve ter um título descritivo em português curto. NÃO escrever lógica de teste real nesta task — só os títulos/scaffold; as Waves 2/3 convertem cada `it.todo` em teste vermelho→verde.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.queries.test.ts tests/dashboards.snapshot.test.ts tests/dashboards.rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 arquivos existem em tests/
    - `npx vitest run tests/dashboards.*.test.ts` passa (todos os casos são it.todo, contam como pendentes, não falham)
    - `grep -c "it.todo" tests/dashboards.queries.test.ts` retorna >= 3 (colaboradores, volume, ranking)
    - tests/dashboards.snapshot.test.ts contém os substrings "frozen", "idempot" e "boundary"
    - tests/dashboards.rbac.test.ts contém um caso com "DONO"
  </acceptance_criteria>
  <done>Os 3 arquivos de teste existem com os casos it.todo nomeados conforme o mapa de VALIDATION.md, prontos para as plans seguintes preencherem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/CLI → repo | `npx shadcn add chart` baixa e copia código-fonte + adiciona dependência npm (recharts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm install transitivo (recharts via shadcn CLI) | mitigate | Pacote `recharts` já auditado em 04-RESEARCH.md (Package Legitimacy Audit, verdict OK, ~10 anos, 53.3M/wk, repo oficial, sem postinstall). Único pacote novo da fase. Nenhum pacote [ASSUMED]/[SUS]/[SLOP] — checkpoint de legitimidade não requerido. |
| T-04-DB | Tampering | model DesempenhoMensal | accept | Tabela só recebe escrita via cron autenticado-pelo-processo (Plan 02), idempotente via @@unique; sem entrada de usuário direta nesta plan. |
</threat_model>

<verification>
- `npx prisma validate` verde; `db.desempenhoMensal` disponível no client.
- `npx tsc --noEmit` sem erros.
- `npx vitest run tests/dashboards.*.test.ts` verde (todos it.todo).
</verification>

<success_criteria>
- Tabela `desempenho_mensal` criada no banco com unique (competencia, colaboradorId).
- `src/components/ui/chart.tsx` presente; `recharts` em package.json.
- `mesesSchema` exportado de src/modules/dashboards/schema.ts.
- 3 arquivos de teste scaffold existentes e verdes.
</success_criteria>

<output>
Create `.planning/phases/04-dashboards-comparativos/04-01-SUMMARY.md` when done
</output>

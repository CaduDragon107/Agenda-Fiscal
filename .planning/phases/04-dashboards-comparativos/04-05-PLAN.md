---
phase: 04-dashboards-comparativos
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "Os 3 gráficos renderizam visualmente corretos para o DONO (eixos, tooltips, formato dos dados)"
    - "Um COLABORADOR é bloqueado (404) ao tentar acessar /dashboards diretamente"
  artifacts: []
  key_links: []
---

<objective>
Checkpoint de verificação humana: confirmar que os 3 dashboards renderizam corretamente para o DONO e que o gate de acesso bloqueia COLABORADOR — o que os testes Vitest não conseguem asserir (rendering DOM/canvas dos gráficos via Recharts).

Purpose: Pitfall 4 (ChartContainer sem altura renderiza 0px sem erro) e a correção visual de eixos/tooltips/dados só são verificáveis por olhos humanos, consistente com os checkpoints visuais de Fase 1 (wizard de importação) e Fase 3 (botão de geração manual).
Output: Nenhum arquivo — confirmação humana de que a fase está visualmente correta antes de /gsd-verify-work.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboards-comparativos/04-UI-SPEC.md
@.planning/phases/04-dashboards-comparativos/04-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verificação visual dos 3 dashboards e do gate de acesso</name>
  <files>none (checkpoint de verificação humana, sem edição de código)</files>
  <action>
    Checkpoint humano (sem edição de código): executar a aplicação e validar manualmente o rendering dos 3 gráficos e o gate de acesso DONO-only, conforme os passos em how-to-verify. Pausar para o usuário confirmar "aprovado" ou reportar problemas antes de /gsd-verify-work.
  </action>
  <what-built>
    A página /dashboards (DONO-only) com 3 seções: bar chart de desempenho por colaborador (DASH-01), line/area de evolução mensal (DASH-02), e ranking de empresas (bar top-N + tabela) por % de atraso (DASH-03). Mais o item de nav Dashboards na sidebar (visível só para DONO) e o guard server-side notFound() para COLABORADOR.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e abrir a aplicação no navegador.
    2. Logar como DONO. Confirmar que o item "Dashboards" aparece na sidebar e navega para /dashboards.
    3. Na página /dashboards, confirmar visualmente:
       - Seção "Desempenho por colaborador": bar chart renderiza (NÃO uma caixa vazia de 0px — Pitfall 4); eixo Y em 0-100% ; passar o mouse mostra tooltip no formato "{percentual}% no prazo ({totalConcluidas} tarefas, {totalEmpresas} empresas)".
       - Seção "Evolução mensal": line/area renderiza com os meses no eixo X; o mês corrente aparece como último ponto.
       - Seção "Empresas com mais atrasos": bar dos piores ofensores + tabela ordenada desc por "% de atraso" com colunas "Empresa"/"% de atraso"/"Total de tarefas"; linhas de alto atraso com badge destructive (vermelho).
       - Alternar dark/light mode: os gráficos continuam visíveis em ambos (tokens var(--chart-N) têm override .dark).
       - Se o banco ainda não tem dados/snapshots: confirmar o empty state "Ainda não há dados suficientes".
    4. Deslogar e logar como COLABORADOR (ex.: Caio/Jessica/Heitor/Felipe). Confirmar:
       - O item "Dashboards" NÃO aparece na sidebar.
       - Navegar manualmente para /dashboards na barra de endereço retorna página 404 (notFound), não os dados.
  </how-to-verify>
  <verify>Confirmação humana "aprovado" após os 4 passos de how-to-verify.</verify>
  <done>Os 3 dashboards renderizam corretos para DONO; COLABORADOR recebe 404 em /dashboards e não vê o item na sidebar.</done>
  <resume-signal>Digite "aprovado" se tudo renderiza correto e o gate bloqueia COLABORADOR, ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /dashboards | Verificação manual de que o gate DONO-only bloqueia COLABORADOR na prática |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01-V | Elevation of Privilege | gate /dashboards verificado em runtime | mitigate | Passo 4 do checkpoint confirma empiricamente que COLABORADOR recebe 404 ao navegar direto — validação humana da mitigação T-4-01 implementada na Plan 04. |
</threat_model>

<verification>
- Confirmação humana ("aprovado") de que os 3 gráficos renderizam e o gate bloqueia COLABORADOR.
</verification>

<success_criteria>
- Os 3 dashboards renderizam visualmente corretos para DONO.
- COLABORADOR recebe 404 em /dashboards e não vê o item na sidebar.
</success_criteria>

<output>
Create `.planning/phases/04-dashboards-comparativos/04-05-SUMMARY.md` when done
</output>

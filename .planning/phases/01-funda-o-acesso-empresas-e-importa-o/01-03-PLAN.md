---
phase: 01-funda-o-acesso-empresas-e-importa-o
plan: 03
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/visibility-scope.ts
  - src/lib/cnpj.ts
  - src/modules/empresas/schema.ts
  - src/modules/empresas/queries.ts
  - tests/visibility-scope.test.ts
  - tests/cnpj.test.ts
autonomous: true
requirements: [AUTH-02, EMPR-01]

must_haves:
  truths:
    - "withVisibilityScope({role:'dono'}) retorna {} (sem filtro); withVisibilityScope({role:'colaborador', id:X}) retorna { responsavelId: X }"
    - "validarCNPJ aceita CNPJs com dígitos verificadores corretos (módulo 11) e rejeita formato errado, tamanho errado, todos-iguais e dígitos inválidos"
    - "listarEmpresas e buscarEmpresaPorId aplicam withVisibilityScope no where — nunca consultam db.empresa diretamente sem escopo"
    - "empresaSchema valida nome, cnpj (via validarCNPJ), regimeTributario (3 valores), responsavelId obrigatórios"
  artifacts:
    - path: "src/lib/visibility-scope.ts"
      provides: "withVisibilityScope() — núcleo do AUTH-02"
      contains: "withVisibilityScope"
    - path: "src/lib/cnpj.ts"
      provides: "validarCNPJ() módulo 11"
      contains: "validarCNPJ"
    - path: "src/modules/empresas/schema.ts"
      provides: "empresaSchema + linhaImportadaSchema (Zod)"
      contains: "empresaSchema"
    - path: "src/modules/empresas/queries.ts"
      provides: "listarEmpresas, buscarEmpresaPorId com escopo aplicado"
      contains: "withVisibilityScope"
  key_links:
    - from: "src/modules/empresas/queries.ts"
      to: "src/lib/visibility-scope.ts"
      via: "spread no where"
      pattern: "withVisibilityScope"
    - from: "src/modules/empresas/schema.ts"
      to: "src/lib/cnpj.ts"
      via: "refine(validarCNPJ)"
      pattern: "validarCNPJ"
---

<objective>
Construir a camada de dados central e testável da fase: a função withVisibilityScope() (núcleo de segurança do AUTH-02), a validação de CNPJ módulo 11, o schema Zod de empresa, e as funções de query escopadas que TODA leitura de empresas deve usar. Plano TDD (RED→GREEN) — os stubs do Plano 01 tornam-se verdes aqui.

Purpose: Centralizar a regra de visibilidade e a validação numa única fonte testada elimina a classe de bug mais perigosa da fase (IDOR / vazamento de carteira). A UI e as Server Actions (Planos 04/05) consomem estas funções sem reimplementar lógica.
Output: 4 módulos lib/modules + 2 suítes de teste verdes (visibility-scope, cnpj).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-PATTERNS.md
@.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-RESEARCH.md
@.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: withVisibilityScope() + validarCNPJ() (RED→GREEN)</name>
  <read_first>
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-PATTERNS.md (skeletons "lib/visibility-scope.ts" linhas 261-283 e "lib/cnpj.ts" linhas 287-315 — algoritmo módulo 11 completo)
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-RESEARCH.md (Pattern 2 linhas 287-320 — contrato dono vs colaborador + nota IDOR; Pattern 4 lib/cnpj linhas 515-538)
    - tests/visibility-scope.test.ts, tests/cnpj.test.ts (stubs RED do Plano 01)
  </read_first>
  <behavior>
    - withVisibilityScope({ id: 'u1', role: 'dono' }) === {} (objeto vazio, sem restrição)
    - withVisibilityScope({ id: 'u1', role: 'colaborador' }) === { responsavelId: 'u1' }
    - validarCNPJ aceita um CNPJ real válido conhecido (ex.: "11.222.333/0001-81") = true
    - validarCNPJ rejeita: string com !=14 dígitos, todos os dígitos iguais ("00.000.000/0000-00"), e CNPJ com dígito verificador trocado = false
  </behavior>
  <action>
    Escrever src/lib/visibility-scope.ts conforme skeleton: tipo SessionUser = { id: string; role: "colaborador" | "dono" } (exportado para reuso em queries.ts e nas Server Actions), e a função withVisibilityScope(user): Prisma.EmpresaWhereInput que retorna {} para dono e { responsavelId: user.id } para colaborador. NOTA: o enum Prisma usa COLABORADOR/DONO em maiúsculas, mas o role na sessão pode chegar em qualquer casing dependendo do callback — padronizar o tipo e a comparação de forma consistente com o que auth.ts (Plano 02) coloca no token (alinhar ao valor do enum; documentar a normalização escolhida). Escrever src/lib/cnpj.ts com validarCNPJ(cnpj) implementando o módulo 11 completo (remove não-dígitos, rejeita !=14 e todos-iguais, calcula d1 com pesos [5,4,3,2,9,8,7,6,5,4,3,2] e d2 com [6,5,4,3,2,9,8,7,6,5,4,3,2]). NUNCA substituir por regex de formato-apenas (a planilha real tem CNPJs com possíveis erros de digitação que só o módulo 11 pega). Tornar tests/visibility-scope.test.ts e tests/cnpj.test.ts verdes cobrindo todos os comportamentos do bloco behavior; adicionar pelo menos 2 CNPJs válidos distintos e 3 inválidos.
  </action>
  <verify>
    <automated>npx vitest run tests/visibility-scope.test.ts tests/cnpj.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/visibility-scope.test.ts passa: dono→{}, colaborador→{responsavelId}
    - tests/cnpj.test.ts passa: ≥2 válidos true, ≥3 inválidos false (tamanho, todos-iguais, dígito trocado)
    - src/lib/cnpj.ts NÃO contém regex que valide apenas o formato sem checar dígitos (sem early-return true baseado só em /\d{2}\.\d{3}.../)
    - src/lib/visibility-scope.ts exporta o tipo SessionUser e a função withVisibilityScope retornando Prisma.EmpresaWhereInput
  </acceptance_criteria>
  <done>Escopo de visibilidade e validação de CNPJ implementados e testados em GREEN</done>
</task>

<task type="tdd">
  <name>Task 2: empresaSchema (Zod) + queries escopadas de empresas</name>
  <read_first>
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-PATTERNS.md (skeletons "modules/empresas/schema.ts" linhas 398-426 e "modules/empresas/queries.ts" linhas 430-453 — incluindo o aviso CRITICAL de aplicar escopo)
    - src/lib/visibility-scope.ts e src/lib/cnpj.ts (criados no Task 1 — consumidos aqui)
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-UI-SPEC.md (Screen 4 campos de empresa linhas 183-189 — regime tem 3 opções, responsável obrigatório)
  </read_first>
  <behavior>
    - empresaSchema.parse de payload válido (regime LUCRO_PRESUMIDO, CNPJ válido, responsavelId presente) sucede
    - empresaSchema rejeita CNPJ com dígito inválido (mensagem "CNPJ inválido") e responsavelId vazio
    - listarEmpresas(user) chama db.empresa.findMany com where contendo o resultado de withVisibilityScope(user)
    - buscarEmpresaPorId(user, id) chama db.empresa.findFirst com { id, ...withVisibilityScope(user) }
  </behavior>
  <action>
    Escrever src/modules/empresas/schema.ts com empresaSchema (Zod): nome string min 1, cnpj string refine(validarCNPJ) "CNPJ inválido", regimeTributario z.enum(["LUCRO_REAL","LUCRO_PRESUMIDO","SIMPLES_NACIONAL"]) (OS 3 VALORES — não 2), responsavelId string min 1 "Responsável é obrigatório", contatos/particularidades opcionais; exportar EmpresaInput = z.infer. Também exportar linhaImportadaSchema (regimeTributario e responsavelId opcionais até a revisão da importação — consumido pelo Plano 05). Escrever src/modules/empresas/queries.ts com listarEmpresas(user, filtros?) e buscarEmpresaPorId(user, id) que SEMPRE espalham withVisibilityScope(user) no where (Pitfall 1 — nenhuma chamada direta a db.empresa.findMany sem escopo); listar com orderBy nome asc; select explícito dos campos necessários (sem trazer relação de Usuario com senhaHash). Cobrir o comportamento com testes (db mockado via tests/setup.ts) verificando que o where inclui o escopo — adicionar asserções ao conjunto de testes (pode reutilizar tests/empresas.crud.test.ts parcialmente ou um novo it focado em escopo de query; o teste de IDOR via Server Action fica no Plano 04).
  </action>
  <verify>
    <automated>npx vitest run tests/visibility-scope.test.ts tests/cnpj.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/modules/empresas/schema.ts: empresaSchema com z.enum de 3 valores e cnpj refine(validarCNPJ); exporta linhaImportadaSchema com regimeTributario/responsavelId optional
    - src/modules/empresas/queries.ts: listarEmpresas e buscarEmpresaPorId espalham withVisibilityScope no where; nenhuma query de empresa sem escopo
    - Build passa (tipos Prisma.EmpresaWhereInput compatíveis)
    - Suíte de scope/cnpj continua verde
  </acceptance_criteria>
  <done>Schema Zod de empresa (3 regimes) e funções de query escopadas implementados; queries nunca ignoram o escopo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action/Component → camada de dados (Prisma) | toda leitura de empresa deve passar por queries escopadas |
| input de formulário/importação → validação | CNPJ e regime validados antes de persistir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-IDOR-SCOPE | Information Disclosure / Elevation of Privilege | queries de empresa | mitigate | withVisibilityScope() central + queries.ts que SEMPRE o espalham no where; nunca db.empresa.findMany direto (a verificação de mutação/IDOR via findFirst é aplicada no Plano 04) |
| T-01-INPUT-CNPJ | Tampering | validação de CNPJ | mitigate | validarCNPJ módulo 11 (não regex de formato); empresaSchema refina cnpj antes de persistir |
| T-01-INPUT-REGIME | Tampering | validação de regime | mitigate | z.enum restringe regimeTributario aos 3 valores válidos; payloads fora do enum são rejeitados |
</threat_model>

<verification>
- tests/visibility-scope.test.ts verde (dono/colaborador)
- tests/cnpj.test.ts verde (válidos/inválidos módulo 11)
- npm run build verde (schema + queries tipados)
</verification>

<success_criteria>
- withVisibilityScope() é a única fonte da regra de visibilidade, testada
- validarCNPJ() módulo 11 testado com casos válidos e inválidos
- empresaSchema com 3 regimes; queries de empresa sempre escopadas
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plano 03)

- Files: src/lib/visibility-scope.ts, src/lib/cnpj.ts, src/modules/empresas/schema.ts, src/modules/empresas/queries.ts
- Functions: withVisibilityScope, validarCNPJ, listarEmpresas, buscarEmpresaPorId
- Types/exports: SessionUser, empresaSchema, EmpresaInput, linhaImportadaSchema
</artifacts_produced>

<output>
Create `.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-03-SUMMARY.md` when done
</output>

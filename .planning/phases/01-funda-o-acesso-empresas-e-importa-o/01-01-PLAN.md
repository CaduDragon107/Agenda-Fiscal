---
phase: 01-funda-o-acesso-empresas-e-importa-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - components.json
  - src/app/globals.css
  - prisma/schema.prisma
  - prisma/seed.ts
  - .env
  - .env.example
  - .gitignore
  - vitest.config.ts
  - tests/setup.ts
  - tests/cnpj.test.ts
  - tests/visibility-scope.test.ts
  - tests/auth.test.ts
  - tests/empresas.crud.test.ts
  - tests/empresas.idor.test.ts
  - tests/import.test.ts
  - tests/import.confirm.test.ts
  - tests/import.upload.test.ts
autonomous: false
requirements: [INFRA-01, AUTH-01, EMPR-01, EMPR-02]
user_setup:
  - service: railway
    why: "PostgreSQL gerenciado + DATABASE_URL para dev e produção (INFRA-01)"
    env_vars:
      - name: DATABASE_URL
        source: "Railway -> projeto -> serviço PostgreSQL -> Connect -> Postgres Connection URL (public)"
      - name: AUTH_SECRET
        source: "Gerar localmente: npx auth secret (ou openssl rand -base64 32)"

must_haves:
  truths:
    - "Projeto Next.js 15.5 (App Router, src/, TypeScript strict) inicializado e compila com `npm run build`"
    - "shadcn/ui inicializado (New York / Neutral / lucide-react) com os componentes da Fase 1 instalados"
    - "Schema Prisma com enums Role e RegimeTributario (3 valores) e modelos Usuario/Empresa/EmpresaRegimeHistorico aplicado ao Postgres via prisma db push"
    - "Seed cria 5 usuários (1 DONO + 4 COLABORADOR) com senha hasheada via bcryptjs"
    - "Vitest roda (npx vitest run) e todos os arquivos de teste-stub existem e falham com mensagem TODO (RED), não com erro de import"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Modelos Usuario, Empresa, EmpresaRegimeHistorico + enums Role, RegimeTributario"
      contains: "model Empresa"
    - path: "prisma/seed.ts"
      provides: "Seed dos 5 usuários com bcrypt"
      contains: "bcrypt.hash"
    - path: "vitest.config.ts"
      provides: "Config Vitest"
    - path: "components.json"
      provides: "Config shadcn/ui"
  key_links:
    - from: "prisma/seed.ts"
      to: "@prisma/client"
      via: "PrismaClient + enum Role"
      pattern: "Role\\.(DONO|COLABORADOR)"
    - from: "package.json"
      to: "vitest"
      via: "script test"
      pattern: "\"test\""
---

<objective>
Estabelecer a fundação do projeto greenfield: inicializar o app Next.js 15.5 + shadcn/ui, instalar e auditar todas as dependências da fase (gate de legitimidade de pacotes), definir o schema Prisma completo (3 regimes desde o v1), aplicá-lo ao Postgres do Railway, seedar os 5 usuários, e montar a infraestrutura de testes Vitest com todos os stubs RED que as fases seguintes vão tornar verdes.

Purpose: Nada nesta fase compila ou testa sem esta base. Este plano é a Wave 1 — todos os outros planos dependem dele.
Output: Projeto buildável, banco com schema aplicado + 5 usuários, suíte Vitest executável com stubs falhando intencionalmente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-RESEARCH.md
@.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-PATTERNS.md
@.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Gate de legitimidade de pacotes + provisionar Postgres (Railway)</name>
  <read_first>
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-RESEARCH.md (seção "Package Legitimacy Audit", linhas 104-123, e "Installation", linhas 65-102)
    - CLAUDE.md (seção Technology Stack — versões recomendadas e o aviso "What NOT to Use" sobre npm install xlsx)
  </read_first>
  <action>
    A pesquisa não pôde rodar npm view (sem shell), então TODOS os pacotes estão [ASSUMED] no Package Legitimacy Audit. Antes de qualquer npm install em massa, o executor DEVE rodar `npm view <pkg> version` e `npm view <pkg> dist.tarball` para cada pacote: next-auth@beta, bcryptjs, @types/bcryptjs, prisma, @prisma/client, @tanstack/react-table, zod, react-hook-form, @hookform/resolvers. Registrar as versões reais resolvidas (next-auth deve resolver para 5.0.0-beta.x; bcryptjs verificar 2.x vs 3.x e decidir — CLAUDE.md cita ^2.4 mas 3.x é estável; usar a resolvida e anotar). NUNCA instalar xlsx do registry npm — usar SOMENTE o tarball CDN https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz (o npm xlsx resolve para 0.18.5 vulnerável). Verificar humanamente next-auth e bcryptjs em npmjs.com/package/<pkg> (idade, downloads, repo github oficial). Este é um checkpoint blocking-human porque todos os pacotes são [ASSUMED] — não auto-aprovável. Em paralelo, o usuário deve criar um projeto Railway com serviço PostgreSQL e fornecer a DATABASE_URL (Public Connection URL) e gerar AUTH_SECRET via `npx auth secret`, para uso no Task 4 e no plano de deploy (06).
  </action>
  <verify>
    <automated>MISSING — checkpoint humano: registrar saída de `npm view next-auth@beta version` e `npm view bcryptjs version` no SUMMARY; DATABASE_URL e AUTH_SECRET confirmados pelo usuário</automated>
  </verify>
  <acceptance_criteria>
    - Saída real de `npm view <pkg> version` registrada no SUMMARY para os 9 pacotes auditados
    - Nenhum pacote com verdict [SLOP]; quaisquer [SUS] resolvidos antes de prosseguir
    - Confirmado que xlsx virá do tarball CDN (string cdn.sheetjs.com/xlsx-0.20.3 no comando de install), nunca do registry npm
    - Usuário forneceu DATABASE_URL válida (Railway Postgres) e AUTH_SECRET (32+ bytes)
  </acceptance_criteria>
  <done>Versões verificadas e aprovadas, fonte do xlsx confirmada como CDN, DATABASE_URL + AUTH_SECRET disponíveis para os próximos tasks</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap Next.js 15.5 + shadcn/ui + instalar dependências auditadas</name>
  <read_first>
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-UI-SPEC.md (seção "Design System" / "Bootstrap commands", linhas 28-37 — preset New York/Neutral, lista exata de componentes shadcn)
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-RESEARCH.md (seção "Installation", linhas 65-92 — comandos exatos, incluindo tarball SheetJS)
    - CLAUDE.md (versões de stack: Next 15.5.x, React 19, Tailwind 4, TypeScript strict)
  </read_first>
  <action>
    Inicializar o app no diretório atual (projeto greenfield — sem subpasta extra; gerar a estrutura src/ na raiz do repo). Usar create-next-app com flags --typescript --tailwind --eslint --app --src-dir --import-alias "@/*", fixando Next 15.5.x (não 16 — CLAUDE.md proíbe). Garantir tsconfig.json com "strict": true. Inicializar shadcn com `npx shadcn@latest init -d` (preset New York, base Neutral, CSS variables, lucide-react). Adicionar os componentes exatos do UI-SPEC: button card table dialog form input label badge select textarea separator dropdown-menu sonner alert tabs checkbox skeleton alert-dialog sidebar avatar. Instalar deps de runtime nas versões auditadas no Task 1: next-auth@beta, bcryptjs (+ @types/bcryptjs como devDep), prisma + @prisma/client, zod, react-hook-form, @hookform/resolvers, @tanstack/react-table. Instalar xlsx EXCLUSIVAMENTE via `npm install https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`. Adicionar output: "standalone" em next.config.ts (necessário para deploy Railway no plano 06). Confirmar que .gitignore cobre .env, .env.local, node_modules, .next.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json lista next 15.5.x, react 19.x, next-auth (beta resolvido), bcryptjs, prisma, @prisma/client, zod, react-hook-form, @hookform/resolvers, @tanstack/react-table
    - Dependência xlsx em package.json aponta para https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz (NÃO ^0.18.5 nem ^0.20.3 do registry)
    - components.json existe com "style": "new-york" e base color neutral
    - Diretório src/components/ui/ contém os 21 componentes shadcn listados (button.tsx, sidebar.tsx, alert-dialog.tsx, etc.)
    - next.config.ts contém output: "standalone"
    - npm run build conclui sem erro de tipo
  </acceptance_criteria>
  <done>App Next.js compila, shadcn instalado com todos os componentes da fase, todas as deps auditadas presentes, xlsx vindo do CDN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schema Prisma (3 regimes), seed dos 5 usuários, e infra Vitest com stubs RED</name>
  <read_first>
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-PATTERNS.md (skeletons "prisma/schema.prisma" linhas 142-205 e "prisma/seed.ts" linhas 209-241 — RegimeTributario TEM 3 valores)
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-VALIDATION.md (seção "Wave 0 Requirements" e "Per-Task Verification Map" — lista exata de arquivos de teste a criar)
    - .planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-RESEARCH.md (Pattern 3.5 — totais esperados do parser: 61/80/50/7=198, usados no stub de import.test.ts)
  </read_first>
  <behavior>
    - Stubs de teste DEVEM falhar em RED por asserção não implementada (ex.: expect.fail('TODO: Plano 03')), NÃO por erro de import de módulo inexistente
    - cnpj.test.ts: cita CNPJs válidos/inválidos conhecidos como TODO para o Plano 03
    - visibility-scope.test.ts: documenta o contrato {role:'dono'} -> {}, {role:'colaborador', id:X} -> {responsavelId:X} como TODO
    - import.test.ts: documenta as contagens esperadas 61 LUCRO_REAL / 80 SIMPLES_NACIONAL / 50 LUCRO_PRESUMIDO / 7 sem-regime / 198 total como TODO
  </behavior>
  <action>
    Rodar `npx prisma init --datasource-provider postgresql` e escrever prisma/schema.prisma EXATAMENTE conforme o skeleton de PATTERNS.md: enum Role (COLABORADOR, DONO), enum RegimeTributario com OS 3 VALORES (LUCRO_REAL, LUCRO_PRESUMIDO, SIMPLES_NACIONAL — não 2; Lucro Presumido = 50/198 empresas reais), model Usuario (id cuid, nome, email @unique, senhaHash, role default COLABORADOR, timestamps, relação empresas, @@map "usuarios"), model Empresa (cnpj @unique, regimeTributario, responsavelId FK, contatos/particularidades opcionais, ativo default true, @@index responsavelId e regimeTributario, @@map "empresas"), model EmpresaRegimeHistorico (@@map "empresa_regime_historico"). Escrever prisma/seed.ts conforme skeleton: upsert de 1 DONO + 4 COLABORADOR com bcrypt.hash(senha, 10); adicionar bloco prisma.seed em package.json apontando para `tsx prisma/seed.ts` (instalar tsx como devDep) — NÃO rodar o seed ainda (banco é populado no Task 4). Instalar Vitest (`npm install -D vitest`) e criar vitest.config.ts (ambiente node para libs). Adicionar script "test": "vitest run" em package.json. Criar tests/setup.ts (helper para usuário/sessão mockados). Criar os 8 arquivos de teste-stub listados no VALIDATION.md (auth, visibility-scope, empresas.idor, empresas.crud, cnpj, import, import.confirm, import.upload), cada um com pelo menos um it() em RED via expect.fail('TODO: implementado no Plano NN') referenciando o plano que o tornará verde. NÃO usar flags de watch.
  </action>
  <verify>
    <automated>npx prisma validate &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma validate passa; o enum RegimeTributario contém os 3 valores LUCRO_REAL, LUCRO_PRESUMIDO, SIMPLES_NACIONAL
    - prisma/schema.prisma contém @@map("usuarios"), @@map("empresas"), @@map("empresa_regime_historico")
    - prisma/seed.ts contém bcrypt.hash e exatamente 5 entradas de usuário (1 role DONO, 4 role COLABORADOR)
    - package.json contém "test": "vitest run" e bloco "prisma": { "seed": ... }
    - npx vitest run executa, encontra os 8 arquivos de teste, e reporta falhas RED por expect.fail (TODO) — NÃO por "Cannot find module"
  </acceptance_criteria>
  <done>Schema validado com 3 regimes, seed escrito (não executado), Vitest configurado, 8 stubs RED por TODO e não por import quebrado</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Aplicar schema ao Postgres (prisma db push) + seed dos usuários</name>
  <read_first>
    - prisma/schema.prisma (estado atual após Task 3 — confirmar os 3 modelos antes do push)
    - prisma/seed.ts (confirmar os 5 usuários antes de seedar)
    - .env (DATABASE_URL fornecida no Task 1)
  </read_first>
  <action>
    Esta tarefa é OBRIGATÓRIA e BLOCKING: sem o push, build/type-check passam (tipos vêm do schema, não do banco vivo), criando falso-positivo de verificação. Gravar a DATABASE_URL (Railway, do Task 1) e AUTH_SECRET em .env (e criar .env.example com as chaves sem valores). Rodar `npx prisma generate` (gera o client tipado) e depois `npx prisma db push` para aplicar usuarios, empresas, empresa_regime_historico ao Postgres do Railway. Esta é a primeira aplicação de schema (banco vazio) — NÃO deve exigir --accept-data-loss; se exigir, parar e investigar (sinal de DATABASE_URL apontando para banco não-vazio). Em seguida rodar o seed (`npx prisma db seed`) para criar os 5 usuários. Confirmar que a tabela usuarios tem 5 linhas.
  </action>
  <verify>
    <automated>npx prisma db push --skip-generate</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma db push conclui com as 3 tabelas criadas no Postgres do Railway, sem --accept-data-loss
    - npx prisma db seed executa e a tabela usuarios passa a ter exatamente 5 linhas (1 DONO + 4 COLABORADOR)
    - .env contém DATABASE_URL e AUTH_SECRET; .env.example contém as mesmas chaves sem valores; nenhum dos dois com segredo commitado (.env no .gitignore)
  </acceptance_criteria>
  <done>Schema aplicado ao banco vivo do Railway, 5 usuários seedados, env configurado e não commitado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI → npm registry/CDN | dependências baixadas; supply-chain risk em pacotes [ASSUMED] |
| app → PostgreSQL (Railway) | DATABASE_URL como segredo; credenciais de banco |
| repo → segredos (.env) | AUTH_SECRET e DATABASE_URL nunca devem ser commitados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (next-auth, bcryptjs, etc. — todos [ASSUMED]) | mitigate | Task 1 blocking-human: `npm view` + verificação manual em npmjs.com antes do install em massa; xlsx só via CDN oficial |
| T-01-XLSX | Tampering | dependência xlsx | mitigate | Forçar tarball CDN cdn.sheetjs.com/xlsx-0.20.3; bloquear `npm install xlsx` (0.18.5 vulnerável a DoS/prototype pollution) |
| T-01-SECRET | Information Disclosure | .env (AUTH_SECRET, DATABASE_URL) | mitigate | .gitignore cobre .env/.env.local; apenas .env.example (sem valores) versionado |
| T-01-CRYPTO | Spoofing | hash de senha no seed | mitigate | bcryptjs com custo 10 no seed; nunca senha em texto plano (mesmo placeholder é hasheada) |
</threat_model>

<verification>
- npm run build verde (Task 2)
- npx prisma validate verde + enum com 3 regimes (Task 3)
- npx vitest run executa com 8 stubs RED por TODO (Task 3)
- npx prisma db push aplica 3 tabelas + seed gera 5 usuários (Task 4)
</verification>

<success_criteria>
- App Next.js 15.5 buildável com shadcn (New York/Neutral) e todos os componentes da fase
- Todas as deps auditadas (npm view registrado), xlsx do CDN
- Schema Prisma com 3 regimes aplicado ao Postgres do Railway
- 5 usuários seedados (bcrypt)
- Vitest configurado com 8 stubs RED prontos para as fases seguintes tornarem verdes
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plano 01)

- Files: package.json, tsconfig.json, next.config.ts, components.json, src/app/globals.css, prisma/schema.prisma, prisma/seed.ts, .env, .env.example, vitest.config.ts, tests/setup.ts, tests/cnpj.test.ts, tests/visibility-scope.test.ts, tests/auth.test.ts, tests/empresas.crud.test.ts, tests/empresas.idor.test.ts, tests/import.test.ts, tests/import.confirm.test.ts, tests/import.upload.test.ts
- Prisma enums: Role (COLABORADOR, DONO), RegimeTributario (LUCRO_REAL, LUCRO_PRESUMIDO, SIMPLES_NACIONAL)
- Prisma models: Usuario, Empresa, EmpresaRegimeHistorico
- Tabelas DB: usuarios, empresas, empresa_regime_historico
- package.json scripts: test (vitest run), prisma.seed
</artifacts_produced>

<output>
Create `.planning/phases/01-funda-o-acesso-empresas-e-importa-o/01-01-SUMMARY.md` when done
</output>

---
phase: 09-decimo-terceiro-salario-automatico
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/modules/tarefas/geracao.ts
  - tests/geracao.idempotencia.test.ts
autonomous: true
requirements: [DP-09]
must_haves:
  truths:
    - "Rodando o cron de competência YYYY-11, toda empresa com temFuncionariosClt=true e responsável de DP recebe 1 tarefa DECIMO_TERCEIRO com competencia=YYYY"
    - "Empresa com temFuncionariosClt=false nunca recebe a tarefa de 13º (não entra na query empresasClt)"
    - "Uma segunda execução da mesma competência YYYY-11 não cria nenhuma tarefa de 13º nova (idempotência via skipDuplicates)"
    - "A tarefa de 13º é criada com tipoObrigacao DECIMO_TERCEIRO, que o mapa de setor classifica como DP — visível para o responsável de DP nas listas e dashboards (success criterion 4)"
    - "O bloco DP-anual reusa empresasClt/comResponsavelDp já buscados — NÃO adiciona nenhuma chamada tx.empresa.findMany nova (mock chain dos testes existentes intocada)"
    - "Empresa CLT sem responsável de DP é pulada (skip+list), nunca lança erro, sem bloquear os demais blocos"
  artifacts:
    - path: "src/modules/tarefas/geracao.ts"
      provides: "6º bloco transacional (DP anual / 13º) reutilizando empresasClt, concatenado ao array tarefas final"
      contains: "obrigacoesDpAnuaisParaCompetencia"
    - path: "tests/geracao.idempotencia.test.ts"
      provides: "Testes de integração do bloco DP-anual: criação em novembro, ausência fora de novembro, idempotência, classificação como DP, gate temFuncionariosClt"
  key_links:
    - from: "src/modules/tarefas/geracao.ts"
      to: "src/lib/geracao-tarefas-dp-anual.ts"
      via: "import de obrigacoesDpAnuaisParaCompetencia/calcularPrazoDpAnual/TITULO_OBRIGACAO_DP_ANUAL (símbolos do Plano 09-01)"
      pattern: "geracao-tarefas-dp-anual"
    - from: "src/modules/tarefas/geracao.ts (tarefasDpAnual)"
      to: "src/lib/tipo-obrigacao-setor.ts (TIPOS_OBRIGACAO_POR_SETOR.DP)"
      via: "tarefa criada com tipoObrigacao DECIMO_TERCEIRO é classificada como DP por tarefaSetorWhere('DP')"
      pattern: "DECIMO_TERCEIRO"
    - from: "tarefasDpAnual"
      to: "empresasClt (query DP mensal já existente)"
      via: "reuso direto de comResponsavelDp, sem novo findMany (Pitfall 4)"
      pattern: "comResponsavelDp"
---

<objective>
Integrar a fundação pura do 13º salário (Plano 09-01) no orquestrador transacional `executarGeracaoMensal`: adicionar o 6º e último bloco da transação (DP anual / 13º), que decide por competência se este mês dispara o 13º, reusa a query `empresasClt` já feita pelo bloco DP mensal, e concatena as tarefas `DECIMO_TERCEIRO` ao array final antes do `tx.tarefa.createMany`. Cobrir o novo bloco com testes de integração no arquivo de mocks existente.

Esta plan fecha o requisito DP-09 ponta a ponta: com a fundação pura (catálogo + enum + mapa de setor) já em produção pelo Plano 01, esta integração é o que faz a tarefa de 13º realmente ser gerada pelo cron mensal e ficar visível nas listas/dashboards de DP.

Purpose: Conectar a regra pura ao motor real, mantendo a separação de domínios (catálogo Contábil intocado, Pitfall 1) e a estabilidade dos testes de regressão existentes (reuso de `empresasClt`, sem nova chamada `findMany` — Pitfall 4). A visibilidade nos dashboards de DP (success criterion 4) é automática: depende apenas da tarefa ser criada com `tipoObrigacao: "DECIMO_TERCEIRO"` (esta plan) somado ao registro já feito no mapa de setor (Plano 01) — nenhuma lógica nova de dashboard é necessária, por isso esta fase não tem uma plan dedicada de dashboard.
Output: `geracao.ts` com o bloco DP-anual integrado e o retorno de `executarGeracaoMensal` inalterado em forma (sem nova chave de retorno, pois a elegibilidade é idêntica à do bloco DP mensal — ver Decisão de Design abaixo); novos testes de integração verdes; suite completa verde (incluindo o teste de completude de setor do Plano 01).
</objective>

<artifacts_this_phase_produces>
Símbolos/mudanças NOVOS criados nesta plan:

- `src/modules/tarefas/geracao.ts`:
  - Novo import: `obrigacoesDpAnuaisParaCompetencia`, `calcularPrazoDpAnual`, `TITULO_OBRIGACAO_DP_ANUAL`, `type TipoObrigacaoDpAnual` de `@/lib/geracao-tarefas-dp-anual` (símbolos criados pelo Plano 09-01).
  - Nova variável local `regrasDpAnuais` (resultado de `obrigacoesDpAnuaisParaCompetencia(competencia)`).
  - Nova variável local `tarefasDpAnual` (array de tarefas de 13º, tipado igual ao `tarefasContabilAnual`).
  - Novo item `...tarefasDpAnual` no array `tarefas` final (6º bloco, após `tarefasContabilAnual`).
- `tests/geracao.idempotencia.test.ts`:
  - Novo bloco `describe`/`it` cobrindo o bloco DP-anual (criação em novembro, ausência fora de novembro, idempotência, classificação DECIMO_TERCEIRO→DP, gate temFuncionariosClt via empresasClt vazio).

Decisão de Design (registrada para o executor): a função `executarGeracaoMensal` NÃO ganha uma nova chave de retorno `semResponsavelDpAnual`. O bloco DP-anual reusa a MESMA lista `semResponsavelDp` já computada pelo bloco DP mensal (mesma query `empresasClt`, mesma elegibilidade `temFuncionariosClt: true` + `setor: "DP"`). Uma empresa CLT sem responsável de DP já é reportada uma única vez em `semResponsavelDp` — não há nova lista nem dedup a fazer (RESEARCH.md Pattern 2 / PATTERNS.md "Return shape extension"). Manter a assinatura de retorno intocada também preserva os testes que afirmam `toEqual({ criadas, puladas, semResponsavelDp, semResponsavelContabil })`.
</artifacts_this_phase_produces>

<execution_context>
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-decimo-terceiro-salario-automatico/09-CONTEXT.md
@.planning/phases/09-decimo-terceiro-salario-automatico/09-RESEARCH.md
@.planning/phases/09-decimo-terceiro-salario-automatico/09-PATTERNS.md
@.planning/phases/09-decimo-terceiro-salario-automatico/09-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar o 6º bloco (DP anual / 13º) em executarGeracaoMensal, reusando empresasClt</name>
  <files>src/modules/tarefas/geracao.ts</files>
  <read_first>
    - src/modules/tarefas/geracao.ts (LER O ARQUIVO VIVO INTEIRO — os números de linha do RESEARCH.md/PATTERNS.md estão DESATUALIZADOS após o merge do quick-260626 que inseriu `tx.usuario.findFirst({ where: { responsavelExtratoBancario: true } })`. A estrutura atual do corpo da transação é: (1) snapshot, (2) findMany Fiscal, (3) findMany empresasClt DP → comResponsavelDp/semResponsavelDp, (4) findMany empresasContabil, (5) usuario.findFirst → responsavelLancamentos, (6) tarefasContabilMensal, (7) loop Contábil ANUAL → tarefasContabilAnual, (8) dedup semResponsavelContabil, (9) array `tarefas = [...]`, (10) createMany.)
    - src/lib/geracao-tarefas-dp-anual.ts (criado no Plano 09-01 — assinaturas exatas de obrigacoesDpAnuaisParaCompetencia, calcularPrazoDpAnual, TITULO_OBRIGACAO_DP_ANUAL, TipoObrigacaoDpAnual)
    - src/lib/geracao-tarefas-contabil-anual.ts (analog EXATO da estrutura do loop anual — espelhar, divergindo apenas no gate de elegibilidade e no reuso de empresasClt)
    - 09-PATTERNS.md (Pattern Assignments → seção `src/modules/tarefas/geracao.ts` — código de referência do bloco, import, reuso de empresasClt, concat final)
  </read_first>
  <action>
    Edite SOMENTE `src/modules/tarefas/geracao.ts`. NÃO toque em `geracao-tarefas-contabil-anual.ts`, `geracao-tarefas-dp-anual.ts`, `tipo-obrigacao-setor.ts` nem em `prisma/schema.prisma` (já feitos no Plano 09-01). NÃO toque na chamada `tx.usuario.findFirst`/`responsavelLancamentos` (feature não relacionada do quick-260626).

    1. Adicione o import no topo, espelhando o import já existente de `@/lib/geracao-tarefas-contabil-anual`:
       importar `obrigacoesDpAnuaisParaCompetencia`, `calcularPrazoDpAnual`, `TITULO_OBRIGACAO_DP_ANUAL` e `type TipoObrigacaoDpAnual` de `@/lib/geracao-tarefas-dp-anual`.

    2. Insira o novo bloco DP-anual como ÚLTIMO bloco da transação: DEPOIS do loop Contábil anual e do dedup de `semResponsavelContabil`, e ANTES da linha que monta `const tarefas = [...]`. O bloco deve:
       - Computar `const regrasDpAnuais = obrigacoesDpAnuaisParaCompetencia(competencia);` (retorna `[]` em 11 dos 12 meses — caminho normal, não erro).
       - Declarar `let tarefasDpAnual: { empresaId: string; responsavelId: string; titulo: string; tipoObrigacao: TipoObrigacaoDpAnual; competencia: string; prazo: Date }[] = [];` (mesmo shape de `tarefasContabilAnual`).
       - Iterar `for (const { regra, competenciaAnual, anoVencimento } of regrasDpAnuais)`. Dentro do loop, REUSAR a variável `comResponsavelDp` JÁ COMPUTADA pelo bloco DP mensal (não fazer novo `tx.empresa.findMany`, não recomputar filtros sobre uma nova query — CRÍTICO, Pitfall 4). Mapear cada empresa de `comResponsavelDp` para uma tarefa:
         - `empresaId: e.id`
         - `responsavelId: e.responsaveisPorSetor[0].usuarioId` (mesmo acesso já usado para `tarefasDp`)
         - `titulo: \`${TITULO_OBRIGACAO_DP_ANUAL[regra.tipo]} - ${competenciaAnual}\`` (D-06/D-07 — ex.: "13º Salário - 2026")
         - `tipoObrigacao: regra.tipo` (será `"DECIMO_TERCEIRO"`)
         - `competencia: competenciaAnual` (formato "YYYY" — D-07)
         - `prazo: calcularPrazoDpAnual(anoVencimento, regra.mesVencimento, regra.diaVencimento)`
       - Acumular via `tarefasDpAnual = tarefasDpAnual.concat(...)` (mesmo idioma do bloco Contábil anual).
       - NÃO criar uma lista `semResponsavelDpAnual` nem dedup: empresas CLT sem responsável de DP já estão em `semResponsavelDp` (mesma query/elegibilidade). Adicione um comentário inline curto explicando esse reuso (gate idêntico ao bloco DP mensal, sem nova chave de retorno).

    3. Adicione `...tarefasDpAnual` como ÚLTIMO item do array `const tarefas = [ ...tarefasFiscal, ...tarefasDp, ...tarefasContabilMensal, ...tarefasContabilAnual, ...tarefasDpAnual ];`.

    4. NÃO altere a assinatura de retorno de `executarGeracaoMensal` (continua `{ criadas, puladas, semResponsavelDp, semResponsavelContabil }`). NÃO altere o `select` da query `empresasClt`. NÃO altere o timeout/maxWait da transação.

    Adicione um comentário de bloco acima do novo código documentando: que é o 6º bloco (DP anual / 13º, Plano 09-02); que reusa `empresasClt`/`comResponsavelDp` deliberadamente para não deslocar a mock chain dos testes (Pitfall 4); e que `anoVencimento` vem do catálogo dedicado de DP (mesmo ano-base, D-02), nunca do motor Contábil anual.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/geracao.idempotencia.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passa (o tipo `TipoObrigacaoDpAnual` e os símbolos importados resolvem corretamente a partir do client Prisma já regenerado no Plano 09-01).
    - `grep -c "tx.empresa.findMany" src/modules/tarefas/geracao.ts` retorna o MESMO valor que antes desta plan (o bloco DP-anual NÃO adiciona nenhuma nova chamada `tx.empresa.findMany` — verificável também por `git diff src/modules/tarefas/geracao.ts` não conter nenhuma linha adicionada com `tx.empresa.findMany`).
    - `grep -c "comResponsavelDp" src/modules/tarefas/geracao.ts` é maior que antes (o bloco novo reusa a variável existente em vez de recomputar/refazer query).
    - `grep -c "obrigacoesDpAnuaisParaCompetencia" src/modules/tarefas/geracao.ts` retorna 2 (1 import + 1 chamada).
    - `grep -n "tarefasDpAnual" src/modules/tarefas/geracao.ts` mostra `...tarefasDpAnual` dentro do array `const tarefas = [...]`, em última posição.
    - `git diff src/modules/tarefas/geracao.ts` NÃO contém nenhuma alteração em `responsavelLancamentos`, `usuario.findFirst`, nem na assinatura de retorno de `executarGeracaoMensal`.
    - `git diff --name-only` para esta task lista APENAS `src/modules/tarefas/geracao.ts` (nenhuma mudança em `geracao-tarefas-contabil-anual.ts`, Pitfall 1).
    - `npx vitest run tests/geracao.idempotencia.test.ts` continua verde para TODOS os testes de regressão pré-existentes (Fiscal/DP/Contábil) — nenhum mock posicional deslocado.
  </acceptance_criteria>
  <done>
    Bloco DP-anual integrado como 6º bloco da transação, reusando `comResponsavelDp` sem nova query; `...tarefasDpAnual` concatenado ao array final; assinatura de retorno e demais blocos intocados; `tsc` limpo e suite de idempotência (regressões) verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar testes de integração do bloco DP-anual em geracao.idempotencia.test.ts</name>
  <files>tests/geracao.idempotencia.test.ts</files>
  <read_first>
    - tests/geracao.idempotencia.test.ts (LER INTEIRO — entender o padrão de mock: `empresaFindManyMock` com `mockResolvedValueOnce` posicional encadeado, `usuarioFindFirstMock` já mockado para `null` no beforeEach, ordem das chamadas findMany por bloco; o novo bloco DP-anual NÃO adiciona chamada findMany, então a cadeia posicional dos testes existentes permanece a mesma)
    - src/modules/tarefas/geracao.ts (versão já modificada pela Task 1 — confirmar a ordem real das chamadas findMany e o shape das tarefas DP-anual)
    - src/lib/tipo-obrigacao-setor.ts (TIPOS_OBRIGACAO_POR_SETOR.DP — para o teste de classificação de setor importar o mapa e afirmar que DECIMO_TERCEIRO está na lista DP)
    - tests/geracao-tarefas-contabil-anual.test.ts (padrão de teste de competência de virada anual)
  </read_first>
  <behavior>
    - Test A (criação em novembro): competência "2026-11", 1 empresa CLT com responsável de DP → `createMany` recebe uma tarefa com `tipoObrigacao: "DECIMO_TERCEIRO"`, `competencia: "2026"`, `responsavelId` = o usuário de DP, e o título começa com "13º Salário - 2026". `resultado.criadas` reflete a tarefa criada.
    - Test B (ausência fora de novembro): competência "2026-07" (ou outro mês != 11), mesma empresa CLT com responsável de DP → NENHUMA tarefa com `tipoObrigacao: "DECIMO_TERCEIRO"` é incluída em `createMany` (apenas as tarefas de DP mensal, se houver). Confirma que `obrigacoesDpAnuaisParaCompetencia` retorna [] fora de novembro.
    - Test C (idempotência): duas execuções de "2026-11" com a mesma empresa CLT → 1ª execução cria a tarefa de 13º, 2ª execução (createMany mockado com count menor) reporta `puladas` cobrindo o 13º — sem duplicação (mesmo padrão dos testes de idempotência Contábil anual existentes).
    - Test D (gate temFuncionariosClt): competência "2026-11" com a query `empresasClt` retornando [] (nenhuma empresa CLT) → nenhuma tarefa DECIMO_TERCEIRO é gerada (a empresa não-CLT nunca entra na query empresasClt, success criterion 2).
    - Test E (classificação de setor — success criterion 4): importar `TIPOS_OBRIGACAO_POR_SETOR` de `@/lib/tipo-obrigacao-setor` e afirmar que `"DECIMO_TERCEIRO"` está em `TIPOS_OBRIGACAO_POR_SETOR.DP` (e NÃO em FISCAL nem CONTABIL) — prova que a tarefa gerada será classificada como DP por `tarefaSetorWhere("DP")`, ficando visível para o responsável de DP nas listas/dashboards.
  </behavior>
  <action>
    Edite SOMENTE `tests/geracao.idempotencia.test.ts`. Adicione um novo bloco `describe("executarGeracaoMensal — bloco DP anual (13º salário)", ...)` (ou um conjunto de `it` no describe existente), cobrindo os 5 comportamentos A-E acima.

    Regras de mock CRÍTICAS (derivadas do padrão já estabelecido no arquivo):
    - Como o bloco DP-anual REUSA `empresasClt` (Task 1, sem novo findMany), a cadeia posicional de `empresaFindManyMock.mockResolvedValueOnce(...)` NÃO muda em quantidade: a 2ª chamada (`mockResolvedValueOnce`) continua sendo a query `empresasClt` do bloco DP — é ELA que deve retornar a empresa CLT com responsável de DP para os testes de criação. Para competência "2026-11", o bloco Contábil anual NÃO dispara (DEFIS=fev, ECD=abr, ECF=jun), então a cadeia de mocks de novembro tem exatamente 3 `mockResolvedValueOnce` (Fiscal, DP, Contábil mensal) — igual aos testes de jul/ago/set existentes.
    - Para Test A/C, a 2ª `mockResolvedValueOnce` (bloco DP) deve retornar `[{ id, nome, responsaveisPorSetor: [{ usuarioId: "dp_user" }] }]`. Não definir `usuarioFindFirstMock` explicitamente (o beforeEach já resolve `null`, suficiente — Lançamentos está fora de escopo aqui).
    - Para Test A, afirmar sobre o conteúdo de `createMany.mock.calls[0][0].data`: deve conter uma entrada com `tipoObrigacao === "DECIMO_TERCEIRO"`, `competencia === "2026"`, `responsavelId === "dp_user"`, e `titulo` começando com "13º Salário - 2026". (O bloco DP mensal também gera tarefas para a mesma empresa — filtrar a asserção pela entrada `DECIMO_TERCEIRO`.)
    - Para Test B, usar competência "2026-07" e afirmar que `createMany.mock.calls[0][0].data.some((t) => t.tipoObrigacao === "DECIMO_TERCEIRO")` é `false`.
    - Para Test D, a 2ª `mockResolvedValueOnce` (bloco DP) retorna `[]` → afirmar que nenhuma entrada `DECIMO_TERCEIRO` aparece em `createMany` (ou que createMany não é chamado, se `tarefas.length === 0`).
    - Para Test E (puramente estático, sem mock de transação): importar o mapa e afirmar a pertinência de `"DECIMO_TERCEIRO"` à lista DP.

    NÃO modificar nenhum teste de regressão existente (Fiscal/DP/Contábil) — eles devem continuar passando sem alteração de mock, precisamente porque o bloco DP-anual não adiciona findMany. Se algum teste existente quebrar, é sinal de que a Task 1 NÃO reusou `empresasClt` corretamente (adicionou um findMany) — nesse caso, corrigir a Task 1, não os testes existentes.
  </action>
  <verify>
    <automated>npx vitest run tests/geracao.idempotencia.test.ts tests/tipo-obrigacao-setor.test.ts tests/geracao-tarefas-dp-anual.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/geracao.idempotencia.test.ts` passa com os 5 novos testes (A-E) verdes E todos os testes de regressão pré-existentes verdes.
    - Pelo menos um novo teste afirma `tipoObrigacao === "DECIMO_TERCEIRO"` e `competencia === "2026"` no payload de `createMany` para competência "2026-11".
    - Pelo menos um novo teste afirma que competência "2026-07" NÃO inclui nenhuma tarefa `DECIMO_TERCEIRO` em `createMany`.
    - Pelo menos um novo teste afirma `TIPOS_OBRIGACAO_POR_SETOR.DP` inclui `"DECIMO_TERCEIRO"` e que FISCAL/CONTABIL não o incluem (success criterion 4).
    - `grep -c "DECIMO_TERCEIRO" tests/geracao.idempotencia.test.ts` retorna >= 3.
    - A suite completa `npx vitest run` fica verde (sem regressão posicional de mocks nos testes Fiscal/DP/Contábil — Pitfall 4).
  </acceptance_criteria>
  <done>
    Cinco testes de integração do bloco DP-anual (criação em novembro, ausência fora de novembro, idempotência, gate temFuncionariosClt, classificação DECIMO_TERCEIRO→DP) verdes; nenhum teste de regressão existente alterado; suite completa verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → motor de geração | O cron mensal roda SEM usuário autenticado; a única entrada que cruza para esta plan é a `competencia` (já validada por `competenciaSchema` dentro de `obrigacoesDpAnuaisParaCompetencia`, Plano 09-01). Nenhuma entrada de usuário arbitrária chega ao bloco. |
| tarefa gerada → visibilidade por setor | A tarefa `DECIMO_TERCEIRO` criada é classificada como DP por `tarefaSetorWhere("DP")` via o mapa estático — o controle de acesso/visibilidade depende do `tipoObrigacao` correto gravado por esta plan + o registro no mapa (Plano 09-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DP09-04 | Information Disclosure | tarefa `DECIMO_TERCEIRO` criada em `geracao.ts` / `tarefaSetorWhere("DP")` | mitigate | Gravar `tipoObrigacao: "DECIMO_TERCEIRO"` na tarefa (Task 1) — combinado com o registro DP no mapa (Plano 09-01), garante que a tarefa só apareça para o setor DP (responsável de DP), nunca vazando para FISCAL/CONTABIL (ASVS V4). Test E desta plan é a mitigação automática que falha se a classificação divergir. |
| T-DP09-05 | Information Disclosure / Tampering | lookup de responsável no bloco DP-anual | mitigate | Reusar `comResponsavelDp` (derivado de `responsaveisPorSetor` filtrado por `setor: "DP"`), nunca `empresa.responsavelId` legado — a tarefa de 13º é atribuída ao responsável de DP correto, nunca ao FISCAL (mesmo Pitfall 2 do Plano 06-02). Coberto pela asserção `responsavelId === "dp_user"` do Test A. |
| T-DP09-06 | Repudiation (duplicação / ruído operacional) | `tx.tarefa.createMany` para entradas DECIMO_TERCEIRO | mitigate | Idempotência via `@@unique([empresaId, tipoObrigacao, competencia])` + `skipDuplicates: true` (sem mecanismo novo) — re-execução do cron na mesma competência não duplica a tarefa de 13º (ASVS — integridade). Coberto pelo Test C (idempotência). |
| T-DP09-07 | Denial of Service (timeout transacional) | transação `executarGeracaoMensal` | accept | O bloco DP-anual NÃO adiciona round-trip de banco (reusa `empresasClt`) — o orçamento de 30s/maxWait 10s já documentado permanece suficiente; risco residual baixo, nenhuma mitigação nova necessária. |
</threat_model>

<verification>
- `npx tsc --noEmit` — limpo (símbolos do Plano 09-01 resolvem no geracao.ts).
- `npx vitest run tests/geracao.idempotencia.test.ts tests/tipo-obrigacao-setor.test.ts tests/geracao-tarefas-dp-anual.test.ts` — todos verdes.
- `npx vitest run` — suite completa verde (gate de wave: confirma que nenhum mock posicional foi deslocado, Pitfall 4).
- `git diff src/modules/tarefas/geracao.ts` — não inclui mudança em `geracao-tarefas-contabil-anual.ts` (Pitfall 1), nem em `usuario.findFirst`/`responsavelLancamentos`, nem nova chamada `tx.empresa.findMany`.
</verification>

<success_criteria>
- 6º bloco (DP anual / 13º) integrado em `executarGeracaoMensal`, reusando `comResponsavelDp` sem nova query (Pitfall 4 respeitado).
- Tarefa de 13º criada com `tipoObrigacao: "DECIMO_TERCEIRO"`, `competencia: "YYYY"`, atribuída ao responsável de DP (success criteria 1, 4 do ROADMAP).
- Empresa sem `temFuncionariosClt` nunca recebe a tarefa (success criterion 2 — via gate da query `empresasClt`).
- Prazo (20/dez) ajustado para dia útil anterior via `calcularPrazoDpAnual` (success criterion 3 — herdado do Plano 09-01).
- Idempotência entre re-execuções via `skipDuplicates` (success criterion 1).
- Suite completa verde, sem regressão nos testes Fiscal/DP/Contábil existentes.
</success_criteria>

<output>
Create `.planning/phases/09-decimo-terceiro-salario-automatico/09-02-SUMMARY.md` when done
</output>

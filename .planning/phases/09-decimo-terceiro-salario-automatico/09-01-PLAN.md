---
phase: 09-decimo-terceiro-salario-automatico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/geracao-tarefas-dp-anual.ts
  - src/lib/tipo-obrigacao-setor.ts
  - tests/geracao-tarefas-dp-anual.test.ts
  - tests/tipo-obrigacao-setor.test.ts
autonomous: true
requirements: [DP-09]
must_haves:
  truths:
    - "Rodando as 12 competências de um ano, o catálogo de DP anual dispara DECIMO_TERCEIRO exatamente 1 vez (em novembro)"
    - "Para competência YYYY-11, anoVencimento é o MESMO ano (YYYY), não o ano seguinte (diverge do Contábil anual)"
    - "O prazo do 13º (20/dez) é antecipado para o dia útil anterior quando cai em fim de semana/feriado"
    - "O enum TipoObrigacao no banco contém o valor DECIMO_TERCEIRO após o push"
    - "DECIMO_TERCEIRO aparece exatamente uma vez no mapa TIPOS_OBRIGACAO_POR_SETOR, na lista DP"
  artifacts:
    - path: "src/lib/geracao-tarefas-dp-anual.ts"
      provides: "Catálogo puro anual de DP (13º salário) + funções de decisão e prazo"
      exports: ["obrigacoesDpAnuaisParaCompetencia", "calcularPrazoDpAnual", "CATALOGO_OBRIGACOES_DP_ANUAIS", "TITULO_OBRIGACAO_DP_ANUAL", "TipoObrigacaoDpAnual"]
      min_lines: 60
    - path: "tests/geracao-tarefas-dp-anual.test.ts"
      provides: "Sweep de 12 meses + asserções de anoVencimento (D-02) + ajuste de dia útil (D-03)"
    - path: "prisma/schema.prisma"
      provides: "Valor DECIMO_TERCEIRO no enum TipoObrigacao"
      contains: "DECIMO_TERCEIRO"
    - path: "src/lib/tipo-obrigacao-setor.ts"
      provides: "DECIMO_TERCEIRO registrado na lista DP do mapa de setor"
      contains: "DECIMO_TERCEIRO"
  key_links:
    - from: "src/lib/tipo-obrigacao-setor.ts"
      to: "prisma/schema.prisma"
      via: "TIPOS_OBRIGACAO_POR_SETOR.DP inclui o novo enum value DECIMO_TERCEIRO (gate do teste de completude)"
      pattern: "DECIMO_TERCEIRO"
    - from: "src/lib/geracao-tarefas-dp-anual.ts"
      to: "src/lib/dia-util.ts"
      via: "calcularPrazoDpAnual chama anticiparParaDiaUtil (reuso direto, D-03)"
      pattern: "anticiparParaDiaUtil"
---

<objective>
Criar a fundação pura e idempotente do 13º salário automático (DP-09): o novo catálogo anual de DP, o registro do novo tipo de obrigação no enum Prisma e no mapa de setor, e a aplicação da mudança de schema no banco Neon.

Esta fundação não toca o orquestrador `geracao.ts` (isolado no Plano 09-02) — entrega apenas as peças puras e o schema, todas testáveis sem banco nem mocks de transação.

Purpose: Estabelecer a regra de negócio do 13º (vence 20/dez do MESMO ano-base, criado em novembro) como função pura testável exaustivamente, sem o risco de regressão de tocar o motor Contábil anual já em produção (D-02, Pitfall 1 do RESEARCH.md). Garantir, no MESMO commit, que o novo valor de enum esteja registrado no mapa de setor — sem isso a tarefa de 13º seria criada mas ficaria invisível nos dashboards de DP (Pitfall 2, success criterion 4).
Output: `geracao-tarefas-dp-anual.ts` (catálogo + decisão + prazo), enum `TipoObrigacao` com `DECIMO_TERCEIRO` aplicado no banco, `TIPOS_OBRIGACAO_POR_SETOR.DP` atualizado, e os dois testes correspondentes verdes.
</objective>

<artifacts_this_phase_produces>
Símbolos NOVOS criados nesta plan (consumidos pelo Plano 09-02):

- `src/lib/geracao-tarefas-dp-anual.ts`:
  - `type TipoObrigacaoDpAnual` — union string `"DECIMO_TERCEIRO"`
  - `type ObrigacaoDpAnualRegra` — `{ tipo; mesCriacao; mesVencimento; diaVencimento }` (SEM campo `regimesElegiveis` — catálogo flat de DP)
  - `const TITULO_OBRIGACAO_DP_ANUAL: Record<TipoObrigacaoDpAnual, string>` — `{ DECIMO_TERCEIRO: "13º Salário" }`
  - `const CATALOGO_OBRIGACOES_DP_ANUAIS: ObrigacaoDpAnualRegra[]`
  - `function obrigacoesDpAnuaisParaCompetencia(competencia: string): { regra: ObrigacaoDpAnualRegra; competenciaAnual: string; anoVencimento: number }[]`
  - `function calcularPrazoDpAnual(anoVencimento: number, mesVencimento: number, diaVencimento: number): Date`
- `prisma/schema.prisma`: novo valor de enum `DECIMO_TERCEIRO` em `TipoObrigacao`
- `src/lib/tipo-obrigacao-setor.ts`: `"DECIMO_TERCEIRO"` adicionado à lista `DP` de `TIPOS_OBRIGACAO_POR_SETOR`
</artifacts_this_phase_produces>

<execution_context>
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-decimo-terceiro-salario-automatico/09-CONTEXT.md
@.planning/phases/09-decimo-terceiro-salario-automatico/09-RESEARCH.md
@.planning/phases/09-decimo-terceiro-salario-automatico/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar o catálogo puro anual de DP (geracao-tarefas-dp-anual.ts) + teste de sweep</name>
  <read_first>
    - src/lib/geracao-tarefas-contabil-anual.ts (analog EXATO — copiar a estrutura, divergir SÓ em anoVencimento)
    - src/lib/geracao-tarefas-dp.ts (precedente do catálogo FLAT de DP, sem regimesElegiveis)
    - src/lib/dia-util.ts (assinatura de anticiparParaDiaUtil, reusado sem modificação — D-03)
    - src/lib/competencia.ts (competenciaSchema, validação do formato mensal "YYYY-MM")
    - tests/geracao-tarefas-contabil-anual.test.ts (analog do teste de sweep de 12 meses)
  </read_first>
  <behavior>
    - Test 1 (sweep): rodando as 12 competências "2026-01".."2026-12", `obrigacoesDpAnuaisParaCompetencia` dispara DECIMO_TERCEIRO exatamente 1 vez no total, e esse disparo ocorre em novembro ("2026-11").
    - Test 2 (D-02, anoVencimento mesmo ano): para "2026-11", o resultado tem length 1, `competenciaAnual` === "2026" e `anoVencimento` === 2026 (NÃO 2027 — diverge do Contábil anual).
    - Test 3 (meses sem disparo): "2026-01" até "2026-10" e "2026-12" retornam [].
    - Test 4 (D-01/D-03, dia útil): `calcularPrazoDpAnual` para um 20/dezembro que cai em fim de semana é antecipado para o dia útil anterior; para um 20/dezembro que já é dia útil, permanece 20/dez. Validar a data concreta de 20/dez do ano testado contra `anticiparParaDiaUtil`/`date-holidays` em tempo de implementação — NÃO assumir o dia da semana sem checar (ver Wave 0 Gaps do RESEARCH.md).
    - Test 5 (validação de formato): `obrigacoesDpAnuaisParaCompetencia` lança erro para "2026-13", "abc", "" e "2026" (mesmo contrato de `competenciaSchema` do analog).
  </behavior>
  <action>
    Escreva PRIMEIRO `tests/geracao-tarefas-dp-anual.test.ts` cobrindo os 5 comportamentos acima (RED), importando de `@/lib/geracao-tarefas-dp-anual`. Em seguida crie `src/lib/geracao-tarefas-dp-anual.ts`.

    O arquivo deve seguir EXATAMENTE a estrutura de `geracao-tarefas-contabil-anual.ts`, com estas adaptações concretas:
    - Importar apenas `anticiparParaDiaUtil` de `./dia-util` e `competenciaSchema` de `./competencia`. NÃO importar `RegimeTributario` (catálogo flat de DP, sem filtro por regime — precedente de `geracao-tarefas-dp.ts`).
    - Exportar `type TipoObrigacaoDpAnual = "DECIMO_TERCEIRO"`.
    - Exportar `type ObrigacaoDpAnualRegra = { tipo: TipoObrigacaoDpAnual; mesCriacao: number; mesVencimento: number; diaVencimento: number }` — SEM campo `regimesElegiveis`.
    - Exportar `const TITULO_OBRIGACAO_DP_ANUAL: Record<TipoObrigacaoDpAnual, string> = { DECIMO_TERCEIRO: "13º Salário" }` (D-06 — sem menção a "2ª parcela" no título).
    - Exportar `const CATALOGO_OBRIGACOES_DP_ANUAIS: ObrigacaoDpAnualRegra[]` com uma única entrada: `tipo: "DECIMO_TERCEIRO"`, `mesCriacao: 11` (novembro, D-04 — 1 mês antes do vencimento), `mesVencimento: 12`, `diaVencimento: 20` (D-01 — 2ª parcela/saldo de dezembro, NUNCA a 1ª parcela de 30/novembro; ver Pitfall 3 do RESEARCH.md).
    - Exportar `function obrigacoesDpAnuaisParaCompetencia(competencia: string)` com o MESMO corpo do analog (`competenciaSchema.safeParse` → throw em formato inválido; split em `[anoAtual, mesAtual]`; filter por `regra.mesCriacao === mesAtual`; map para `{ regra, competenciaAnual: String(anoAtual), anoVencimento }`), EXCETO que `anoVencimento` deve ser `anoAtual` (D-02 — MESMO ano-base), com um comentário inline explicando a divergência em relação ao padrão Contábil (`anoAtual + 1`).
    - Exportar `function calcularPrazoDpAnual(anoVencimento, mesVencimento, diaVencimento): Date` com corpo idêntico a `calcularPrazoAnual` do analog: `new Date(anoVencimento, mesVencimento - 1, diaVencimento)` antecipado por `anticiparParaDiaUtil`.

    Inclua um comentário de cabeçalho documentando que este é um catálogo PARALELO dedicado a DP, deliberadamente NÃO reusando `obrigacoesAnuaisParaCompetencia`/`calcularPrazoAnual` do módulo Contábil (Pitfall 1: o módulo Contábil hardcoda `anoAtual + 1`; o 13º precisa de `anoAtual`).

    NÃO modifique `geracao-tarefas-contabil-anual.ts`. NÃO adicione um parâmetro `vencimentoMesmoAno` a nenhuma função existente.
  </action>
  <verify>
    <automated>npx vitest run tests/geracao-tarefas-dp-anual.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/geracao-tarefas-dp-anual.test.ts` passa, com pelo menos 5 testes verdes cobrindo sweep, anoVencimento=mesmo ano, meses vazios, ajuste de dia útil e validação de formato.
    - `grep -n "anoVencimento" src/lib/geracao-tarefas-dp-anual.ts` mostra `anoAtual` (não `anoAtual + 1`) na função de decisão.
    - `grep -c "regimesElegiveis" src/lib/geracao-tarefas-dp-anual.ts` retorna 0 (catálogo flat, sem campo de regime).
    - `src/lib/geracao-tarefas-dp-anual.ts` não importa nada de `geracao-tarefas-contabil-anual`: `grep -c "geracao-tarefas-contabil-anual" src/lib/geracao-tarefas-dp-anual.ts` retorna 0.
    - O catálogo tem `mesVencimento: 12` e `diaVencimento: 20` (não 11/30) para DECIMO_TERCEIRO.
  </acceptance_criteria>
  <done>
    Catálogo puro anual de DP criado e exportando os 6 símbolos listados em artifacts; teste de sweep de 12 meses verde; nenhum import do módulo Contábil anual; vencimento fixado em 20/dez com anoVencimento = ano-base.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar DECIMO_TERCEIRO ao enum Prisma e ao mapa de setor (mesmo commit) + atualizar teste de completude</name>
  <read_first>
    - prisma/schema.prisma (enum TipoObrigacao, ~linhas 36-57 — 20 valores atuais)
    - src/lib/tipo-obrigacao-setor.ts (mapa TIPOS_OBRIGACAO_POR_SETOR, lista DP na linha 23)
    - tests/tipo-obrigacao-setor.test.ts (teste de completude — asserções de contagem DP=4 e soma total=20)
  </read_first>
  <action>
    Faça as três edições abaixo numa única task (Pitfall 2 — a edição do mapa e a do enum DEVEM andar juntas; o TypeScript não conecta o enum ao mapa automaticamente, então só o teste de completude pega o esquecimento):

    1. `prisma/schema.prisma`: adicione `DECIMO_TERCEIRO` como último valor do enum `TipoObrigacao`, logo após `DEFIS` (passa de 20 para 21 valores) — novo valor de enum distinto de todos os tipos mensais/anuais existentes, preservando o invariante de unicidade entre eixos mensal/anual já estabelecido em T-07-03 (D-05).

    2. `src/lib/tipo-obrigacao-setor.ts`: na lista `DP` de `TIPOS_OBRIGACAO_POR_SETOR`, adicione `"DECIMO_TERCEIRO"` ao final — a lista passa de `["FOLHA", "ESOCIAL", "FGTS", "INSS"]` para `["FOLHA", "ESOCIAL", "FGTS", "INSS", "DECIMO_TERCEIRO"]`. Não toque nas listas FISCAL nem CONTABIL.

    3. `tests/tipo-obrigacao-setor.test.ts`: atualize as expectativas explícitas de contagem para o setor DP — onde o teste afirma `toHaveLength(4)` para a cláusula de recorrentes do DP, mude para `5`; onde afirma `arrayContaining(["FOLHA", "ESOCIAL", "FGTS", "INSS"])` para o DP, inclua `"DECIMO_TERCEIRO"`; e no teste de completude onde afirma `expect(somaTotal).toBe(20)`, mude para `21`. O teste de completude `ocorrenciasPorValor.get(valor)` deve continuar passando para todos os 21 valores do enum (incluindo o novo).

    Após editar o schema, execute o push de schema (ver Task 3) — esta task NÃO precisa do push para o teste passar (o teste lê `Object.values(TipoObrigacao)` do client gerado), mas `npx prisma generate` deve rodar para o client refletir o novo valor antes de qualquer `tsc`/import do enum em teste.
  </action>
  <verify>
    <automated>npx prisma generate && npx vitest run tests/tipo-obrigacao-setor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DECIMO_TERCEIRO" prisma/schema.prisma` retorna 1 (valor presente no enum).
    - `grep -n "DECIMO_TERCEIRO" src/lib/tipo-obrigacao-setor.ts` mostra o valor dentro da lista DP (não em FISCAL nem CONTABIL).
    - `npx vitest run tests/tipo-obrigacao-setor.test.ts` passa: teste de completude verde com soma total 21 e DP com 5 valores.
    - O teste de completude continua afirmando que cada valor do enum aparece em exatamente um setor (sem sobreposição/omissão).
  </acceptance_criteria>
  <done>
    Enum Prisma com 21 valores incluindo DECIMO_TERCEIRO; mapa de setor com DECIMO_TERCEIRO na lista DP; teste de completude atualizado (soma 21, DP=5) e verde.
  </done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Aplicar a mudança de enum no banco Neon (prisma db push) e confirmar</name>
  <read_first>
    - prisma/schema.prisma (deve já conter DECIMO_TERCEIRO da Task 2)
    - .planning/STATE.md (precedente Phase 02-01 / 03-01: prisma db push em ambiente Neon sem shadow database; Phase 07-01: confirmação via prisma db pull --print)
  </read_first>
  <action>
    Esta task é OBRIGATÓRIA e BLOQUEANTE: build e `tsc` passam sem o push (os tipos vêm do client gerado a partir de `schema.prisma`, não do banco vivo), criando um falso-positivo de verificação. A tarefa de 13º só pode ser persistida em produção se o valor de enum existir no Postgres.

    Execute `npx prisma db push` para aplicar o novo valor de enum ao banco Neon. Adicionar um valor a um enum é uma operação NÃO-destrutiva e normalmente não deve reportar perda de dados — só adicione `--accept-data-loss` se a saída do próprio push reportar perda de dados (e confirme na saída antes de adicionar a flag). Em seguida rode `npx prisma generate` para garantir que o client local reflete o schema aplicado.

    Confirme que o push landou consultando o banco diretamente: `npx prisma db pull --print` e verifique que o enum `TipoObrigacao` no banco contém `DECIMO_TERCEIRO` (mesmo padrão de confirmação usado em Phase 07-01).

    Se o push exigir prompts interativos que não podem ser suprimidos no ambiente não-TTY, pare e sinalize para intervenção manual (não force flags destrutivas para contornar o prompt).
  </action>
  <verify>
    <automated>npx prisma db pull --print | grep -A 25 "enum TipoObrigacao" | grep -c "DECIMO_TERCEIRO"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db pull --print | grep -A 25 "enum TipoObrigacao" | grep -c "DECIMO_TERCEIRO"` retorna 1 — o banco Neon contém o novo valor de enum.
    - `npx prisma db push` reportou sucesso sem aplicar perda de dados (ou, se reportou, a flag `--accept-data-loss` só foi adicionada após confirmação explícita na saída do push).
    - `npx prisma generate` rodou após o push.
  </acceptance_criteria>
  <done>
    Valor de enum DECIMO_TERCEIRO presente no banco Neon (confirmado via prisma db pull --print) e no client Prisma gerado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → motor de geração | O cron mensal roda SEM usuário autenticado; nenhuma entrada de usuário cruza para esta plan — a única entrada é a `competencia` derivada pelo próprio motor |
| catálogo puro → persistência | A regra pura produz `tipoObrigacao`/`competencia`/`prazo` que viram linhas `Tarefa`; o setor da tarefa é derivado do `tipoObrigacao` via mapa estático |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DP09-01 | Information Disclosure | `TIPOS_OBRIGACAO_POR_SETOR` / `tarefaSetorWhere("DP")` | mitigate | Registrar `DECIMO_TERCEIRO` na lista DP (Task 2) — sem isso a tarefa de 13º não seria classificada como DP e poderia vazar/sumir das queries por setor (ASVS V4). O teste de completude `tests/tipo-obrigacao-setor.test.ts` é a mitigação automática que falha se esquecido. |
| T-DP09-02 | Tampering (Invalid Date silencioso) | `obrigacoesDpAnuaisParaCompetencia` (entrada `competencia`) | mitigate | Validar formato via `competenciaSchema.safeParse` e lançar erro em formato não canônico (ASVS V5) — mesmo contrato do analog Contábil, coberto pelo Test 5 da Task 1. |
| T-DP09-03 | Repudiation (ano de vencimento errado) | `calcularPrazoDpAnual` / `anoVencimento` | mitigate | Hardcode `anoVencimento = anoAtual` com comentário e teste explícito (D-02); não reusar o motor Contábil que hardcoda `anoAtual + 1` (Pitfall 1), evitando rastrear o 13º no ano errado. |
</threat_model>

<verification>
- `npx vitest run tests/geracao-tarefas-dp-anual.test.ts tests/tipo-obrigacao-setor.test.ts` — ambos verdes.
- `npx prisma db pull --print` confirma `DECIMO_TERCEIRO` no enum do banco.
- Nenhuma modificação em `geracao-tarefas-contabil-anual.ts` (Pitfall 1): `git diff --name-only` não inclui esse arquivo.
</verification>

<success_criteria>
- Catálogo `geracao-tarefas-dp-anual.ts` puro, exportando os 6 símbolos, com `anoVencimento = ano-base` (D-02) e vencimento 20/dez (D-01).
- Enum `TipoObrigacao` no banco e no schema contém `DECIMO_TERCEIRO`.
- `TIPOS_OBRIGACAO_POR_SETOR.DP` inclui `DECIMO_TERCEIRO`; teste de completude verde (soma 21).
- Schema push aplicado e confirmado no banco Neon.
</success_criteria>

<output>
Create `.planning/phases/09-decimo-terceiro-salario-automatico/09-01-SUMMARY.md` when done
</output>

---
phase: quick-260625-p9h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/dashboards/queries.ts
  - src/app/(app)/dashboards/guard.ts
  - src/modules/dashboards/snapshot.ts
  - src/modules/usuarios/schema.ts
  - tests/dashboards.queries.test.ts
  - tests/dashboards.snapshot.test.ts
  - tests/usuarios.schema.test.ts
autonomous: true
requirements: [WR-01, WR-02, WR-03, IN-01, IN-02]

must_haves:
  truths:
    - "O ponto live de listarEvolucaoMensal usa contagens inteiras exatas de noPrazo, sem reverter o percentual arredondado"
    - "O card de ranking de empresas respeita o parametro ?meses= (mesma janela do grafico de evolucao)"
    - "Um COLABORADOR nao pode ser validado sem setor; a validacao falha explicitamente"
    - "O totalEmpresas do snapshot congelado e escopado por setor, igual ao caminho live"
    - "O default de quantidadeMeses em listarEvolucaoMensal e 6, alinhado ao default de producao em guard.ts"
  artifacts:
    - path: "src/modules/dashboards/queries.ts"
      provides: "totalNoPrazo exposto em DesempenhoColaborador; soma de inteiros no ponto live; default 6"
      contains: "totalNoPrazo"
    - path: "src/modules/usuarios/schema.ts"
      provides: "usuarioSchema com refine COLABORADOR-exige-setor"
      contains: "usuarioSchema"
    - path: "tests/usuarios.schema.test.ts"
      provides: "cobertura da regra COLABORADOR-exige-setor"
  key_links:
    - from: "src/app/(app)/dashboards/guard.ts"
      to: "src/modules/dashboards/queries.ts"
      via: "quantidadeMeses propagado para a janela do ranking via subMonths(hoje, quantidadeMeses)"
      pattern: "subMonths\\(hoje, quantidadeMeses\\)"
    - from: "src/modules/dashboards/snapshot.ts"
      to: "src/lib/tipo-obrigacao-setor.ts"
      via: "filtro de empresa por setor (empresaWhereExtra-equivalente) aplicado ao groupBy de carteira"
      pattern: "temFuncionariosClt"
---

<objective>
Corrigir 5 achados diferidos do code review da Fase 8 (08-REVIEW.md): WR-01, WR-02,
WR-03, IN-01 e IN-02. Sao bugs de baixo-medio severidade nos dashboards multi-setor,
todos com fix bem-especificado no proprio relatorio. CR-01 (ja corrigido, commit
d885854) e CR-02 (descartado como nao-issue) NAO sao tocados.

Purpose: eliminar erros de arredondamento, inconsistencia de janela temporal por
parametro de URL, perda silenciosa de dados de tarefas avulsas, e descontinuidade
live->frozen na carteira por setor — fechando os follow-ups conhecidos da Fase 8.
Output: queries.ts, guard.ts, snapshot.ts ajustados; novo schema usuarios/schema.ts;
testes atualizados/adicionados nas 3 suites cobrindo cada mudanca de comportamento.
</objective>

<execution_context>
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/Desktop/teste/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-dashboards-multi-setor-dp-e-cont-bil/08-REVIEW.md
@src/modules/dashboards/queries.ts
@src/modules/dashboards/snapshot.ts
@src/app/(app)/dashboards/guard.ts
@src/lib/tipo-obrigacao-setor.ts
@src/modules/empresas/schema.ts
@tests/dashboards.queries.test.ts
@tests/dashboards.snapshot.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 + IN-02 — expor totalNoPrazo inteiro e alinhar default para 6 (queries.ts)</name>
  <files>src/modules/dashboards/queries.ts, tests/dashboards.queries.test.ts</files>
  <behavior>
    - Test (WR-01): dado totalConcluidas=150 e noPrazo=1 para um colaborador no mes
      corrente, o ponto live somado em listarEvolucaoMensal usa o inteiro real (1),
      nao o valor revertido do percentual arredondado (que daria 2). Construir o caso
      via mock de tarefa.findMany do mes corrente e assertar o percentual/contagem do
      ponto live final.
    - Test (WR-01): cada item de listarDesempenhoColaboradoresMesAtual expoe
      totalNoPrazo como inteiro igual a noPrazo agregado (ex.: 1 de 2 concluidas ->
      totalNoPrazo=1, totalConcluidas=2, percentualNoPrazo=50).
    - Test (IN-02): listarEvolucaoMensal chamado sem o argumento quantidadeMeses
      produz 6 pontos (5 fechados + 1 live), provando default=6.
  </behavior>
  <action>
    WR-01 (queries.ts:27-33, 119-131, 292-300): adicionar o campo `totalNoPrazo: number`
    ao type `DesempenhoColaborador` (apos `totalConcluidas`). No `.map` final de
    `listarDesempenhoColaboradoresMesAtual` (linhas 119-131), incluir
    `totalNoPrazo: dados.noPrazo` no objeto retornado (o agregado `dados` ja carrega
    `noPrazo` como inteiro). Em `listarEvolucaoMensal`, no `reduce` de `totalAtual`
    (linhas 292-300), trocar a linha que faz
    `Math.round((c.percentualNoPrazo / 100) * c.totalConcluidas)` por `c.totalNoPrazo`
    — somando o inteiro exato em vez de reverter o percentual arredondado. Isso remove
    o round-trip lossy descrito em WR-01 e alinha o ponto live ao `_sum` exato dos
    snapshots congelados.
    IN-02 (queries.ts:231): trocar o default do parametro de
    `quantidadeMeses = 3` para `quantidadeMeses = 6`, alinhando ao default real de
    producao em guard.ts:46 (`?meses=` ausente -> 6). Atualizar tambem o comentario
    JSDoc da funcao (linhas 220-229) que ainda menciona "Default quantidadeMeses=3
    (ultimos 3 meses)" e "resolvida como 'ultimos 3 meses'" para refletir 6 meses,
    para o comentario nao contradizer o codigo. Nao mudar nenhum call site (guard.ts
    sempre passa valor explicito).
    Testes (tests/dashboards.queries.test.ts): no describe "listarDesempenhoColaboradoresMesAtual",
    estender pelo menos um caso existente para assertar `totalNoPrazo` (inteiro) ao
    lado de `percentualNoPrazo`. No describe "listarEvolucaoMensal", adicionar um caso
    novo para WR-01 que monta o mock do mes corrente com totalConcluidas alto e
    noPrazo=1 (cenario onde o metodo antigo over-contava em +1) e assere o
    percentual/contagem do ponto live correto. Adicionar um caso IN-02 chamando
    `listarEvolucaoMensal(undefined as never, "FISCAL")` ou via wrapper que omite o
    argumento, esperando `resultado.length === 6`. Manter todos os casos existentes
    passando — o novo campo `totalNoPrazo` e aditivo (nao remover `percentualNoPrazo`
    nem `totalConcluidas`).
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.queries.test.ts</automated>
  </verify>
  <done>totalNoPrazo presente em DesempenhoColaborador e somado como inteiro no ponto live; default de quantidadeMeses=6; comentario JSDoc atualizado; suite dashboards.queries verde com os novos casos WR-01 e IN-02.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — janela do ranking deriva de quantidadeMeses (guard.ts)</name>
  <files>src/app/(app)/dashboards/guard.ts</files>
  <action>
    WR-02 (guard.ts:48-49, 61-66): substituir a constante hardcoded
    `const inicio3Meses = subMonths(hoje, 3);` (linha 49) por uma janela derivada do
    parametro ja validado: `const inicioRanking = subMonths(hoje, quantidadeMeses);`
    (a variavel `quantidadeMeses` ja existe na linha 46, default 6, range 1-24 via
    mesesSchema). Atualizar a chamada de `listarRankingEmpresas` (linhas 61-66) para
    passar `inicioRanking` em vez de `inicio3Meses` como `periodoInicio`. Manter `hoje`
    como `periodoFim`. Assim o `?meses=` afeta consistentemente os dois cards com janela
    temporal (evolucao + ranking), eliminando a inconsistencia de UX descrita em WR-02.
    Renomear a variavel evita confusao residual ("3Meses" deixaria de ser verdade);
    garantir que nao restem outras referencias a `inicio3Meses` no arquivo. O import
    de `subMonths` (linha 2) permanece em uso.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>guard.ts deriva a janela do ranking de quantidadeMeses (subMonths(hoje, quantidadeMeses)); nenhuma referencia remanescente a inicio3Meses; tsc --noEmit limpo.</done>
</task>

<task type="auto">
  <name>Task 3: IN-01 — totalEmpresas do snapshot escopado por setor (snapshot.ts)</name>
  <files>src/modules/dashboards/snapshot.ts, tests/dashboards.snapshot.test.ts</files>
  <action>
    IN-01 (snapshot.ts:221-230, 306-319): hoje a carteira (`totalEmpresas`) e calculada
    com um unico `tx.empresa.groupBy({ where: { ativo: true } })` (sem filtro por
    setor) e o mesmo numero e copiado em toda linha do colaborador, independente do
    setor da linha. Isso diverge do caminho live (queries.ts:98-102), que aplica
    `empresaWhereExtra` (ex.: DP usa `{ temFuncionariosClt: true }`) ao groupBy de
    carteira. Fix: tornar a carteira escopada por setor.
    Definir, no topo do modulo (junto a `SETOR_POR_TIPO_OBRIGACAO`), um mapa de filtro
    de empresa por setor espelhando `empresaScopePorSetor` de guard.ts:34-38 —
    `const empresaWhereExtraPorSetor: Record<Setor, Prisma.EmpresaWhereInput> =
    { FISCAL: {}, DP: { temFuncionariosClt: true }, CONTABIL: {} }`. Em vez de um unico
    groupBy global, computar a carteira por setor: para cada setor presente nas linhas
    (ou para os 3 setores), rodar
    `tx.empresa.groupBy({ by: ["responsavelId"], where: { ativo: true,
    ...empresaWhereExtraPorSetor[setor] }, _count: { id: true } })` e indexar o
    resultado por chave composta `(colaboradorId, setor)`. Na montagem final das linhas
    (loop em `chaves`, linhas 288-320), substituir
    `totalEmpresas: totalEmpresasPorColaborador.get(colaboradorId) ?? 0` por uma busca
    escopada `(colaboradorId, setor)` usando a mesma convencao de chave de
    `chaveColaboradorSetor`. Evitar N+1 desnecessario: rodar no maximo 1 groupBy por
    setor distinto (3 no pior caso) — preferir um `Promise.all`/loop sobre os setores
    presentes. NAO alterar o tipo `LinhaSnapshotMensal` (totalEmpresas permanece
    number). Importar `Prisma` de "@prisma/client" se ainda nao importado (o arquivo ja
    importa `Prisma, Setor, TipoObrigacao` na linha 30 — reusar).
    Testes (tests/dashboards.snapshot.test.ts): o mock atual de `empresaGroupByMock` em
    `criarTxMock()` retorna um unico resultado para todas as chamadas — com a mudanca,
    `tx.empresa.groupBy` passa a ser chamado uma vez por setor. Ajustar os testes
    afetados para que `empresaGroupByMock` aceite chamadas por setor (ex.: usar
    `mockImplementation` que inspeciona `where.temFuncionariosClt` e retorna a carteira
    apropriada, OU `mockResolvedValue` consistente quando o teste so tem um setor).
    Adicionar 1 teste novo IN-01: colaborador DP com `temFuncionariosClt`-filtrado
    retorna totalEmpresas escopado (ex.: groupBy global daria 12, mas o filtro DP da 4)
    — assertar que a linha DP usa 4, provando que o filtro de setor foi aplicado ao
    groupBy da carteira. Manter os ~20 testes existentes de snapshot verdes (a maioria
    usa setor FISCAL, cujo empresaWhereExtra e `{}` — comportamento inalterado).
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.snapshot.test.ts</automated>
  </verify>
  <done>totalEmpresas no snapshot e escopado por setor via empresaWhereExtra-equivalente; carteira DP exclui empresas nao-CLT; teste IN-01 novo verde; suite snapshot inteira verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: WR-03 — schema de validacao que exige setor para COLABORADOR</name>
  <files>src/modules/usuarios/schema.ts, tests/usuarios.schema.test.ts</files>
  <behavior>
    - Test: usuarioSchema rejeita { role: "COLABORADOR", setor: null/undefined }
      (success === false), com mensagem de erro indicando que setor e obrigatorio
      para colaborador.
    - Test: usuarioSchema aceita { role: "COLABORADOR", setor: "DP" } (success === true).
    - Test: usuarioSchema aceita { role: "DONO", setor: null } (DONO nao precisa de
      setor — coluna permanece nullable no DB).
    - Test: usuarioSchema aceita { role: "DONO", setor: "FISCAL" } ou rejeita conforme
      a regra escolhida (DONO sem setor e o caso canonico; permitir setor presente para
      DONO e aceitavel — documentar a decisao no schema).
  </behavior>
  <action>
    WR-03 (origem do achado: tipo-obrigacao-setor.ts:52-59, onde uma tarefa avulsa de
    colaborador com setor=null some de todos os dashboards). O fix prescrito e validar
    na camada de aplicacao que um COLABORADOR nunca seja salvo sem setor — NAO mudar a
    coluna nullable no DB (DONO legitimamente tem setor=null, schema.prisma:65).
    REALIDADE DO CODEBASE: nao existe hoje nenhuma Server Action nem UI de
    criacao/edicao de usuario (usuarios sao criados apenas via prisma/seed.ts e
    scripts). Portanto criamos o PRIMITIVO de validacao reutilizavel — um schema Zod
    `usuarioSchema` — pronto para ser plugado em qualquer futura Server Action de
    usuario, e testavel agora. Isto NAO e uma versao reduzida: e exatamente o
    "validate at the Zod schema / Server Action layer" que o achado pede; a Server
    Action ainda nao existe, entao entregamos a unidade que existe (o schema) com a
    regra correta e cobertura de teste.
    Criar src/modules/usuarios/schema.ts seguindo a convencao de
    src/modules/empresas/schema.ts (zod, export do schema + type inferido, JSDoc
    explicando a regra e citando WR-03). Definir:
    `usuarioSchema = z.object({ nome, email (z.string().email()), role:
    z.enum(["COLABORADOR","DONO"]), setor: z.enum(["FISCAL","DP","CONTABIL"]).nullable()
    .optional() }).refine(...)`. O `.refine` (ou `.superRefine` para mensagem
    path-scoped em `setor`) deve falhar quando `role === "COLABORADOR" && setor == null`,
    com mensagem do tipo "Colaborador exige um setor (FISCAL, DP ou CONTABIL)". DONO
    com setor null passa. Exportar `export type UsuarioInput = z.infer<typeof
    usuarioSchema>`. Usar os mesmos literais de enum ja usados no projeto (Setor:
    FISCAL/DP/CONTABIL; Role: COLABORADOR/DONO — maiusculas, conforme decisao
    01-03/STATE.md). NAO importar enums do @prisma/client no schema se a convencao do
    projeto usa z.enum com literais (empresas/schema.ts usa literais) — manter
    consistencia.
    Testes (tests/usuarios.schema.test.ts): cobrir os 4 casos do bloco <behavior> via
    `usuarioSchema.safeParse(...)` e assertar `.success` e (no caso de falha) que a
    issue aponta para o campo `setor` com a mensagem da regra.
  </action>
  <verify>
    <automated>npx vitest run tests/usuarios.schema.test.ts</automated>
  </verify>
  <done>usuarioSchema existe em src/modules/usuarios/schema.ts e rejeita COLABORADOR sem setor enquanto aceita DONO sem setor; tests/usuarios.schema.test.ts verde cobrindo os 4 casos; coluna DB inalterada.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/dashboards.queries.test.ts tests/dashboards.snapshot.test.ts tests/usuarios.schema.test.ts` — todas as 3 suites verdes.
- `npx vitest run` — suite completa do projeto verde (nenhuma regressao em IDOR/visibilidade/geracao).
- `npx tsc --noEmit -p tsconfig.json` — zero erros de tipo (totalNoPrazo aditivo, guard.ts renomeado, snapshot.ts groupBy por setor, novo usuarioSchema).
</verification>

<success_criteria>
- WR-01: listarEvolucaoMensal soma totalNoPrazo inteiro; nenhum round-trip via percentual arredondado; ponto live bate com o criterio exato do snapshot congelado.
- WR-02: card de ranking respeita ?meses= (janela = subMonths(hoje, quantidadeMeses)).
- WR-03: usuarioSchema reutilizavel rejeita COLABORADOR sem setor; DONO sem setor permitido; coluna DB nullable preservada.
- IN-01: totalEmpresas do snapshot escopado por setor (DP exclui nao-CLT), sem degrau live->frozen na carteira.
- IN-02: default quantidadeMeses=6 alinhado ao default de producao; comentario JSDoc coerente.
- Todos os testes existentes continuam passando; novos testes cobrem cada mudanca de comportamento.
</success_criteria>

<output>
Create `.planning/quick/260625-p9h-corrigir-achados-wr-01-wr-02-wr-03-in-01/260625-p9h-SUMMARY.md` when done
</output>

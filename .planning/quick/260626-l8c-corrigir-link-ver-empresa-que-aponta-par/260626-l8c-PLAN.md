---
phase: quick-260626-l8c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/tarefas/[id]/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicar em 'Ver empresa' na tela de detalhe de uma tarefa abre a página da empresa (rota /empresas/{id}/editar) sem 404"
  artifacts:
    - path: "src/app/(app)/tarefas/[id]/page.tsx"
      provides: "Link 'Ver empresa' apontando para rota existente"
      contains: "/empresas/${tarefa.empresa.id}/editar"
  key_links:
    - from: "src/app/(app)/tarefas/[id]/page.tsx"
      to: "/empresas/[id]/editar"
      via: "next/link href"
      pattern: "empresas/\\$\\{tarefa\\.empresa\\.id\\}/editar"
---

<objective>
Corrigir o link "Ver empresa" na página de detalhe da tarefa, que aponta para a rota inexistente `/empresas/{id}` e causa erro 404. Trocar o `href` para a única rota de empresa que existe: `/empresas/{id}/editar`.

Purpose: Restaurar a navegação da tela de detalhe da tarefa para a empresa associada, eliminando o 404.
Output: `src/app/(app)/tarefas/[id]/page.tsx` com o `href` do link corrigido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/app/(app)/tarefas/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir href do link "Ver empresa"</name>
  <files>src/app/(app)/tarefas/[id]/page.tsx</files>
  <action>Na linha ~196, alterar o `href` do componente `Link` de `/empresas/${tarefa.empresa.id}` para `/empresas/${tarefa.empresa.id}/editar`. A rota `/empresas/[id]` (visualização) não existe no projeto — a única rota de empresa por id é `/empresas/[id]/editar` (confirmado em src/app/(app)/empresas/[id]/editar/page.tsx). Não criar nova página de visualização nem alterar o texto do botão "Ver empresa"; apenas trocar o destino do link.</action>
  <verify>
    <automated>grep -n "empresas/\${tarefa.empresa.id}/editar" "src/app/(app)/tarefas/[id]/page.tsx" && ! grep -nE "empresas/\\\$\{tarefa\.empresa\.id\}\"" "src/app/(app)/tarefas/[id]/page.tsx"</automated>
  </verify>
  <done>O `Link` na página de detalhe da tarefa aponta para `/empresas/${tarefa.empresa.id}/editar`; nenhuma ocorrência do href antigo `/empresas/${tarefa.empresa.id}` (sem `/editar`) permanece no arquivo.</done>
</task>

</tasks>

<verification>
- `grep` confirma o novo href com sufixo `/editar` e ausência do href antigo.
- Build/TypeScript permanece válido (`npm run build` ou `npx tsc --noEmit` não introduz erros novos — alteração é apenas de string literal).
</verification>

<success_criteria>
Clicar em "Ver empresa" a partir da tela de detalhe de uma tarefa navega para `/empresas/{id}/editar` (página existente) em vez de gerar 404.
</success_criteria>

<output>
Create `.planning/quick/260626-l8c-corrigir-link-ver-empresa-que-aponta-par/260626-l8c-SUMMARY.md` when done
</output>

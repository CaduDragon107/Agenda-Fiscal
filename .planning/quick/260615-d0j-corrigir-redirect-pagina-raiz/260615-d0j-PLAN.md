---
phase: quick
plan: 260615-d0j
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [QUICK-260615-d0j]
must_haves:
  truths:
    - "Acessar a URL raiz '/' sem sessão redireciona para /login"
    - "Acessar a URL raiz '/' com sessão válida redireciona para /empresas"
    - "O template padrão do create-next-app (logo Next.js, 'Get started by editing') não aparece mais em nenhum lugar"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Server Component que faz auth() + redirect baseado em sessão"
      contains: "redirect"
  key_links:
    - from: "src/app/page.tsx"
      to: "@/auth"
      via: "import { auth }"
      pattern: "from \"@/auth\""
    - from: "src/app/page.tsx"
      to: "next/navigation"
      via: "import { redirect }"
      pattern: "from \"next/navigation\""
---

<objective>
Corrigir a rota raiz '/' que ainda serve o template padrão do create-next-app. Como `src/app/page.tsx` está fora do grupo `(app)`, ela não passa pelo auth check de `src/app/(app)/layout.tsx`, e a Railway serve esse template ao acessar a URL raiz — fazendo o usuário achar que "o site não abre".

Purpose: Restaurar o ponto de entrada correto do app em produção (bug ativo no site live).
Output: `src/app/page.tsx` convertido em Server Component que decide o destino com base na sessão.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/app/page.tsx
@src/app/(app)/layout.tsx
@src/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir o template raiz por redirect baseado em sessão</name>
  <files>src/app/page.tsx</files>
  <action>Reescrever completamente `src/app/page.tsx` como um Server Component assíncrono (sem `"use client"`), removendo todo o JSX do template create-next-app e o import de `next/image`. Importar `auth` de `@/auth` e `redirect` de `next/navigation`, seguindo exatamente o mesmo padrão de `src/app/(app)/layout.tsx`. A função default exportada deve ser `async`, chamar `const session = await auth();` e então: se `session?.user` existir, chamar `redirect("/empresas")`; caso contrário, chamar `redirect("/login")`. A função não retorna JSX — `redirect()` interrompe a execução (lança internamente), então não há fallback renderizável. NÃO mexer nos arquivos SVG em `public/` (next.svg, vercel.svg, etc.) — eles podem permanecer; apenas o uso deles em page.tsx é removido. NÃO usar `signOut`/`signIn`; apenas `auth` para leitura de sessão. Manter o destino `/empresas` (rota confirmada existente em src/app/(app)/empresas/page.tsx) e `/login` (mesmo alvo usado pelo layout autenticado).</action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "Compiled successfully|✓ Compiled" && npm run lint</automated>
  </verify>
  <done>`src/app/page.tsx` não contém mais nenhum import de `next/image` nem JSX do template; importa `auth` de `@/auth` e `redirect` de `next/navigation`; `npm run build` compila sem erros e `npm run lint` passa sem warnings/erros novos.</done>
</task>

</tasks>

<verification>
- `grep -n "next/image" src/app/page.tsx` retorna vazio (import do template removido).
- `grep -n "Get started by editing" src/app/page.tsx` retorna vazio (template removido).
- `grep -nE "from \"@/auth\"|from \"next/navigation\"" src/app/page.tsx` retorna ambos os imports.
- `npm run build` compila com sucesso.
- `npm run lint` passa.
</verification>

<success_criteria>
- A rota raiz '/' redireciona para /login (sem sessão) ou /empresas (com sessão), nunca renderizando o template default.
- Build e lint passam localmente, garantindo que o deploy da Railway servirá a página corrigida.
</success_criteria>

<output>
Create `.planning/quick/260615-d0j-corrigir-redirect-pagina-raiz/260615-d0j-SUMMARY.md` when done
</output>

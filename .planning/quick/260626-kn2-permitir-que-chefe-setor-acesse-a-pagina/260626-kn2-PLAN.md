---
phase: quick-260626-kn2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/dashboards/guard.ts
  - src/app/(app)/dashboards/page.tsx
  - src/app/(app)/app-sidebar.tsx
  - tests/dashboards.rbac.test.ts
autonomous: true
requirements: [DASH-RBAC-CHEFE]

must_haves:
  truths:
    - "CHEFE_SETOR consegue abrir /dashboards (não recebe notFound)"
    - "CHEFE_SETOR vê apenas os dados do próprio setor, sem o seletor de abas Fiscal/DP/Contábil"
    - "DONO continua vendo as 3 abas com os 3 setores, comportamento inalterado"
    - "COLABORADOR e usuário não autenticado continuam bloqueados (notFound/redirect) antes de qualquer query"
    - "CHEFE_SETOR sem setor definido falha seguro (não vaza dados de outro setor)"
    - "Link 'Dashboards' na sidebar aparece para DONO e CHEFE_SETOR"
  artifacts:
    - path: "src/app/(app)/dashboards/guard.ts"
      provides: "Guard que permite DONO (3 setores) e CHEFE_SETOR (1 setor da sessão)"
      contains: "CHEFE_SETOR"
    - path: "src/app/(app)/dashboards/page.tsx"
      provides: "Render com Tabs para DONO e card único para CHEFE_SETOR"
    - path: "tests/dashboards.rbac.test.ts"
      provides: "Cobertura do branch CHEFE_SETOR (acesso, escopo de 1 setor, isolamento)"
      contains: "CHEFE_SETOR"
  key_links:
    - from: "src/app/(app)/dashboards/guard.ts"
      to: "session.user.setor"
      via: "leitura do setor da sessão para escopar 1 setor"
      pattern: "session\\.user\\.setor"
    - from: "src/app/(app)/app-sidebar.tsx"
      to: "/dashboards link"
      via: "condição de role incluindo CHEFE_SETOR"
      pattern: "CHEFE_SETOR"
---

<objective>
Permitir que um usuário com role `CHEFE_SETOR` acesse a página `/dashboards`, vendo
apenas os dados do próprio setor (FISCAL, DP ou CONTABIL — o setor da própria
sessão), sem o seletor de abas Fiscal/DP/Contábil que o DONO usa. O DONO continua
com as 3 abas inalteradas; COLABORADOR e usuários não autenticados continuam
bloqueados.

Purpose: Estender o papel CHEFE_SETOR (criado em 260626-dfc para empresas/tarefas)
à página de Dashboards, mantendo o mesmo padrão de segurança "guard antes da query,
never 403" e o mesmo padrão de escopo-por-setor-da-sessão já usado em
src/lib/visibility-scope.ts.

Output: guard.ts e page.tsx ajustados, link da sidebar liberado para CHEFE_SETOR,
e novos casos de teste em tests/dashboards.rbac.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/app/(app)/dashboards/guard.ts
@src/app/(app)/dashboards/page.tsx
@src/app/(app)/app-sidebar.tsx
@src/lib/visibility-scope.ts
@src/types/next-auth.d.ts
@tests/dashboards.rbac.test.ts
@tests/setup.ts
@.planning/quick/260626-dfc-criar-role-chefe-setor-caio-fiscal-elisa/260626-dfc-SUMMARY.md

Notas de contrato relevantes:
- `carregarDadosDashboards` retorna hoje `Record<"FISCAL"|"DP"|"CONTABIL", DadosSetor>`
  (sempre os 3 setores). page.tsx consome esse Record via `dados[setor]` dentro de
  Tabs.
- `session.user.setor` é `"FISCAL" | "DP" | "CONTABIL" | null` (src/types/next-auth.d.ts).
  Para CHEFE_SETOR o setor real vem da sessão — NUNCA de query string/input do client
  (mesmo padrão de visibility-scope.ts).
- `empresaScopePorSetor` (D-02 DP=temFuncionariosClt:true; D-03 CONTABIL=universo cheio)
  NÃO muda — apenas QUANTOS setores são buscados muda.
- tests/setup.ts já expõe `mockChefeFiscalUser`, `mockChefeDpUser`,
  `mockChefeContabilUser`, `mockColaboradorUser`, `mockDonoUser`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender guard.ts para permitir CHEFE_SETOR escopado a 1 setor</name>
  <files>src/app/(app)/dashboards/guard.ts, tests/dashboards.rbac.test.ts</files>
  <behavior>
    RED primeiro: adicionar testes a tests/dashboards.rbac.test.ts (seguir o padrão
    de mocks existente — authMock, notFoundMock que lança "NEXT_NOT_FOUND",
    redirectMock, e os 3 mocks de query). Importar os helpers de tests/setup.ts.
    Novos casos:
    - CHEFE_SETOR (mockChefeFiscalUser) NÃO chama notFound; chama as 3 queries
      (desempenho/evolução/ranking) exatamente UMA vez cada, sempre com o argumento
      de setor === "FISCAL" (asserir via `toHaveBeenCalledWith` no parâmetro setor).
      O retorno deve conter a chave "FISCAL" e NÃO conter "DP" nem "CONTABIL".
    - CHEFE_SETOR de DP (mockChefeDpUser) escopa só "DP": queries chamadas com setor
      "DP" e com o empresaScopePorSetor.DP (temFuncionariosClt:true); retorno só tem
      a chave "DP" — isolamento entre setores (não vaza FISCAL/CONTABIL).
    - DONO (mockDonoUser) continua chamando as queries para os 3 setores (9 chamadas
      no total: 3 queries × 3 setores) e retorna as 3 chaves — caso de regressão.
    - CHEFE_SETOR com setor null (mockChefeFiscalUser({ setor: null })) aplica o
      fail-safe consistente com visibility-scope: NÃO vaza dados de setor nenhum
      (nenhuma das 3 queries é chamada) e retorna um Record vazio. (Escolha de
      fail-safe: dataset vazio, não notFound — alinhado a "fail seguro = sem dados"
      do branch CHEFE_SETOR de visibility-scope; documentar essa escolha no comentário
      do guard.)
    Os 2 testes existentes (não autenticado → redirect; COLABORADOR → notFound)
    permanecem inalterados e devem continuar passando.
  </behavior>
  <action>
    Implementar (GREEN) em guard.ts mantendo o padrão "guard antes da query". Trocar
    `if (session.user.role !== "DONO") notFound();` por: permitir DONO e CHEFE_SETOR,
    e fazer `notFound()` para qualquer outra role (preservando a barreira anti-IDOR
    "não encontrado, never 403"). Após o guard, decidir a LISTA de setores a buscar:
    para DONO usar o array completo `["FISCAL","DP","CONTABIL"]`; para CHEFE_SETOR usar
    apenas `[session.user.setor]` quando o setor da sessão for um setor válido. Quando
    CHEFE_SETOR tem `setor` null/inválido, aplicar fail-safe: retornar um Record vazio
    sem disparar nenhuma query (mesma filosofia "fail seguro = sem dados" do branch
    CHEFE_SETOR em src/lib/visibility-scope.ts) — NÃO ampliar para os 3 setores. Reusar
    `empresaScopePorSetor` exatamente como está (D-02/D-03 inalterados). Reaproveitar o
    `Promise.all` sobre a lista de setores escolhida; o retorno continua sendo um Record
    chaveado por setor, agora podendo conter 1, 3 ou 0 chaves. Atualizar o comentário do
    topo de guard.ts para descrever que DONO=3 setores e CHEFE_SETOR=1 setor da sessão,
    com o fail-safe de setor null. NÃO ler setor de query string — sempre de
    `session.user.setor`. NÃO colocar blocos de código em prosa fora deste plano.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboards.rbac.test.ts</automated>
  </verify>
  <done>
    Todos os casos de tests/dashboards.rbac.test.ts passam: não autenticado→redirect,
    COLABORADOR→notFound, CHEFE_SETOR(FISCAL/DP)→1 setor com isolamento, DONO→3 setores,
    CHEFE_SETOR setor null→Record vazio sem queries. tsc limpo no arquivo.
  </done>
</task>

<task type="auto">
  <name>Task 2: page.tsx renderiza card único para CHEFE_SETOR e Tabs para DONO; sidebar libera o link</name>
  <files>src/app/(app)/dashboards/page.tsx, src/app/(app)/app-sidebar.tsx</files>
  <action>
    Em page.tsx: o `dados` retornado por `carregarDadosDashboards` agora pode ter 1 ou
    3 chaves. Derivar a lista de setores presentes a partir das chaves de `dados`
    (`Object.keys(dados)` tipado como `Setor[]`), em vez do array fixo `SETORES`. Se
    houver mais de um setor presente (DONO), renderizar exatamente o bloco atual de
    `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` (comportamento DONO inalterado). Se
    houver exatamente um setor presente (CHEFE_SETOR), renderizar apenas o
    `<SectorDashboard setor={setor} dados={dados[setor]} />` daquele setor, SEM nenhum
    componente Tabs/TabsList/TabsTrigger (sem o seletor Fiscal/DP/Contábil). Se houver
    zero setores (CHEFE_SETOR com setor null, fail-safe), renderizar uma mensagem
    simples de estado vazio (reusar o componente EmptyState ou um parágrafo de
    "Nenhum dado disponível para o seu setor") — não quebrar. Manter o header
    "Dashboards" e o subtítulo em todos os casos. Não alterar o componente
    `SectorDashboard`. Não importar Tabs onde não for usado, mas pode mantê-lo
    importado já que o ramo DONO ainda o usa.

    Em app-sidebar.tsx: o link "Dashboards" hoje é gated por `{isDono && (...)}`.
    Trocar a condição APENAS desse item para incluir CHEFE_SETOR — calcular um
    booleano (ex: `const podeVerDashboards = user.role === "DONO" || user.role === "CHEFE_SETOR";`)
    e usá-lo no gate do `SidebarMenuItem` de Dashboards. NÃO alterar o gate do item
    "Usuários" (continua `isDono`). `AppSidebarUser.role` já inclui "CHEFE_SETOR"
    (ajustado em 260626-dfc), então nenhum tipo precisa mudar.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    tsc limpo. page.tsx renderiza Tabs quando dados tem ≥2 setores e um único
    SectorDashboard sem Tabs quando tem exatamente 1; estado vazio quando 0 setores.
    Sidebar mostra o link "Dashboards" para DONO e CHEFE_SETOR; "Usuários" continua
    DONO-only.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificação completa de regressão</name>
  <files>—</files>
  <action>
    Rodar a suite completa de testes e o build de produção para garantir que nenhum
    teste de regressão (COLABORADOR/DONO de dashboards, visibility-scope, IDOR)
    quebrou e que o app compila. Se algum teste pré-existente falhar por mudança de
    contrato do retorno de `carregarDadosDashboards` (de "sempre 3 chaves" para
    "1/3/0 chaves"), corrigir o consumidor/teste mantendo o comportamento DONO
    idêntico. Não introduzir novos arquivos.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    Toda a suite vitest passa (incluindo os novos casos CHEFE_SETOR e os de regressão
    inalterados); tsc limpo. `npm run build` conclui sem novos erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /dashboards (Server Component) | Usuário autenticado navega direto para a rota; role/setor vêm da sessão JWT assinada, não do request body |
| session.user.setor → query de dashboard | O setor que define o escopo do CHEFE_SETOR precisa vir da sessão, nunca de query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-kn2-01 | Elevation of Privilege | carregarDadosDashboards (guard) | mitigate | Guard antes de qualquer query permite só DONO e CHEFE_SETOR; qualquer outra role → notFound() (padrão anti-IDOR "never 403"). Coberto por teste COLABORADOR→notFound. |
| T-kn2-02 | Information Disclosure | escopo de setor do CHEFE_SETOR | mitigate | Setor lido exclusivamente de `session.user.setor`; CHEFE_SETOR busca SOMENTE seu setor. Teste de isolamento asserta que DP não retorna chaves FISCAL/CONTABIL. |
| T-kn2-03 | Information Disclosure | CHEFE_SETOR com setor null | mitigate | Fail-safe: Record vazio, nenhuma query disparada — NUNCA amplia para os 3 setores (alinhado a Pitfall B3 de visibility-scope). Coberto por teste de setor null. |
| T-kn2-04 | Spoofing | escolha de setor via input | mitigate | Setor jamais lido de query string/searchParams; apenas `searchParams.meses` (já validado por mesesSchema) é consumido do client. |
</threat_model>

<verification>
- `npx vitest run tests/dashboards.rbac.test.ts` — todos os casos (antigos + novos CHEFE_SETOR) verdes.
- `npx vitest run` — suite completa sem regressões.
- `npx tsc --noEmit -p tsconfig.json` — sem erros de tipo.
- `npm run build` — build de produção sem novos erros.
</verification>

<success_criteria>
- CHEFE_SETOR acessa /dashboards e vê apenas o card do próprio setor, sem o seletor de abas.
- DONO continua com as 3 abas, comportamento idêntico ao atual.
- COLABORADOR e não autenticado continuam bloqueados antes de qualquer query.
- Link "Dashboards" visível para DONO e CHEFE_SETOR na sidebar; "Usuários" continua DONO-only.
- Escopo de CHEFE_SETOR isolado por setor da sessão, com fail-safe em setor null.
</success_criteria>

<output>
Create `.planning/quick/260626-kn2-permitir-que-chefe-setor-acesse-a-pagina/260626-kn2-SUMMARY.md` when done
</output>

---
phase: quick-260618-kk8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/tarefas/tarefas-table.tsx
autonomous: false
requirements: [QUICK-260618-kk8]
must_haves:
  truths:
    - "Na tela de Tarefas existe um filtro de status com opcao 'Pendentes'"
    - "Selecionar 'Pendentes' exibe apenas tarefas com status PENDENTE"
    - "Selecionar 'Concluidas' exibe apenas tarefas com status CONCLUIDA"
    - "Selecionar 'Todas' exibe tarefas pendentes e concluidas"
  artifacts:
    - path: "src/app/(app)/tarefas/tarefas-table.tsx"
      provides: "Select de status (Pendentes/Concluidas/Todas) integrado ao filtro client-side existente"
      contains: "PENDENTE"
  key_links:
    - from: "src/app/(app)/tarefas/tarefas-table.tsx"
      to: "dadosFiltrados useMemo"
      via: "estado statusFiltro filtra t.status"
      pattern: "statusFiltro"
---

<objective>
Adicionar uma opcao de filtro "Pendentes" na tela de Tarefas, permitindo ao usuario ver apenas tarefas com status PENDENTE.

Purpose: A equipe precisa focar rapidamente nas tarefas em aberto (pendentes) sem o ruido das concluidas. Hoje so existe um checkbox binario "Mostrar concluidas", que nao expressa claramente a intencao "ver so as pendentes".

Output: O checkbox "Mostrar concluidas" e substituido por um Select de status com tres opcoes — "Pendentes" (default), "Concluidas" e "Todas" — reaproveitando o componente shadcn Select ja importado e a logica de filtro client-side (useMemo) ja existente. Status continua representado pelo enum existente "PENDENTE" | "CONCLUIDA"; nenhum novo padrao de status e introduzido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/(app)/tarefas/tarefas-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir checkbox "Mostrar concluidas" por Select de status (Pendentes/Concluidas/Todas)</name>
  <files>src/app/(app)/tarefas/tarefas-table.tsx</files>
  <action>
No componente TarefasTable, trocar o filtro binario de concluidas por um filtro de status de tres estados, reusando o padrao ja presente no arquivo:

1. Remover o estado `mostrarConcluidas` (useState boolean, linha ~102) e introduzir no lugar `const [statusFiltro, setStatusFiltro] = useState<"PENDENTE" | "CONCLUIDA" | "TODOS">("PENDENTE")`. O default "PENDENTE" preserva o comportamento atual (esconder concluidas por padrao) e atende diretamente ao pedido "filtro Pendentes".

2. No useMemo `dadosFiltrados` (linhas ~115-139), substituir o bloco que filtra `if (!mostrarConcluidas) { dados = dados.filter((t) => t.status !== "CONCLUIDA"); }` por uma filtragem baseada em `statusFiltro`: quando `statusFiltro !== "TODOS"`, aplicar `dados = dados.filter((t) => t.status === statusFiltro)`. Manter inalterados os demais filtros (responsavel e busca) e a lista de dependencias do useMemo, trocando apenas `mostrarConcluidas` por `statusFiltro`.

3. Na toolbar (linhas ~346-353), remover o `<label>` com o `<Checkbox id="mostrar-concluidas">` e adicionar um `<Select value={statusFiltro} onValueChange={(v) => setStatusFiltro(v as typeof statusFiltro)}>` seguindo exatamente o mesmo padrao visual do Select de responsavel ja existente logo acima (mesmo SelectTrigger com largura ~`w-[180px]`, SelectContent com SelectItem). Itens, nesta ordem: SelectItem value="PENDENTE" -> "Pendentes"; value="CONCLUIDA" -> "Concluidas"; value="TODOS" -> "Todas". O SelectTrigger deve ter um SelectValue (placeholder "Status"). Este Select aparece para todos os usuarios (diferente do filtro de responsavel, que e so para DONO).

4. Nao alterar o componente Checkbox usado nas COLUNAS da tabela (coluna "concluir", linhas ~176-189) — esse checkbox conclui a tarefa e nao tem relacao com o filtro. Remover apenas o checkbox da toolbar.

Reaproveitar imports ja presentes (Select, SelectContent, SelectItem, SelectTrigger, SelectValue ja importados nas linhas 25-31). Se o `Checkbox` deixar de ser usado em qualquer outro ponto do arquivo, manter o import apenas se ainda houver uso (a coluna "concluir" ainda usa Checkbox, entao o import permanece).
  </action>
  <verify>
    <automated>cd "c:/Users/Novo/Desktop/criação de planilha modulos/teste" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -i "tarefas-table" ; echo "tsc-done"</automated>
  </verify>
  <done>tsc nao reporta erros em tarefas-table.tsx; o arquivo contem um Select de status com os value "PENDENTE", "CONCLUIDA" e "TODOS"; o estado `statusFiltro` controla o filtro no useMemo `dadosFiltrados`; o checkbox "Mostrar concluidas" da toolbar foi removido; o checkbox da coluna "concluir" permanece intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Filtro de status na tela de Tarefas: o antigo checkbox "Mostrar concluidas" foi substituido por um Select com "Pendentes" (default), "Concluidas" e "Todas".</what-built>
  <how-to-verify>
1. Rode `npm run dev` e acesse `/tarefas`.
2. Confirme que, ao abrir a pagina, o filtro de status ja vem em "Pendentes" e a lista mostra apenas tarefas pendentes (nenhuma concluida).
3. Mude o filtro para "Concluidas" — a lista deve mostrar apenas tarefas com status concluido (texto/opacidade reduzida nas linhas concluidas).
4. Mude para "Todas" — devem aparecer tarefas pendentes e concluidas juntas.
5. Confirme que a busca por titulo/empresa e (como DONO) o filtro por responsavel continuam funcionando em combinacao com o filtro de status.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva qualquer ajuste necessario (ex: ordem das opcoes, default diferente, rotulos).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sem novos erros relacionados a tarefas-table.tsx.
- Filtro de status funciona client-side reusando o useMemo `dadosFiltrados`, sem chamadas extras ao servidor (mantem o padrao D-08: carregar todas e filtrar no cliente).
</verification>

<success_criteria>
- A tela de Tarefas oferece uma opcao explicita "Pendentes" no filtro de status.
- Selecionar "Pendentes" mostra somente tarefas PENDENTE; "Concluidas" somente CONCLUIDA; "Todas" mostra ambas.
- Nenhum novo padrao de status foi inventado — reusa o enum "PENDENTE" | "CONCLUIDA" existente.
- O filtro integra-se ao mecanismo de filtro ja existente (Select shadcn + useMemo), sem filtro ad-hoc separado.
</success_criteria>

<output>
Create `.planning/quick/260618-kk8-adicionar-opcao-de-filtro-pendentes-na-t/260618-kk8-SUMMARY.md` when done
</output>

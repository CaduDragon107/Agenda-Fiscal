---
phase: quick-260615-ci1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/theme-provider.tsx
  - src/components/mode-toggle.tsx
  - src/app/layout.tsx
  - src/app/(app)/app-sidebar.tsx
autonomous: false
requirements: [QUICK-CI1]

must_haves:
  truths:
    - "Usuário pode alternar entre tema claro, escuro e do sistema a partir de qualquer página autenticada (Empresas/Tarefas/Dashboards)"
    - "O toggle de tema também funciona na página /login (fora do shell autenticado)"
    - "Ao recarregar a página, o tema escolhido persiste e não há flash de tema errado (sem hydration mismatch warning)"
  artifacts:
    - path: "src/components/theme-provider.tsx"
      provides: "Client wrapper around next-themes ThemeProvider"
      contains: "next-themes"
    - path: "src/components/mode-toggle.tsx"
      provides: "UI control that calls useTheme() to switch light/dark/system"
      contains: "useTheme"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "ThemeProvider wrapping {children} in <body>"
      pattern: "ThemeProvider"
    - from: "src/app/(app)/app-sidebar.tsx"
      to: "src/components/mode-toggle.tsx"
      via: "import + render inside SidebarFooter dropdown"
      pattern: "mode-toggle|ModeToggle|setTheme"
---

<objective>
Adicionar um toggle de modo escuro (claro / escuro / sistema) à interface do Agenda Fiscal usando o pacote `next-themes` (já em package.json, porém não conectado) e o padrão shadcn/ui.

Purpose: Permitir que a equipe e o dono usem o app em tema claro ou escuro, com persistência entre sessões e respeito à preferência do SO.
Output: ThemeProvider conectado no root layout, componente de toggle reutilizável, e o toggle integrado ao dropdown de usuário na sidebar (acessível de todas as páginas autenticadas e também em /login via root layout).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/app/layout.tsx
@src/app/globals.css
@src/app/(app)/app-sidebar.tsx
@src/components/ui/dropdown-menu.tsx
@src/components/ui/button.tsx

# Estado verificado durante o planejamento (NÃO re-investigar do zero):
# - next-themes@^0.4.6 está instalado mas NÃO está conectado: não há ThemeProvider em app/layout.tsx
#   nem nenhum uso de useTheme no código da app (apenas em components/ui/sonner.tsx).
# - Tailwind v4 dark variant JÁ está corretamente configurado em globals.css:
#     `@custom-variant dark (&:is(.dark *));` + bloco `.dark { ... }` completo.
#   Isto é compatível com a estratégia attribute="class" do next-themes. NÃO alterar globals.css.
# - dropdown-menu.tsx exporta DropdownMenuRadioGroup, DropdownMenuRadioItem,
#   DropdownMenuSub/SubTrigger/SubContent — disponíveis para o toggle.
# - O bloco de usuário (DropdownMenu) fica em SidebarFooter de app-sidebar.tsx; é o ponto
#   natural para adicionar as opções de tema, acessível em todas as rotas autenticadas.
# - app/layout.tsx hoje tem `<html lang="en">` SEM suppressHydrationWarning e sem ThemeProvider.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar ThemeProvider e conectar no root layout</name>
  <files>src/components/theme-provider.tsx, src/app/layout.tsx</files>
  <action>
    Criar `src/components/theme-provider.tsx` como Client Component ("use client") que reexporta um wrapper fino em torno do `ThemeProvider` de `next-themes`, repassando `children` e `...props` (tipados com `React.ComponentProps<typeof NextThemesProvider>`), seguindo o padrão documentado do shadcn para next-themes.

    Editar `src/app/layout.tsx`:
    1. Adicionar `suppressHydrationWarning` na tag `<html lang="en">` (necessário porque next-themes injeta a classe de tema antes da hidratação; sem isso o React emite warning de mismatch).
    2. Importar o ThemeProvider criado e envolver `{children}` (e o `<Toaster />`) dentro do `<body>` com as props: `attribute="class"`, `defaultTheme="system"`, `enableSystem` e `disableTransitionOnChange`. NÃO mover o ThemeProvider para o layout `(app)` — ele DEVE ficar no root layout para que /login (fora do shell autenticado) também seja temático.

    NÃO tocar em `globals.css` — o `@custom-variant dark` e o bloco `.dark` já estão corretos e compatíveis com `attribute="class"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>theme-provider.tsx existe e exporta um componente; app/layout.tsx tem `suppressHydrationWarning` no `<html>` e envolve children com ThemeProvider usando attribute="class"/defaultTheme="system"/enableSystem; `npx tsc --noEmit` passa sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Criar componente ModeToggle e integrá-lo na sidebar</name>
  <files>src/components/mode-toggle.tsx, src/app/(app)/app-sidebar.tsx</files>
  <action>
    Criar `src/components/mode-toggle.tsx` como Client Component ("use client") que usa `useTheme()` de `next-themes` para ler/definir o tema. Implementar como um submenu dentro de um dropdown já existente, OU como um componente de itens de tema reutilizável, expondo três escolhas: Claro, Escuro, Sistema. Usar os ícones `Sun` e `Moon` de `lucide-react` (já dependência) — padrão shadcn: ícone Sun visível no claro, Moon no escuro, com transição via classes. Cada opção chama `setTheme("light" | "dark" | "system")`.

    Integrar no dropdown de usuário em `src/app/(app)/app-sidebar.tsx` (o `DropdownMenu` dentro de `SidebarFooter`): adicionar, antes do `DropdownMenuSeparator`/item "Sair", um sub-bloco de tema usando `DropdownMenuSub` + `DropdownMenuSubTrigger` (rótulo "Tema" com ícone Sun/Moon) e `DropdownMenuSubContent` com um `DropdownMenuRadioGroup value={theme}` contendo três `DropdownMenuRadioItem` (light/dark/system). Para isso, marcar a leitura do tema via `useTheme()` — o componente app-sidebar.tsx já é "use client", então pode usar o hook diretamente OU consumir o ModeToggle exportado. Garantir que o estado inicial não cause mismatch: ao renderizar o indicador do tema atual, proteger contra `theme` indefinido no primeiro render (ex.: usar `resolvedTheme`/`theme` somente após mount com um estado `mounted`, ou renderizar o RadioGroup sem depender de window).

    O toggle DEVE permanecer acessível em todas as rotas autenticadas (a sidebar é compartilhada pelo layout `(app)`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>mode-toggle.tsx existe e chama useTheme()/setTheme com as três opções; app-sidebar.tsx renderiza o controle de tema dentro do dropdown do SidebarFooter; `npx tsc --noEmit` e `npm run lint` passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ThemeProvider conectado no root layout + toggle de tema (claro/escuro/sistema) no dropdown de usuário da sidebar, usando next-themes e o padrão shadcn. Tailwind v4 dark variant já estava configurado.</what-built>
  <how-to-verify>
    1. Rode `npm run dev` e abra o app no navegador, faça login.
    2. Em qualquer página autenticada (ex.: /empresas), abra o dropdown do usuário na base da sidebar e selecione "Escuro" → a interface inteira deve mudar para tema escuro imediatamente, sem flash.
    3. Selecione "Claro" → volta ao tema claro. Selecione "Sistema" → segue a preferência do SO.
    4. Recarregue a página (F5) com tema escuro ativo → deve permanecer escuro, SEM flash de tema claro e SEM warning de hydration mismatch no console do navegador.
    5. Faça logout / vá para /login → a página de login deve respeitar o tema escolhido (confirma que o ThemeProvider está no root layout, não só no shell autenticado).
  </how-to-verify>
  <resume-signal>Digite "approved" se tudo funciona, ou descreva o problema (ex.: flash de tema, mismatch no console, login não temático).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa.
- `npm run lint` passa.
- ThemeProvider presente no root layout (app/layout.tsx) com attribute="class".
- `<html>` tem suppressHydrationWarning.
- Toggle alterna light/dark/system e persiste após reload, sem hydration warning, incluindo em /login.
</verification>

<success_criteria>
Usuário consegue alternar entre claro/escuro/sistema em qualquer página (autenticada e /login), a escolha persiste entre reloads, e não há flash nem warning de hydration mismatch.
</success_criteria>

<output>
Create `.planning/quick/260615-ci1-adicionar-toggle-de-modo-escuro-dark-mod/260615-ci1-SUMMARY.md` when done
</output>

---
phase: quick-260626-d1a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/usuarios/actions.ts
  - src/app/(app)/usuarios/page.tsx
  - src/app/(app)/usuarios/usuarios-table.tsx
  - src/app/(app)/app-sidebar.tsx
  - tests/usuarios.actions.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "DONO acessa /usuarios e vê todos os usuários (nome, email, role, setor)"
    - "DONO edita o nome de um usuário via Dialog e a lista reflete o novo nome"
    - "COLABORADOR que acessa /usuarios é redirecionado para /empresas (sem ver a página)"
    - "Server Action rejeita COLABORADOR antes de tocar no banco"
    - "Server Action atualiza SOMENTE o campo nome (nunca email/role/setor/senhaHash)"
    - "Item 'Usuários' aparece no sidebar somente para DONO"
  artifacts:
    - path: "src/app/(app)/usuarios/actions.ts"
      provides: "editarNomeUsuarioAction com guard DONO-only e update só do nome"
      contains: "editarNomeUsuarioAction"
    - path: "src/app/(app)/usuarios/page.tsx"
      provides: "Server Component DONO-only que lista usuários"
    - path: "src/app/(app)/usuarios/usuarios-table.tsx"
      provides: "Tabela client com Dialog de editar nome"
    - path: "tests/usuarios.actions.test.ts"
      provides: "Testes RBAC + sucesso do update de nome"
  key_links:
    - from: "src/app/(app)/usuarios/usuarios-table.tsx"
      to: "editarNomeUsuarioAction"
      via: "import from ./actions"
      pattern: "editarNomeUsuarioAction"
    - from: "src/app/(app)/app-sidebar.tsx"
      to: "/usuarios"
      via: "Link condicional isDono"
      pattern: "/usuarios"
---

<objective>
Adicionar uma página `/usuarios` (somente DONO) que lista todos os usuários e permite
editar SOMENTE o campo `nome` de cada um diretamente pela interface, eliminando a
necessidade de rodar scripts one-off de renomeação.

Purpose: O dono renomeia colaboradores sozinho, sem depender de deploy/script (já foram
2 renomeações por script hoje — 260625-mgd e 260626-c8i).
Output: rota `/usuarios`, Server Action `editarNomeUsuarioAction`, item de sidebar
DONO-only e testes cobrindo o guard de autorização e o update do nome.

Escopo deliberadamente mínimo: APENAS edição do `nome`. Fora de escopo (não implementar):
criar usuário, editar email/senha/role/setor, excluir usuário.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Schema de validação já existente (usar usuarioSchema.shape.nome)
@src/modules/usuarios/schema.ts

# Padrão CRÍTICO de Server Action: guard auth() + role DONO antes de tocar no banco,
# retorno { ok } | { ok:false, error }, revalidatePath
@src/app/(app)/actions.ts

# Padrão de Server Component DONO-only e composição page -> table client
@src/app/(app)/empresas/page.tsx
@src/app/(app)/empresas/empresas-table.tsx

# Padrão de form client (React Hook Form + zodResolver + chamada à Server Action + toast)
@src/app/(app)/empresas/empresa-form.tsx

# Sidebar: padrão de item de navegação condicional por isDono
@src/app/(app)/app-sidebar.tsx

# Padrão de teste de Server Action com db/auth/next-cache mockados (db NÃO chamado em rejeição)
@tests/geracao.actions.test.ts
@tests/setup.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Action editarNomeUsuarioAction + testes</name>
  <files>src/app/(app)/usuarios/actions.ts, tests/usuarios.actions.test.ts</files>
  <behavior>
    - Rejeita chamador não autenticado (auth() null) com { ok:false } sem chamar db.usuario.update
    - Rejeita COLABORADOR com { ok:false, error:"não autorizado" } ANTES de tocar no banco
      (db.usuario.update NÃO deve ter sido chamado)
    - DONO com payload válido: chama db.usuario.update exatamente uma vez, com
      where:{ id: usuarioId } e data contendo SOMENTE { nome } (nunca email/role/setor/senhaHash);
      retorna { ok:true }
    - Nome vazio ou só espaços é rejeitado com { ok:false } (validação Zod), sem update
  </behavior>
  <action>
    Criar `src/app/(app)/usuarios/actions.ts` com `"use server"` no topo e exportar
    `editarNomeUsuarioAction(usuarioId: string, novoNome: string)` retornando
    `Promise<{ ok: true } | { ok: false; error: string }>`.

    Ordem dos checks (mesmo padrão crítico de gerarTarefasDoMesAction em src/app/(app)/tarefas/actions.ts
    e do guard DONO de actions.ts): (1) `const session = await auth()` — se `!session?.user` retornar
    `{ ok:false, error:"Não autenticado" }`; (2) se `session.user.role !== "DONO"` retornar
    `{ ok:false, error:"não autorizado" }` — este check vem ANTES de qualquer acesso ao banco, nunca
    confiar só na UI escondida; (3) validar o nome via `usuarioSchema.shape.nome` (importado de
    @/modules/usuarios/schema) com `.safeParse(novoNome.trim())` — se falhar retornar
    `{ ok:false, error:"Nome inválido" }`; (4) `await db.usuario.update({ where:{ id: usuarioId },
    data: { nome: <nome validado e trimado> } })` — SOMENTE o campo `nome`, jamais email/role/setor/senhaHash;
    (5) `revalidatePath("/usuarios")`; (6) retornar `{ ok:true }`.

    Imports: `auth` de @/auth, `db` de @/lib/db, `revalidatePath` de next/cache, `usuarioSchema`
    de @/modules/usuarios/schema. Adicionar um comentário de cabeçalho explicando o guard DONO-only
    server-side (escopo: só `nome`), espelhando o tom dos comentários de actions.ts.

    Criar `tests/usuarios.actions.test.ts` seguindo EXATAMENTE o padrão de tests/geracao.actions.test.ts:
    `vi.mock("@/lib/db", ...)` expondo `usuario.update` via um `updateMock = vi.fn()`;
    `vi.mock("@/auth", () => ({ auth: () => authMock() }))`; `vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))`.
    Importar `mockDonoUser`/`mockColaboradorUser` de ./setup. Casos: (a) não autenticado → ok:false +
    updateMock não chamado; (b) COLABORADOR → { ok:false, error:"não autorizado" } + updateMock NÃO chamado;
    (c) DONO + nome válido → ok:true, updateMock chamado 1x e o argumento tem
    `data` === `{ nome: "<esperado>" }` (assert que NÃO contém email/role/setor — ex.: verificar
    `Object.keys(call.data)` é `["nome"]`); (d) DONO + nome só-espaços → ok:false + updateMock não chamado.
    Importar a action via `await import("@/app/(app)/usuarios/actions")` dentro de cada teste (padrão da suite).
  </action>
  <verify>
    <automated>npx vitest run tests/usuarios.actions.test.ts</automated>
  </verify>
  <done>tests/usuarios.actions.test.ts passa; action rejeita COLABORADOR sem chamar db; DONO atualiza só o nome.</done>
</task>

<task type="auto">
  <name>Task 2: Página /usuarios (Server Component DONO-only) + tabela com Dialog de editar nome</name>
  <files>src/app/(app)/usuarios/page.tsx, src/app/(app)/usuarios/usuarios-table.tsx</files>
  <action>
    Criar `src/app/(app)/usuarios/page.tsx` como Server Component async (sem "use client").
    Guard: `const session = await auth()`; se `!session?.user` → `redirect("/login")`; se
    `session.user.role !== "DONO"` → `redirect("/empresas")` (padrão DONO-only; o projeto usa
    redirect, não notFound, para gating de rota — ver empresas/page.tsx que faz redirect("/login")).
    Buscar usuários: `const usuarios = await db.usuario.findMany({ select: { id:true, nome:true,
    email:true, role:true, setor:true }, orderBy: { nome: "asc" } })`. Renderizar um título
    `<h1>Usuários</h1>` (mesmo layout de empresas/page.tsx: `<div className="flex flex-col gap-6">`)
    e `<UsuariosTable usuarios={usuarios} />`. Imports: `auth` de @/auth, `redirect` de next/navigation,
    `db` de @/lib/db.

    Criar `src/app/(app)/usuarios/usuarios-table.tsx` com `"use client"`. Props:
    `{ usuarios: { id:string; nome:string; email:string; role:"COLABORADOR"|"DONO";
    setor:"FISCAL"|"DP"|"CONTABIL"|null }[] }`. Dado ~12 usuários, usar a tabela shadcn simples
    (componentes Table/TableHeader/TableBody/TableRow/TableHead/TableCell de @/components/ui/table) —
    SEM TanStack Table, SEM paginação/filtro. Colunas: Nome, Email, Role (label "Dono"/"Colaborador"),
    Setor (mostrar "—" se null), Ações. Em Ações, um Button (variant="ghost", size="sm", com ícone
    Pencil de lucide-react) "Editar nome" que abre um Dialog (componentes de @/components/ui/dialog:
    Dialog/DialogContent/DialogHeader/DialogTitle/DialogFooter/DialogTrigger ou controle via estado
    `open`).

    No Dialog, um form com React Hook Form + zodResolver sobre um schema mínimo derivado só do nome:
    `z.object({ nome: usuarioSchema.shape.nome })` (importar `usuarioSchema` de @/modules/usuarios/schema
    e `z` de zod). Usar os componentes Form/FormField/FormItem/FormLabel/FormControl/FormMessage +
    Input (mesmo padrão de empresa-form.tsx). `defaultValues` = nome atual do usuário em edição.
    Estado local: qual usuário está sendo editado (`useState<Usuario|null>`) + `isSubmitting`.
    onSubmit: `await editarNomeUsuarioAction(usuario.id, values.nome)`; se `!resultado.ok` →
    `toast.error("Não foi possível salvar. Tente novamente.")`; senão `toast.success("Nome atualizado.")`,
    fechar o Dialog e `router.refresh()` (padrão usado em GerarTarefasButton/ConcluirButton — refresh
    para repopular o Server Component, ver STATE 03-03/02-04). Imports: `editarNomeUsuarioAction` de
    ./actions, `toast` de sonner, `useRouter` de next/navigation, `useForm`/`zodResolver`, `Pencil`/`Loader2`
    de lucide-react.

    NÃO incluir campos de email/role/setor no form (fora de escopo). NÃO usar SelectItem com value=""
    (proibido pelo Radix — ver comentário SEM_RESPONSAVEL em empresa-form.tsx), mas aqui não há Select.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>tsc --noEmit limpo; /usuarios redireciona COLABORADOR para /empresas; DONO vê tabela e Dialog de editar nome salva via action.</done>
</task>

<task type="auto">
  <name>Task 3: Item de navegação "Usuários" no sidebar (DONO-only)</name>
  <files>src/app/(app)/app-sidebar.tsx</files>
  <action>
    Em `src/app/(app)/app-sidebar.tsx`, adicionar um novo `<SidebarMenuItem>` para "Usuários",
    visível SOMENTE para DONO — usar o mesmo padrão condicional `{isDono && ( ... )}` já aplicado
    ao item "Dashboards" (linhas ~110-122). Colocar o item após "Dashboards" dentro do mesmo
    `<SidebarMenu>`. Usar `<SidebarMenuButton asChild isActive={pathname?.startsWith("/usuarios")}>`
    envolvendo `<Link href="/usuarios">` com um ícone (importar `Users` de lucide-react, adicionando-o
    ao import existente de "lucide-react") e `<span>Usuários</span>`. Não alterar mais nada no arquivo.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>tsc limpo; o item "Usuários" aparece no sidebar apenas quando isDono é true e leva a /usuarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Qualquer usuário autenticado pode invocar editarNomeUsuarioAction diretamente, contornando a UI escondida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d1a-01 | Elevation of Privilege | editarNomeUsuarioAction | mitigate | Guard `role === "DONO"` como check obrigatório ANTES de qualquer db.usuario.update; testado em usuarios.actions.test.ts (COLABORADOR rejeitado sem tocar no banco). Sidebar/UI escondida é só defesa em profundidade. |
| T-d1a-02 | Tampering | editarNomeUsuarioAction | mitigate | `data` do update contém SOMENTE `{ nome }` — email/role/setor/senhaHash nunca são gravados, mesmo que o cliente envie outros campos (a action ignora qualquer coisa além de usuarioId+novoNome). Asserção explícita no teste de sucesso. |
| T-d1a-03 | Information Disclosure | usuarios/page.tsx | mitigate | findMany seleciona apenas id/nome/email/role/setor — `senhaHash` NUNCA entra no select nem no payload RSC. |
</threat_model>

<verification>
- `npx vitest run tests/usuarios.actions.test.ts` — RBAC + update só-nome verde
- `npx tsc --noEmit` — sem erros de tipo
- Manual (DONO): /usuarios lista usuários, "Editar nome" abre Dialog, salvar reflete o novo nome
- Manual (COLABORADOR): acessar /usuarios redireciona para /empresas; item "Usuários" ausente do sidebar
</verification>

<success_criteria>
- editarNomeUsuarioAction rejeita não-DONO antes de tocar no banco e atualiza apenas o campo nome
- /usuarios é DONO-only (redirect para /empresas se COLABORADOR)
- Tabela lista nome/email/role/setor; Dialog edita o nome via React Hook Form + Zod
- Item "Usuários" no sidebar visível somente para DONO
- Nenhum campo fora de escopo (email/role/setor/senha) editável; nenhuma migração de banco
- Todos os testes verdes e tsc --noEmit limpo
</success_criteria>

<output>
Create `.planning/quick/260626-d1a-pagina-usuarios-dono-para-editar-o-nome-/260626-d1a-SUMMARY.md` when done
</output>

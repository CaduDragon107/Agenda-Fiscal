---
phase: quick-260626-c8i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/seed.ts
  - scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs
autonomous: true
requirements: [QUICK-260626-c8i]
must_haves:
  truths:
    - "seed.ts has nome literals Andre/Mirella/Lorraine/Rany/Sarah for the 5 target emails"
    - "A new one-off script exists that renames the 5 users by email, dry-run by default"
    - "Only the nome field is ever written — email/senhaHash/role/setor untouched"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "Updated placeholder names for the 5 users"
      contains: 'nome: "Andre"'
    - path: "scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs"
      provides: "Dry-run/--apply production rename script for 5 users"
      contains: "--apply"
  key_links:
    - from: "scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs"
      to: "db.usuario.update"
      via: "where: { email }, data: { nome }"
      pattern: "data: \\{ nome:"
---

<objective>
Renomear 5 usuarios placeholder para nomes reais — somente o campo `nome`, identificados pelo `email` (campo estavel):

- dp2@escritorio.com.br: "DP2" -> "Andre"
- dp3@escritorio.com.br: "DP3" -> "Mirella"
- dp4@escritorio.com.br: "DP4" -> "Lorraine"
- contabil2@escritorio.com.br: "Contabil2" -> "Rany"
- contabil3@escritorio.com.br: "Contabil3" -> "Sarah"

Purpose: o `upsert` do seed usa `update: {}`, que NUNCA retroage `nome` em usuarios ja existentes no banco. Por isso sao necessarios DOIS pontos de mudanca: (1) corrigir os literais do seed para futuros bancos limpos, e (2) um script one-off dedicado que aplica o rename em producao por email. Mesmo padrao exato do quick task anterior (260625-mgd).

Output: `prisma/seed.ts` atualizado + novo script `scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@prisma/seed.ts
@scripts/renomear-usuarios-neto-lauany-elisabete.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar literais nome no seed.ts</name>
  <files>prisma/seed.ts</files>
  <action>No array `usuarios` de prisma/seed.ts, alterar SOMENTE o valor do campo `nome` das 5 linhas identificadas pelo email correspondente: dp2@escritorio.com.br "DP2" -> "Andre"; dp3@escritorio.com.br "DP3" -> "Mirella"; dp4@escritorio.com.br "DP4" -> "Lorraine"; contabil2@escritorio.com.br "Contabil2" -> "Rany"; contabil3@escritorio.com.br "Contabil3" -> "Sarah". NAO tocar em email, role, setor, nem em qualquer outra linha (Neto, Lauany, Elisabete, colaborador1-4 e demais permanecem inalterados). NAO alterar a logica do loop upsert.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('prisma/seed.ts','utf8'); const need=['\"Andre\"','\"Mirella\"','\"Lorraine\"','\"Rany\"','\"Sarah\"']; const bad=['\"DP2\"','\"DP3\"','\"DP4\"','\"Contabil2\"','\"Contabil3\"']; const miss=need.filter(n=>!s.includes(n)); const left=bad.filter(n=>s.includes(n)); if(miss.length||left.length){console.error('miss',miss,'left',left);process.exit(1)} console.log('seed OK')"</automated>
  </verify>
  <done>seed.ts contem os 5 novos nomes e nenhum dos 5 nomes placeholder antigos; email/role/setor das linhas e do restante do array intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Criar script one-off de rename para producao</name>
  <files>scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs</files>
  <action>Criar scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs espelhando EXATAMENTE o padrao de scripts/renomear-usuarios-neto-lauany-elisabete.mjs: shebang node, comentario de cabecalho documentando o porque (upsert com update:{} nao retroage nome) e o uso (node --env-file=.env.production ... dry-run; --apply para aplicar), `const APPLY = process.argv.includes("--apply")`, array `RENOMEACOES` com os 5 pares { email, nome } (dp2->Andre, dp3->Mirella, dp4->Lorraine, contabil2->Rany, contabil3->Sarah), bloco que faz findMany dos emails e loga "atual -> novo" para cada, early-return em dry-run, bloco APPLY que faz db.usuario.update por email gravando SOMENTE { nome }, e a verificacao final obrigatoria que re-consulta e seta process.exitCode = 1 em qualquer divergencia. Atualizar os textos do cabecalho/log para os 5 usuarios desta tarefa. NAO executar o script.</action>
  <verify>
    <automated>node --check scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs && node -e "const s=require('fs').readFileSync('scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs','utf8'); const ok=['--apply','RENOMEACOES','dp2@escritorio.com.br','Andre','contabil3@escritorio.com.br','Sarah','process.exitCode'].every(t=>s.includes(t)); if(!ok){console.error('faltando tokens');process.exit(1)} if(/data: \{ [^}]*senhaHash|data: \{ [^}]*email|data: \{ [^}]*role|data: \{ [^}]*setor/.test(s)){console.error('escreve campo proibido');process.exit(1)} console.log('script OK')"</automated>
  </verify>
  <done>Script existe, passa `node --check`, contem os 5 pares email/nome, roda dry-run por padrao, so escreve com --apply, escreve apenas `nome`, e tem verificacao final com exitCode. Script NAO foi executado.</done>
</task>

</tasks>

<verification>
- `node --check` passa no novo script.
- seed.ts contem os 5 novos nomes, zero placeholders antigos restantes.
- Nenhum `db.usuario.update`/`upsert` grava email/senhaHash/role/setor — somente `nome`.
- O agente NAO executou o script (apenas criou). Execucao manual fica para o orquestrador com acesso ao banco.
</verification>

<success_criteria>
- prisma/seed.ts atualizado com Andre/Mirella/Lorraine/Rany/Sarah.
- scripts/renomear-usuarios-andre-mirella-lorraine-rany-sarah.mjs criado, espelhando o padrao do script de referencia, dry-run por padrao.
- Apenas o campo `nome` e afetado em ambos os pontos.
</success_criteria>

<output>
Create `.planning/quick/260626-c8i-renomear-dp2-dp3-dp4-para-andre-mirella-/260626-c8i-SUMMARY.md` when done.
</output>

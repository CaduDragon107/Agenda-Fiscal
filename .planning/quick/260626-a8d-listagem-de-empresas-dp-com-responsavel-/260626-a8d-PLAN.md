---
phase: quick-260626-a8d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/modules/empresas/schema.ts
  - src/app/(app)/actions.ts
  - src/modules/empresas/queries.ts
  - src/app/(app)/empresas/empresa-form.tsx
  - src/app/(app)/empresas/novo/page.tsx
  - src/app/(app)/empresas/[id]/editar/page.tsx
  - src/app/(app)/empresas/dp/page.tsx
  - src/app/(app)/empresas/dp/empresas-dp-table.tsx
  - src/app/(app)/app-sidebar.tsx
autonomous: true
requirements: [QUICK-260626-a8d]

must_haves:
  truths:
    - "Existe uma tela acessível em /empresas/dp que lista empresas mostrando o responsável de DP de cada uma."
    - "Empresas sem responsável DP aparecem na listagem com a coluna de responsável DP vazia (célula em branco, sem placeholder 'N/A' ou badge 'Sem responsável'), e a linha NÃO é omitida."
    - "Empresa tem um novo campo booleano informativo de empregada doméstica (temEmpregadaDomestica), default false, persistido no banco."
    - "O campo temEmpregadaDomestica é exibido e editável no formulário de empresa (novo e editar) via checkbox, igual ao padrão de temFuncionariosClt."
    - "O campo temEmpregadaDomestica é exibido como coluna/indicador na listagem DP."
    - "Marcar temEmpregadaDomestica NÃO dispara nenhuma geração automática de tarefas DP — é apenas marcação informativa."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Campo temEmpregadaDomestica Boolean @default(false) no model Empresa"
      contains: "temEmpregadaDomestica"
    - path: "src/app/(app)/empresas/dp/page.tsx"
      provides: "Server Component da listagem DP filtrada por setor DP"
      min_lines: 20
    - path: "src/app/(app)/empresas/dp/empresas-dp-table.tsx"
      provides: "Tabela client (TanStack Table) com coluna Responsável DP (vazia quando null) e indicador empregada doméstica"
      min_lines: 30
  key_links:
    - from: "src/app/(app)/empresas/dp/page.tsx"
      to: "listarEmpresas"
      via: "import de @/modules/empresas/queries com withVisibilityScope aplicado"
      pattern: "listarEmpresas"
    - from: "src/app/(app)/actions.ts"
      to: "Empresa.temEmpregadaDomestica"
      via: "criarEmpresa/editarEmpresa gravam o campo via Prisma"
      pattern: "temEmpregadaDomestica"
    - from: "src/app/(app)/app-sidebar.tsx"
      to: "/empresas/dp"
      via: "novo link de navegação"
      pattern: "/empresas/dp"
---

<objective>
Criar uma tela de listagem de empresas do setor DP mostrando o responsável de DP de cada empresa (coluna em branco quando não houver responsável atribuído, nunca placeholder), e adicionar um campo booleano informativo `temEmpregadaDomestica` na Empresa — exibido/editável no formulário de empresa e visível na listagem DP.

Purpose: Dar ao setor de Departamento Pessoal uma visão dedicada de quem é o responsável DP por empresa e marcar quais empresas têm vínculo de empregada doméstica (distinto do CLT comercial já coberto por `temFuncionariosClt`).

Output: Nova rota `/empresas/dp` + tabela DP reaproveitando o padrão TanStack Table existente, migração Prisma do novo campo booleano, e o campo propagado por schema/Server Actions/query/formulário.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260626-a8d-listagem-de-empresas-dp-com-responsavel-/260626-a8d-CONTEXT.md

# Schema e camada de dados
@prisma/schema.prisma
@src/modules/empresas/schema.ts
@src/modules/empresas/queries.ts
@src/app/(app)/actions.ts

# UI existente a reaproveitar (padrão de tabela, form, derivação de linhas, sidebar)
@src/app/(app)/empresas/empresas-table.tsx
@src/app/(app)/empresas/derive-rows.ts
@src/app/(app)/empresas/empresa-form.tsx
@src/app/(app)/empresas/novo/page.tsx
@src/app/(app)/empresas/[id]/editar/page.tsx
@src/app/(app)/empresas/page.tsx
@src/app/(app)/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo temEmpregadaDomestica (schema + migração + camada de dados)</name>
  <files>prisma/schema.prisma, src/modules/empresas/schema.ts, src/modules/empresas/queries.ts, src/app/(app)/actions.ts</files>
  <action>
    Adicionar ao model `Empresa` em `prisma/schema.prisma` o campo `temEmpregadaDomestica Boolean @default(false)`, posicionado logo abaixo de `temFuncionariosClt` para manter coesão. Aplicar a alteração no banco com `npx prisma db push` (NÃO `prisma migrate dev`) seguido de `npx prisma generate` — esta é a convenção registrada deste projeto para o Neon, que não tem shadow database (ver STATE.md, decisões 02-01/03-01). Como o campo tem `@default(false)`, linhas existentes recebem `false` sem perda de dados.

    Em `src/modules/empresas/schema.ts`, adicionar `temEmpregadaDomestica: z.boolean().default(false)` ao `empresaSchema`, espelhando exatamente o tratamento de `temFuncionariosClt`.

    Em `src/app/(app)/actions.ts`: incluir `temEmpregadaDomestica: formData.get("temEmpregadaDomestica") === "true"` em `dadosFormulario`; e gravar `temEmpregadaDomestica: dados.temEmpregadaDomestica` no `data` tanto de `criarEmpresa` (tx.empresa.create) quanto de `editarEmpresa` (tx.empresa.update), ao lado de `temFuncionariosClt`. NÃO aplicar nenhum guard DONO-only a este campo (não é um campo de responsável) — qualquer usuário no escopo de edição pode marcar/desmarcar.

    Em `src/modules/empresas/queries.ts`, adicionar `temEmpregadaDomestica: true` ao objeto `EMPRESA_SELECT` para que o campo seja retornado por `listarEmpresas`/`buscarEmpresaPorId`.

    IMPORTANTE (fora de escopo, não fazer): não tocar no motor de geração mensal de tarefas (src/modules/geracao/* ou similar). Este campo é puramente informativo nesta task — marcá-lo NÃO deve gerar tarefas DP automáticas (CONTEXT.md, decisão explícita).
  </action>
  <verify>
    <automated>cd "c:/Users/Novo/Desktop/criação de planilha modulos/teste" && npx prisma validate && npx tsc --noEmit</automated>
  </verify>
  <done>schema.prisma valida com o novo campo; `npx prisma db push` + `npx prisma generate` aplicados; `empresaSchema`, `dadosFormulario`, `criarEmpresa`, `editarEmpresa` e `EMPRESA_SELECT` referenciam `temEmpregadaDomestica`; `tsc --noEmit` passa sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Expor temEmpregadaDomestica no formulário de empresa</name>
  <files>src/app/(app)/empresas/empresa-form.tsx, src/app/(app)/empresas/novo/page.tsx, src/app/(app)/empresas/[id]/editar/page.tsx</files>
  <action>
    Em `src/app/(app)/empresas/empresa-form.tsx`:
    - Adicionar `temEmpregadaDomestica: boolean` ao tipo `empresa` em `EmpresaFormProps` (ao lado de `temFuncionariosClt`).
    - Adicionar `temEmpregadaDomestica: empresa?.temEmpregadaDomestica ?? false` aos `defaultValues` do `useForm`.
    - Em `onSubmit`, adicionar `formData.set("temEmpregadaDomestica", String(values.temEmpregadaDomestica ?? false));`.
    - Adicionar um novo `FormField` com `name="temEmpregadaDomestica"` usando o MESMO padrão visual do `FormField` de `temFuncionariosClt` (Checkbox + FormLabel "Tem empregada doméstica?" + texto auxiliar curto). O texto auxiliar deve deixar claro que é apenas marcação informativa e NÃO dispara geração automática de tarefas (ex.: "Marcação informativa de vínculo de empregada doméstica. Não gera tarefas automaticamente.").

    Em `src/app/(app)/empresas/[id]/editar/page.tsx`: adicionar `temEmpregadaDomestica: empresa.temEmpregadaDomestica` ao objeto `empresa` passado para `<EmpresaForm>`.

    Em `src/app/(app)/empresas/novo/page.tsx`: nenhuma mudança de dados necessária (sem prop `empresa`, o default `false` do form cobre o caso novo) — confirmar apenas que continua compilando.
  </action>
  <verify>
    <automated>cd "c:/Users/Novo/Desktop/criação de planilha modulos/teste" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>O formulário de empresa renderiza um checkbox "Tem empregada doméstica?"; criar e editar empresa persistem o valor; a página de edição pré-popula o checkbox com o valor atual; `tsc --noEmit` e `lint` passam.</done>
</task>

<task type="auto">
  <name>Task 3: Criar rota /empresas/dp com listagem DP e link na sidebar</name>
  <files>src/app/(app)/empresas/dp/page.tsx, src/app/(app)/empresas/dp/empresas-dp-table.tsx, src/app/(app)/app-sidebar.tsx</files>
  <action>
    Criar `src/app/(app)/empresas/dp/page.tsx` (Server Component) seguindo o padrão de `src/app/(app)/empresas/page.tsx`:
    - `const session = await auth();` com `redirect("/login")` se ausente.
    - Chamar `listarEmpresas(session.user)` (mantém withVisibilityScope — colaborador vê só sua carteira, dono vê todas; NÃO adicionar filtro client-side por responsavelId).
    - Mapear o resultado para linhas que expõem: id, nome, cnpj, o responsável DP (derivado de `responsaveisPorSetor.find(r => r.setor === "DP")?.usuario ?? null`) e `temEmpregadaDomestica`. Você pode reusar `deriveEmpresaRows` (de `../derive-rows`) para obter `responsavelDp` respeitando a fronteira de segurança D-10, OU derivar inline a partir de `responsaveisPorSetor`; se reusar `deriveEmpresaRows`, lembre que ele não inclui `temEmpregadaDomestica`, então combine `responsavelDp` com `temEmpregadaDomestica` da empresa original ao montar a linha. Título da página: "Empresas — DP".
    - Renderizar `<EmpresasDpTable empresas={rows} />`.

    Criar `src/app/(app)/empresas/dp/empresas-dp-table.tsx` (client component, "use client") reaproveitando o padrão de `empresas-table.tsx` (TanStack Table com getCoreRowModel/getFilteredRowModel/getPaginationRowModel, Input de busca por nome/CNPJ, paginação de 20). Tipo de linha: `{ id, nome, cnpj, responsavelDp: { id, nome } | null, temEmpregadaDomestica: boolean }`. Colunas:
    - Nome
    - CNPJ (formatado, reusando a mesma lógica de `formatarCnpj`)
    - Responsável DP — célula renderiza `row.original.responsavelDp?.nome ?? ""` (string vazia / célula em branco; NÃO usar badge "Sem responsável" nem "N/A" — CONTEXT.md é explícito: vazio mesmo). Linhas sem responsável DP NÃO são omitidas.
    - Empregada doméstica — exibir um Badge "Sim" quando `temEmpregadaDomestica === true`, e célula vazia quando false (indicador discreto).
    - Ações — botão de editar (Link para `/empresas/${id}/editar`), seguindo o padrão de ícone de lápis existente. (Excluir é opcional; pode omitir para manter a tela enxuta.)
    Tratar empilha vazia com uma mensagem simples ("Nenhuma empresa encontrada").

    Em `src/app/(app)/app-sidebar.tsx`: adicionar um novo `SidebarMenuItem` linkando para `/empresas/dp` com label "Empresas DP" e um ícone do lucide-react (ex.: `Users` ou `UserCheck`, importar no topo). Definir `isActive={pathname?.startsWith("/empresas/dp")}`. Para o item "Empresas" existente, ajustar o `isActive` para NÃO acender quando a rota for `/empresas/dp` (ex.: `pathname?.startsWith("/empresas") && !pathname.startsWith("/empresas/dp")`), evitando dois itens ativos simultâneos.
  </action>
  <verify>
    <automated>cd "c:/Users/Novo/Desktop/criação de planilha modulos/teste" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Rota /empresas/dp existe e compila no build; a tabela lista empresas com coluna "Responsável DP" em branco quando não há responsável (sem omitir linha, sem placeholder) e indicador de empregada doméstica; a sidebar tem um link "Empresas DP" e os itens de navegação não ficam ambos ativos na rota DP; `tsc --noEmit` e `next build` passam.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` confirma o novo campo no schema.
- `npx prisma db push` aplicado sem perda de dados (campo com @default(false)).
- `npx tsc --noEmit` passa após cada task.
- `npm run lint` passa.
- `npm run build` conclui (rota /empresas/dp incluída).
- `npm test` (vitest) continua verde — nenhuma regressão nas suites empresas.* (crud/idor/queries/derive-rows). Se `EMPRESA_SELECT` ou `empresaSchema` quebrar alguma asserção existente, ajustar o teste apenas para refletir o novo campo, nunca afrouxar uma asserção de segurança/IDOR.
</verification>

<success_criteria>
- Existe `/empresas/dp` mostrando, por empresa, o responsável DP (vazio quando não atribuído, sem placeholder) e um indicador de empregada doméstica.
- `Empresa.temEmpregadaDomestica` existe no banco (default false), é editável no formulário de empresa (novo + editar) e é persistido pelas Server Actions.
- Marcar empregada doméstica não dispara geração de tarefas (motor de geração intocado).
- Sidebar oferece navegação para a nova listagem DP.
- Suites de teste existentes permanecem verdes; tsc/lint/build passam.
</success_criteria>

<output>
Create `.planning/quick/260626-a8d-listagem-de-empresas-dp-com-responsavel-/260626-a8d-SUMMARY.md` when done
</output>
